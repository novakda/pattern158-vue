---
phase: 037-sfc-content-extraction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/sections/pattern158Origin.ts
  - src/content/sections/howIWork.ts
  - src/content/sections/aiClarity.ts
  - src/content/philosophy.ts
  - src/components/Pattern158OriginSection.vue
  - src/components/HowIWorkSection.vue
  - src/components/AiClaritySection.vue
  - src/pages/PhilosophyPage.vue
autonomous: true
requirements:
  - SFC-02
must_haves:
  truths:
    - "Pattern158OriginSection.vue, HowIWorkSection.vue, AiClaritySection.vue, and PhilosophyPage.vue contain no hardcoded English prose in their templates — all prose imported from @/content/sections/* or @/content/philosophy"
    - "The Moe Howard blockquote on PhilosophyPage (moral-spine section) still renders as <blockquote> with <cite> attribution after refactor"
    - "The 3 methodology steps in HowIWorkSection render via v-for over imported methodologySteps"
    - "All 127 existing unit tests still pass"
  artifacts:
    - path: "src/content/sections/pattern158Origin.ts"
      provides: "Pattern 158 Myst origin story prose"
      contains: "export const pattern158Origin"
    - path: "src/content/sections/howIWork.ts"
      provides: "Three methodology steps (heading + paragraphs)"
      contains: "export const methodologySteps"
    - path: "src/content/sections/aiClarity.ts"
      provides: "AI clarity section prose"
      contains: "export const aiClarity"
    - path: "src/content/philosophy.ts"
      provides: "PhilosophyPage page-level prose including designThinking, moralSpine (with quote), influencesHeading, brandElementsHeading, colleagueQuotes, closingLine"
      contains: "export const moralSpine"
  key_links:
    - from: "src/pages/PhilosophyPage.vue"
      to: "src/content/philosophy.ts"
      via: "named imports"
      pattern: "from ['\"]@/content/philosophy['\"]"
    - from: "src/components/HowIWorkSection.vue"
      to: "src/content/sections/howIWork.ts"
      via: "import methodologySteps"
      pattern: "from ['\"]@/content/sections/howIWork['\"]"
---

<objective>
Extract prose from PhilosophyPage.vue (page-level sections) plus its 3 delegated section components (Pattern158OriginSection, HowIWorkSection, AiClaritySection) into `src/content/philosophy.ts` and `src/content/sections/*.ts`. This is the LARGEST prose-volume plan in Wave 1 — four SFCs, ~30 paragraph-equivalent units, plus the structurally load-bearing Moe Howard blockquote with cite attribution.

Purpose: Make PhilosophyPage markdown-exportable by Phase 39 without parsing `.vue` SFCs.

Output: 4 new content modules, 4 refactored SFCs.

**Strategy:** Refactor child components FIRST (tasks 1–3), page LAST (task 4). This ensures the existing 127 unit tests validate each component independently before the page refactor touches anything.

**Structural markup policy:** Plain `string` paragraphs everywhere EXCEPT:
  1. Moral-spine `<blockquote>` → structured `{ text: string; cite: string }` (Pitfall 6).
  2. Inline `<strong>`, `<em>`, `<cite>` tags inside paragraphs: per RESEARCH.md §Pattern 2 recommendation, **drop inline emphasis** from the refactored prose (plain `string` paragraphs). The existing visual emphasis is nice-to-have but not semantically critical. Phase 39 will decide whether to re-segment. This is the consistent policy for Phase 37 — NO mixing of segmented + plain across modules.
  3. The `<cite>` inside the blockquote stays in the template, bound to `moralSpine.quote.cite`.
  4. The `<em>` wrappers around "how" and "why" in moralSpine.intro, and around "this changed how I solve problems" in influencesHeading.intro — drop them. The sentence still reads correctly as plain text.
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/037-sfc-content-extraction/037-RESEARCH.md
@src/pages/PhilosophyPage.vue
@src/components/Pattern158OriginSection.vue
@src/components/HowIWorkSection.vue
@src/components/AiClaritySection.vue

<interfaces>
<!-- Exhaustive prose transcription target for the executor.
     Every string below must appear verbatim (with HTML entities normalized) in the content module. -->

**src/components/Pattern158OriginSection.vue** — 5 paragraphs under H2 "Pattern 158":

1. "Near the end of Myst, the brothers Sirrus and Achenar whisper a clue through their prison books: \"The final page is in the fireplace... Pattern 158.\""
2. "You find the pattern book in the library, enter the code on the fireplace grid, and a wall rotates open to reveal a hidden chamber. It works. You've solved the puzzle."
3. "Except you haven't. The brothers are manipulating you. They want you to retrieve their final pages so they can escape. The obvious answer — the red and blue pages — is a trap. The real solution is the green book sitting right next to them. Their father Atrus is inside it. He knows what they did. And the entire puzzle was designed to stop you from seeing that."
4. "Pattern 158 = the code that reveals both the trap and the true solution."
5. "That's how I approach engineering problems. The obvious answer is often a trap. The real problem is usually sitting right next to the stated problem — you have to see through the framing to find it. Every forensic investigation I've run has had this shape: what the client reported and what was actually happening were related, but not the same thing."

Target shape:
```typescript
// src/content/sections/pattern158Origin.ts
export const pattern158Origin = {
  heading: 'Pattern 158',
  paragraphs: [
    'Near the end of Myst, the brothers Sirrus and Achenar whisper a clue through their prison books: \u201CThe final page is in the fireplace... Pattern 158.\u201D',
    'You find the pattern book in the library, enter the code on the fireplace grid, and a wall rotates open to reveal a hidden chamber. It works. You\u2019ve solved the puzzle.',
    'Except you haven\u2019t. The brothers are manipulating you. They want you to retrieve their final pages so they can escape. The obvious answer \u2014 the red and blue pages \u2014 is a trap. The real solution is the green book sitting right next to them. Their father Atrus is inside it. He knows what they did. And the entire puzzle was designed to stop you from seeing that.',
    'Pattern 158 = the code that reveals both the trap and the true solution.',
    'That\u2019s how I approach engineering problems. The obvious answer is often a trap. The real problem is usually sitting right next to the stated problem \u2014 you have to see through the framing to find it. Every forensic investigation I\u2019ve run has had this shape: what the client reported and what was actually happening were related, but not the same thing.',
  ],
}
```

**src/components/HowIWorkSection.vue** — 3 methodology steps, each with H3 + 2-3 paragraphs:

```typescript
// src/content/sections/howIWork.ts
export interface MethodologyStep {
  heading: string
  paragraphs: string[]
}

export const methodologySteps: MethodologyStep[] = [
  {
    heading: '1. Deconstruct the Chaos',
    paragraphs: [
      'Forensic engineering before solution engineering. Understand the actual system \u2014 not the documented one, not the stated one, not the one someone remembers from three years ago. The real one, running in production, behaving the way it behaves.',
      'Never blame the human first. When a system fails, the system designed the conditions for that failure. The Swiss cheese model: disasters require contributing factors to align. The person who clicked the wrong button is almost never the root cause.',
      'This is empathy with teeth. It\u2019s not about being charitable \u2014 it\u2019s about being accurate.',
    ],
  },
  {
    heading: '2. Build the Tool',
    paragraphs: [
      'Once you understand the real problem, don\u2019t fix the instance. Fix the category.',
      'A one-time fix is technical debt in disguise. A tool is a permanent reduction in the problem surface. The SCORM Debugger didn\u2019t fix one testing cycle \u2014 it eliminated hours of manual clicking from every testing cycle, permanently, for everyone who used it. The cross-domain SCORM framework didn\u2019t solve one client\u2019s delivery problem \u2014 it solved the structural impossibility that was blocking all cross-domain delivery, five years before the vendor community caught up.',
      'The goal is always the tool, never the ticket.',
    ],
  },
  {
    heading: '3. Empower the User',
    paragraphs: [
      'A tool that requires its creator to operate it is a dependency, not a solution. Everything I build is documented, maintainable, and designed to outlast my involvement.',
      'This is the hardest part to get right, and the part most engineers skip. You haven\u2019t solved the problem until someone else can own the solution.',
    ],
  },
]
```

**src/components/AiClaritySection.vue** — heading + intro + 10 body paragraphs + closing methodology note:

```typescript
// src/content/sections/aiClarity.ts
export const aiClarity = {
  heading: 'On AI and This Site',
  intro: 'The same philosophy applies to how this site was written.',
  paragraphs: [
    'I write in streams of consciousness. What comes out is tangled, recursive, full of half-finished thoughts that loop back on themselves. It\u2019s accurate. It\u2019s complete. It\u2019s also exhausting to read.',
    'For years, the solution was to agonize \u2014 wordsmith the sentence, spend twenty minutes sounding \u201Cprofessional,\u201D which usually meant sounding like everyone else: fine, forgettable, friction-filled to produce.',
    'I brain-dump now, hand it to an AI, and review what comes back. The ideas are mine. The AI\u2019s job is to make it readable \u2014 to do what a good editor does, which is get out of the way of the meaning.',
    'This isn\u2019t laziness. It\u2019s the same philosophy I apply to code.',
    'Good code is self-documenting. When I inherit a codebase that requires a novel of inline comments to navigate, that\u2019s a smell \u2014 the code is failing to communicate. Professional writing has the same failure mode. The friction of converting thought to readable prose was never adding value to the thinking itself.',
    '\u201CAI wrote it\u201D is a mischaracterization. AI clarified it.',
    'The thinking, the investigation, the conclusions \u2014 those are mine. What I\u2019m outsourcing is the translation friction. My prime directive is clarity. If an AI can take a tangled first draft and make it immediately readable \u2014 and I can review it, own it, and stand behind every word \u2014 the communication succeeded.',
    'The question isn\u2019t \u201Cdid AI touch this.\u201D',
    'The question is: does the reader understand what I meant to say?',
  ],
  methodologyNote: 'Much of this site was built with AI assistance \u2014 content, code, and design. The thinking behind it is mine. The clarity is the point. That\u2019s Provider of Clarity applied to its own medium.',
}
```

Note: The AiClaritySection.vue template currently wraps "AI wrote it" and "AI clarified it" in `<em>`, and wraps the final question in `<strong>`, and the `methodology-note` paragraph in a class with inline `<em>`. Per the structural markup policy above, these are dropped in the extracted module. The template will NOT re-wrap them. This is a deliberate Phase 37 simplification; Phase 39 renders plain markdown from the same data.

**src/pages/PhilosophyPage.vue** — page-level prose (NOT counting the 3 delegated child components):

```typescript
// src/content/philosophy.ts
export interface PhilosophyQuote {
  text: string
  cite: string
}

export const designThinking = {
  heading: 'This Is What Design Thinking Looks Like When It\u2019s a Personality, Not a Process',
  paragraphs: [
    'Design thinking was invented to teach people who don\u2019t naturally do certain things to do them systematically. Empathize before you solve. Define the real problem before you build. Iterate through failure without shame.',
    'I didn\u2019t learn this from a framework. I absorbed it from air crash investigators, speedrunners, and a Three Stooges short from 1938.',
    'The result is the same disposition \u2014 but without the scaffolding, because the scaffolding was never load-bearing for me. The fish doesn\u2019t have a water strategy.',
    'What I do have is a three-step pattern that has repeated across every significant project in 28 years:',
  ],
}

export const moralSpine = {
  heading: 'The Moral Spine',
  intro: 'The three-step pattern is how I work. This is why:',
  quote: {
    text: '\u201CYou want to cheat, cheat fair \u2014 anything I hate is a crookin\u2019 crook.\u201D',
    cite: '\u2014 Moe Howard, Healthy, Wealthy and Dumb, 1938',
  } satisfies PhilosophyQuote,
  paragraphs: [
    'Every tool I build is a cheat. Automating a manual process is cheating. Jumping to any course state in seconds instead of clicking through every section is cheating. Building a framework that eliminates a class of problem is cheating.',
    'But cheating fair means the output is proper, documented, and maintainable. It means you could hand it to another engineer and they could understand, extend, and improve it. Hoarding a clever solution \u2014 keeping the elegant shortcut to yourself \u2014 is the real dishonesty. The knowledge isn\u2019t yours. It came from everyone who documented the problem before you, and it belongs to everyone who\u2019ll encounter it after you.',
    'Clever shortcuts and corner-cutting look identical from the outside. The difference is in the craftsmanship.',
  ],
}

export const influencesHeading = {
  heading: 'Five Sources That Shaped the Method',
  intro: 'These aren\u2019t abstract inspirations. They\u2019re specific sources I can point to and say: this changed how I solve problems.',
}

export const brandElementsHeading = {
  heading: 'The Six Brand Elements',
  intro: 'Six patterns extracted from 28 years of actual work \u2014 not marketing slogans, but recurring themes from real projects.',
}

export interface ColleagueQuote {
  quote: string
  cite?: string
  context?: string
  variant?: 'secondary'
}

export const colleagueQuotes: ColleagueQuote[] = [
  {
    quote: 'Thank you so much for Dan for putting time into thinking this through and writing it up.',
    cite: 'Program Manager, Microsoft Account',
    context: 'GP Strategies \u2014 on a SCORM architecture analysis document',
  },
  {
    quote: 'Dan, thank you for being such a team player with the GP team and the client, and thank you for your incredible knowledge and expertise.',
    cite: 'Account Manager, GP Strategies',
    context: 'Entergy engagement \u2014 on cross-team collaboration',
    variant: 'secondary',
  },
]

export const colleagueQuotesHeading = 'What Colleagues Say'

export const closingLine = 'Pattern 158: I cheat, but I cheat fair.'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/content/sections/ + extract Pattern158OriginSection + HowIWorkSection + AiClaritySection content modules</name>
  <read_first>
    - src/components/Pattern158OriginSection.vue (full file)
    - src/components/HowIWorkSection.vue (full file)
    - src/components/AiClaritySection.vue (full file)
    - .planning/phases/037-sfc-content-extraction/037-RESEARCH.md §Pattern 2, §Pitfall 1 (HTML entity normalization)
  </read_first>
  <files>
    src/content/sections/pattern158Origin.ts,
    src/content/sections/howIWork.ts,
    src/content/sections/aiClarity.ts
  </files>
  <action>
    1. `mkdir -p src/content/sections`.
    2. Create `src/content/sections/pattern158Origin.ts` with exactly the shape shown in the `<interfaces>` block above. Normalize: `&mdash;` → `\u2014`, `&ldquo;` → `\u201C`, `&rdquo;` → `\u201D`, `'` → `\u2019`, `<em>...</em>` → unwrap (keep the text, drop the tags).
    3. Create `src/content/sections/howIWork.ts` with the `MethodologyStep` interface and `methodologySteps` array per the `<interfaces>` block. Drop `<strong>Never blame the human first.</strong>` → plain text `"Never blame the human first. When a system fails..."` (the sentences are merged into a single paragraph).
    4. Create `src/content/sections/aiClarity.ts` with the `aiClarity` object per the `<interfaces>` block. Drop all inline `<em>` and `<strong>` wrappers. Preserve the "AI wrote it" and "AI clarified it" phrases as part of a single plain-text paragraph joined with a period. Preserve the `methodologyNote` field as a separate string (it currently has a distinct `class="methodology-note"` paragraph in the template, which the refactored template will re-add via a hardcoded `class` binding on a plain `<p>` element).
    5. NO new dependencies. NO imports from other `src/content/*` files. NO `v-html`. NO HTML entity names (`&mdash;`, `&ldquo;`, etc).
  </action>
  <verify>
    <automated>npx vue-tsc -b --force 2>&amp;1 | tail -20 &amp;&amp; npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/content/sections/pattern158Origin.ts` exits 0.
    - `test -f src/content/sections/howIWork.ts` exits 0.
    - `test -f src/content/sections/aiClarity.ts` exits 0.
    - Grep: `grep -c "export const pattern158Origin" src/content/sections/pattern158Origin.ts` returns `1`.
    - Grep: `grep -c "export const methodologySteps" src/content/sections/howIWork.ts` returns `1`.
    - Grep: `grep -c "export interface MethodologyStep" src/content/sections/howIWork.ts` returns `1`.
    - Grep: `grep -c "export const aiClarity" src/content/sections/aiClarity.ts` returns `1`.
    - Grep: `grep -cE "&mdash|&ldquo|&rdquo|&amp" src/content/sections/` returns `0`.
    - Grep across new files: `grep -cE "<em>|<strong>|v-html" src/content/sections/` returns `0`.
    - `npx vue-tsc -b --force` exits 0.
    - `npm run test:unit` exits 0 with ≥ 127 tests passing (components not yet refactored, so tests still pass against old templates).
  </acceptance_criteria>
  <done>
    Three new section content modules created with all required exports, HTML entities normalized, type check clean, tests still green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor Pattern158OriginSection.vue + HowIWorkSection.vue + AiClaritySection.vue to consume content modules</name>
  <read_first>
    - src/components/Pattern158OriginSection.vue (current full file — structure to preserve)
    - src/components/HowIWorkSection.vue (current full file)
    - src/components/AiClaritySection.vue (current full file)
    - src/content/sections/pattern158Origin.ts (just created)
    - src/content/sections/howIWork.ts (just created)
    - src/content/sections/aiClarity.ts (just created)
    - .planning/phases/037-sfc-content-extraction/037-RESEARCH.md §Pattern 3 (v-for consumption)
  </read_first>
  <files>
    src/components/Pattern158OriginSection.vue,
    src/components/HowIWorkSection.vue,
    src/components/AiClaritySection.vue
  </files>
  <action>
    **Pattern158OriginSection.vue:**
    - Add a `<script setup lang="ts">` block: `import { pattern158Origin } from '@/content/sections/pattern158Origin'`.
    - Replace `<h2>Pattern 158</h2>` with `<h2>{{ pattern158Origin.heading }}</h2>`.
    - Replace the 5 literal `<p>` elements inside `<div class="origin-story">` with `<p v-for="(para, i) in pattern158Origin.paragraphs" :key="i">{{ para }}</p>`.
    - Preserve the outer `<section id="pattern158-origin" class="content-section">` and `<div class="origin-story">` structure — only the text nodes change.

    **HowIWorkSection.vue:**
    - Add `<script setup lang="ts">` with `import { methodologySteps } from '@/content/sections/howIWork'`.
    - Replace the three hand-written `<li>` blocks with:
      ```vue
      <li v-for="step in methodologySteps" :key="step.heading">
        <h3>{{ step.heading }}</h3>
        <p v-for="(para, i) in step.paragraphs" :key="i">{{ para }}</p>
      </li>
      ```
    - Preserve the outer `<section id="how-i-work" class="content-section">` and `<ol class="methodology-steps">` wrappers.

    **AiClaritySection.vue:**
    - Add `<script setup lang="ts">` with `import { aiClarity } from '@/content/sections/aiClarity'`.
    - Replace `<h2>On AI and This Site</h2>` with `<h2>{{ aiClarity.heading }}</h2>`.
    - Replace `<p class="section-intro">The same philosophy applies to how this site was written.</p>` with `<p class="section-intro">{{ aiClarity.intro }}</p>`.
    - Replace the 9 literal `<p>` body paragraphs with `<p v-for="(para, i) in aiClarity.paragraphs" :key="i">{{ para }}</p>`. (Dropping the `<em>` wrappers on "AI wrote it"/"AI clarified it" is intentional per the structural markup policy.)
    - Replace the final `<p class="methodology-note">` paragraph with `<p class="methodology-note">{{ aiClarity.methodologyNote }}</p>` — keep the `class` binding, replace only the text node.
    - Preserve the outer `<section id="ai-and-clarity" class="content-section">` and `<div class="origin-story">` wrappers.

    **Forbidden for all three:**
    - No `v-html`.
    - No removal of the outer `<section>`/`<div>`/`<ol>` structural wrappers.
    - No changes to `class=` or `id=` attributes.
    - No new dependencies.
  </action>
  <verify>
    <automated>npm run test:unit &amp;&amp; npx vue-tsc -b --force</automated>
  </verify>
  <acceptance_criteria>
    - Grep: `grep -c "from '@/content/sections/pattern158Origin'" src/components/Pattern158OriginSection.vue` returns `1`.
    - Grep: `grep -c "from '@/content/sections/howIWork'" src/components/HowIWorkSection.vue` returns `1`.
    - Grep: `grep -c "from '@/content/sections/aiClarity'" src/components/AiClaritySection.vue` returns `1`.
    - Grep: `grep -c "Near the end of Myst" src/components/Pattern158OriginSection.vue` returns `0`.
    - Grep: `grep -c "Deconstruct the Chaos" src/components/HowIWorkSection.vue` returns `0`.
    - Grep: `grep -c "On AI and This Site" src/components/AiClaritySection.vue` returns `0`.
    - Grep: `grep -c "v-html" src/components/Pattern158OriginSection.vue src/components/HowIWorkSection.vue src/components/AiClaritySection.vue` returns `0`.
    - Grep: `grep -c 'v-for' src/components/HowIWorkSection.vue` returns at least `2` (outer step loop + inner paragraph loop).
    - `npx vue-tsc -b --force` exits 0.
    - `npm run test:unit` exits 0 with ≥ 127 tests passing.
  </acceptance_criteria>
  <done>
    Three delegated section components now consume their content modules via v-for; structural wrappers preserved; tests green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create src/content/philosophy.ts + refactor PhilosophyPage.vue</name>
  <read_first>
    - src/pages/PhilosophyPage.vue (current full file)
    - .planning/phases/037-sfc-content-extraction/037-RESEARCH.md §Pitfall 6 (Blockquote structure preservation), §C2
  </read_first>
  <files>
    src/content/philosophy.ts,
    src/pages/PhilosophyPage.vue
  </files>
  <action>
    1. Create `src/content/philosophy.ts` with the EXACT shape in the `<interfaces>` block above:
       - `export interface PhilosophyQuote`
       - `export const designThinking`
       - `export const moralSpine` (with `quote: { text, cite } satisfies PhilosophyQuote`)
       - `export const influencesHeading`
       - `export const brandElementsHeading`
       - `export interface ColleagueQuote`
       - `export const colleagueQuotes: ColleagueQuote[]`
       - `export const colleagueQuotesHeading`
       - `export const closingLine`

    2. Edit `src/pages/PhilosophyPage.vue`:
       - Add to script setup: `import { designThinking, moralSpine, influencesHeading, brandElementsHeading, colleagueQuotes, colleagueQuotesHeading, closingLine } from '@/content/philosophy'`.
       - **design-thinking section:** Replace `<h2>This Is What Design Thinking...</h2>` with `<h2>{{ designThinking.heading }}</h2>`. Replace the 4 literal `<p>` elements with `<p v-for="(para, i) in designThinking.paragraphs" :key="i">{{ para }}</p>`. Keep `<section id="design-thinking" class="content-section">` and `<div class="origin-story">` wrappers.
       - **moral-spine section:** Replace `<h2>The Moral Spine</h2>` with `<h2>{{ moralSpine.heading }}</h2>`. Replace `<p>The three-step pattern is <em>how</em> I work. This is <em>why</em>:</p>` with `<p>{{ moralSpine.intro }}</p>` (drop the `<em>` per policy). **PRESERVE the `<blockquote>` element** — replace its inner content with:
         ```vue
         <blockquote>
           <p>{{ moralSpine.quote.text }}</p>
           <cite>{{ moralSpine.quote.cite }}</cite>
         </blockquote>
         ```
         (Per Pitfall 6: keep the structural `<blockquote>` + `<cite>` tags; only the text nodes bind to data.) Replace the 3 body paragraphs with `<p v-for="(para, i) in moralSpine.paragraphs" :key="i">{{ para }}</p>`.
       - **influences section:** Replace `<h2>Five Sources That Shaped the Method</h2>` with `<h2>{{ influencesHeading.heading }}</h2>`. Replace `<p class="section-intro">` content with `{{ influencesHeading.intro }}` (drop the `<em>`). Leave `<InfluenceArticle v-for="inf in philosophyInfluences" .../>` untouched — it already consumes `@/data/philosophyInfluences`.
       - **brand-elements section:** Replace `<h2>The Six Brand Elements</h2>` with `<h2>{{ brandElementsHeading.heading }}</h2>`. Replace `<p class="section-intro">` content with `{{ brandElementsHeading.intro }}`. Leave `<BrandElement v-for="b in brandElements" .../>` untouched.
       - **testimonial section:** Replace `<h2 class="section-heading-styled">What Colleagues Say</h2>` with `<h2 class="section-heading-styled">{{ colleagueQuotesHeading }}</h2>`. Replace the 2 hardcoded `<TestimonialQuote>` elements with `<TestimonialQuote v-for="q in colleagueQuotes" :key="q.quote" :quote="q.quote" :cite="q.cite" :context="q.context" :variant="q.variant" />`.
       - **closing line:** Replace `<p class="content-section" style="text-align: center; font-style: italic;">Pattern 158: I cheat, but I cheat fair.</p>` with `<p class="content-section" style="text-align: center; font-style: italic;">{{ closingLine }}</p>`.
       - Leave `<Pattern158OriginSection />`, `<HowIWorkSection />`, `<AiClaritySection />`, `<HeroMinimal>`, and all `@/data/*` imports untouched.

    3. **Forbidden:**
       - No `v-html`.
       - No removal of `<blockquote>`, `<cite>`, `<section>`, `<div class="origin-story">` structural wrappers.
       - No changes to `useSeo({...})` arguments.
       - No changes to `useBodyClass('page-philosophy')`.
  </action>
  <verify>
    <automated>npm run test:unit &amp;&amp; npx vue-tsc -b --force &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/content/philosophy.ts` exits 0.
    - Grep: `grep -c "export const moralSpine" src/content/philosophy.ts` returns `1`.
    - Grep: `grep -c "satisfies PhilosophyQuote" src/content/philosophy.ts` returns `1`.
    - Grep: `grep -c "export const colleagueQuotes" src/content/philosophy.ts` returns `1`.
    - Grep: `grep -c "export const closingLine" src/content/philosophy.ts` returns `1`.
    - Grep: `grep -c "from '@/content/philosophy'" src/pages/PhilosophyPage.vue` returns `1`.
    - Grep: `grep -c "Design Thinking Looks Like" src/pages/PhilosophyPage.vue` returns `0`.
    - Grep: `grep -c "Moe Howard" src/pages/PhilosophyPage.vue` returns `0`.
    - Grep: `grep -c "Pattern 158: I cheat" src/pages/PhilosophyPage.vue` returns `0`.
    - Grep: `grep -c "Pattern 158: I cheat" src/content/philosophy.ts` returns `1`.
    - Grep: `grep -c "<blockquote>" src/pages/PhilosophyPage.vue` returns `1` (preserved).
    - Grep: `grep -c "<cite>" src/pages/PhilosophyPage.vue` returns `1`.
    - Grep: `grep -c "v-html" src/pages/PhilosophyPage.vue` returns `0`.
    - `npx vue-tsc -b --force` exits 0.
    - `npm run test:unit` exits 0 with ≥ 127 tests passing.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>
    `src/content/philosophy.ts` created with all page-level prose + blockquote structure; PhilosophyPage.vue renders via imported content modules; blockquote structural wrappers preserved; tests and build green.
  </done>
</task>

</tasks>

<verification>
- `npm run test:unit` passes with ≥ 127 tests.
- `npm run build` passes.
- `grep -r "Moe Howard" src/pages/ src/components/` returns no results (prose fully extracted).
- `grep -r "Moe Howard" src/content/` returns exactly 1 match (src/content/philosophy.ts).
- `grep -r "Near the end of Myst" src/components/` returns no results.
- `grep -r "Deconstruct the Chaos" src/components/` returns no results.
</verification>

<success_criteria>
- 4 new content modules exist (`pattern158Origin.ts`, `howIWork.ts`, `aiClarity.ts`, `philosophy.ts`).
- 4 SFCs refactored (Pattern158OriginSection, HowIWorkSection, AiClaritySection, PhilosophyPage).
- Moral-spine blockquote preserved as `<blockquote>` + `<cite>` (structural, not prose).
- All 127 existing unit tests still green.
- No new dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/037-sfc-content-extraction/037-02-SUMMARY.md` describing:
- Inline `<em>`/`<strong>` handling decisions (dropped vs preserved per policy)
- Blockquote structure preservation
- Any deviations from the plan
</output>
