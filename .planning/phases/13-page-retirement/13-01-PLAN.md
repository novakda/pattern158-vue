---
phase: 13-page-retirement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/PortfolioPage.vue
  - src/pages/TestimonialsPage.vue
  - src/components/FlagshipCard.vue
  - src/components/TestimonialsMetrics.vue
  - src/pages/PortfolioPage.stories.ts
  - src/pages/TestimonialsPage.stories.ts
  - src/components/FlagshipCard.stories.ts
  - src/assets/css/main.css
autonomous: true
requirements:
  - CLN-03
must_haves:
  truths:
    - "PortfolioPage.vue and TestimonialsPage.vue do not exist in the codebase"
    - "No TypeScript import or Vue Router component reference to deleted pages exists anywhere in src/"
    - "No CSS rules scoped to .page-portfolio or .page-testimonials remain in main.css"
    - "The application builds without errors after all deletions"
    - "All existing tests pass after all deletions"
  artifacts:
    - path: "src/assets/css/main.css"
      provides: "Global stylesheet with dead page CSS removed"
      contains_not: "page-portfolio"
  key_links:
    - from: "src/router.ts"
      to: "/case-files"
      via: "redirect objects for /portfolio and /testimonials"
      pattern: "redirect.*case-files"
---

<objective>
Delete 7 orphaned files (2 page components, 2 supporting components, 3 Storybook stories) and remove all dead CSS rules scoped to `.page-portfolio` and `.page-testimonials` from main.css.

Purpose: Complete the v2.0 IA restructure by removing dead code left behind after Case Files replaced Portfolio and Testimonials pages.
Output: Clean codebase with no orphaned files or dead CSS.
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-page-retirement/13-CONTEXT.md
@.planning/phases/13-page-retirement/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete 7 orphaned files and clean CSS</name>
  <read_first>
    - src/assets/css/main.css (current state -- understand mixed selector lists before editing)
    - src/router.ts (confirm redirect-only references, no component imports to deleted pages)
  </read_first>
  <files>
    src/pages/PortfolioPage.vue
    src/pages/TestimonialsPage.vue
    src/components/FlagshipCard.vue
    src/components/TestimonialsMetrics.vue
    src/pages/PortfolioPage.stories.ts
    src/pages/TestimonialsPage.stories.ts
    src/components/FlagshipCard.stories.ts
    src/assets/css/main.css
  </files>
  <action>
    **Step 1 — Delete 7 files (per D-01, D-02):**
    ```
    git rm src/pages/PortfolioPage.vue
    git rm src/pages/TestimonialsPage.vue
    git rm src/components/FlagshipCard.vue
    git rm src/components/TestimonialsMetrics.vue
    git rm src/pages/PortfolioPage.stories.ts
    git rm src/pages/TestimonialsPage.stories.ts
    git rm src/components/FlagshipCard.stories.ts
    ```

    **Step 2 — Clean CSS in src/assets/css/main.css (per D-04):**

    Remove ALL `.page-portfolio` and `.page-testimonials` rules. The regions to remove are:

    **A) Dark theme override (line ~221-223):** Delete the entire rule block:
    ```css
    [data-theme="dark"] .page-testimonials .detail-exhibit .exhibit-label {
        color: var(--color-on-accent);
    }
    ```

    **B) Mixed selector list (lines ~749-754) — SURGICAL:** Remove only these 6 selectors from the comma-separated list, keeping the rule and the `.page-philosophy` and `.page-contact` selectors intact:
    - Remove: `.page-testimonials .exhibits a:not(.exhibit-card a),`
    - Remove: `.page-testimonials .exhibits .link-primary,`
    - Remove: `.page-portfolio .portfolio-flagships a,`
    - Remove: `.page-portfolio .portfolio-flagships .link-primary,`
    - Remove: `.page-portfolio .portfolio-stats a,`
    - Remove: `.page-portfolio .portfolio-stats .link-primary {`
    The last remaining selector before the opening `{` is `.page-contact .contact-link-item .link-primary` -- change its trailing comma to ` {`.

    **C) Testimonials page block (lines ~1755-2149):** Delete the entire section from the `/* PAGE: Testimonials */` comment header through the last `.page-testimonials .metric-card li::before` rule (ends at line ~2149). The next section starts at line ~2151 with `/* PAGES: Exhibits (shared) */`.

    **D) Investigation reports heading block (lines ~3124-3143):** Delete from the comment `/* INVESTIGATION REPORTS HEADING */` through the `.page-testimonials .investigation-reports-heading .section-subtitle` rule.

    **E) Responsive mixed selector lists (lines ~3467, 3477, 3484, 3487-3488):** Within the `@media (max-width: 480px)` block, remove ONLY these selectors from their comma-separated lists:
    - Line ~3467: Remove `.page-testimonials .container,` from the container padding rule (keep `.container,`, `.page-index .container,`, `.page-exhibit .container`)
    - Line ~3477: Remove `.page-testimonials section,` from the section padding rule (keep all other selectors)
    - Line ~3484: Remove `.testimonial,` from the section padding rule (keep all other selectors)
    - Lines ~3487-3488: Remove `.page-portfolio .portfolio-flagships,` and `.page-portfolio .portfolio-directory {` from the section padding rule. The last remaining selector before `{` is `.page-technologies .tech-category` -- change its trailing comma to ` {`.

    **F) Responsive `.page-portfolio .flagship-card` (lines ~3518-3520):** Delete this standalone rule block entirely.

    **G) Responsive testimonials hero (lines ~3512-3515):** Remove `.page-testimonials .hero,` from the selector list. Keep `.page-exhibit .hero` with `{`.

    **H) Responsive testimonials-specific rules (lines ~3717-3732):** Delete all 4 rule blocks:
    - `.page-testimonials .hero h1`
    - `.page-testimonials .hero .subtitle`
    - `.page-testimonials .exhibit-header`
    - `.page-testimonials .exhibit-date`

    **I) Responsive blockquote (line ~3849-3851):** Delete the `.page-testimonials blockquote` rule block.

    **J) Portfolio page block (lines ~3971-4218):** Delete the entire section from the `/* PORTFOLIO PAGE */` comment header through the `/* Stats-bar ... */` comment (line ~4218).

    **K) Portfolio responsive block (lines ~4220-4252):** Within the `@media (max-width: 768px)` block, delete the `/* --- Portfolio Responsive --- */` comment and all `.page-portfolio` rules (lines ~4220-4251). Keep the `@media` block open -- the Industries Responsive section follows at line ~4253.

    **Step 3 — Verify router is untouched (per D-03):**
    Do NOT modify src/router.ts. The `/portfolio` and `/testimonials` redirect objects stay permanently.
  </action>
  <verify>
    <automated>cd /home/xhiris/projects/pattern158-vue && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/PortfolioPage.vue` does not exist on disk
    - `src/pages/TestimonialsPage.vue` does not exist on disk
    - `src/components/FlagshipCard.vue` does not exist on disk
    - `src/components/TestimonialsMetrics.vue` does not exist on disk
    - `src/pages/PortfolioPage.stories.ts` does not exist on disk
    - `src/pages/TestimonialsPage.stories.ts` does not exist on disk
    - `src/components/FlagshipCard.stories.ts` does not exist on disk
    - `grep -c "page-portfolio\|page-testimonials" src/assets/css/main.css` returns 0
    - `grep -c "\.page-philosophy .methodology-note" src/assets/css/main.css` returns at least 1 (philosophy selectors preserved in mixed rule)
    - `grep -c "\.page-contact .contact-link-item" src/assets/css/main.css` returns at least 1 (contact selectors preserved in mixed rule)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All 7 files deleted. All .page-portfolio and .page-testimonials CSS rules removed from main.css. Mixed selector lists preserved for live pages. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Full verification sweep</name>
  <read_first>
    - src/assets/css/main.css (confirm CSS cleanup is complete)
    - src/router.ts (confirm redirects still in place)
  </read_first>
  <files>
    (no files modified -- verification only)
  </files>
  <action>
    Run comprehensive verification to confirm no orphaned references remain:

    1. **Grep for any remaining references to deleted files in src/:**
       ```
       grep -r "PortfolioPage\|TestimonialsPage\|FlagshipCard\|TestimonialsMetrics" src/ --include="*.ts" --include="*.vue"
       ```
       Expected: Only matches in `src/router.ts` for redirect path strings (e.g., `path: '/portfolio'`), NOT component imports. Zero matches in any `.vue` file.

    2. **Grep for dead CSS selectors:**
       ```
       grep -c "page-portfolio\|page-testimonials" src/assets/css/main.css
       ```
       Expected: 0

    3. **Confirm router redirects intact:**
       ```
       grep "redirect.*case-files" src/router.ts
       ```
       Expected: 2 matches (one for /portfolio, one for /testimonials)

    4. **Run full test suite:**
       ```
       npm test
       ```
       Expected: All tests pass

    5. **Run production build:**
       ```
       npm run build
       ```
       Expected: Exit 0, no errors

    If any check fails, diagnose and fix before marking complete.
  </action>
  <verify>
    <automated>cd /home/xhiris/projects/pattern158-vue && grep -r "PortfolioPage\|TestimonialsPage\|FlagshipCard\|TestimonialsMetrics" src/ --include="*.ts" --include="*.vue" | grep -v "redirect\|path:" | grep -c "." && echo "FAIL: orphaned references found" || echo "PASS: no orphaned references" && grep -c "page-portfolio\|page-testimonials" src/assets/css/main.css | grep -q "^0$" && echo "PASS: no dead CSS" || echo "FAIL: dead CSS remains" && npm test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "PortfolioPage\|TestimonialsPage\|FlagshipCard\|TestimonialsMetrics" src/ --include="*.ts" --include="*.vue" | grep -v "redirect\|path:"` returns empty (no orphaned imports)
    - `grep -c "page-portfolio\|page-testimonials" src/assets/css/main.css` outputs `0`
    - `grep -c "redirect.*case-files" src/router.ts` outputs `2` (redirects preserved per D-03)
    - `npm test` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Zero orphaned references to deleted pages in src/. Zero dead CSS selectors. Router redirects intact. All tests pass. Build succeeds. CLN-03 complete.</done>
</task>

</tasks>

<verification>
Phase 13 is complete when:
1. All 7 files from D-01 are deleted from the codebase
2. No `.page-portfolio` or `.page-testimonials` CSS rules remain in main.css (per D-04)
3. Router redirects for `/portfolio` and `/testimonials` remain intact (per D-03)
4. `npm run build` exits 0
5. `npm test` exits 0
6. No grep hits for deleted component names in src/ (excluding router redirect path strings)
</verification>

<success_criteria>
- CLN-03 satisfied: PortfolioPage.vue and TestimonialsPage.vue retired
- 7 files deleted, ~116 CSS lines removed, 0 new files created
- Application builds and all tests pass
- Mixed CSS selector lists for philosophy and contact pages preserved
</success_criteria>

<output>
After completion, create `.planning/phases/13-page-retirement/13-01-SUMMARY.md`
</output>
