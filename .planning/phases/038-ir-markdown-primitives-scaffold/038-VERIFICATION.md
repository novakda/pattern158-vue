---
phase: 038-ir-markdown-primitives-scaffold
verified: 2026-04-10T19:15:00Z
status: passed
score: 16/16 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 38: IR + Markdown Primitives + Scaffold — Verification Report

**Phase Goal:** Establish the Phase 38 foundation of the Static Markdown Export Pipeline — pnpm + scripts tsconfig + vitest scripts project + three new devDeps, the Intermediate Representation (DocNode discriminated union, InlineSpan inline tree, PageDoc wrapper), four context-specific escape helpers (prose, table-cell, wikilink, code-block), deterministic frontmatter YAML serializer with canonical key order, nine IR primitive factories (text/heading/paragraph/link/wikilink/caption + list/table/blockquote), and the docs/ directory collision audit for Phase 44.

**Verified:** 2026-04-10T19:15:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                                                                                                                                       | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `scripts/markdown-export/` scaffold exists with ir/, escape/, frontmatter/, primitives/ subdirs and no `.gitkeep` placeholders for future-phase dirs                                                                                                                                          | VERIFIED   | `ls scripts/markdown-export/` returns exactly: `escape`, `frontmatter`, `index.ts`, `ir`, `primitives`. No `extractors/`, `renderers/`, `fs-writer/`, or `.gitkeep` files (`find … -name .gitkeep` empty).                                                                                                                                                                                                                                                                                              |
| 2   | `tsconfig.scripts.json` exists at repo root and is wired into root `tsconfig.json` via project references                                                                                                                                                                                     | VERIFIED   | `tsconfig.scripts.json` (22 lines): composite=true, rootDir=., include=`scripts/markdown-export/**/*.ts`, paths={} (forbids `@/` aliases). Root `tsconfig.json` line 21-23: `"references": [{ "path": "./tsconfig.scripts.json" }]`.                                                                                                                                                                                                                                                                    |
| 3   | Vitest `scripts` project added to `vitest.config.ts`                                                                                                                                                                                                                                        | VERIFIED   | `vitest.config.ts` lines 40-48: third project named `scripts`, include=`scripts/markdown-export/**/*.test.ts`, environment=`node`, globals=true. Coexists with `unit` and `browser` projects.                                                                                                                                                                                                                                                                                                            |
| 4   | `pnpm-lock.yaml` exists; `package-lock.json` deleted                                                                                                                                                                                                                                          | VERIFIED   | `ls /home/xhiris/projects/pattern158-vue/pnpm-lock.yaml` succeeds; `ls package-lock.json` returns "No such file or directory".                                                                                                                                                                                                                                                                                                                                                                          |
| 5   | `tsx`, `yaml`, `github-slugger` added as devDependencies in `package.json`                                                                                                                                                                                                                  | VERIFIED   | `package.json` devDependencies: `github-slugger: 2.0.0` (line 35), `tsx: 4.21.0` (line 39), `yaml: 2.8.3` (line 46). All three present and pinned.                                                                                                                                                                                                                                                                                                                                                       |
| 6   | `build:markdown` and `test:scripts` scripts present in `package.json`; `build` chain updated                                                                                                                                                                                                  | VERIFIED   | `package.json` scripts: `"build": "vue-tsc -b && vite build && pnpm build:markdown"` (line 8), `"build:markdown": "tsx scripts/markdown-export/index.ts"` (line 9), `"test:scripts": "vitest run --project scripts"` (line 16).                                                                                                                                                                                                                                                                          |
| 7   | `scripts/markdown-export/ir/types.ts` exports DocNode discriminated union, InlineSpan, PageDoc, HeadingLevel, assertNever                                                                                                                                                                     | VERIFIED   | `ir/types.ts` (98 lines): exports `DocNode` (6 kinds: heading\|paragraph\|list\|table\|blockquote\|hr), `InlineSpan` (6 kinds: text\|strong\|emphasis\|code\|link\|image), `PageDoc` interface, `HeadingLevel = 1\|2\|3\|4\|5\|6`, `assertNever`. Plus per-kind node types (HeadingNode, ParagraphNode, ListNode, ListItem, TableNode, BlockquoteNode, HRNode, TextSpan, StrongSpan, EmphasisSpan, CodeSpan, LinkSpan, ImageSpan).                                                                          |
| 8   | Four escape helpers exist at `scripts/markdown-export/escape/{prose,table-cell,wikilink,code-block}.ts` with co-located test files                                                                                                                                                            | VERIFIED   | All 4 source files present: `prose.ts` (45 lines, full backslash + special-char + BOM strip + amp entity), `table-cell.ts` (20 lines, calls `escapeProse` + CRLF/LF→`<br>`), `wikilink.ts` (33 lines, filesystem sanitize + markdown escape per D-19), `code-block.ts` (31 lines, fence-length escalation only, content untouched). All 4 co-located `.test.ts` files exist (50 + 81 + 73 + 45 lines).                                                                                                       |
| 9   | Frontmatter serializer at `scripts/markdown-export/frontmatter/serialize.ts` with canonical key order (title → aliases → tags → date → cssclasses), plural-only keys, block-style arrays                                                                                                       | VERIFIED   | `serialize.ts` (101 lines): builds `ordered` object in canonical order title→aliases→tags→date→cssclasses (lines 62-75); `FORBIDDEN_SINGULAR_KEYS = ['tag','alias','cssclass']` throws with descriptive message (lines 11, 48-55); title is forced `Scalar.QUOTE_DOUBLE` via `quotedScalar` (lines 23-27, 63); `Document.toString` called with `collectionStyle: 'block'` and `lineWidth: 0` (lines 89-96); empty arrays omitted (`if (input.aliases && input.aliases.length > 0)`); LF-only output (`---\n…---\n`). |
| 10  | Nine primitives at `scripts/markdown-export/primitives/` — text, heading, paragraph, link, wikilink, caption, list, table, blockquote — each with co-located test file                                                                                                                       | VERIFIED   | All 9 source files present (text 11, heading 16, paragraph 14, link 12, wikilink 26, caption 19, list 51, table 37, blockquote 22 lines), all 9 co-located `.test.ts` files present (25 + 55 + 38 + 38 + 38 + 41 + 67 + 73 + 45 lines). Each is a thin factory returning a typed IR object.                                                                                                                                                                                                                |
| 11  | Primitives contain ZERO rendering logic and ZERO escape logic                                                                                                                                                                                                                                | VERIFIED   | `grep -rn "escape\|render\|markdown" primitives/*.ts` shows only doc comments asserting "ZERO escaping, ZERO rendering logic" and references to D-09. No call to any escape helper, no string concatenation that produces markdown syntax. Test files explicitly assert "does NOT escape" behaviour. The single `WIKILINK_HREF_PREFIX = 'wikilink://'` sentinel is metadata for the renderer, not rendering itself.                                                                                          |
| 12  | `038-DOCS-AUDIT.md` exists with verdict for Phase 44's docs/ directory                                                                                                                                                                                                                       | VERIFIED   | `.planning/phases/038-ir-markdown-primitives-scaffold/038-DOCS-AUDIT.md` (130 lines) covers Vite (`dist/`), Storybook (`storybook-static/`), Vitest coverage (`coverage/`), Wrangler (`./dist`), other generators, current `.gitignore` state (no `docs` matches), current `.gitattributes` state (file does not exist), and ends with explicit "GO — Phase 44 may create and track docs/" verdict plus 4 derived prerequisites for Phase 44.                                                                  |
| 13  | `pnpm test:scripts` passes (expect 143/143)                                                                                                                                                                                                                                                  | VERIFIED   | `pnpm test:scripts` output: `Test Files  15 passed (15)`, `Tests  143 passed (143)`. Duration 268 ms.                                                                                                                                                                                                                                                                                                                                                                                                  |
| 14  | `pnpm test` passes (expect 292/292)                                                                                                                                                                                                                                                          | VERIFIED   | `pnpm test` output: `Test Files  34 passed (34)`, `Tests  292 passed (292)`. Duration 3.28 s.                                                                                                                                                                                                                                                                                                                                                                                                            |
| 15  | `pnpm build` passes                                                                                                                                                                                                                                                                          | VERIFIED   | `pnpm build` ran `vue-tsc -b && vite build && pnpm build:markdown` end-to-end. Vite produced 24 dist artifacts in 880 ms; build:markdown emitted the generated banner; exit code 0.                                                                                                                                                                                                                                                                                                                     |
| 16  | `pnpm build:markdown` runs successfully                                                                                                                                                                                                                                                      | VERIFIED   | `pnpm build:markdown` → `tsx scripts/markdown-export/index.ts` → emits `<!-- auto-generated by scripts/markdown-export — do not edit by hand -->` to stdout, exits 0. Placeholder orchestrator (real wiring is Phase 43) confirmed at `scripts/markdown-export/index.ts` lines 1-15.                                                                                                                                                                                                                       |

**Score:** 16/16 truths verified

### Required Artifacts

| Artifact                                                  | Expected                                                                              | Status     | Details                                                                                                                                                            |
| --------------------------------------------------------- | ------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `tsconfig.scripts.json`                                   | Composite project, includes `scripts/markdown-export/**/*.ts`, NodeNext, paths={}     | VERIFIED   | 22 lines. composite=true, rootDir=., target=ES2022, module=NodeNext, types=["node"], paths={} (forbids @-aliases), include matches scripts only.                    |
| `tsconfig.json` references                                | Project reference to `./tsconfig.scripts.json`                                        | VERIFIED   | Line 21-23 contains the reference.                                                                                                                                  |
| `vitest.config.ts` `scripts` project                      | Third project named `scripts`, include scripts/markdown-export tests                 | VERIFIED   | Lines 40-48: name `scripts`, environment `node`, include `scripts/markdown-export/**/*.test.ts`.                                                                    |
| `pnpm-lock.yaml`                                          | Present at repo root                                                                  | VERIFIED   | Present.                                                                                                                                                            |
| `package-lock.json`                                       | Absent                                                                                | VERIFIED   | Not present.                                                                                                                                                        |
| `package.json` devDeps                                    | tsx, yaml, github-slugger pinned                                                      | VERIFIED   | Lines 35 (`github-slugger: 2.0.0`), 39 (`tsx: 4.21.0`), 46 (`yaml: 2.8.3`).                                                                                         |
| `package.json` scripts                                    | `build:markdown`, `test:scripts`, `build` chain                                       | VERIFIED   | Lines 8, 9, 16; `build` chains vue-tsc → vite build → build:markdown.                                                                                              |
| `scripts/markdown-export/index.ts`                        | Placeholder orchestrator that exits 0                                                 | VERIFIED   | 16 lines, writes generated banner to stdout, returns. Phase 43 will replace with real orchestrator.                                                                  |
| `scripts/markdown-export/ir/types.ts`                     | DocNode + InlineSpan + PageDoc + HeadingLevel + assertNever                           | VERIFIED   | 98 lines. All required exports present. `assertNever(value: never): never` exhaustiveness helper at lines 95-97.                                                    |
| `scripts/markdown-export/ir/types.test.ts`                | Type-shape assertions                                                                 | VERIFIED   | 180 lines, 17 `it` blocks. Asserts shape of every node type and exhaustiveness via `assertNever`.                                                                    |
| `scripts/markdown-export/escape/prose.ts`                 | escapeProse with full D-19 character set                                              | VERIFIED   | 45 lines. Order: backslash first, then `*_[]\`<>#!\|~`. Strips BOM, converts `&` → `&amp;`, preserves NBSP per spec.                                                  |
| `scripts/markdown-export/escape/table-cell.ts`            | escapeTableCell = escapeProse + newline → `<br>`                                      | VERIFIED   | 20 lines. Imports `escapeProse`, then CRLF→`<br>` then LF→`<br>` (correct order).                                                                                  |
| `scripts/markdown-export/escape/wikilink.ts`              | Filesystem sanitize + markdown escape per D-19                                        | VERIFIED   | 33 lines. Filesystem set `[/, \, :, *, ?, ", <, >]` → `-`; markdown set `[\|, [, ], #, ^]` → `\\…`. Filesystem precedence documented.                              |
| `scripts/markdown-export/escape/code-block.ts`            | Fence-length escalation only, content unchanged                                       | VERIFIED   | 31 lines. Returns `{ content, fence }`; computes `Math.max(3, longestInnerRun + 1)`. Content NEVER mutated.                                                          |
| `scripts/markdown-export/escape/*.test.ts` (4 files)      | Co-located unit tests                                                                 | VERIFIED   | prose 81 / table-cell 45 / wikilink 73 / code-block 50 lines. All pass.                                                                                              |
| `scripts/markdown-export/frontmatter/serialize.ts`        | Canonical key order, plural-only, block-style, deterministic                          | VERIFIED   | 101 lines. Locked decisions D-16/D-17 + PROJECT.md singular-key ban. Title forced QUOTE_DOUBLE; arrays block-style; lineWidth 0; LF-only doc separators.                 |
| `scripts/markdown-export/frontmatter/serialize.test.ts`   | Coverage for canonical order, forbidden keys, byte-determinism                        | VERIFIED   | 142 lines, 21 `it` blocks. Includes determinism, forbidden-key error, wikilink quoting.                                                                              |
| `scripts/markdown-export/primitives/text.ts`              | TextSpan factory                                                                      | VERIFIED   | 11 lines. `text(value)` returns `{kind:'text', value}`.                                                                                                              |
| `scripts/markdown-export/primitives/heading.ts`           | HeadingNode factory accepting string \| InlineSpan[]                                  | VERIFIED   | 16 lines. Sugar wraps strings via `text()`. Level constrained by HeadingLevel type.                                                                                  |
| `scripts/markdown-export/primitives/paragraph.ts`         | ParagraphNode factory                                                                 | VERIFIED   | 14 lines. Sugar wraps strings via `text()`. No newline splitting.                                                                                                    |
| `scripts/markdown-export/primitives/link.ts`              | LinkSpan factory                                                                      | VERIFIED   | 12 lines. `link(href, content)`; sugar wraps strings via `text()`.                                                                                                   |
| `scripts/markdown-export/primitives/wikilink.ts`          | LinkSpan with sentinel prefix for Phase 42 renderer                                   | VERIFIED   | 26 lines. `WIKILINK_HREF_PREFIX = 'wikilink://'`. No escape — D-09 documented.                                                                                      |
| `scripts/markdown-export/primitives/caption.ts`           | ParagraphNode wrapping EmphasisSpan wrapping TextSpan                                 | VERIFIED   | 19 lines. Per VAULT-09; visual italic in both renderers.                                                                                                             |
| `scripts/markdown-export/primitives/list.ts`              | ListNode factory with array \| object sugar                                           | VERIFIED   | 51 lines. String items wrapped via `paragraph()` to satisfy block-only ListItem children invariant.                                                                  |
| `scripts/markdown-export/primitives/table.ts`             | TableNode factory with per-cell string sugar                                          | VERIFIED   | 37 lines. Cells: string → `[text(s)]`, InlineSpan[] → unchanged. No pipe escaping (renderer's job).                                                                  |
| `scripts/markdown-export/primitives/blockquote.ts`        | BlockquoteNode wrapping DocNode[] (NOT InlineSpan[])                                  | VERIFIED   | 22 lines. String sugar wraps via `[paragraph(s)]`. Plain DocNode[] passed through (no double wrap).                                                                   |
| `scripts/markdown-export/primitives/*.test.ts` (9 files)  | Co-located unit tests                                                                 | VERIFIED   | text 25 / heading 55 / paragraph 38 / link 38 / wikilink 38 / caption 41 / list 67 / table 73 / blockquote 45 lines. All pass.                                       |
| `.planning/phases/038-…/038-DOCS-AUDIT.md`                | Audit + GO/NO-GO verdict for Phase 44                                                 | VERIFIED   | 130 lines. All 5 audited tools listed; .gitignore + .gitattributes state captured; verdict GO; 4 Phase 44 prerequisites derived.                                     |

### Key Link Verification

| From                                                       | To                                            | Via                                                  | Status | Details                                                                                                                                            |
| ---------------------------------------------------------- | --------------------------------------------- | ---------------------------------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| `tsconfig.json`                                            | `tsconfig.scripts.json`                       | `references: [{ path: "./tsconfig.scripts.json" }]`  | WIRED  | Line 21-23.                                                                                                                                        |
| `vitest.config.ts` `scripts` project                       | `scripts/markdown-export/**/*.test.ts`        | `include` glob                                       | WIRED  | Lines 43-44; 15 test files matched, 143 tests collected and passed.                                                                                |
| `package.json` `build:markdown`                            | `scripts/markdown-export/index.ts`            | `tsx scripts/markdown-export/index.ts`               | WIRED  | Line 9; `pnpm build:markdown` runs and emits banner; exit 0.                                                                                       |
| `package.json` `build`                                     | `pnpm build:markdown`                         | `&& pnpm build:markdown`                             | WIRED  | Line 8; full chain executed end-to-end successfully.                                                                                               |
| `package.json` `test:scripts`                              | Vitest scripts project                        | `vitest run --project scripts`                       | WIRED  | Line 16; resolves to the project defined in `vitest.config.ts`.                                                                                    |
| `escape/table-cell.ts`                                     | `escape/prose.ts`                             | `import { escapeProse } from './prose.js'`           | WIRED  | Line 11; called at line 15 of `escapeTableCell`.                                                                                                   |
| `frontmatter/serialize.ts`                                 | `yaml` package                                | `import { Document, Scalar } from 'yaml'`            | WIRED  | Line 9; `Document` and `Scalar` used for canonical-order serialization.                                                                            |
| `primitives/heading.ts`                                    | `ir/types.ts` + `primitives/text.ts`          | `import type { HeadingLevel, … }` + `import { text }` | WIRED  | Lines 7-8.                                                                                                                                         |
| `primitives/paragraph.ts`                                  | `ir/types.ts` + `primitives/text.ts`          | type-only + value imports                            | WIRED  | Lines 8-9.                                                                                                                                         |
| `primitives/link.ts`                                       | `ir/types.ts` + `primitives/text.ts`          | type-only + value imports                            | WIRED  | Lines 6-7.                                                                                                                                         |
| `primitives/wikilink.ts`                                   | `ir/types.ts` + `primitives/text.ts`          | type-only + value imports                            | WIRED  | Lines 13-14.                                                                                                                                       |
| `primitives/caption.ts`                                    | `ir/types.ts` + `primitives/text.ts`          | type-only + value imports                            | WIRED  | Lines 10-11.                                                                                                                                       |
| `primitives/list.ts`                                       | `ir/types.ts` + `primitives/paragraph.ts`     | type-only + value imports                            | WIRED  | Lines 15-16. (`paragraph` used to wrap string items into ListItem children.)                                                                       |
| `primitives/table.ts`                                      | `ir/types.ts` + `primitives/text.ts`          | type-only + value imports                            | WIRED  | Lines 17-18.                                                                                                                                       |
| `primitives/blockquote.ts`                                 | `ir/types.ts` + `primitives/paragraph.ts`     | type-only + value imports                            | WIRED  | Lines 15-16.                                                                                                                                       |

### Data-Flow Trace (Level 4)

Phase 38 ships only IR primitives, escape helpers, a YAML serializer, and a placeholder orchestrator. No artifact in this phase renders dynamic data sourced from a store, fetch, or DB query — every public function takes its inputs directly as arguments and returns a pure value. Level 4 (data-flow trace) is therefore N/A for this phase. The "data flow" of the orchestrator (`index.ts`) is intentionally a no-op until Phase 43 (`ORCH-01`) wires extractors → renderers → writer.

### Behavioral Spot-Checks

| Behavior                                                       | Command                       | Result                                                          | Status |
| -------------------------------------------------------------- | ----------------------------- | --------------------------------------------------------------- | ------ |
| Scripts test project produces 143 passing tests                | `pnpm test:scripts`           | `Test Files 15 passed (15)`, `Tests 143 passed (143)`          | PASS   |
| Full Vitest run (unit + browser + scripts) produces 292 passes | `pnpm test`                   | `Test Files 34 passed (34)`, `Tests 292 passed (292)`          | PASS   |
| Build chain runs end-to-end (vue-tsc → vite → build:markdown)  | `pnpm build`                  | vue-tsc clean, Vite built 24 artifacts in 880 ms, build:markdown banner emitted, exit 0 | PASS   |
| Markdown-export placeholder orchestrator runs                  | `pnpm build:markdown`         | Emits `<!-- auto-generated by scripts/markdown-export — do not edit by hand -->` and exits 0 | PASS   |

### Requirements Coverage

REQUIREMENTS.md is intentionally absent from this repo (per prior phase summaries — the project tracks requirement IDs in PROJECT.md and per-plan frontmatter, not in a separate REQUIREMENTS.md). Cross-reference is performed against PLAN frontmatter `requirements:` arrays plus the explicit ID list provided in the verification request.

| Requirement | Source Plan      | Description                                                                | Status     | Evidence                                                                                                                          |
| ----------- | ---------------- | -------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------- |
| SCAF-01     | 038-01-PLAN.md   | pnpm migration (`package-lock.json` deleted, `pnpm-lock.yaml` present)    | SATISFIED  | `pnpm-lock.yaml` exists; `package-lock.json` absent; all 3 new devDeps installed via pnpm.                                        |
| SCAF-02     | 038-01-PLAN.md   | `tsconfig.scripts.json` + project reference + `scripts` Vitest project   | SATISFIED  | Both configs present and wired (see truths #2, #3).                                                                                |
| SCAF-03     | 038-01-PLAN.md   | `scripts/markdown-export/` scaffold + build script chain                  | SATISFIED  | Truths #1, #6 verified; `pnpm build` runs full chain (truth #15).                                                                   |
| IR-01       | 038-02-PLAN.md   | `DocNode` discriminated union (6 kinds)                                   | SATISFIED  | `ir/types.ts` lines 38-44.                                                                                                         |
| IR-02       | 038-02-PLAN.md   | `InlineSpan` discriminated union (6 kinds) + `PageDoc` interface         | SATISFIED  | `ir/types.ts` lines 11-32 (InlineSpan) and 84-91 (PageDoc).                                                                        |
| ESCP-01     | 038-03-PLAN.md   | `escapeProse` covering D-19 character set                                 | SATISFIED  | `escape/prose.ts`, 81-line test file passes.                                                                                       |
| ESCP-02     | 038-03-PLAN.md   | `escapeTableCell` superset (newline → `<br>`)                            | SATISFIED  | `escape/table-cell.ts`, 45-line test file passes.                                                                                  |
| ESCP-03     | 038-03-PLAN.md   | `escapeWikilinkTarget` (filesystem + markdown escape)                    | SATISFIED  | `escape/wikilink.ts`, 73-line test file passes.                                                                                   |
| ESCP-04     | 038-03-PLAN.md   | `escapeCodeBlockContent` fence-length escalation                          | SATISFIED  | `escape/code-block.ts`, 50-line test file passes.                                                                                  |
| FM-01       | 038-04-PLAN.md   | `serializeFrontmatter` canonical order + plural-only enforcement          | SATISFIED  | `frontmatter/serialize.ts`, 142-line test file passes.                                                                             |
| FM-02       | 038-04-PLAN.md   | Block-style arrays, byte-deterministic output, LF-only line endings      | SATISFIED  | `serialize.ts` `Document.toString({collectionStyle: 'block', lineWidth: 0})`; output wrapped in `---\n…---\n`.                      |
| PRIM-01     | 038-06 + 038-07  | Nine IR primitives with ZERO escape and ZERO render logic                 | SATISFIED  | All 9 source files + 9 co-located test files; grep confirms no escape/render calls in primitives (truth #11).                       |
| AUDT-01     | 038-05-PLAN.md   | `docs/` directory collision audit with verdict                            | SATISFIED  | `038-DOCS-AUDIT.md` 130 lines, GO verdict, 4 Phase 44 prerequisites derived.                                                       |

**Decision references in shipped code (D-01..D-19):**

| Decision | Referenced In                                              | Status     |
| -------- | ---------------------------------------------------------- | ---------- |
| D-06     | `ir/types.ts` line 7 (DocNode union shape)                 | TRACED     |
| D-07     | `ir/types.ts` line 7 (structured InlineSpan tree)          | TRACED     |
| D-08     | `ir/types.ts` line 7 (PageDoc shape)                       | TRACED     |
| D-09     | `primitives/text.ts`, `list.ts`, `table.ts`, `blockquote.ts`, `wikilink.ts` | TRACED     |
| D-10     | `primitives/list.ts` line 5, `table.ts` line 5, `heading.test.ts` line 22 | TRACED     |
| D-16     | `frontmatter/serialize.ts` line 6 (canonical key order)    | TRACED     |
| D-17     | `frontmatter/serialize.ts` line 6, line 58 (wikilink quoting) | TRACED     |
| D-18     | `038-DOCS-AUDIT.md` line 5 (collision audit decision)      | TRACED     |
| D-19     | `escape/prose.ts` line 4, `wikilink.ts` line 4 (escape character set) | TRACED     |
| D-01..D-05, D-11..D-15 | Locked in 038-CONTEXT.md but not directly cited inside Phase 38 source code (informational decisions about data flow / pipeline shape that bind future phases) | INFORMATIONAL — no Phase 38 artifact required to cite them; they constrain Phase 39+ implementations |

All decision IDs that bind shipped Phase 38 artifacts are traceable. Decisions that bind only future-phase artifacts (D-01 through D-05, D-11 through D-15) are intentionally not referenced in Phase 38 code — they govern Phase 39 (extractors), Phase 41 (mono renderer), Phase 42 (Obsidian renderer), and Phase 43 (orchestrator). This is correct behavior per the Phase 38 scope definition in `038-CONTEXT.md`.

### Anti-Patterns Found

| File                                         | Line | Pattern                              | Severity | Impact                                                                                                                                                |
| -------------------------------------------- | ---- | ------------------------------------ | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| `scripts/markdown-export/index.ts`           | 8-13 | Empty `main()` function              | INFO     | Intentional placeholder per PLAN — comments at lines 9-11 explicitly call out that real orchestrator wiring is Phase 43 (`ORCH-01`). Not a stub.      |

No blocker or warning anti-patterns found. The placeholder orchestrator is documented in the file header, in the SUMMARY, and in the ROADMAP — it satisfies the SCAF-03 must-have ("`pnpm build:markdown` exits 0") and explicitly defers real wiring to Phase 43. No `TODO`/`FIXME`/`XXX`/`HACK` markers found in any shipped Phase 38 source file. No empty handlers, no `return null` stubs, no hardcoded empty data flowing to user-visible output. Tests assert positive behavior (`does NOT escape`, `preserves content verbatim`) as a deliberate D-09 contract, not as missing functionality.

### Inversion Pass (Could This Be Wrong?)

Per the Inversion thinking model, three ways this phase could appear correct but actually be broken:

1. **Primitives could secretly call escape helpers via dynamic import or string concat.** Checked with `grep -rn "escape\|render\|markdown" primitives/*.ts` — only doc-comment matches (assertions of "ZERO escape"); no `import.*escape`, no template literal that looks like markdown syntax. Also confirmed by reading every primitive source file end-to-end.
2. **`tsconfig.scripts.json` could be present but not actually used by `vue-tsc -b`.** The `pnpm build` command was run end-to-end and completed successfully. `vue-tsc -b` honors project references, and `tsconfig.scripts.json` is `composite: true` so it participates in the build graph.
3. **`pnpm test:scripts` could pass but actually run zero tests.** Output explicitly says `Test Files 15 passed (15)` and `Tests 143 passed (143)`, matching the expected count and the count of `.test.ts` files in `scripts/markdown-export/**`.

### Confirmation Bias Counter (Disconfirmation Pass)

1. **Partially-met requirement?** Searched for any plan must-have not fully delivered. The 038-01 truth "no `extractors/`, no `renderers/`, no `fs-writer/`, no `.gitkeep` files" is verified by directory listing — only the four expected subdirs and `index.ts` are present. No partial coverage found.
2. **Test that passes but does not test the stated behavior?** Spot-checked `escape/prose.test.ts` — its assertions cover empty string, plain text, every escape character in D-19, BOM strip, ampersand entity, idempotence concerns. Spot-checked `frontmatter/serialize.test.ts` — its 21 `it` blocks include determinism (running twice and comparing bytes), forbidden-singular-key error, wikilink quoting, and key order. Tests are substantive, not snapshot-only.
3. **Error path with no test coverage?** `serializeFrontmatter` throws on forbidden singular keys — this branch is covered (test asserts the throw with descriptive message). `escapeCodeBlockContent` does not throw at all (it always returns a result), so no error path to test. `assertNever` in `ir/types.ts` is an unreachable-by-design helper; its purpose is compile-time exhaustiveness, not runtime testing — the runtime branch fires only on type-system bypass.

No disconfirmation findings — verification result is robust.

### Human Verification Required

None. Phase 38 ships only library code (IR types, escape helpers, frontmatter serializer, IR primitives, scaffold + tooling), not user-facing UI or visual artifacts. All deliverables are verifiable programmatically via grep, file inspection, and the test suite (which passes 292/292). No real-time behavior, no external services, no visual appearance changes, no user flows. The next phase that requires human verification is Phase 42 (Obsidian Vault Renderer — manual Obsidian QA checkpoint per ROADMAP line 170).

### Gaps Summary

No gaps found. Every must-have, every required artifact, every key link, and every behavioral spot-check passes. All declared requirement IDs (SCAF-01/02/03, IR-01/02, ESCP-01/02/03/04, FM-01/02, PRIM-01, AUDT-01) are satisfied by shipped code and tests. Decision references D-06/07/08/09/10/16/17/18/19 are traceable inside the codebase; D-01..D-05 and D-11..D-15 govern future-phase artifacts and are correctly absent from Phase 38 source. The build chain works end-to-end, all 292 tests pass (including the 143 new scripts-project tests), and the docs/ collision audit reaches a clear GO verdict for Phase 44.

Phase 38 achieves its goal: the Static Markdown Export Pipeline foundation is in place — pnpm migration complete, scripts tsconfig wired, vitest scripts project running, three new devDeps installed, IR types defined as discriminated unions with exhaustiveness helpers, four context-specific escape helpers shipped with co-located tests, a deterministic block-style frontmatter YAML serializer in place, nine pure IR primitive factories with ZERO escape/render logic, and the docs/ directory cleared for Phase 44.

---

_Verified: 2026-04-10T19:15:00Z_
_Verifier: Claude (gsd-verifier)_
