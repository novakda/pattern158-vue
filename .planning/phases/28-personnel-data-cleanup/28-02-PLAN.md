---
phase: 28-personnel-data-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - src/data/json/exhibits.json
  - src/data/exhibits.test.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-04
  - DATA-05
must_haves:
  truths:
    - "No personnel entry has a title or role stored in the name field"
    - "Every personnel entry across all 14 exhibits has an entryType value"
    - "All 7 group entries are marked entryType group"
    - "All anonymized/title-only entries are marked entryType anonymized"
    - "All named individuals are marked entryType individual"
  artifacts:
    - path: "src/data/json/exhibits.json"
      provides: "Clean personnel data with entryType on all entries"
    - path: "src/data/exhibits.test.ts"
      provides: "Validation tests for DATA-01, DATA-04, DATA-05"
  key_links:
    - from: "src/data/exhibits.test.ts"
      to: "src/data/json/exhibits.json"
      via: "test assertions verify data correctness"
      pattern: "entryType.*group|entryType.*anonymized|entryType.*individual"
---

<objective>
Fix all title-as-name field misplacements (DATA-01), add entryType markers to every personnel entry across all 14 exhibits (DATA-04, DATA-05), and add comprehensive validation tests.

Purpose: Completes the personnel data cleanup so all entries have correct field placement and typed classification, enabling Phase 29 card UX rendering.
Output: Clean personnel data, comprehensive test coverage.
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-personnel-data-cleanup/28-CONTEXT.md
@.planning/phases/28-personnel-data-cleanup/28-01-SUMMARY.md

<interfaces>
From src/types/exhibit.ts (after Plan 01):
```typescript
export interface PersonnelEntry {
  name?: string
  title?: string
  organization?: string
  role?: string
  involvement?: string
  entryType?: 'individual' | 'group' | 'anonymized'
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix title-as-name entries and add entryType to all personnel</name>
  <files>src/data/json/exhibits.json</files>
  <read_first>
    - src/data/json/exhibits.json (all personnel arrays across all exhibits)
    - src/types/exhibit.ts (PersonnelEntry interface for reference)
  </read_first>
  <action>
Modify every personnel entry in `src/data/json/exhibits.json`. This is the complete specification of changes per exhibit. For each entry, perform TWO operations: (A) fix title-as-name if applicable, and (B) add entryType.

**Classification rules:**
- `entryType: "individual"` — entry represents a single named or anonymized-but-singular person
- `entryType: "group"` — entry represents multiple people (contains numbers like "15+", "22+", "Multiple", "Team")
- `entryType: "anonymized"` — entry has no person name, only a title/role description (e.g., "Program Manager", "QA Lead")

**EXHIBIT A** (12 entries):
- [0] Dan Novak: ADD `"entryType": "individual"`. No name fix needed. ALSO add `"isSelf": true` if not already present (it is).
- [1] Tracey Nicholson: ADD `"entryType": "individual"`. No fix.
- [2] Quinn Gladu: ADD `"entryType": "individual"`. No fix.
- [3] Chris Sproule: ADD `"entryType": "individual"`. No fix.
- [4] Josh Stoudt: ADD `"entryType": "individual"`. No fix.
- [5] Chris Emmons: ADD `"entryType": "individual"`. No fix.
- [6] "Multiple EB Personnel": ADD `"entryType": "group"`. Name field is acceptable for groups (it IS the group name).
- [7] Program Manager (no name): ADD `"entryType": "anonymized"`. No name fix (name is already absent).
- [8] Co-Investigator (no name): ADD `"entryType": "anonymized"`. No name fix.
- [9] Curriculum Developer (no name): ADD `"entryType": "anonymized"`. No name fix.
- [10] Curriculum Developer (no name): ADD `"entryType": "anonymized"`. No name fix.
- [11] Training Analyst Specialist (no name): ADD `"entryType": "anonymized"`. No name fix.

**EXHIBIT B** (7 entries):
- [0] Dan Novak: ADD `"entryType": "individual"`. No fix.
- [1] Chris Sproule: ADD `"entryType": "individual"`. No fix.
- [2] Josh Stoudt: ADD `"entryType": "individual"`. No fix.
- [3] FIX NAME: Current name is `"Senior Vice President"` which is a title. REMOVE `name` field. Keep `title` as `"SVP of Learning Solutions (initiated EB engagement Sept 2017)"`. Keep organization. ADD `"entryType": "anonymized"`.
  Result: `{"title": "SVP of Learning Solutions (initiated EB engagement Sept 2017)", "organization": "GP Strategies", "entryType": "anonymized"}`
- [4] Tracey Nicholson: ADD `"entryType": "individual"`. No fix.
- [5] Quinn Gladu: ADD `"entryType": "individual"`. No fix.
- [6] "GP On-Site EB Team": ADD `"entryType": "group"`. Group name in name field is acceptable.

**EXHIBIT C** (4 entries):
- [0] Dan Novak: ADD `"entryType": "individual"`. No fix.
- [1] FIX NAME: Current name is `"Content Team Manager"` which is a title. REMOVE `name` field. Keep `title` as `"Manager, Content Team (led the overall refresh project)"`. ADD `"entryType": "anonymized"`.
  Result: `{"title": "Manager, Content Team (led the overall refresh project)", "organization": "GP Strategies", "entryType": "anonymized"}`
- [2] Name is `"Colleague (\"The Slacker\")"` — this is a descriptive name/nickname, not a title. Keep as-is. ADD `"entryType": "anonymized"` (person is anonymized via nickname).
- [3] "15+ Team Members": ADD `"entryType": "group"`. Group name in name field is acceptable.

**EXHIBIT D** (6 entries):
- [0] Dan Novak: ADD `"entryType": "individual"`. No fix.
- [1] Chris Sproule: ADD `"entryType": "individual"`. No fix.
- [2] FIX NAME: Current name is `"QA Lead"` which is a title. REMOVE `name` field. Keep `title` as `"QA Coordinator (close collaborator on functional testing)"`. ADD `"entryType": "anonymized"`.
  Result: `{"title": "QA Coordinator (close collaborator on functional testing)", "organization": "GP Strategies", "entryType": "anonymized"}`
- [3] FIX NAME: Current name is `"Program Manager"` which is a title. REMOVE `name` field. Keep `title` as `"Engagement coordination and client relationships"`. ADD `"entryType": "anonymized"`.
  Result: `{"title": "Program Manager — Engagement coordination and client relationships", "organization": "GP Strategies", "entryType": "anonymized"}`
  Note: Merge the old name value into title since the current title is a description, not a title. Format: "Program Manager — Engagement coordination and client relationships".
- [4] FIX NAME: Current name is `"Project Lead"` which is a title. REMOVE `name` field. Keep `title` as `"Coordinated rapid-turnaround testing requests with Dan"`. ADD `"entryType": "anonymized"`.
  Result: `{"title": "Project Lead — Coordinated rapid-turnaround testing requests with Dan", "organization": "GP Strategies", "entryType": "anonymized"}`
  Note: Same merge pattern — old name becomes prefix of title.
- [5] FIX NAME: Current name is `"Wells Fargo Contacts"` which is a group. REMOVE `name` field, move to title. ADD `"entryType": "group"`.
  Result: `{"title": "Wells Fargo Contacts — Subject matter experts and content owners", "organization": "Wells Fargo", "entryType": "group"}`

**EXHIBIT E** (4 entries):
- [0] Dan Novak: ADD `"entryType": "individual"`. No fix.
- [1] Zach Taylor: ADD `"entryType": "individual"`. No fix.
- [2] Ron Cyran: ADD `"entryType": "individual"`. No fix.
- [3] Tom Pizer: ADD `"entryType": "individual"`. No fix.

**EXHIBIT F** (7 entries):
- [0] Dan Novak: ADD `"entryType": "individual"`. No fix.
- [1] FIX NAME: Current name `"Director of Learning Technologies"` is a title. REMOVE `name` field. Merge into title. ADD `"entryType": "anonymized"`.
  Result: `{"title": "Director of Learning Technologies — Management chain and technical oversight", "organization": "GP Strategies", "entryType": "anonymized"}`
- [2] FIX NAME: Current name `"HSBC Account PM/BA"` is a title. REMOVE `name` field. Merge into title. ADD `"entryType": "anonymized"`.
  Result: `{"title": "HSBC Account PM/BA — Engagement coordination, client relationship management", "organization": "GP Strategies", "entryType": "anonymized"}`
- [3] FIX NAME: Current name `"Lead Developer"` is a title. REMOVE `name` field. Merge into title. ADD `"entryType": "anonymized"`.
  Result: `{"title": "Lead Developer — HSBC deliverables development", "organization": "GP Strategies", "entryType": "anonymized"}`
- [4] FIX NAME: Current name `"Performance Consultant (Mexico)"` is a title. REMOVE `name` field. Merge into title. ADD `"entryType": "anonymized"`.
  Result: `{"title": "Performance Consultant (Mexico) — Localized deployments", "organization": "GP Strategies", "entryType": "anonymized"}`
- [5] FIX NAME: Current name `"Learning Consultant (UK)"` is a title. REMOVE `name` field. Merge into title. ADD `"entryType": "anonymized"`.
  Result: `{"title": "Learning Consultant (UK) — HSBC global operations", "organization": "GP Strategies", "entryType": "anonymized"}`
- [6] FIX NAME: Current name `"22+ US Contacts"` is a group. REMOVE `name` field. ADD `"entryType": "group"`.
  Result: `{"title": "22+ US Contacts — Plus regional representatives", "organization": "HSBC (US, UK, Mexico, India, Philippines)", "entryType": "group"}`

**EXHIBIT G** (4 entries):
- [0] Dan Novak: ADD `"entryType": "individual"`. No fix.
- [1] FIX NAME: Current name `"SunTrust Developer"` is a title. REMOVE `name` field. Merge into title. ADD `"entryType": "anonymized"`.
  Result: `{"title": "SunTrust Developer — API and backend development", "organization": "SunTrust (now Truist)", "entryType": "anonymized"}`
- [2] FIX NAME: Current name `"Project Manager/Account Lead"` is a title. REMOVE `name` field. Merge into title. ADD `"entryType": "anonymized"`.
  Result: `{"title": "Project Manager/Account Lead — SunTrust account management and engagement coordination", "organization": "GP Strategies", "entryType": "anonymized"}`
- [3] FIX NAME: Current name `"Director of Learning Technologies"` is a title. REMOVE `name` field. Merge into title. ADD `"entryType": "anonymized"`.
  Result: `{"title": "Director of Learning Technologies — Technical leadership and oversight", "organization": "GP Strategies", "entryType": "anonymized"}`

**EXHIBIT H** (3 entries):
- [0] Dan Novak: ADD `"entryType": "individual"`. No fix.
- [1] FIX NAME: Current name `"LMS Configuration Specialist"` is a title. REMOVE `name` field. Merge into title. ADD `"entryType": "anonymized"`.
  Result: `{"title": "LMS Configuration Specialist — LMS Administrator", "organization": "GP Strategies", "entryType": "anonymized"}`
  Wait — current title is "LMS Administrator" which is redundant with name "LMS Configuration Specialist". Consolidate:
  Result: `{"title": "LMS Configuration Specialist / LMS Administrator", "organization": "GP Strategies", "entryType": "anonymized"}`
- [2] FIX NAME: Current name `"Client Liaison"` is a title. REMOVE `name` field. Merge into title. ADD `"entryType": "anonymized"`.
  Result: `{"title": "Client Liaison / Project Manager", "organization": "GP Strategies / Client", "entryType": "anonymized"}`
  Note: Current title is "Project Manager" — merge both roles with slash.

**EXHIBIT I** (3 entries):
- [0] Dan Novak: ADD `"entryType": "individual"`. No fix.
- [1] FIX NAME: Current name `"2 Additional Testers"` is a group. REMOVE `name` field. ADD `"entryType": "group"`.
  Result: `{"title": "2 Additional Testers — GP Accessibility Testing Team for TD MIC", "organization": "GP Strategies", "entryType": "group"}`
- [2] FIX NAME: Current name `"Senior Learning PM"` is a title. REMOVE `name` field. Merge into title. ADD `"entryType": "anonymized"`.
  Result: `{"title": "Senior Learning PM — TD MIC project coordination and program oversight", "organization": "GP Strategies", "entryType": "anonymized"}`

**EXHIBIT J** (3 entries):
- [0] FIX NAME: Current name `"Investigation Lead"` is a title. REMOVE `name` field. Keep existing `title` ("Director, GP Strategies") and `role`. Merge old name into title. ADD `"entryType": "anonymized"`.
  Result: `{"title": "Investigation Lead / Director", "organization": "GP Strategies", "role": "Project authorization, stakeholder management, investigation oversight", "entryType": "anonymized"}`
  Note: Extract "GP Strategies" from old title to organization field.
- [1] Dan Novak: ADD `"entryType": "individual"`. No fix.
- [2] FIX NAME: Current name `"Data Analyst / UX Researcher"` is a title. REMOVE `name` field. The current `title` is "GP Strategies" which is actually an organization. Fix: move to organization, use old name as title. ADD `"entryType": "anonymized"`.
  Result: `{"title": "Data Analyst / UX Researcher", "organization": "GP Strategies", "role": "Data analysis, UX evaluation, user interviews, behavioral research", "entryType": "anonymized"}`

**EXHIBIT K** (5 entries):
- [0] Max Glick: ADD `"entryType": "individual"`. No fix.
- [1] Dan Novak: ADD `"entryType": "individual"`. No fix.
- [2] FIX NAME: Current name `"Instructional Designer"` is a title. REMOVE `name` field. Merge into title. ADD `"entryType": "anonymized"`.
  Result: `{"title": "Instructional Designer — Content Design", "organization": "GP Strategies", "entryType": "anonymized"}`
- [3] FIX NAME: Current name `"Project Manager"` is a title. REMOVE `name` field. Merge into title. ADD `"entryType": "anonymized"`.
  Result: `{"title": "Project Manager — Program Coordination", "organization": "GP Strategies", "entryType": "anonymized"}`
- [4] FIX NAME: Current name `"MCAPS Lead"` is a title. REMOVE `name` field. Merge into title. ADD `"entryType": "anonymized"`.
  Result: `{"title": "MCAPS Lead — Microsoft Account Lead", "organization": "Microsoft", "entryType": "anonymized"}`

**EXHIBIT L** (4 entries, already normalized in Plan 01):
- [0] Dan Novak: ADD `"entryType": "individual"`.
- [1] External Power Platform Consultancy (no name): ADD `"entryType": "anonymized"`.
- [2] Lead Developer (anonymized) (no name): ADD `"entryType": "anonymized"`.
- [3] Governance Architect (anonymized) (no name): ADD `"entryType": "anonymized"`.

**EXHIBIT M** (2 entries):
- [0] Dan Novak: ADD `"entryType": "individual"`. No fix.
- [1] Max Glick: ADD `"entryType": "individual"`. No fix.

**EXHIBIT N** (2 entries):
- [0] Dan Novak: ADD `"entryType": "individual"`. No fix.
- [1] FIX NAME: Current name `"Leo Learning Team"` is a group. REMOVE `name` field. ADD `"entryType": "group"`.
  Result: `{"title": "Leo Learning Team — Platform Development", "organization": "Leo Learning / GP Strategies", "entryType": "group"}`

**SUMMARY COUNTS for verification:**
- Total personnel entries across 14 exhibits: 83
- entryType "individual": 35 (all named persons)
- entryType "group": 7 (Exhibit A[6], B[6], C[3], D[5], F[6], I[1], N[1])
- entryType "anonymized": 41 (title-only entries + Exhibit A[7-11] + Exhibit C[2] + Exhibit L[1-3] + all fixed title-as-name entries)
- Total: 35 + 7 + 41 = 83

**Title merge pattern:** When removing a title-as-name and the entry already has a title that is descriptive (not a proper title), merge as "OldName — OldTitle". When both are proper titles, merge with " / ".
  </action>
  <verify>
    <automated>cd /home/xhiris/projects/pattern158-vue && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - No personnel entry has a `name` field containing a non-person value (titles, roles, group names are removed from name)
    - Exception: group entries like "Multiple EB Personnel", "GP On-Site EB Team", "15+ Team Members" had name moved to title
    - Exception: "Colleague (\"The Slacker\")" keeps name as-is (it is a descriptive person name)
    - Every personnel entry has an `entryType` field with value "individual", "group", or "anonymized"
    - Exactly 7 entries have `entryType: "group"`
    - TypeScript compiles with zero errors
  </acceptance_criteria>
  <done>All 83 personnel entries have correct field placement and entryType markers</done>
</task>

<task type="auto">
  <name>Task 2: Add comprehensive validation tests for DATA-01, DATA-04, DATA-05</name>
  <files>src/data/exhibits.test.ts</files>
  <read_first>
    - src/data/exhibits.test.ts (current test file after Plan 01 updates)
    - src/data/json/exhibits.json (to verify entry counts)
  </read_first>
  <action>
Add a new `describe` block in `src/data/exhibits.test.ts` after the existing personnel tests:

```typescript
describe('DATA-01/DATA-04/DATA-05: personnel data cleanup', () => {
  const allPersonnel = exhibits.flatMap(e => e.personnel || [])

  it('no personnel entry has a title stored in the name field (DATA-01)', () => {
    // Titles/roles that must NOT appear in name fields
    const titlePatterns = [
      /^Senior Vice President$/,
      /^Content Team Manager$/,
      /^QA Lead$/,
      /^Program Manager$/,
      /^Project Lead$/,
      /^Director of/,
      /^Lead Developer$/,
      /^Performance Consultant/,
      /^Learning Consultant/,
      /^HSBC Account/,
      /^SunTrust Developer$/,
      /^LMS Configuration/,
      /^Client Liaison$/,
      /^Senior Learning PM$/,
      /^Investigation Lead$/,
      /^Data Analyst/,
      /^Instructional Designer$/,
      /^MCAPS Lead$/,
    ]
    const entriesWithName = allPersonnel.filter(p => p.name)
    entriesWithName.forEach(p => {
      titlePatterns.forEach(pattern => {
        expect(p.name).not.toMatch(pattern)
      })
    })
  })

  it('every personnel entry has an entryType (DATA-03 populated)', () => {
    allPersonnel.forEach(p => {
      expect(p.entryType).toBeDefined()
      expect(['individual', 'group', 'anonymized']).toContain(p.entryType)
    })
  })

  it('total personnel count is 83', () => {
    expect(allPersonnel).toHaveLength(83)
  })

  it('exactly 7 group entries (DATA-04)', () => {
    const groups = allPersonnel.filter(p => p.entryType === 'group')
    expect(groups).toHaveLength(7)
  })

  it('group entries are in the expected exhibits', () => {
    const exhibitsWithGroups = exhibits
      .filter(e => (e.personnel || []).some(p => p.entryType === 'group'))
      .map(e => e.label)
      .sort()
    expect(exhibitsWithGroups).toEqual([
      'Exhibit A', 'Exhibit B', 'Exhibit C', 'Exhibit D',
      'Exhibit F', 'Exhibit I', 'Exhibit N'
    ])
  })

  it('Exhibit A has 5 anonymized entries (DATA-05)', () => {
    const exhibitA = exhibits.find(e => e.label === 'Exhibit A')
    const anonymized = (exhibitA?.personnel || []).filter(p => p.entryType === 'anonymized')
    expect(anonymized).toHaveLength(5)
  })

  it('individual entries all have a name field', () => {
    const individuals = allPersonnel.filter(p => p.entryType === 'individual')
    individuals.forEach(p => {
      expect(p.name).toBeTruthy()
    })
  })

  it('anonymized entries do not have a name field (except nickname entries)', () => {
    const anonymized = allPersonnel.filter(p => p.entryType === 'anonymized')
    const withName = anonymized.filter(p => p.name)
    // Only Exhibit C "Colleague (\"The Slacker\")" should have a name
    expect(withName).toHaveLength(1)
    expect(withName[0].name).toContain('Slacker')
  })
})
```

Run the full test suite to confirm all tests pass.
  </action>
  <verify>
    <automated>cd /home/xhiris/projects/pattern158-vue && npm test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - New describe block "DATA-01/DATA-04/DATA-05: personnel data cleanup" exists in test file
    - Test "every personnel entry has an entryType" passes
    - Test "exactly 7 group entries" passes
    - Test "Exhibit A has 5 anonymized entries" passes
    - Test "no personnel entry has a title stored in the name field" passes
    - `npm test` exits with 0 failures across ALL test files
  </acceptance_criteria>
  <done>All validation tests pass, confirming DATA-01, DATA-04, DATA-05 requirements are met</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected. Static JSON data modifications and test additions only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02 | I (Info Disclosure) | exhibits.json | accept | Personnel data is public portfolio content; field corrections do not change exposure |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits with zero errors
2. `npm test` passes all tests
3. No `name` field contains a title/role (grep verification: no entry should match title patterns in name)
4. Every personnel entry has `entryType` field
5. Exactly 7 entries have `entryType: "group"`
6. Exhibit A entries 7-11 have `entryType: "anonymized"`
</verification>

<success_criteria>
- All title-as-name field misplacements corrected (DATA-01)
- All 7 group entries marked with entryType "group" (DATA-04)
- All anonymized entries marked with entryType "anonymized" (DATA-05)
- All named individuals marked with entryType "individual"
- Comprehensive test suite validates all requirements
- TypeScript compiles cleanly, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/28-personnel-data-cleanup/28-02-SUMMARY.md`
</output>
