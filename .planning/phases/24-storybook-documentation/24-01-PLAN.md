---
phase: 24-storybook-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/FindingsTable.stories.ts
autonomous: true
requirements:
  - DOC-01

must_haves:
  truths:
    - "Storybook renders a 2-column FindingsTable story (finding + description)"
    - "Storybook renders a 3-column FindingsTable story with severity badges"
    - "Storybook renders a 3-column FindingsTable story with background/resolution columns"
  artifacts:
    - path: "src/components/FindingsTable.stories.ts"
      provides: "CSF3 Storybook stories for FindingsTable"
      contains: "TwoColumnVariant"
      exports: ["default", "TwoColumnVariant", "SeverityVariant", "BackgroundResolutionVariant"]
  key_links:
    - from: "src/components/FindingsTable.stories.ts"
      to: "src/components/FindingsTable.vue"
      via: "default import"
      pattern: "import FindingsTable from './FindingsTable.vue'"
---

<objective>
Create Storybook stories for FindingsTable covering all three column detection variants.

Purpose: Document the FindingsTable component's automatic column adaptation so variants are visually reviewable without navigating exhibit pages.
Output: FindingsTable.stories.ts with 3 named story exports.
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Component props from FindingsTable.vue -->
```typescript
// Props
findings: ExhibitFindingEntry[]
heading?: string

// Column pattern detection (internal computed):
// - severity: if any entry has .severity
// - background: if any entry has .background
// - default: finding + description only
```

<!-- Type from src/data/exhibits.ts -->
```typescript
export interface ExhibitFindingEntry {
  finding: string
  description?: string
  background?: string
  resolution?: string
  severity?: string
}
```

<!-- CSF3 story pattern from PersonnelCard.stories.ts -->
```typescript
import type { Meta, StoryObj } from '@storybook/vue3-vite'
import Component from './Component.vue'

const meta = {
  title: 'Components/ComponentName',
  component: Component,
} satisfies Meta<typeof Component>

export default meta
type Story = StoryObj<typeof meta>

export const VariantName: Story = {
  args: {
    propName: [/* inline mock data */],
  },
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FindingsTable Storybook stories</name>
  <files>src/components/FindingsTable.stories.ts</files>
  <read_first>
    - src/components/PersonnelCard.stories.ts (CSF3 pattern reference)
    - src/components/FindingsTable.vue (component props and column detection logic)
    - src/data/exhibits.ts (ExhibitFindingEntry interface — lines 41-47)
  </read_first>
  <action>
Create src/components/FindingsTable.stories.ts following the exact CSF3 pattern from PersonnelCard.stories.ts:

1. Meta configuration:
   - title: 'Components/FindingsTable'
   - component: FindingsTable

2. Story: TwoColumnVariant
   - Mimics Exhibit E pattern (finding + description, no severity/background)
   - args.findings: 3 entries with only `finding` and `description` fields populated
   - Use realistic data like: "Cross-Domain SCORM Framework" / "Engineered cross-domain communication..."

3. Story: SeverityVariant
   - Mimics Exhibit L pattern (finding + severity column)
   - args.findings: 3 entries with `finding`, `description`, and `severity` fields
   - Severity values like "High", "Medium", "Low"
   - args.heading: "Audit Findings" (tests custom heading)

4. Story: BackgroundResolutionVariant
   - Mimics Exhibit A pattern (finding + background + resolution)
   - args.findings: 3 entries with `finding`, `background`, and `resolution` fields
   - Use realistic data about process improvements/engineering decisions

All mock data should be inline (not imported), with plausible professional content.
  </action>
  <verify>
    <automated>npx storybook build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "TwoColumnVariant" src/components/FindingsTable.stories.ts
    - grep -q "SeverityVariant" src/components/FindingsTable.stories.ts
    - grep -q "BackgroundResolutionVariant" src/components/FindingsTable.stories.ts
    - grep -q "title: 'Components/FindingsTable'" src/components/FindingsTable.stories.ts
    - grep -q "import FindingsTable" src/components/FindingsTable.stories.ts
  </acceptance_criteria>
  <done>FindingsTable.stories.ts exists with 3 named exports covering all column detection patterns, Storybook builds without errors</done>
</task>

</tasks>

<verification>
- `npx storybook build` completes without errors
- File exports: default, TwoColumnVariant, SeverityVariant, BackgroundResolutionVariant
- Each story uses distinct column pattern triggering different FindingsTable rendering modes
</verification>

<success_criteria>
All three FindingsTable column variants are documented as Storybook stories and render correctly in a Storybook build.
</success_criteria>

<output>
After completion, create `.planning/phases/24-storybook-documentation/24-01-SUMMARY.md`
</output>
