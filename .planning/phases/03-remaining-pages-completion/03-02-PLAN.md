---
phase: 03-remaining-pages-completion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/AccessibilityPage.vue
  - src/pages/ReviewPage.vue
  - src/pages/PhilosophyPage.vue
autonomous: true
requirements: [PAGE-04, PAGE-06, PAGE-07]
must_haves:
  truths:
    - "AccessibilityPage renders all 8+ prose sections from 11ty source with no TODO text"
    - "ReviewPage renders a professional placeholder page, not a TODO stub"
    - "PhilosophyPage uses HeroMinimal component instead of inline hero markup"
    - "All three pages use HeroMinimal component"
    - "No .html hrefs exist in any of the three pages"
  artifacts:
    - path: "src/pages/AccessibilityPage.vue"
      provides: "Full accessibility statement content from 11ty source"
      contains: "HeroMinimal"
    - path: "src/pages/ReviewPage.vue"
      provides: "Professional placeholder page"
      contains: "HeroMinimal"
    - path: "src/pages/PhilosophyPage.vue"
      provides: "Existing page with HeroMinimal adopted"
      contains: "import HeroMinimal"
  key_links:
    - from: "src/pages/AccessibilityPage.vue"
      to: "HeroMinimal component"
      via: "import and usage"
      pattern: "import HeroMinimal from '@/components/HeroMinimal.vue'"
    - from: "src/pages/ReviewPage.vue"
      to: "HeroMinimal component"
      via: "import and usage"
      pattern: "import HeroMinimal from '@/components/HeroMinimal.vue'"
---

<objective>
Port AccessibilityPage and ReviewPage from 11ty HTML (or as placeholder), and adopt HeroMinimal on PhilosophyPage. Also audit ContactPage for content parity (PAGE-04).

Purpose: Complete Wave 1 content porting for the simpler/prose-heavy pages. These pages run parallel with Plan 01's data-heavy pages.

Output: Three updated Vue page files — AccessibilityPage with full prose content, ReviewPage with professional placeholder, PhilosophyPage with HeroMinimal replacing inline hero.
</objective>

<execution_context>
@C:/main/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@C:/main/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-remaining-pages-completion/03-CONTEXT.md
@.planning/phases/03-remaining-pages-completion/03-RESEARCH.md

<interfaces>
From src/components/HeroMinimal.vue:
```vue
<script setup lang="ts">
defineProps<{
  title: string
  subtitle?: string
}>()
</script>
<template>
  <section class="hero-minimal">
    <div class="container">
      <h1>{{ title }}</h1>
      <p v-if="subtitle" class="subtitle">{{ subtitle }}</p>
      <slot />
    </div>
  </section>
</template>
```
</interfaces>

Canonical 11ty source: https://github.com/novakda/pattern158.solutions/tree/deploy/20260315-feat-auto-generate-deploy-branch-names-f
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port AccessibilityPage + ReviewPage from 11ty HTML</name>
  <files>src/pages/AccessibilityPage.vue, src/pages/ReviewPage.vue</files>
  <read_first>
    - src/pages/AccessibilityPage.vue (current TODO stub)
    - src/pages/ReviewPage.vue (current TODO stub)
    - src/components/HeroMinimal.vue (component interface)
    - src/pages/HomePage.vue (established page template pattern — composable calls, imports, fragment structure)
    - Fetch 11ty source: https://github.com/novakda/pattern158.solutions/blob/deploy/20260315-feat-auto-generate-deploy-branch-names-f/accessibility.html
  </read_first>
  <action>
    **AccessibilityPage.vue** — Replace the TODO stub with full content from the 11ty accessibility.html source.

    Script setup section:
    - Keep existing useBodyClass('page-accessibility') and useSeo() calls
    - Add import: `import HeroMinimal from '@/components/HeroMinimal.vue'`

    Template structure (Vue 3 fragment — no wrapper div):
    1. `<HeroMinimal title="Accessibility Statement">` with lead paragraph in default slot if present in 11ty source
    2. All prose sections from 11ty source — expected sections from research:
       - Commitment section
       - Standards section (ul list)
       - Testing section with `<dl class="definition-list">` (PRESERVE the `definition-list` CSS class)
       - Current Status section (ul list)
       - Browsers section
       - Features section
       - Known Issues section
       - Feedback section
       - Technical Specs section
    3. Each section wrapped in `<section class="content-section">` with `<div class="container">`
    4. Preserve all CSS classes from 11ty source
    5. Convert ANY .html hrefs to `<router-link :to="...">` — all internal links must use router-link
    6. Content stays inline per locked decision (prose-heavy pages do not get data files)

    IMPORTANT: Copy ALL sections from the 11ty source verbatim. Do not summarize or skip sections.

    **ReviewPage.vue** — Replace the TODO stub with a professional placeholder (NO review.html exists in 11ty source).

    Script setup section:
    - Keep existing useBodyClass and useSeo() calls
    - Add import: `import HeroMinimal from '@/components/HeroMinimal.vue'`

    Template structure:
    1. `<HeroMinimal title="Review" subtitle="Stakeholder review area" />`
    2. One content section with a brief professional note: "This page is reserved for stakeholder review of ongoing engagements. For project inquiries, please visit the contact page." Include a `<router-link to="/contact">contact page</router-link>` link.
    3. Keep it minimal — this is intentionally sparse, not a TODO stub

    The ReviewPage is NOT in the NavBar and has no 11ty source. It is a Vue-only route for private use.
  </action>
  <verify>
    <automated>grep -c "TODO" src/pages/AccessibilityPage.vue src/pages/ReviewPage.vue | grep -v ":0$" | wc -l | grep "^0$" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/AccessibilityPage.vue contains `import HeroMinimal from '@/components/HeroMinimal.vue'`
    - src/pages/AccessibilityPage.vue contains `<HeroMinimal title="Accessibility Statement"`
    - src/pages/AccessibilityPage.vue contains NO string "TODO"
    - src/pages/AccessibilityPage.vue contains `definition-list` class on a dl element
    - src/pages/AccessibilityPage.vue contains `useBodyClass('page-accessibility')` (or equivalent)
    - `grep '\.html"' src/pages/AccessibilityPage.vue` returns zero results
    - src/pages/ReviewPage.vue contains `import HeroMinimal from '@/components/HeroMinimal.vue'`
    - src/pages/ReviewPage.vue contains `<HeroMinimal title="Review"`
    - src/pages/ReviewPage.vue contains NO string "TODO"
    - src/pages/ReviewPage.vue contains `router-link` to contact page
    - Template uses Vue 3 fragment pattern (no wrapper div at root) for both pages
  </acceptance_criteria>
  <done>AccessibilityPage renders all prose sections from 11ty source with HeroMinimal and no TODO text. ReviewPage renders a professional placeholder with HeroMinimal and router-link to contact.</done>
</task>

<task type="auto">
  <name>Task 2: Adopt HeroMinimal on PhilosophyPage + audit ContactPage</name>
  <files>src/pages/PhilosophyPage.vue, src/pages/ContactPage.vue</files>
  <read_first>
    - src/pages/PhilosophyPage.vue (179 lines — currently has inline hero markup to replace)
    - src/pages/ContactPage.vue (128 lines — already uses HeroMinimal, audit content parity)
    - src/components/HeroMinimal.vue (component interface)
    - Fetch 11ty source for contact audit: https://github.com/novakda/pattern158.solutions/blob/deploy/20260315-feat-auto-generate-deploy-branch-names-f/contact.html
    - Fetch 11ty source for philosophy: https://github.com/novakda/pattern158.solutions/blob/deploy/20260315-feat-auto-generate-deploy-branch-names-f/philosophy.html
  </read_first>
  <action>
    **PhilosophyPage.vue** — Replace the inline hero section with HeroMinimal component.

    Changes:
    1. Add `import HeroMinimal from '@/components/HeroMinimal.vue'` (keep existing TestimonialQuote import)
    2. Replace the inline hero block:
       ```vue
       <!-- REMOVE THIS: -->
       <section class="hero-minimal">
         <div class="container">
           <h1>Philosophy</h1>
           <p class="subtitle">How I think about engineering problems</p>
         </div>
       </section>

       <!-- REPLACE WITH: -->
       <HeroMinimal title="Philosophy" subtitle="How I think about engineering problems" />
       ```
    3. Check 11ty philosophy.html source — verify title and subtitle match. If 11ty has different hero text, use the 11ty version.
    4. Do NOT extract any other sections in this task — PhilosophyPage component extraction happens in Plan 04
    5. Verify no .html hrefs exist (existing router-links to exhibits should already be correct)

    **ContactPage.vue** — Audit for content parity with 11ty source.

    Changes:
    1. ContactPage already imports and uses HeroMinimal — confirm it matches 11ty hero content
    2. Compare ContactPage.vue content against 11ty contact.html section by section:
       - Contact methods (email, social links)
       - Guidance section (what to include)
       - Preferences (work types)
       - Exclusions
       - Testimonial section
    3. If any content is missing from the 11ty source, add it
    4. If content matches, make no changes (do not refactor for line count yet — that's COMP-03 in Plan 04)
    5. Verify no .html hrefs exist: `grep '\.html"' src/pages/ContactPage.vue` returns zero

    PAGE-04 is satisfied when ContactPage content matches 11ty source. Line count reduction (COMP-03) is addressed in Plan 04.
  </action>
  <verify>
    <automated>grep -c "import HeroMinimal" src/pages/PhilosophyPage.vue | grep -v "^0$" && grep -c '<section class="hero-minimal">' src/pages/PhilosophyPage.vue | grep "^0$" && grep -c '\.html"' src/pages/ContactPage.vue | grep "^0$" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/PhilosophyPage.vue contains `import HeroMinimal from '@/components/HeroMinimal.vue'`
    - src/pages/PhilosophyPage.vue contains `<HeroMinimal title=`
    - src/pages/PhilosophyPage.vue does NOT contain `<section class="hero-minimal">` (replaced by component)
    - src/pages/PhilosophyPage.vue still contains `import TestimonialQuote` (existing import preserved)
    - `grep '\.html"' src/pages/PhilosophyPage.vue` returns zero results
    - `grep '\.html"' src/pages/ContactPage.vue` returns zero results
    - ContactPage.vue content verified against 11ty contact.html (all sections present)
  </acceptance_criteria>
  <done>PhilosophyPage uses HeroMinimal instead of inline hero. ContactPage audited for content parity with 11ty source — no .html hrefs remain in either page.</done>
</task>

</tasks>

<verification>
After both tasks:
- `grep -r 'TODO' src/pages/AccessibilityPage.vue src/pages/ReviewPage.vue` returns zero results
- `grep -r '\.html"' src/pages/AccessibilityPage.vue src/pages/ReviewPage.vue src/pages/PhilosophyPage.vue src/pages/ContactPage.vue` returns zero results
- All four pages import and use HeroMinimal (ContactPage already does)
- `npm run build` completes without errors
</verification>

<success_criteria>
AccessibilityPage and ReviewPage are ported (or placeholdered), PhilosophyPage adopts HeroMinimal, and ContactPage is audited for content parity. All four pages have no TODO stubs and no .html hrefs.
</success_criteria>

<output>
After completion, create `.planning/phases/03-remaining-pages-completion/03-02-SUMMARY.md`
</output>
