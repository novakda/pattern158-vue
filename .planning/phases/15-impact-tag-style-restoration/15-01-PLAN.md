---
phase: 15-impact-tag-style-restoration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/assets/css/main.css
autonomous: false
requirements:
  - CSS-01
  - CSS-02

must_haves:
  truths:
    - "Impact tags on the Case Files listing page render as styled pills with background color, border-radius, and padding"
    - "Impact tags on exhibit detail pages render with the same pill styling"
    - "Impact tag containers use flexbox wrap layout so tags flow naturally with consistent gap spacing"
  artifacts:
    - path: "src/assets/css/main.css"
      provides: "Base .impact-tag and .impact-tags CSS rules"
      contains: ".impact-tag {"
  key_links:
    - from: "src/components/TechTags.vue"
      to: "src/assets/css/main.css"
      via: "class='impact-tag' on span elements"
      pattern: "\\.impact-tag \\{"
    - from: "src/components/ExhibitCard.vue"
      to: "src/assets/css/main.css"
      via: "class='impact-tags' wrapper div"
      pattern: "\\.impact-tags \\{"
---

<objective>
Restore the base `.impact-tag` and `.impact-tags` CSS rules that were accidentally deleted in Phase 13 (commit 72b0a11) as "dead CSS". These rules provide pill/badge styling for technology impact tags used on both the Case Files listing page (via ExhibitCard.vue > TechTags.vue) and exhibit detail pages (via EngineeringBriefLayout.vue / InvestigationReportLayout.vue > TechTags.vue).

Purpose: Impact tags currently render as unstyled inline text because the base CSS was removed. The contextual override rules (`.tech-tags .impact-tag`, `.page-exhibit .impact-tag`, `.page-exhibit-detail .exhibit-impact-tags`) still exist but have no base rule to override.

Output: Restored CSS in main.css, impact tags visually render as pills.
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- The deleted CSS (from git show 72b0a11) that must be restored: -->

```css
/* Impact tags — shared across testimonials and exhibit pages */
.impact-tags {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-sm);
    margin-top: var(--space-md);
}

.impact-tag {
    background: var(--color-inverse);
    color: var(--color-inverse-text);
    padding: var(--space-xs) var(--space-sm);
    border-radius: var(--radius-pill);
    font-size: var(--font-size-sm);
}

.impact-tag.highlight {
    background: var(--color-primary);
}
```

<!-- Existing selectors in main.css that depend on these base rules: -->
<!-- Line 205: [data-theme="dark"] .impact-tag.highlight -->
<!-- Line 209: [data-theme="dark"] .impact-tag.highlight .key-fact -->
<!-- Line 1891: .impact-tag.highlight .key-fact -->
<!-- Line 2135: .impact-tags-container (margin wrapper, separate concept) -->
<!-- Line 2961: .tech-tags .impact-tag (font-size/padding override for tech cards) -->
<!-- Line 3468: .page-exhibit .impact-tags (mobile padding override) -->
<!-- Line 3472: .page-exhibit .impact-tag (mobile font-size/padding override) -->
<!-- Line 4024: .page-exhibit-detail .exhibit-impact-tags (margin/padding/border wrapper) -->

<!-- Components that use these classes: -->
<!-- src/components/TechTags.vue: <span class="impact-tag"> -->
<!-- src/components/ExhibitCard.vue: <div class="impact-tags"> wrapping <TechTags> -->
<!-- src/components/exhibit/EngineeringBriefLayout.vue: <div class="exhibit-impact-tags"> wrapping <TechTags> -->
<!-- src/components/exhibit/InvestigationReportLayout.vue: <div class="exhibit-impact-tags"> wrapping <TechTags> -->

<!-- CSS tokens already defined in :root: -->
<!-- --color-inverse, --color-inverse-text, --color-primary -->
<!-- --space-xs, --space-sm, --space-md -->
<!-- --radius-pill: 20px -->
<!-- --font-size-sm -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore base .impact-tag and .impact-tags CSS rules</name>
  <files>src/assets/css/main.css</files>
  <read_first>
    - src/assets/css/main.css (see lines 2935-2965 for insertion context — after badge rules, before .tech-tags)
    - src/components/TechTags.vue (confirm class="impact-tag" usage)
    - src/components/ExhibitCard.vue (confirm class="impact-tags" wrapper)
  </read_first>
  <action>
Insert the following CSS block into `src/assets/css/main.css` immediately BEFORE the `.tech-tags` rule (currently at line 2955). The insertion point is after line 2945 (the `.page-case-files .exhibit-card.type-engineering-brief` closing brace) and before line 2947 (`.tech-summary`).

Add these exact rules:

```css
/* Impact tags — pill badges for technology/skill tags (CSS-01, CSS-02) */
.impact-tags {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-sm);
    margin-top: var(--space-md);
}

.impact-tag {
    background: var(--color-inverse);
    color: var(--color-inverse-text);
    padding: var(--space-xs) var(--space-sm);
    border-radius: var(--radius-pill);
    font-size: var(--font-size-sm);
}

.impact-tag.highlight {
    background: var(--color-primary);
}
```

These are the exact rules deleted in commit 72b0a11. The CSS tokens (`--color-inverse`, `--color-inverse-text`, `--space-xs`, `--space-sm`, `--space-md`, `--radius-pill`, `--font-size-sm`, `--color-primary`) are all already defined in the `:root` block.

Do NOT modify any existing override rules (`.tech-tags .impact-tag`, `.page-exhibit .impact-tag`, `.page-exhibit-detail .exhibit-impact-tags`). They are contextual overrides that layer on top of these base rules.
  </action>
  <verify>
    <automated>grep -n "^\.impact-tag {" src/assets/css/main.css && grep -n "^\.impact-tags {" src/assets/css/main.css && grep -n "border-radius: var(--radius-pill)" src/assets/css/main.css | grep impact-tag && grep -n "^\.impact-tag\.highlight {" src/assets/css/main.css</automated>
  </verify>
  <acceptance_criteria>
    - src/assets/css/main.css contains a standalone `.impact-tags {` rule with `display: flex` and `flex-wrap: wrap` and `gap: var(--space-sm)`
    - src/assets/css/main.css contains a standalone `.impact-tag {` rule (not prefixed by .tech-tags or .page-exhibit) with `background: var(--color-inverse)`, `border-radius: var(--radius-pill)`, `padding: var(--space-xs) var(--space-sm)`
    - src/assets/css/main.css contains `.impact-tag.highlight {` with `background: var(--color-primary)`
    - The new rules appear BEFORE the `.tech-tags` block (which is the contextual override)
    - No duplicate rules — there should be exactly one standalone `.impact-tag {` and one standalone `.impact-tags {`
  </acceptance_criteria>
  <done>Base .impact-tag and .impact-tags CSS rules restored with pill styling (background, border-radius, padding, flexbox wrap)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify impact tag pill styling on Case Files and exhibit detail pages</name>
  <what-built>Restored base CSS for impact tags — they should now render as styled pills instead of unstyled text</what-built>
  <how-to-verify>
    1. Run `npm run dev` (or confirm dev server is running)
    2. Visit http://localhost:5173/case-files
       - Each exhibit card should show impact tags (e.g., "Docker", "CI/CD", "AWS") as small rounded pills with dark background and light text
       - Tags should wrap to multiple lines if needed, with consistent gap spacing between them
    3. Click into any exhibit detail page (e.g., the first investigation report)
       - Scroll to the "Skills and Technologies" section near the bottom
       - Impact tags should display as the same rounded pills with dark background
    4. Verify dark mode (toggle theme):
       - Impact tags should remain readable with appropriate contrast
       - Highlighted tags (if any) should use the primary accent color background
  </how-to-verify>
  <resume-signal>Type "approved" if tags display as styled pills on both pages, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- `grep -c "^\.impact-tag {" src/assets/css/main.css` returns 1
- `grep -c "^\.impact-tags {" src/assets/css/main.css` returns 1
- `grep -c "^\.impact-tag\.highlight {" src/assets/css/main.css` returns 1
- `npm run build` succeeds without errors
- Visual: impact tags on /case-files render as pills, not plain text
</verification>

<success_criteria>
- Impact tags display as rounded pill badges with background color, padding, and border-radius on both Case Files listing and exhibit detail pages
- Impact tag containers use flexbox wrap layout with gap spacing
- No CSS regressions — existing contextual overrides (.tech-tags .impact-tag, .page-exhibit .impact-tag) continue to work
</success_criteria>

<output>
After completion, create `.planning/phases/15-impact-tag-style-restoration/15-01-SUMMARY.md`
</output>
