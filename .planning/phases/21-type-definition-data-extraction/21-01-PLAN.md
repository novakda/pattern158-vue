---
phase: 21-type-definition-data-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/exhibits.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05

must_haves:
  truths:
    - "ExhibitFindingEntry interface exists with finding (required), description, background, resolution, severity (all optional)"
    - "Exhibit interface has optional findings[] array and optional findingsHeading string"
    - "All 7 table-type exhibits (A, E, J, L, M, N, O) have findings[] arrays with correct data"
    - "Exhibits J and L have findingsHeading values preserving original section titles"
  artifacts:
    - path: "src/data/exhibits.ts"
      provides: "ExhibitFindingEntry interface + findings data on 7 exhibits"
      contains: "export interface ExhibitFindingEntry"
  key_links:
    - from: "ExhibitFindingEntry"
      to: "Exhibit.findings[]"
      via: "typed array field"
      pattern: "findings\\?: ExhibitFindingEntry\\[\\]"
---

<objective>
Define the ExhibitFindingEntry interface and populate findings[] arrays on all 7 table-type exhibits with data extracted from their existing table sections.

Purpose: Promote exhibit findings from embedded table sections to first-class typed arrays, enabling purpose-built rendering in later phases.
Output: ExhibitFindingEntry interface, Exhibit interface extended with findings[] and findingsHeading, 35 finding entries across 7 exhibits.
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-type-definition-data-extraction/21-RESEARCH.md

<interfaces>
<!-- Current ExhibitPersonnelEntry pattern to follow -->
From src/data/exhibits.ts:
```typescript
export interface ExhibitPersonnelEntry {
  name?: string
  title?: string
  organization?: string
  role?: string
  isSelf?: boolean
}

export interface Exhibit {
  label: string
  client: string
  date: string
  title: string
  exhibitType: ExhibitType
  quotes?: ExhibitQuote[]
  personnel?: ExhibitPersonnelEntry[]
  contextHeading?: string
  contextText?: string
  resolutionTable?: ExhibitResolutionRow[]
  sections?: ExhibitSection[]
  impactTags: string[]
  exhibitLink: string
  isFlagship?: boolean
  summary?: string
  emailCount?: string
  role?: string
}
```
</interfaces>

**IMPORTANT CORRECTION from research:** The correct 7 table-type exhibits are A, E, J, L, M, N, O (NOT A, E, F, J, L, N, O). Exhibit F has text-type findings. Exhibit M has table-type findings. Plan accordingly.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define ExhibitFindingEntry interface and extend Exhibit interface</name>
  <files>src/data/exhibits.ts</files>
  <read_first>
    - src/data/exhibits.ts (current interfaces, line 1-75 for interface definitions)
    - src/data/findings.ts (verify naming collision avoidance — existing Finding interface is unrelated)
  </read_first>
  <action>
1. Add the ExhibitFindingEntry interface AFTER ExhibitPersonnelEntry (after line 48), BEFORE `export type ExhibitType`:

```typescript
export interface ExhibitFindingEntry {
  finding: string
  description?: string
  background?: string
  resolution?: string
  severity?: string
}
```

2. Add two new optional fields to the Exhibit interface, AFTER `personnel?: ExhibitPersonnelEntry[]` (after the personnel field):

```typescript
  findings?: ExhibitFindingEntry[]
  findingsHeading?: string
```

The interface MUST be named `ExhibitFindingEntry` (not `Finding` or `FindingEntry`) to avoid collision with the existing `Finding` interface in src/data/findings.ts.
  </action>
  <verify>
    <automated>grep -n "export interface ExhibitFindingEntry" src/data/exhibits.ts && grep -n "findings?: ExhibitFindingEntry\[\]" src/data/exhibits.ts && grep -n "findingsHeading?: string" src/data/exhibits.ts && npx vue-tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/data/exhibits.ts contains `export interface ExhibitFindingEntry {`
    - src/data/exhibits.ts contains `finding: string` inside ExhibitFindingEntry
    - src/data/exhibits.ts contains `description?: string` inside ExhibitFindingEntry
    - src/data/exhibits.ts contains `background?: string` inside ExhibitFindingEntry
    - src/data/exhibits.ts contains `resolution?: string` inside ExhibitFindingEntry
    - src/data/exhibits.ts contains `severity?: string` inside ExhibitFindingEntry
    - src/data/exhibits.ts contains `findings?: ExhibitFindingEntry[]` in Exhibit interface
    - src/data/exhibits.ts contains `findingsHeading?: string` in Exhibit interface
    - `npx vue-tsc --noEmit` exits 0 (no type errors)
  </acceptance_criteria>
  <done>ExhibitFindingEntry interface exists with 5 fields (1 required, 4 optional). Exhibit interface has findings[] and findingsHeading fields. Type-checks pass.</done>
</task>

<task type="auto">
  <name>Task 2: Populate findings[] arrays on all 7 table-type exhibits</name>
  <files>src/data/exhibits.ts</files>
  <read_first>
    - src/data/exhibits.ts (FULL FILE — need to locate each exhibit's findings table section and copy row data exactly)
  </read_first>
  <action>
For each of the 7 exhibits below, add a `findings` array (and `findingsHeading` where noted) as a top-level field on the exhibit object. Place `findings` and `findingsHeading` after the `personnel` field, following the same positioning pattern used for personnel data.

**CRITICAL: Map columns by header name, not position index.** Each exhibit has different column orders.

**Exhibit A** (exhibitLink: '/exhibits/exhibit-a'):
- Columns: `['Finding', 'Background', 'Resolution']` — map to `finding`, `background`, `resolution`
- 5 rows. Copy each row's cell text verbatim from the existing `rows` array.
- No findingsHeading needed (default "Findings" applies).

**Exhibit E** (exhibitLink: '/exhibits/exhibit-e'):
- Columns: `['Finding', 'Description']` — map to `finding`, `description`
- 5 rows. Copy cell text verbatim.
- No findingsHeading needed.

**Exhibit J** (exhibitLink: '/exhibits/exhibit-j'):
- Columns: `['Finding', 'Description']` — map to `finding`, `description`
- 5 rows. Copy cell text verbatim.
- SET `findingsHeading: 'Findings \u2014 Five Concurrent Systemic Failures (Swiss Cheese Model)'` (copy the heading value from the existing section's `heading` field — contains em-dash \u2014, not hyphen).

**Exhibit L** (exhibitLink: '/exhibits/exhibit-l'):
- Columns: `['Finding', 'Severity', 'Description']` — map to `finding`, `severity`, `description`
- 5 rows. Copy cell text verbatim. Note column order: severity is column index 1, description is column index 2.
- SET `findingsHeading: 'Findings \u2014 Five Foundational Gaps'` (copy from existing section heading — em-dash).

**Exhibit M** (exhibitLink: '/exhibits/exhibit-m'):
- Columns: `['Finding', 'Description']` — map to `finding`, `description`
- 5 rows. Copy cell text verbatim.
- No findingsHeading needed.

**Exhibit N** (exhibitLink: '/exhibits/exhibit-n'):
- Columns: `['Finding', 'Description']` — map to `finding`, `description`
- 5 rows. Copy cell text verbatim.
- No findingsHeading needed.

**Exhibit O** (exhibitLink: '/exhibits/exhibit-o'):
- Columns: `['Finding', 'Description']` — map to `finding`, `description`
- 5 rows. Copy cell text verbatim.
- No findingsHeading needed.

Copy ALL text content verbatim from existing `rows[][]` arrays. Do not paraphrase, truncate, or modify any finding text.
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit 2>&1 | tail -5 && node -e "const {exhibits}=require('./src/data/exhibits.ts'.replace('.ts','')); ['a','e','j','l','m','n','o'].forEach(l=>{const ex=exhibits.find(e=>e.exhibitLink==='/exhibits/exhibit-'+l); console.log(l.toUpperCase()+': '+(ex?.findings?.length||0)+' findings')})" 2>/dev/null || npx tsx -e "import {exhibits} from './src/data/exhibits'; ['a','e','j','l','m','n','o'].forEach(l=>{const ex=exhibits.find(e=>e.exhibitLink==='/exhibits/exhibit-'+l); console.log(l.toUpperCase()+': '+(ex?.findings?.length||0)+' findings')})"</automated>
  </verify>
  <acceptance_criteria>
    - Exhibit A has `findings:` array with 5 entries, each having `finding`, `background`, `resolution` fields
    - Exhibit E has `findings:` array with 5 entries, each having `finding`, `description` fields
    - Exhibit J has `findings:` array with 5 entries, each having `finding`, `description` fields
    - Exhibit J has `findingsHeading: 'Findings \u2014 Five Concurrent Systemic Failures (Swiss Cheese Model)'`
    - Exhibit L has `findings:` array with 5 entries, each having `finding`, `severity`, `description` fields
    - Exhibit L has `findingsHeading: 'Findings \u2014 Five Foundational Gaps'`
    - Exhibit M has `findings:` array with 5 entries, each having `finding`, `description` fields
    - Exhibit N has `findings:` array with 5 entries, each having `finding`, `description` fields
    - Exhibit O has `findings:` array with 5 entries, each having `finding`, `description` fields
    - Total: 35 finding entries across 7 exhibits
    - `npx vue-tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All 7 table-type exhibits have findings[] arrays with correct field mappings. Exhibits J and L have custom findingsHeading values with em-dashes. 35 total entries.</done>
</task>

</tasks>

<verification>
- `npx vue-tsc --noEmit` passes (no type errors)
- `grep -c "findings:" src/data/exhibits.ts` shows findings arrays present
- `grep "findingsHeading:" src/data/exhibits.ts` shows exactly 2 entries (J and L)
</verification>

<success_criteria>
- ExhibitFindingEntry interface exported from exhibits.ts
- Exhibit interface has findings[] and findingsHeading fields
- 7 exhibits have populated findings[] arrays (5 entries each = 35 total)
- 2 exhibits have custom findingsHeading values
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/21-type-definition-data-extraction/21-01-SUMMARY.md`
</output>
