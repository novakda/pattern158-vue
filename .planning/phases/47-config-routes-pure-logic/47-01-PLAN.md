---
phase: 47-config-routes-pure-logic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/editorial/types.ts
autonomous: true
requirements:
  - CAPT-01
  - WRIT-01
tags: [editorial-capture, typescript, interfaces, contracts, phase-47]

must_haves:
  truths:
    - "types.ts re-exports EditorialConfig, Route, CapturedPage, ConvertedPage (smoke.test.ts contract)"
    - "EditorialConfig has readonly fields outputPath, baseUrl, headful, mirror, exhibitsJsonPath"
    - "Route has readonly fields path, label, category ('static' | 'exhibit'), optional sourceSlug"
    - "ConfigError is an exported Error subclass usable across config.ts / index.ts boundary"
    - "SCAF-08 descriptive banner preserved verbatim in every modified file"
  artifacts:
    - path: scripts/editorial/types.ts
      provides: "Re-export hub for EditorialConfig, Route, CapturedPage, ConvertedPage (existing contract preserved)"
      contains: "export type { EditorialConfig }"
    - path: scripts/editorial/config.ts
      provides: "EditorialConfig interface extended + ConfigError class + throwing loadEditorialConfig stub preserved for this plan"
      contains: "interface EditorialConfig"
    - path: scripts/editorial/routes.ts
      provides: "Route interface extended with category + sourceSlug; throwing buildRoutes stub preserved"
      contains: "interface Route"
  key_links:
    - from: scripts/editorial/types.ts
      to: scripts/editorial/config.ts
      via: "export type { EditorialConfig, ConfigError } from './config.ts'"
      pattern: "export type.*EditorialConfig.*from.*config"
    - from: scripts/editorial/types.ts
      to: scripts/editorial/routes.ts
      via: "export type { Route } from './routes.ts'"
      pattern: "export type.*Route.*from.*routes"
---

<objective>
Expand the Phase 46 interface stubs in `config.ts` and `routes.ts` to the final locked shapes (per CONTEXT.md D-XX), add a `ConfigError` error class, and extend `types.ts` to re-export `ConfigError` alongside the four existing type names. The function bodies for `loadEditorialConfig()` and `buildRoutes()` remain throwing stubs in this plan — Plans 02 and 03 replace the bodies.

Purpose: Downstream plans (02 config impl, 03 routes impl, 04/05 tests) depend on the final interface shapes. Locking the contracts in a single small plan avoids interface churn across the wave.
Output: Updated `types.ts`, `config.ts`, `routes.ts` with final interface shapes + `ConfigError` class. Smoke test still compiles and passes.
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/47-config-routes-pure-logic/47-CONTEXT.md
@.planning/phases/46-scaffold/46-REVIEW.md
@scripts/editorial/types.ts
@scripts/editorial/config.ts
@scripts/editorial/routes.ts
@scripts/editorial/__tests__/smoke.test.ts

<interfaces>
<!-- Phase 46 EXISTING state (keep these re-exports intact) — extracted from scripts/editorial/types.ts: -->
```ts
// types.ts (current)
export type { EditorialConfig } from './config.ts'
export type { Route } from './routes.ts'
export type { CapturedPage } from './capture.ts'
export type { ConvertedPage } from './convert.ts'
```

<!-- Phase 46 EXISTING interface shapes (replace with expanded forms below) — from scripts/editorial/config.ts + routes.ts: -->
```ts
// config.ts (current)
export interface EditorialConfig {
  readonly outputPath: string
  readonly baseUrl: string
  readonly headful: boolean
  readonly mirror: boolean
}

// routes.ts (current)
export interface Route {
  readonly path: string
  readonly label: string
}
```

<!-- FINAL shapes this plan lands (per 47-CONTEXT.md <specifics>) — MUST match exactly: -->
```ts
// config.ts (final shape after this plan)
export interface EditorialConfig {
  readonly outputPath: string;        // resolved absolute path
  readonly baseUrl: string;           // validated https:// URL, no trailing slash
  readonly headful: boolean;
  readonly mirror: boolean;
  readonly exhibitsJsonPath: string;  // absolute path to src/data/json/exhibits.json
}

export class ConfigError extends Error {
  constructor(message: string, public readonly cause?: unknown) {
    super(message)
    this.name = 'ConfigError'
  }
}

// routes.ts (final shape after this plan)
export interface Route {
  readonly path: string;              // begins with "/"; no trailing slash except "/"
  readonly label: string;             // human-readable, used in ToC anchors in Phase 50
  readonly category: 'static' | 'exhibit';
  readonly sourceSlug?: string;       // exhibits.json-derived slug (e.g. "exhibit-a"), exhibit routes only
}
```

<!-- SCAF-08 descriptive banner (Phase 46 convention — preserve VERBATIM at top of every modified file): -->
```
// SCAF-08 forbidden in this directory (see .planning/REQUIREMENTS.md SCAF-08):
//   - path-alias imports (use relative paths)
//   - non-deterministic timestamp APIs (use injected/fixed timestamps)
//   - platform-specific line endings (use literal newline only)
//   - parallel iteration over the ordered route list (use sequential for-of)
```

<!-- Smoke test contract (scripts/editorial/__tests__/smoke.test.ts imports these four names from types.ts — DO NOT rename or remove): -->
```ts
import type { EditorialConfig, Route, CapturedPage, ConvertedPage } from '../types.ts'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend EditorialConfig and add ConfigError in config.ts (WRIT-01)</name>
  <files>scripts/editorial/config.ts</files>
  <read_first>
    - scripts/editorial/config.ts (the file being modified — current Phase 46 placeholder)
    - scripts/editorial/types.ts (cross-module re-export contract — must stay valid after edit)
    - .planning/phases/47-config-routes-pure-logic/47-CONTEXT.md (locked decisions for interface shape)
    - .planning/phases/46-scaffold/46-01-SUMMARY.md (SCAF-08 banner convention — descriptive form is load-bearing for grep gate)
  </read_first>
  <behavior>
    - Behavior 1: `EditorialConfig` interface exports five readonly fields (outputPath, baseUrl, headful, mirror, exhibitsJsonPath).
    - Behavior 2: `ConfigError` class is a throwable Error subclass with `name === 'ConfigError'` and optional `cause` field.
    - Behavior 3: `loadEditorialConfig()` still throws "not implemented until Phase 47 (WRIT-01)" — body stays a stub until Plan 02. (Kept as stub so smoke.test.ts compiles; Plan 02 replaces the body.)
    - Behavior 4: File starts with the Phase 46 SCAF-08 descriptive banner block (4 rule lines preceded by "SCAF-08 forbidden in this directory").
    - Behavior 5: No forbidden patterns introduced — `grep -RE "@/|Date\.now\(\)|new Date\(|os\.EOL|Promise\.all" scripts/editorial/config.ts` returns nothing.
  </behavior>
  <action>
    Replace the body of `scripts/editorial/config.ts` with exactly this content (preserving the SCAF-08 descriptive banner block verbatim from Phase 46):

    ```ts
    // scripts/editorial/config.ts
    // Phase 47 contract — real implementation in Plan 47-02 (loadEditorialConfig body).
    // SCAF-08 forbidden in this directory (see .planning/REQUIREMENTS.md SCAF-08):
    //   - path-alias imports (use relative paths)
    //   - non-deterministic timestamp APIs (use injected/fixed timestamps)
    //   - platform-specific line endings (use literal newline only)
    //   - parallel iteration over the ordered route list (use sequential for-of)

    export interface EditorialConfig {
      readonly outputPath: string
      readonly baseUrl: string
      readonly headful: boolean
      readonly mirror: boolean
      readonly exhibitsJsonPath: string
    }

    export class ConfigError extends Error {
      public readonly cause?: unknown
      constructor(message: string, cause?: unknown) {
        super(message)
        this.name = 'ConfigError'
        this.cause = cause
      }
    }

    export function loadEditorialConfig(): EditorialConfig {
      throw new Error('loadEditorialConfig: not implemented until Phase 47 Plan 02 (WRIT-01)')
    }
    ```

    Constraints:
    - Preserve the SCAF-08 descriptive banner VERBATIM (see `<interfaces>` block above). Do NOT spell out the literal forbidden tokens — the grep gate depends on the banner staying descriptive.
    - Class name MUST be `ConfigError`, name property MUST equal the string `'ConfigError'`, and it MUST extend `Error`.
    - `cause` field MUST be declared `readonly` on the class and MUST be accessible via `instanceof ConfigError && err.cause`.
    - Do NOT import anything from `node:*` modules in this file (keeps this plan body zero-side-effect).
    - Throwing-stub message text is not load-bearing but must contain the string "not implemented" so downstream test scaffolding can detect the stub if needed.
  </action>
  <verify>
    <automated>grep -q "interface EditorialConfig" scripts/editorial/config.ts && grep -q "exhibitsJsonPath: string" scripts/editorial/config.ts && grep -q "class ConfigError extends Error" scripts/editorial/config.ts && grep -q "this.name = 'ConfigError'" scripts/editorial/config.ts && grep -q "SCAF-08 forbidden in this directory" scripts/editorial/config.ts && ! grep -qE "Date\.now\(\)|new Date\(|os\.EOL|Promise\.all|from '@/" scripts/editorial/config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "readonly outputPath: string" scripts/editorial/config.ts` passes
    - `grep -q "readonly baseUrl: string" scripts/editorial/config.ts` passes
    - `grep -q "readonly headful: boolean" scripts/editorial/config.ts` passes
    - `grep -q "readonly mirror: boolean" scripts/editorial/config.ts` passes
    - `grep -q "readonly exhibitsJsonPath: string" scripts/editorial/config.ts` passes
    - `grep -q "class ConfigError extends Error" scripts/editorial/config.ts` passes
    - `grep -q "this.name = 'ConfigError'" scripts/editorial/config.ts` passes
    - `grep -q "path-alias imports" scripts/editorial/config.ts` passes (SCAF-08 banner intact)
    - `grep -RE "@/|Date\.now\(\)|new Date\(|os\.EOL|Promise\.all" scripts/editorial/config.ts` returns no matches (exit 1)
    - `pnpm build 2>&1 | tee /tmp/p47-01-t1-build.log; echo EXIT=$?` shows `EXIT=0`
  </acceptance_criteria>
  <done>
    `config.ts` holds the final `EditorialConfig` shape (5 fields), exports `ConfigError` as a throwable Error subclass, preserves the SCAF-08 banner, keeps `loadEditorialConfig()` as a throwing stub. `pnpm build` remains green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend Route interface in routes.ts + re-export ConfigError from types.ts (CAPT-01)</name>
  <files>scripts/editorial/routes.ts, scripts/editorial/types.ts</files>
  <read_first>
    - scripts/editorial/routes.ts (the file being modified — current Phase 46 placeholder)
    - scripts/editorial/types.ts (cross-module re-export surface — must add ConfigError)
    - scripts/editorial/__tests__/smoke.test.ts (contract: imports EditorialConfig, Route, CapturedPage, ConvertedPage from '../types.ts' — must stay compiling)
    - .planning/phases/47-config-routes-pure-logic/47-CONTEXT.md (locked Route shape in <specifics>)
  </read_first>
  <behavior>
    - Behavior 1: `Route` interface exports four readonly fields: `path: string`, `label: string`, `category: 'static' | 'exhibit'`, `sourceSlug?: string`.
    - Behavior 2: `buildRoutes()` stays a throwing stub ("not implemented until Phase 47 Plan 03 (CAPT-01)") — body lands in Plan 03.
    - Behavior 3: `types.ts` re-exports all four existing type names (EditorialConfig, Route, CapturedPage, ConvertedPage) AND additionally re-exports `ConfigError` from `./config.ts`.
    - Behavior 4: `ConfigError` is re-exported as a runtime value (not `export type`) — it's a class, used for `instanceof` checks in index.ts in later phases.
    - Behavior 5: SCAF-08 descriptive banner present in both files.
  </behavior>
  <action>
    Replace the body of `scripts/editorial/routes.ts` with:

    ```ts
    // scripts/editorial/routes.ts
    // Phase 47 contract — real implementation in Plan 47-03 (buildRoutes body).
    // SCAF-08 forbidden in this directory (see .planning/REQUIREMENTS.md SCAF-08):
    //   - path-alias imports (use relative paths)
    //   - non-deterministic timestamp APIs (use injected/fixed timestamps)
    //   - platform-specific line endings (use literal newline only)
    //   - parallel iteration over the ordered route list (use sequential for-of)

    export interface Route {
      readonly path: string
      readonly label: string
      readonly category: 'static' | 'exhibit'
      readonly sourceSlug?: string
    }

    export function buildRoutes(): readonly Route[] {
      throw new Error('buildRoutes: not implemented until Phase 47 Plan 03 (CAPT-01)')
    }
    ```

    Replace the body of `scripts/editorial/types.ts` with (keeping the SCAF-08 banner and adding the ConfigError re-export):

    ```ts
    // scripts/editorial/types.ts
    // Shared type + value re-exports for the editorial capture tool.
    // Real type bodies live in their producing modules (config.ts, routes.ts, etc.).
    // SCAF-08 forbidden in this directory (see .planning/REQUIREMENTS.md SCAF-08):
    //   - path-alias imports (use relative paths)
    //   - non-deterministic timestamp APIs (use injected/fixed timestamps)
    //   - platform-specific line endings (use literal newline only)
    //   - parallel iteration over the ordered route list (use sequential for-of)

    export type { EditorialConfig } from './config.ts'
    export type { Route } from './routes.ts'
    export type { CapturedPage } from './capture.ts'
    export type { ConvertedPage } from './convert.ts'

    // ConfigError is re-exported as a runtime value (class), not `export type`,
    // so downstream code can use `err instanceof ConfigError` at runtime.
    export { ConfigError } from './config.ts'
    ```

    Constraints:
    - `category` union values are the two string literals `'static'` and `'exhibit'` — no other values, no broadening to `string`.
    - `sourceSlug` is OPTIONAL (question mark) — only populated for exhibit routes in Plan 03.
    - `ConfigError` re-export uses plain `export` (not `export type`) because it's a class instantiated at runtime.
    - SCAF-08 banner preserved verbatim in both files (descriptive form from Phase 46 — see `<interfaces>` block).
    - Do NOT change the four existing `export type { ... }` lines; only ADD the ConfigError re-export.
  </action>
  <verify>
    <automated>grep -q "category: 'static' | 'exhibit'" scripts/editorial/routes.ts && grep -q "readonly sourceSlug?: string" scripts/editorial/routes.ts && grep -q "export { ConfigError } from './config.ts'" scripts/editorial/types.ts && grep -q "export type { EditorialConfig }" scripts/editorial/types.ts && grep -q "SCAF-08 forbidden in this directory" scripts/editorial/types.ts && ! grep -qE "@/|Date\.now\(\)|new Date\(|os\.EOL|Promise\.all" scripts/editorial/routes.ts scripts/editorial/types.ts && pnpm build 2>&1 | tee /tmp/p47-01-t2-build.log && echo BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "readonly path: string" scripts/editorial/routes.ts` passes
    - `grep -q "readonly label: string" scripts/editorial/routes.ts` passes
    - `grep -q "category: 'static' | 'exhibit'" scripts/editorial/routes.ts` passes
    - `grep -q "readonly sourceSlug\?: string" scripts/editorial/routes.ts` passes
    - `grep -q "export { ConfigError } from './config.ts'" scripts/editorial/types.ts` passes (runtime re-export, not type-only)
    - `grep -q "export type { EditorialConfig } from './config.ts'" scripts/editorial/types.ts` passes (existing contract preserved)
    - `grep -q "export type { Route } from './routes.ts'" scripts/editorial/types.ts` passes
    - `grep -q "export type { CapturedPage } from './capture.ts'" scripts/editorial/types.ts` passes
    - `grep -q "export type { ConvertedPage } from './convert.ts'" scripts/editorial/types.ts` passes
    - `grep -q "SCAF-08 forbidden in this directory" scripts/editorial/routes.ts` passes
    - `grep -q "SCAF-08 forbidden in this directory" scripts/editorial/types.ts` passes
    - `grep -RE "@/|Date\.now\(\)|new Date\(|os\.EOL|Promise\.all" scripts/editorial/routes.ts scripts/editorial/types.ts` returns no matches
    - `pnpm build 2>&1 | tail -5; echo EXIT=$?` shows `EXIT=0` (vue-tsc -b still compiles the editorial project)
    - `pnpm test:scripts 2>&1 | tee /tmp/p47-01-t2-test.log | grep -qE 'passed'` — smoke.test.ts still green (contract preserved)
  </acceptance_criteria>
  <done>
    `routes.ts` holds the final Route shape (4 fields including `category` union + optional `sourceSlug`). `types.ts` adds runtime `ConfigError` re-export while preserving the four existing type-only re-exports. `pnpm build` green; `pnpm test:scripts` green (smoke.test.ts still compiles against the re-export surface).
  </done>
</task>

</tasks>

<verification>
After both tasks:

- `pnpm build` exits 0 (vue-tsc -b walks all composite projects, including tsconfig.editorial.json, with zero errors).
- `pnpm test:scripts` exits 0 — smoke.test.ts is still green because its import surface (`EditorialConfig`, `Route`, `CapturedPage`, `ConvertedPage` from `../types.ts`) is preserved and the new `exhibitsJsonPath` / `category` / `sourceSlug` fields are additive (optional field + interface extension, both structurally compatible with the existing `const _config: EditorialConfig | undefined = undefined` usage in the smoke test).
- SCAF-08 grep gate clean across all three modified files: `grep -RE "@/|Date\.now\(\)|new Date\(|os\.EOL|Promise\.all" scripts/editorial/types.ts scripts/editorial/config.ts scripts/editorial/routes.ts` returns no matches.
- SCAF-08 banner present in all three files.
</verification>

<success_criteria>
- `scripts/editorial/config.ts` has final `EditorialConfig` (5 readonly fields) + `ConfigError extends Error` class + stub `loadEditorialConfig()` that throws.
- `scripts/editorial/routes.ts` has final `Route` (4 fields: path, label, category union, optional sourceSlug) + stub `buildRoutes()` that throws.
- `scripts/editorial/types.ts` re-exports all four type names (EditorialConfig, Route, CapturedPage, ConvertedPage) AND `ConfigError` as a runtime value.
- SCAF-08 banner intact in every modified file; grep gate clean.
- `pnpm build` green; `pnpm test:scripts` green.
</success_criteria>

<output>
After completion, create `.planning/phases/47-config-routes-pure-logic/47-01-SUMMARY.md` documenting the interface shapes landed and confirming the smoke.test.ts contract held.
</output>
