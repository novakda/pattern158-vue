---
phase: quick
plan: 260402-3b9
type: execute
wave: 1
depends_on: []
files_modified:
  - src/assets/css/main.css
autonomous: true
must_haves:
  truths:
    - "ExhibitCard header displays as a horizontal flex row with label, client, and date"
    - "Exhibit label has dark background pill styling with monospace uppercase text"
    - "Exhibit client name shows in heading font with primary color"
    - "Exhibit date is right-aligned with muted color"
    - "Exhibit title has proper heading size and spacing"
    - "On mobile, header stacks vertically and date loses right-alignment"
  artifacts:
    - path: "src/assets/css/main.css"
      provides: "ExhibitCard header styling under .exhibit-card scope"
      contains: ".exhibit-card .exhibit-header"
  key_links:
    - from: "src/components/ExhibitCard.vue"
      to: "src/assets/css/main.css"
      via: "class names: exhibit-header, exhibit-label, exhibit-client, exhibit-date, exhibit-title"
      pattern: "exhibit-header"
---

<objective>
Restore ExhibitCard header styling that was accidentally deleted in Phase 13's dead CSS cleanup.

Purpose: Phase 13 (commit 72b0a11) deleted all `.page-testimonials` scoped CSS as "dead CSS" after the page was renamed to CaseFiles (class `page-case-files`). The exhibit-header, exhibit-label, exhibit-client, exhibit-date, and exhibit-title rules were never recreated under the new scope. The ExhibitCard component still uses these class names but they render unstyled.

Output: Restored CSS rules scoped to `.exhibit-card` (component-level) rather than page-level, plus mobile responsive override.
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/ExhibitCard.vue
@src/assets/css/main.css (lines 2938-2945 — existing .page-case-files rules)

<interfaces>
From src/components/ExhibitCard.vue (the HTML structure needing CSS):
```html
<div class="exhibit-card detail-exhibit type-{exhibitType}">
  <div class="exhibit-header">
    <span class="exhibit-label">{{ exhibit.label }}</span>
    <span class="exhibit-client">{{ exhibit.client }}</span>
    <span class="exhibit-date">{{ exhibit.date }}</span>
  </div>
  <h3 class="exhibit-title">{{ exhibit.title }}</h3>
  ...
</div>
```

The original CSS (deleted in Phase 13, from pre-deletion worktree):
```css
/* Desktop — was under .page-testimonials scope */
.exhibit-header {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    margin-bottom: var(--space-md);
    flex-wrap: wrap;
}
.exhibit-label {
    background: var(--color-inverse);
    color: var(--color-inverse-text);
    padding: var(--space-xs) var(--space-md);
    font-family: var(--font-mono);
    font-size: var(--font-size-xs);
    letter-spacing: 2px;
    text-transform: uppercase;
    white-space: nowrap;
}
.exhibit-client {
    font-family: var(--font-heading);
    font-size: var(--font-size-md);
    color: var(--color-primary);
    letter-spacing: 1px;
}
.exhibit-date {
    font-size: var(--font-size-sm);
    color: var(--color-text-light);
    margin-left: auto;
}
.exhibit-title {
    font-size: var(--font-size-xl);
    color: var(--color-heading);
    margin-bottom: var(--space-md);
}

/* Mobile — was under @media max-width */
.exhibit-header {
    flex-direction: column;
    align-items: flex-start;
}
.exhibit-date {
    margin-left: 0;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore ExhibitCard header CSS rules scoped to .exhibit-card</name>
  <files>src/assets/css/main.css</files>
  <action>
Add the following CSS rules to main.css, inserting them immediately BEFORE the existing
`.page-case-files .exhibit-card.type-investigation-report` block (around line 2939).

Scope to `.exhibit-card` (component-level, not page-level) so the styles work
wherever ExhibitCard is rendered. This is the corrected approach — the original CSS
was page-scoped to `.page-testimonials` which broke when the page was renamed.

Desktop rules to add:
```css
/* ExhibitCard header — restored from Phase 13 dead CSS deletion (was .page-testimonials scoped) */
.exhibit-card .exhibit-header {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    margin-bottom: var(--space-md);
    flex-wrap: wrap;
}

.exhibit-card .exhibit-label {
    background: var(--color-inverse);
    color: var(--color-inverse-text);
    padding: var(--space-xs) var(--space-md);
    font-family: var(--font-mono);
    font-size: var(--font-size-xs);
    letter-spacing: 2px;
    text-transform: uppercase;
    white-space: nowrap;
}

.exhibit-card .exhibit-client {
    font-family: var(--font-heading);
    font-size: var(--font-size-md);
    color: var(--color-primary);
    letter-spacing: 1px;
}

.exhibit-card .exhibit-date {
    font-size: var(--font-size-sm);
    color: var(--color-text-light);
    margin-left: auto;
}

.exhibit-card .exhibit-title {
    font-size: var(--font-size-xl);
    color: var(--color-heading);
    margin-bottom: var(--space-md);
}
```

Also add mobile responsive rules inside the EXISTING `@media (max-width: 768px)` block
(find the section near where `.exhibit-card` padding is already set, around line 3082-3088):
```css
    .exhibit-card .exhibit-header {
        flex-direction: column;
        align-items: flex-start;
    }

    .exhibit-card .exhibit-date {
        margin-left: 0;
    }
```

Do NOT re-add `.page-testimonials` scoped rules — that class no longer exists.
Do NOT touch any other existing CSS rules.
  </action>
  <verify>
    <automated>grep -c 'exhibit-card .exhibit-header' src/assets/css/main.css | grep -q '2' && echo "PASS: desktop + mobile rules present" || echo "FAIL"</automated>
  </verify>
  <done>
    - `.exhibit-card .exhibit-header` has flex row layout
    - `.exhibit-card .exhibit-label` has dark pill styling
    - `.exhibit-card .exhibit-client` has heading font with primary color
    - `.exhibit-card .exhibit-date` is right-aligned with muted color
    - `.exhibit-card .exhibit-title` has XL heading size
    - Mobile breakpoint stacks header vertically
    - Existing unit tests still pass (npx vitest run)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Restored ExhibitCard header styling (exhibit-header flex row with label pill, client name, date, and title)</what-built>
  <how-to-verify>
    1. Run `npx vite dev` and open http://localhost:5173/case-files
    2. Verify each ExhibitCard shows a header row with:
       - Dark pill label (e.g., "EXHIBIT A") in monospace uppercase
       - Client name in primary color with heading font
       - Date right-aligned in muted text
    3. Verify exhibit titles are properly sized below the header
    4. Resize browser to mobile width (<768px) and verify header stacks vertically
    5. Check an individual exhibit detail page to confirm no regression there
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `grep -c 'exhibit-card .exhibit-header' src/assets/css/main.css` returns 2 (desktop + mobile)
- `grep -c 'exhibit-card .exhibit-label' src/assets/css/main.css` returns at least 1
- `npx vitest run` passes with no regressions
</verification>

<success_criteria>
ExhibitCard header on CaseFilesPage displays with the original styled layout: horizontal flex row with dark label pill, primary-colored client name, right-aligned date, and properly sized title. Mobile view stacks vertically.
</success_criteria>

<output>
After completion, create `.planning/quick/260402-3b9-exhibitcard-has-lost-styling-on-the-exhi/260402-3b9-SUMMARY.md`
</output>
