---
phase: quick-260317-tl7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/assets/css/main.css
autonomous: true
requirements:
  - TL7-CSS-TOKENS
  - TL7-BTN-TOUCH
  - TL7-RADIUS-TOKEN
  - TL7-DEAD-FAQ-CSS
  - TL7-ORPHANED-RULE

must_haves:
  truths:
    - "ExhibitDetailPage CSS uses only canonical design system token names"
    - ".btn base class enforces 44px minimum touch target via min-height"
    - ".page-contact .timezone-note border-radius uses --radius-md token"
    - "Dead .page-faq details/summary CSS (never matches FaqItem div.faq-item) is removed"
    - "Orphaned .section-alt link rule (class never applied) is removed"
  artifacts:
    - path: "src/assets/css/main.css"
      provides: "Single CSS file with all five fixes applied"
  key_links:
    - from: "src/assets/css/main.css"
      to: "design system token names"
      via: "var() references in ExhibitDetailPage block (~lines 4353–4493)"
      pattern: "var\\(--space-l\\b|var\\(--space-m\\b|var\\(--space-s\\b|var\\(--text-|var\\(--color-inverse-bg|var\\(--color-surface-raised"
---

<objective>
Fix five UX audit findings in `src/assets/css/main.css`: wrong CSS token names in the ExhibitDetailPage block, missing touch target on `.btn`, hardcoded border-radius on `.page-contact .timezone-note`, dead FAQ details/summary CSS that never matches the real DOM, and an orphaned `.section-alt` link rule.

Purpose: Bring the stylesheet into conformance with the canonical design system token names (`system.md`) and remove dead code that creates false maintenance surface.
Output: One modified file — `src/assets/css/main.css`.
</objective>

<execution_context>
@C:/main/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@C:/main/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.interface-design/system.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ExhibitDetailPage wrong token names (~lines 4353–4493)</name>
  <files>src/assets/css/main.css</files>
  <action>
In the `/* ─── ExhibitDetailPage ─────────────────────────────────────────────── */` block (lines ~4353–4493) make the following token substitutions. Apply ONLY inside this block — do not touch other parts of the file.

Token renames (exact find → replace):
- `var(--space-l)` → `var(--space-lg)`  (appears on line ~4358)
- `var(--space-m)` → `var(--space-md)`  (appears on lines ~4362, 4419, 4420, 4425, 4460, 4461, 4468)
- `var(--space-s)` → `var(--space-sm)`  (appears on lines ~4380, 4419, 4420, 4425, 4460, 4461, 4468, 4471)
- `var(--text-sm)` → `var(--font-size-sm)`  (appears on lines ~4368, 4381, 4435, 4462, 4471)
- `var(--text-2xl)` → `var(--font-size-2xl)`  (appears on line ~4404)
- `var(--color-inverse-bg, #1a2332)` → `var(--color-inverse)`  (appears on line ~4356; remove the fallback entirely — the token is defined in :root)
- `var(--color-surface-raised, #f5f4f0)` → `var(--color-background-alt)`  (appears on lines ~4421, 4459, 4475)

Also fix the dark mode overrides block (~lines 4483–4493):
- `var(--color-surface-raised)` → `var(--color-background-alt)`  (appears on lines ~4484, 4488, 4492; these have no fallback already)

The tokens `--color-primary-on-dark` and `--color-inverse-text` and `--color-inverse-text-muted` with their hardcoded fallbacks are FINE — those tokens exist in the system, leave them unchanged.

After editing, grep the ExhibitDetailPage block to confirm no `--space-l\b`, `--space-m\b`, `--space-s\b`, `--text-`, `--color-inverse-bg`, or `--color-surface-raised` remain.
  </action>
  <verify>
    <automated>grep -n "\-\-space-l\b\|\-\-space-m\b\|\-\-space-s\b\|\-\-text-\|\-\-color-inverse-bg\|\-\-color-surface-raised" src/assets/css/main.css | grep -v "^Binary"</automated>
  </verify>
  <done>grep returns zero matches — no legacy/nonexistent token aliases remain anywhere in main.css.</done>
</task>

<task type="auto">
  <name>Task 2: Fix .btn touch target + .timezone-note radius + remove dead/orphaned CSS</name>
  <files>src/assets/css/main.css</files>
  <action>
Make three targeted edits to `src/assets/css/main.css`:

**2a — .btn touch target (line ~596–604, inside `@layer components`):**
The current `.btn` rule has `display: inline-block`. Replace it to enforce the 44px touch target:

Current:
```css
.btn {
    display: inline-block;
    padding: var(--space-md) var(--space-xl);
    margin: 0 var(--space-sm);
    text-decoration: none;
    border-radius: var(--radius-md);
    transition: all 0.3s;
    font-weight: 500;
}
```

Replace with:
```css
.btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 44px;
    padding: var(--space-md) var(--space-xl);
    margin: 0 var(--space-sm);
    text-decoration: none;
    border-radius: var(--radius-md);
    transition: all 0.3s;
    font-weight: 500;
}
```

**2b — .page-contact .timezone-note border-radius (line ~1686):**
Change `border-radius: 8px;` to `border-radius: var(--radius-md);`.

**2c — Remove dead FAQ details/summary CSS:**
Delete the entire block from the comment `/* Details/Summary styling */` through the end of `.page-faq .faq-answer a:hover` and `.page-faq .faq-answer strong` rules — lines ~1457–1561. The exact boundary:
- Start: line containing `/* Details/Summary styling */`  (just before `.page-faq details {`)
- End: line containing the closing `}` of `.page-faq .faq-answer strong { color: var(--color-heading); }` (the last rule before the `/* PAGE: Contact` comment)

Also delete the mobile override block for the same dead selectors (inside `@media (max-width: 768px)`):
- Start: line containing `.page-faq details summary {` (the mobile override, ~line 3680)
- End: closing `}` of `.page-faq .faq-answer { padding: var(--space-sm); font-size: var(--font-size-sm); }` (~line 3693)

The `.page-faq .faq-item`, `.page-faq .category-intro`, and all other `.page-faq` rules are LIVE — keep them. Only remove `details`, `summary`, `details[open]`, `details summary`, `details[open] summary`, `.faq-answer` (but NOT `.faq-item`) rules.

**2d — Remove orphaned .section-alt link rule (~lines 751–754):**
In the `@layer base` block containing the long link-color selector list, remove only these two lines from the selector list:
```
    .section-alt a,
    .section-alt .link-primary {
```
The surrounding selectors (`.page-testimonials .exhibits a:not(.exhibit-card a)`, etc.) must remain. The closing `color: var(--color-primary-on-alt);` and `}` must remain. Only remove the two `.section-alt` selector lines.
  </action>
  <verify>
    <automated>grep -n "details summary\|\.page-faq details\|\-\-color-surface-raised\|\.section-alt a\|border-radius: 8px\|display: inline-block" src/assets/css/main.css</automated>
  </verify>
  <done>
- grep returns zero matches for `details summary`, `.page-faq details`, `--color-surface-raised`, `.section-alt a`, `border-radius: 8px` (in timezone-note context), and `display: inline-block` (in .btn context).
- `.btn` has `display: inline-flex`, `min-height: 44px`, `align-items: center`, `justify-content: center`.
- `.page-contact .timezone-note` has `border-radius: var(--radius-md)`.
  </done>
</task>

</tasks>

<verification>
Run both automated verify commands. Both must return zero output (no matches).

Then confirm the `.btn` rule has the four new properties by running:
```
grep -A 10 "^\.btn {" src/assets/css/main.css
```
Expected output includes `display: inline-flex`, `align-items: center`, `justify-content: center`, `min-height: 44px`.
</verification>

<success_criteria>
- Zero remaining references to non-existent tokens: `--space-l`, `--space-m`, `--space-s`, `--text-sm`, `--text-2xl`, `--color-inverse-bg`, `--color-surface-raised`
- `.btn` base rule enforces 44px touch target via `min-height: 44px` + `display: inline-flex`
- `.page-contact .timezone-note` uses `var(--radius-md)` not `8px`
- Dead `.page-faq details`/`summary` CSS block removed (FaqItem renders div.faq-item, not details)
- `.section-alt a` / `.section-alt .link-primary` selector lines removed
- All live CSS rules (`.faq-item`, `.faq-category`, `.faq-question`, `.faq-answer` content rules that are actually used, etc.) remain intact
</success_criteria>

<output>
After completion, create `.planning/quick/260317-tl7-fix-ux-audit-findings-exhibitdetailpage-/260317-tl7-SUMMARY.md` summarising what was changed and confirming all five fixes were applied.
</output>
