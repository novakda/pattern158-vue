---
phase: quick
plan: 260402-45z
type: execute
wave: 1
depends_on: []
files_modified: [src/assets/css/main.css]
autonomous: true
requirements: [GH-1]
github_issue: 1

must_haves:
  truths:
    - "Logo sits in a visually contained, framed space against the dark nav bar"
    - "Border/padding treatment looks intentional, not like a bug fix"
    - "Logo appearance is consistent across light and dark themes"
  artifacts:
    - path: "src/assets/css/main.css"
      provides: "Logo framing styles on nav .logo-img"
      contains: "border"
  key_links:
    - from: "nav .logo-img"
      to: "nav background"
      via: "border/padding creating visual separation"
---

<objective>
Fix navbar logo contrast issue (GitHub Issue #1). The logo PNG has a white/light background that creates jarring contrast against the dark nav bar (`--color-inverse: #1a2838`). Add a subtle dark border and padding around the logo so it appears as a deliberately framed element.

Purpose: Resolve visual bug on live site where logo looks accidentally pasted onto the dark nav.
Output: Updated CSS rules for `nav .logo-img` that frame the logo intentionally.
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/assets/css/main.css (lines 399-407 — current nav .logo-link and nav .logo-img rules)
@src/components/NavBar.vue (lines 54-61 — logo img element)

<interfaces>
<!-- Current CSS rules -->
From src/assets/css/main.css:
```css
/* Nav background */
nav {
    background: var(--color-inverse);  /* #1a2838 — dark navy */
}

/* Current logo rules (lines 399-407) */
nav .logo-link {
    display: flex;
    align-items: center;
}

nav .logo-img {
    height: 48px;
    width: auto;
}
```

<!-- CSS variables available -->
```css
--color-inverse: #1a2838;
--color-inverse-text: #faf9f6;
--space-xs: 0.25rem;
--space-sm: 0.5rem;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add framed logo styling to nav .logo-img</name>
  <files>src/assets/css/main.css</files>
  <action>
Update the `nav .logo-img` rule block (around line 404) to add framing treatment:

- Add `padding: 4px` to give breathing room between the logo content and the border
- Add `border: 2px solid rgba(250, 249, 246, 0.15)` — a very subtle light border that echoes the nav text color at low opacity, creating a deliberate "frame" effect against the dark nav
- Add `border-radius: 4px` for slightly rounded corners matching the site's design language
- Add `background: #fff` to ensure the logo's white background is explicitly set and consistent
- Add `box-sizing: border-box` so padding does not expand the element beyond its height

Keep the existing `height: 48px` and `width: auto` properties.

The result should look like a small framed badge — the border is subtle enough to feel designed-in, not patched. The rgba opacity (0.15) keeps it from competing with the nav links visually.

Do NOT modify any other CSS rules or the NavBar.vue template.
  </action>
  <verify>
    <automated>cd /home/xhiris/projects/pattern158-vue && grep -A 10 "nav .logo-img" src/assets/css/main.css | grep -q "border" && echo "PASS: border rule found" || echo "FAIL: no border rule"</automated>
  </verify>
  <done>The `nav .logo-img` rule includes border, padding, and border-radius properties that create a subtle framed appearance for the logo against the dark nav bar.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Framed logo treatment on the navbar — subtle border and padding around the logo PNG so it sits in a contained space against the dark nav background.</what-built>
  <how-to-verify>
    1. Run `npm run dev` (or the project's dev server)
    2. Open the site in a browser
    3. Look at the navbar logo — it should appear as a small framed element with a subtle border
    4. Toggle dark mode — verify the logo frame still looks intentional in both themes
    5. Resize to mobile width — verify the logo frame scales appropriately
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to adjust</resume-signal>
</task>

</tasks>

<verification>
- `nav .logo-img` CSS rule contains border, padding, and border-radius properties
- Logo visually appears framed/contained in the navbar (human verify)
- No other CSS rules or Vue templates were modified
</verification>

<success_criteria>
Logo displays as a deliberately framed element in the dark navbar, resolving the jarring contrast. The treatment looks intentional, not like a workaround. GitHub Issue #1 can be closed.
</success_criteria>

<output>
After completion, create `.planning/quick/260402-45z-1-bug-caught-on-pattern158-solutions-log/260402-45z-SUMMARY.md`
</output>
