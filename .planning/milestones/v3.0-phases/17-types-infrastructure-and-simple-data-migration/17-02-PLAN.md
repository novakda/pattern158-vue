---
phase: 17-types-infrastructure-and-simple-data-migration
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/data/json/stats.json
  - src/data/json/techPills.json
  - src/data/json/specialties.json
  - src/data/json/brandElements.json
  - src/data/json/methodologySteps.json
  - src/data/stats.ts
  - src/data/techPills.ts
  - src/data/specialties.ts
  - src/data/brandElements.ts
  - src/data/methodologySteps.ts
autonomous: true
requirements:
  - SMPL-01
  - SMPL-02
  - SMPL-03
  - SMPL-04
  - SMPL-05
  - VALD-01
  - VALD-02
  - VALD-03

must_haves:
  truths:
    - "stats data is loaded from stats.json, not hardcoded in TypeScript"
    - "techPills data is loaded from techPills.json, not hardcoded in TypeScript"
    - "specialties data is loaded from specialties.json, not hardcoded in TypeScript"
    - "brandElements data is loaded from brandElements.json, not hardcoded in TypeScript"
    - "methodologySteps data is loaded from methodologySteps.json, not hardcoded in TypeScript"
    - "All existing component imports from @/data/* paths resolve unchanged"
    - "All 64 existing tests pass"
    - "Production build succeeds"
    - "Zero .vue and zero .test.ts files are modified"
  artifacts:
    - path: "src/data/json/stats.json"
      provides: "Stats data as JSON"
      contains: "28+"
    - path: "src/data/json/techPills.json"
      provides: "TechPills data as JSON"
      contains: "JavaScript"
    - path: "src/data/json/specialties.json"
      provides: "Specialties data as JSON"
      contains: "Legacy System Rescue"
    - path: "src/data/json/brandElements.json"
      provides: "BrandElements data as JSON"
      contains: "Provider of Clarity"
    - path: "src/data/json/methodologySteps.json"
      provides: "MethodologySteps data as JSON"
      contains: "Deconstruct the Chaos"
    - path: "src/data/stats.ts"
      provides: "Thin loader re-exporting typed stats from JSON"
      contains: "import statsData from './json/stats.json'"
    - path: "src/data/techPills.ts"
      provides: "Thin loader re-exporting typed techPills from JSON"
      contains: "import techPillsData from './json/techPills.json'"
    - path: "src/data/specialties.ts"
      provides: "Thin loader re-exporting typed specialties from JSON"
      contains: "import specialtiesData from './json/specialties.json'"
    - path: "src/data/brandElements.ts"
      provides: "Thin loader re-exporting typed brandElements from JSON"
      contains: "import brandElementsData from './json/brandElements.json'"
    - path: "src/data/methodologySteps.ts"
      provides: "Thin loader re-exporting typed methodologySteps from JSON"
      contains: "import methodologyStepsData from './json/methodologySteps.json'"
  key_links:
    - from: "src/data/stats.ts"
      to: "src/data/json/stats.json"
      via: "JSON import"
      pattern: "import.*from './json/stats.json'"
    - from: "src/data/stats.ts"
      to: "src/types/index.ts"
      via: "type import"
      pattern: "import type.*from '@/types'"
    - from: "HomePage.vue"
      to: "src/data/stats.ts"
      via: "unchanged import path"
      pattern: "import.*from '@/data/stats'"
---

<objective>
Migrate 5 simple data files to JSON + thin TypeScript loader pattern, proving the externalization approach works.

Purpose: Each data file's content moves to `src/data/json/*.json`, and the original `.ts` file becomes a thin loader that imports JSON, asserts types, and re-exports -- so all downstream consumers see no change.
Output: 5 JSON files in `src/data/json/`, 5 thin loaders replacing the original data files.
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-types-infrastructure-and-simple-data-migration/17-RESEARCH.md
@.planning/phases/17-types-infrastructure-and-simple-data-migration/17-01-SUMMARY.md

<interfaces>
<!-- Types created by Plan 01, available from @/types barrel -->

From src/types/stat.ts:
```typescript
export interface Stat {
  number: string
  label: string
}
```

From src/types/specialty.ts:
```typescript
export interface Specialty {
  title: string
  description: string
}
```

From src/types/brandElement.ts:
```typescript
export interface BrandElement {
  title: string
  label: string
  description: string
  sourceNote?: string
}
```

From src/types/methodologyStep.ts:
```typescript
export interface MethodologyStep {
  title: string
  description: string
}
```

From src/types/index.ts (barrel):
```typescript
export type { Stat } from './stat'
export type { Specialty } from './specialty'
export type { BrandElement } from './brandElement'
export type { MethodologyStep } from './methodologyStep'
```
</interfaces>

<!-- Import map: consumers that must not break -->
<!-- HomePage.vue imports: { techPills } from @/data/techPills, { specialties } from @/data/specialties, { stats } from @/data/stats -->
<!-- PhilosophyPage.vue imports: { brandElements } from @/data/brandElements -->
<!-- StatsSection.vue imports: type { Stat } from @/data/stats -->
<!-- BrandElement.vue imports: type { BrandElement } from @/data/brandElements -->
<!-- MethodologyStep.vue imports: type { MethodologyStep } from @/data/methodologySteps -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create JSON files and thin loaders for stats, techPills, and specialties (SMPL-01, SMPL-02, SMPL-03)</name>
  <files>
    src/data/json/stats.json,
    src/data/json/techPills.json,
    src/data/json/specialties.json,
    src/data/stats.ts,
    src/data/techPills.ts,
    src/data/specialties.ts
  </files>
  <read_first>
    src/data/stats.ts,
    src/data/techPills.ts,
    src/data/specialties.ts,
    src/types/index.ts
  </read_first>
  <action>
Create the `src/data/json/` directory, then create 3 JSON files and replace 3 TypeScript data files with thin loaders.

**JSON files** -- create with exact content from the current data arrays. Preserve all Unicode characters (em dashes, curly quotes). Omit optional keys that have no value.

1. `src/data/json/stats.json`:
```json
[
  { "number": "28+", "label": "Years" },
  { "number": "5,200+", "label": "Projects" },
  { "number": "40+", "label": "Clients" },
  { "number": "930+", "label": "Testimonials" }
]
```

2. `src/data/json/techPills.json`:
```json
[
  "JavaScript",
  "Node.js",
  "Vue.js",
  "TypeScript",
  "SQL",
  "REST APIs",
  "AI Integration",
  "Power Platform"
]
```

3. `src/data/json/specialties.json`:
```json
[
  {
    "title": "Legacy System Rescue",
    "description": "Forensic reverse-engineering of \"black box\" systems. Making undocumented legacy systems work when documentation doesn't exist."
  },
  {
    "title": "Cross-Platform Integration",
    "description": "Building bridges between systems that weren't designed to talk. SCORM, xAPI, AICC protocol expertise."
  },
  {
    "title": "AI Implementation",
    "description": "Thoughtful AI/LLM application. Understanding when to use generative AI vs. structured data for production systems."
  },
  {
    "title": "Systems Architecture",
    "description": "Building foundational systems that harmonize\u2014like the diapason register that gives a pipe organ its character."
  }
]
```

**Thin loaders** -- replace the entire contents of each `.ts` file:

1. `src/data/stats.ts`:
```typescript
import type { Stat } from '@/types'
import statsData from './json/stats.json'

export type { Stat }
export const stats: Stat[] = statsData
```

2. `src/data/techPills.ts` -- no named type needed, `string[]` suffices:
```typescript
import techPillsData from './json/techPills.json'

export const techPills: string[] = techPillsData
```

3. `src/data/specialties.ts`:
```typescript
import type { Specialty } from '@/types'
import specialtiesData from './json/specialties.json'

export type { Specialty }
export const specialties: Specialty[] = specialtiesData
```

After creating all 6 files, run `npx vitest run --project unit` to verify all 64 tests still pass.
  </action>
  <verify>
    <automated>npx vitest run --project unit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cat src/data/json/stats.json | python3 -m json.tool` exits 0 (valid JSON)
    - `cat src/data/json/techPills.json | python3 -m json.tool` exits 0
    - `cat src/data/json/specialties.json | python3 -m json.tool` exits 0
    - `grep "import statsData from './json/stats.json'" src/data/stats.ts` matches
    - `grep "export const stats: Stat\[\]" src/data/stats.ts` matches
    - `grep "export type { Stat }" src/data/stats.ts` matches
    - `grep "import techPillsData from './json/techPills.json'" src/data/techPills.ts` matches
    - `grep "export const techPills: string\[\]" src/data/techPills.ts` matches
    - `grep "import specialtiesData from './json/specialties.json'" src/data/specialties.ts` matches
    - `grep "export type { Specialty }" src/data/specialties.ts` matches
    - `npx vitest run --project unit` outputs "64 passed"
    - No interface definitions remain in the 3 thin loader files (grep "export interface" returns no matches)
  </acceptance_criteria>
  <done>3 JSON files created in src/data/json/, 3 thin loaders replace original data files, all 64 tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create JSON files and thin loaders for brandElements and methodologySteps, run full validation (SMPL-04, SMPL-05, VALD-01, VALD-02, VALD-03)</name>
  <files>
    src/data/json/brandElements.json,
    src/data/json/methodologySteps.json,
    src/data/brandElements.ts,
    src/data/methodologySteps.ts
  </files>
  <read_first>
    src/data/brandElements.ts,
    src/data/methodologySteps.ts,
    src/types/index.ts
  </read_first>
  <action>
Create 2 more JSON files and replace 2 more TypeScript data files with thin loaders. Then run full phase validation.

**JSON files** -- Preserve all Unicode carefully. brandElements has em dashes (\u2014), curly quotes (\u2019, \u201c, \u201d), and optional `sourceNote` fields. Omit `sourceNote` key entirely for items that don't have one (items 1 and 3).

1. `src/data/json/brandElements.json`:
```json
[
  {
    "title": "1. Provider of Clarity",
    "label": "The outcome.",
    "description": "Enter chaos, deliver understanding. Legacy systems, undefined requirements, failing courses \u2014 hand Dan a mess, get back clarity."
  },
  {
    "title": "2. I Cheat, But I Cheat Fair",
    "label": "The method.",
    "description": "Build tools that skip tedium through proper engineering \u2014 documented, maintainable, ethical optimization.",
    "sourceNote": "The Three Stooges, \u201cHealthy, Wealthy and Dumb,\u201d 1938"
  },
  {
    "title": "3. Don\u2019t Fix the Bug, Build the Tool",
    "label": "The approach.",
    "description": "Don\u2019t solve individual problems; prevent entire categories of problems."
  },
  {
    "title": "4. I Seek Knowledge Not for Gain, But to Better Understand Myself",
    "label": "The motivation.",
    "description": "Learn to understand systems and your own thinking, not to pad a resume.",
    "sourceNote": "Harry Potter fanfic \u2014 password to the Department of Mysteries"
  },
  {
    "title": "5. The Machine to Undo Time After Disaster",
    "label": "The role.",
    "description": "Tier 3 escalation \u2014 forensic engineering to understand what went wrong, then build the tool that prevents it again.",
    "sourceNote": "Same fanfic \u2014 the artifact guarded behind the password"
  },
  {
    "title": "6. TASBot for [Domain]",
    "label": "The creative application.",
    "description": "Tool-assisted optimization applied to professional work \u2014 understand the rules deeply, then build tools that operate beyond manual precision.",
    "sourceNote": "Tool-Assisted Speedrunning culture"
  }
]
```

2. `src/data/json/methodologySteps.json`:
```json
[
  {
    "title": "Deconstruct the Chaos",
    "description": "I use forensic engineering to understand \u201cblack box\u201d legacy systems. Never blame the human first \u2014 investigate the design. Systems thinking over code debugging."
  },
  {
    "title": "Build the Tool",
    "description": "I create reusable frameworks (middleware, CMS templates, AI agents) to solve the root cause. Don\u2019t fix individual bugs \u2014 prevent entire categories of problems."
  },
  {
    "title": "Empower the User",
    "description": "I hand off robust tools that make complex processes feel simple. Documented, maintainable, and built to outlast my involvement."
  }
]
```

**Thin loaders** -- replace entire contents:

1. `src/data/brandElements.ts`:
```typescript
import type { BrandElement } from '@/types'
import brandElementsData from './json/brandElements.json'

export type { BrandElement }
export const brandElements: BrandElement[] = brandElementsData
```

2. `src/data/methodologySteps.ts`:
```typescript
import type { MethodologyStep } from '@/types'
import methodologyStepsData from './json/methodologySteps.json'

export type { MethodologyStep }
export const methodologySteps: MethodologyStep[] = methodologyStepsData
```

**Full phase validation** -- after writing all 4 files, run:
1. `npx vitest run --project unit` -- all 64 tests must pass (VALD-01)
2. `npx vue-tsc -b --noEmit` -- TypeScript compilation clean
3. `npx vite build` -- production build succeeds (VALD-02)
4. `git diff --name-only | grep -E '\.(vue|test\.ts)$'` -- must produce no output (VALD-03)
5. `ls src/data/json/*.json | wc -l` -- must output 5
  </action>
  <verify>
    <automated>npx vitest run --project unit && npx vue-tsc -b --noEmit && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cat src/data/json/brandElements.json | python3 -m json.tool` exits 0 (valid JSON)
    - `cat src/data/json/methodologySteps.json | python3 -m json.tool` exits 0
    - `grep "import brandElementsData from './json/brandElements.json'" src/data/brandElements.ts` matches
    - `grep "export const brandElements: BrandElement\[\]" src/data/brandElements.ts` matches
    - `grep "export type { BrandElement }" src/data/brandElements.ts` matches
    - `grep "import methodologyStepsData from './json/methodologySteps.json'" src/data/methodologySteps.ts` matches
    - `grep "export type { MethodologyStep }" src/data/methodologySteps.ts` matches
    - `npx vitest run --project unit` outputs "64 passed"
    - `npx vue-tsc -b --noEmit` exits 0
    - `npx vite build` exits 0
    - `git diff --name-only | grep -E '\.(vue|test\.ts)$'` produces no output
    - `ls src/data/json/*.json | wc -l` outputs "5"
    - `grep "Provider of Clarity" src/data/json/brandElements.json` matches (content preserved)
    - Items 1 and 3 in brandElements.json do NOT have a sourceNote key (optional field correctly omitted)
  </acceptance_criteria>
  <done>All 5 simple data files migrated to JSON + thin loader, all 64 tests pass, production build clean, zero .vue/.test.ts file modifications, 5 JSON files in src/data/json/</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure internal refactor -- data content unchanged, no user input, no API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02 | Tampering | src/data/json/*.json | accept | JSON files are version-controlled, compile-time bundled, no runtime fetch. Same trust model as current .ts files. |
</threat_model>

<verification>
1. `ls src/data/json/*.json | wc -l` -- outputs 5
2. `npx vitest run --project unit` -- all 64 tests pass
3. `npx vue-tsc -b --noEmit` -- TypeScript compiles clean
4. `npx vite build` -- production build succeeds
5. `git diff --name-only | grep -E '\.(vue|test\.ts)$'` -- no .vue or .test.ts files modified
6. Each thin loader imports from `./json/*.json` and `@/types`, re-exports both data and type
</verification>

<success_criteria>
- 5 JSON files exist in src/data/json/ with correct data content
- 5 thin loaders in src/data/ import JSON, assert types, re-export
- Each thin loader re-exports the type (e.g., `export type { Stat }`) so type imports from `@/data/stats` still work
- All 64 existing tests pass unchanged
- Clean production build
- Zero .vue and zero .test.ts file modifications
</success_criteria>

<output>
After completion, create `.planning/phases/17-types-infrastructure-and-simple-data-migration/17-02-SUMMARY.md`
</output>
