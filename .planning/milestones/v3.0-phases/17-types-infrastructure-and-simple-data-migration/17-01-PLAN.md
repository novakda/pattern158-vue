---
phase: 17-types-infrastructure-and-simple-data-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/index.ts
  - src/types/stat.ts
  - src/types/specialty.ts
  - src/types/brandElement.ts
  - src/types/methodologyStep.ts
  - src/types/tag.ts
  - src/types/expertiseLevel.ts
  - src/components/TechTags.types.ts
  - src/components/ExpertiseBadge.types.ts
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02
  - TYPE-03

must_haves:
  truths:
    - "All data interfaces (Stat, Specialty, BrandElement, MethodologyStep) are importable from @/types"
    - "Cross-boundary types (Tag, ExpertiseLevel) are importable from @/types"
    - "expertiseLevels runtime const array is importable from @/types"
    - "All existing component imports of Tag and ExpertiseLevel from component .types.ts files still resolve"
    - "vue-tsc and vite build succeed with zero errors"
  artifacts:
    - path: "src/types/index.ts"
      provides: "Barrel export for all data interfaces"
      exports: ["Stat", "Specialty", "BrandElement", "MethodologyStep", "Tag", "ExpertiseLevel", "expertiseLevels"]
    - path: "src/types/stat.ts"
      provides: "Stat interface"
      contains: "export interface Stat"
    - path: "src/types/specialty.ts"
      provides: "Specialty interface"
      contains: "export interface Specialty"
    - path: "src/types/brandElement.ts"
      provides: "BrandElement interface"
      contains: "export interface BrandElement"
    - path: "src/types/methodologyStep.ts"
      provides: "MethodologyStep interface"
      contains: "export interface MethodologyStep"
    - path: "src/types/tag.ts"
      provides: "Tag interface"
      contains: "export interface Tag"
    - path: "src/types/expertiseLevel.ts"
      provides: "expertiseLevels const and ExpertiseLevel type"
      contains: "export const expertiseLevels"
  key_links:
    - from: "src/types/index.ts"
      to: "src/types/*.ts"
      via: "barrel re-exports"
      pattern: "export.*from './"
    - from: "src/components/TechTags.types.ts"
      to: "src/types/index.ts"
      via: "backward-compat re-export"
      pattern: "export.*from '@/types'"
    - from: "src/components/ExpertiseBadge.types.ts"
      to: "src/types/index.ts"
      via: "backward-compat re-export"
      pattern: "export.*from '@/types'"
---

<objective>
Create the centralized `src/types/` directory with all data interfaces extracted from existing data files and cross-boundary component type files, plus barrel exports.

Purpose: Establish the types infrastructure that all subsequent data migration phases (17, 18, 19) depend on.
Output: `src/types/` directory with 7 type files and barrel index, plus backward-compatible shims in component .types.ts files.
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-types-infrastructure-and-simple-data-migration/17-RESEARCH.md

<interfaces>
<!-- Current type definitions to extract. These are the source of truth. -->

From src/data/stats.ts:
```typescript
export interface Stat {
  number: string
  label: string
}
```

From src/data/specialties.ts:
```typescript
export interface Specialty {
  title: string
  description: string
}
```

From src/data/brandElements.ts:
```typescript
export interface BrandElement {
  title: string
  label: string
  description: string
  sourceNote?: string
}
```

From src/data/methodologySteps.ts:
```typescript
export interface MethodologyStep {
  title: string
  description: string
}
```

From src/components/TechTags.types.ts:
```typescript
export interface Tag {
  label: string
  title: string
}
```

From src/components/ExpertiseBadge.types.ts:
```typescript
export const expertiseLevels = ['deep', 'working', 'aware'] as const
export type ExpertiseLevel = (typeof expertiseLevels)[number]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/types/ directory with all type definition files and barrel index</name>
  <files>
    src/types/stat.ts,
    src/types/specialty.ts,
    src/types/brandElement.ts,
    src/types/methodologyStep.ts,
    src/types/tag.ts,
    src/types/expertiseLevel.ts,
    src/types/index.ts
  </files>
  <read_first>
    src/data/stats.ts,
    src/data/specialties.ts,
    src/data/brandElements.ts,
    src/data/methodologySteps.ts,
    src/components/TechTags.types.ts,
    src/components/ExpertiseBadge.types.ts
  </read_first>
  <action>
Create the `src/types/` directory and 7 files:

1. `src/types/stat.ts` -- Extract Stat interface from src/data/stats.ts:
```typescript
export interface Stat {
  number: string
  label: string
}
```

2. `src/types/specialty.ts` -- Extract Specialty interface from src/data/specialties.ts:
```typescript
export interface Specialty {
  title: string
  description: string
}
```

3. `src/types/brandElement.ts` -- Extract BrandElement interface from src/data/brandElements.ts:
```typescript
export interface BrandElement {
  title: string
  label: string
  description: string
  sourceNote?: string
}
```

4. `src/types/methodologyStep.ts` -- Extract MethodologyStep interface from src/data/methodologySteps.ts:
```typescript
export interface MethodologyStep {
  title: string
  description: string
}
```

5. `src/types/tag.ts` -- Move Tag interface from src/components/TechTags.types.ts (TYPE-02):
```typescript
export interface Tag {
  label: string
  title: string
}
```

6. `src/types/expertiseLevel.ts` -- Move expertiseLevels const and ExpertiseLevel type from src/components/ExpertiseBadge.types.ts (TYPE-02). The `as const` MUST stay in TypeScript, not JSON:
```typescript
export const expertiseLevels = ['deep', 'working', 'aware'] as const
export type ExpertiseLevel = (typeof expertiseLevels)[number]
```

7. `src/types/index.ts` -- Barrel export (TYPE-01):
```typescript
export type { Stat } from './stat'
export type { Specialty } from './specialty'
export type { BrandElement } from './brandElement'
export type { MethodologyStep } from './methodologyStep'
export type { Tag } from './tag'
export type { ExpertiseLevel } from './expertiseLevel'
export { expertiseLevels } from './expertiseLevel'
```

Note: `expertiseLevels` is a runtime value, so use `export { expertiseLevels }` (not `export type`).
  </action>
  <verify>
    <automated>npx vue-tsc -b --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `src/types/index.ts` exists and contains exactly 7 export lines
    - `grep 'export type { Stat }' src/types/index.ts` matches
    - `grep 'export type { Tag }' src/types/index.ts` matches
    - `grep 'export { expertiseLevels }' src/types/index.ts` matches (value export, not type-only)
    - `grep 'export interface Stat' src/types/stat.ts` matches
    - `grep 'export interface Tag' src/types/tag.ts` matches
    - `grep "as const" src/types/expertiseLevel.ts` matches
    - `grep 'sourceNote?' src/types/brandElement.ts` matches
  </acceptance_criteria>
  <done>All 7 type files exist in src/types/ with correct interface definitions, barrel index exports all types and the expertiseLevels runtime value, vue-tsc compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Update cross-boundary type files to re-export from @/types (TYPE-03)</name>
  <files>
    src/components/TechTags.types.ts,
    src/components/ExpertiseBadge.types.ts
  </files>
  <read_first>
    src/components/TechTags.types.ts,
    src/components/ExpertiseBadge.types.ts,
    src/types/index.ts,
    src/data/technologies.ts,
    src/components/TechCard.vue,
    src/components/ExpertiseBadge.vue,
    src/components/TechTags.vue
  </read_first>
  <action>
Replace the contents of the two cross-boundary type files with backward-compatible re-export shims (TYPE-02, TYPE-03). These files currently define the types; after this task they re-export from `@/types` so all existing component imports continue working.

1. `src/components/TechTags.types.ts` -- replace entire file with:
```typescript
export type { Tag } from '@/types'
```

2. `src/components/ExpertiseBadge.types.ts` -- replace entire file with:
```typescript
export { expertiseLevels } from '@/types'
export type { ExpertiseLevel } from '@/types'
```

After writing both files, run `npx vitest run --project unit` to confirm all 64 existing tests still pass, then run `vue-tsc -b && npx vite build` to confirm clean build. Also verify no `.vue` or `.test.ts` files were modified using `git diff --name-only`.
  </action>
  <verify>
    <automated>npx vitest run --project unit && npx vue-tsc -b --noEmit && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cat src/components/TechTags.types.ts` contains exactly `export type { Tag } from '@/types'`
    - `cat src/components/ExpertiseBadge.types.ts` contains `export { expertiseLevels } from '@/types'` AND `export type { ExpertiseLevel } from '@/types'`
    - `npx vitest run --project unit` outputs "64 passed"
    - `npx vue-tsc -b --noEmit` exits 0
    - `npx vite build` exits 0
    - `git diff --name-only | grep -E '\.(vue|test\.ts)$'` produces no output (no .vue or .test.ts files modified)
  </acceptance_criteria>
  <done>Cross-boundary type files are backward-compatible shims re-exporting from @/types, all 64 tests pass, build clean, zero .vue/.test.ts changes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure internal refactor -- no user input, no API calls, no runtime behavior changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | src/types/*.ts | accept | Type definitions are compile-time only; no runtime security impact. Version-controlled in git. |
</threat_model>

<verification>
1. `npx vitest run --project unit` -- all 64 tests pass
2. `npx vue-tsc -b --noEmit` -- TypeScript compiles clean
3. `npx vite build` -- production build succeeds
4. `git diff --name-only | grep -E '\.(vue|test\.ts)$'` -- no .vue or .test.ts files modified
5. `ls src/types/*.ts | wc -l` -- 7 files exist
</verification>

<success_criteria>
- src/types/ directory exists with 7 files (6 type definitions + 1 barrel index)
- All types importable from `@/types` barrel
- Component .types.ts files are backward-compatible shims
- All 64 existing tests pass
- Clean production build
</success_criteria>

<output>
After completion, create `.planning/phases/17-types-infrastructure-and-simple-data-migration/17-01-SUMMARY.md`
</output>
