---
phase: 12-navigation-and-route-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/router.ts
  - src/router.test.ts
  - src/components/NavBar.vue
  - src/components/HomeHero.vue
  - src/pages/HomePage.vue
  - src/components/ContactMethods.vue
  - src/components/exhibit/EngineeringBriefLayout.vue
  - src/components/exhibit/InvestigationReportLayout.vue
  - src/pages/ExhibitDetailPage.test.ts
  - src/components/CtaButtons.stories.ts
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04
  - NAV-05
  - CLN-04
  - CLN-05

must_haves:
  truths:
    - "NavBar shows exactly 6 entries: Home, Philosophy, FAQ, Technologies, Case Files, Contact"
    - "Visiting /case-files loads the CaseFilesPage component"
    - "Visiting /portfolio redirects to /case-files without rendering PortfolioPage"
    - "Visiting /testimonials redirects to /case-files without rendering TestimonialsPage"
    - "Homepage hero CTA reads View Case Files and links to /case-files"
    - "Homepage testimonial CTA reads View All Case Files and links to /case-files"
    - "Contact page shows pattern158.solutions/case-files link"
    - "Detail page back-nav says Back to Case Files linking to /case-files"
  artifacts:
    - path: "src/router.ts"
      provides: "Route registry with /case-files route and redirect entries"
      contains: "path: '/case-files'"
    - path: "src/router.test.ts"
      provides: "Route tests for new route and redirects"
      contains: "case-files"
    - path: "src/components/NavBar.vue"
      provides: "Updated nav with Case Files entry"
      contains: "Case Files"
    - path: "src/pages/ExhibitDetailPage.test.ts"
      provides: "Updated back-nav test assertion"
      contains: 'to="/case-files"'
  key_links:
    - from: "src/router.ts"
      to: "src/pages/CaseFilesPage.vue"
      via: "lazy import in route definition"
      pattern: "import.*CaseFilesPage"
    - from: "src/components/NavBar.vue"
      to: "src/router.ts"
      via: "navLinks array to prop on router-link"
      pattern: "/case-files.*Case Files"
    - from: "src/components/exhibit/EngineeringBriefLayout.vue"
      to: "src/router.ts"
      via: "router-link to attribute"
      pattern: 'to="/case-files"'
---

<objective>
Atomic migration of all route references from /portfolio and /testimonials to /case-files across the entire codebase. Adds the /case-files route, sets up redirects for old URLs, updates NavBar, homepage CTAs, contact page, detail page back-nav, and all tests/stories.

Purpose: Complete the navigation switchover so users reach Case Files through every path that previously led to Portfolio or Field Reports, with zero broken links.
Output: 9 source files updated, 1 test file updated, all tests green.
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-navigation-and-route-migration/12-CONTEXT.md
@.planning/phases/12-navigation-and-route-migration/12-RESEARCH.md

<interfaces>
<!-- Key file states the executor needs. Extracted from codebase. -->

From src/router.ts (current):
```typescript
import type { RouteRecordRaw } from 'vue-router'

export const routes: RouteRecordRaw[] = [
  { path: '/', component: () => import('./pages/HomePage.vue') },
  { path: '/philosophy', component: () => import('./pages/PhilosophyPage.vue') },
  { path: '/faq', component: () => import('./pages/FaqPage.vue') },
  { path: '/technologies', component: () => import('./pages/TechnologiesPage.vue') },
  { path: '/portfolio', component: () => import('./pages/PortfolioPage.vue') },
  { path: '/contact', component: () => import('./pages/ContactPage.vue') },
  { path: '/testimonials', component: () => import('./pages/TestimonialsPage.vue') },
  { path: '/accessibility', component: () => import('./pages/AccessibilityPage.vue') },
  { path: '/review', component: () => import('./pages/ReviewPage.vue') },
  { path: '/exhibits/:slug', component: () => import('./pages/ExhibitDetailPage.vue') },
  { path: '/:pathMatch(.*)*', name: 'not-found', component: () => import('./pages/NotFoundPage.vue') },
]
```

From src/components/NavBar.vue (lines 41-49):
```typescript
const navLinks = [
  { to: '/', label: 'Home' },
  { to: '/philosophy', label: 'Philosophy' },
  { to: '/faq', label: 'FAQ' },
  { to: '/technologies', label: 'Technologies' },
  { to: '/portfolio', label: 'Portfolio' },
  { to: '/contact', label: 'Contact' },
  { to: '/testimonials', label: 'Field Reports' },
]
```

From src/router.test.ts (current):
```typescript
describe('router routes', () => {
  it('has a /exhibits/:slug dynamic route', () => { ... })
  it('/exhibits/:slug route appears before the catch-all', () => { ... })
  it('catch-all route name is not-found', () => { ... })
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /case-files route, redirects, and route tests</name>
  <files>src/router.ts, src/router.test.ts</files>
  <read_first>
    - src/router.ts
    - src/router.test.ts
    - .planning/phases/12-navigation-and-route-migration/12-RESEARCH.md
  </read_first>
  <action>
**src/router.ts** — Replace the full routes array with this exact content (per D-01, D-02):

```typescript
import type { RouteRecordRaw } from 'vue-router'

export const routes: RouteRecordRaw[] = [
  { path: '/', component: () => import('./pages/HomePage.vue') },
  { path: '/philosophy', component: () => import('./pages/PhilosophyPage.vue') },
  { path: '/faq', component: () => import('./pages/FaqPage.vue') },
  { path: '/technologies', component: () => import('./pages/TechnologiesPage.vue') },
  { path: '/case-files', component: () => import('./pages/CaseFilesPage.vue') },
  { path: '/portfolio', redirect: '/case-files' },
  { path: '/testimonials', redirect: '/case-files' },
  { path: '/contact', component: () => import('./pages/ContactPage.vue') },
  { path: '/accessibility', component: () => import('./pages/AccessibilityPage.vue') },
  { path: '/review', component: () => import('./pages/ReviewPage.vue') },
  { path: '/exhibits/:slug', component: () => import('./pages/ExhibitDetailPage.vue') },
  { path: '/:pathMatch(.*)*', name: 'not-found', component: () => import('./pages/NotFoundPage.vue') },
]
```

Key changes:
- New `/case-files` route with lazy import of CaseFilesPage.vue (BEFORE catch-all per Pitfall 1)
- `/portfolio` changed from component import to `redirect: '/case-files'` (NO component property)
- `/testimonials` changed from component import to `redirect: '/case-files'` (NO component property)

**src/router.test.ts** — Add three new tests to the existing describe block:

```typescript
it('has a /case-files route with a component', () => {
  const route = routes.find(r => r.path === '/case-files')
  expect(route).toBeDefined()
  expect(route).toHaveProperty('component')
})

it('/case-files route appears before the catch-all', () => {
  const caseFilesIndex = routes.findIndex(r => r.path === '/case-files')
  const catchAllIndex = routes.findIndex(r => r.name === 'not-found')
  expect(caseFilesIndex).toBeGreaterThan(-1)
  expect(caseFilesIndex).toBeLessThan(catchAllIndex)
})

it('/portfolio redirects to /case-files', () => {
  const route = routes.find(r => r.path === '/portfolio')
  expect(route).toBeDefined()
  expect(route).toHaveProperty('redirect', '/case-files')
  expect(route).not.toHaveProperty('component')
})

it('/testimonials redirects to /case-files', () => {
  const route = routes.find(r => r.path === '/testimonials')
  expect(route).toBeDefined()
  expect(route).toHaveProperty('redirect', '/case-files')
  expect(route).not.toHaveProperty('component')
})
```

Keep all 3 existing tests unchanged. Append the 4 new tests inside the same `describe('router routes', ...)` block.
  </action>
  <verify>
    <automated>cd /home/xhiris/projects/pattern158-vue && npx vitest run src/router.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/router.ts contains `path: '/case-files', component: () => import('./pages/CaseFilesPage.vue')`
    - src/router.ts contains `path: '/portfolio', redirect: '/case-files'` with NO component property on that route
    - src/router.ts contains `path: '/testimonials', redirect: '/case-files'` with NO component property on that route
    - src/router.ts does NOT contain `import('./pages/PortfolioPage.vue')`
    - src/router.ts does NOT contain `import('./pages/TestimonialsPage.vue')`
    - src/router.test.ts contains `it('has a /case-files route`
    - src/router.test.ts contains `it('/portfolio redirects to /case-files`
    - src/router.test.ts contains `it('/testimonials redirects to /case-files`
    - `npx vitest run src/router.test.ts` exits 0 with 7 tests passing
  </acceptance_criteria>
  <done>Route registry has /case-files route pointing to CaseFilesPage, /portfolio and /testimonials redirect to /case-files with no component imports, and 4 new router tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update all hardcoded route references across 8 files</name>
  <files>src/components/NavBar.vue, src/components/HomeHero.vue, src/pages/HomePage.vue, src/components/ContactMethods.vue, src/components/exhibit/EngineeringBriefLayout.vue, src/components/exhibit/InvestigationReportLayout.vue, src/pages/ExhibitDetailPage.test.ts, src/components/CtaButtons.stories.ts</files>
  <read_first>
    - src/components/NavBar.vue
    - src/components/HomeHero.vue
    - src/pages/HomePage.vue
    - src/components/ContactMethods.vue
    - src/components/exhibit/EngineeringBriefLayout.vue
    - src/components/exhibit/InvestigationReportLayout.vue
    - src/pages/ExhibitDetailPage.test.ts
    - src/components/CtaButtons.stories.ts
    - .planning/phases/12-navigation-and-route-migration/12-CONTEXT.md
  </read_first>
  <action>
Update all 8 files with exact string replacements. Each change is listed with the exact before/after values.

**1. src/components/NavBar.vue** (per D-03, D-04) — Replace the entire navLinks array (lines 41-49) with:
```typescript
const navLinks = [
  { to: '/', label: 'Home' },
  { to: '/philosophy', label: 'Philosophy' },
  { to: '/faq', label: 'FAQ' },
  { to: '/technologies', label: 'Technologies' },
  { to: '/case-files', label: 'Case Files' },
  { to: '/contact', label: 'Contact' },
]
```
Key: Remove the Portfolio and Field Reports entries entirely. Add single Case Files entry in 5th position. Array goes from 7 items to 6 items.

**2. src/components/HomeHero.vue** (per D-05) — Line 29-30, change the CtaButtons props:
- `secondary-label="View My Work"` becomes `secondary-label="View Case Files"`
- `secondary-to="/portfolio"` becomes `secondary-to="/case-files"`

**3. src/pages/HomePage.vue** (per D-06) — Line 80, change:
- `<router-link to="/testimonials" class="btn btn-primary">View All Field Reports</router-link>`
  becomes
  `<router-link to="/case-files" class="btn btn-primary">View All Case Files</router-link>`

**4. src/components/ContactMethods.vue** (per D-07) — Lines 28-29, change all three values:
- `<strong>Portfolio</strong>` becomes `<strong>Case Files</strong>`
- `href="/portfolio"` becomes `href="/case-files"`
- `pattern158.solutions/portfolio` (visible text) becomes `pattern158.solutions/case-files`

Note: This file uses `<a href>` NOT `<router-link>`. Keep it as an `<a>` tag.

**5. src/components/exhibit/EngineeringBriefLayout.vue** (per D-08) — Line 13, change:
- `<router-link to="/portfolio">&larr; Back to Portfolio</router-link>`
  becomes
  `<router-link to="/case-files">&larr; Back to Case Files</router-link>`

**6. src/components/exhibit/InvestigationReportLayout.vue** (per D-08) — Line 13, change:
- `<router-link to="/portfolio">&larr; Back to Portfolio</router-link>`
  becomes
  `<router-link to="/case-files">&larr; Back to Case Files</router-link>`

**7. src/pages/ExhibitDetailPage.test.ts** (Discretion) — Lines 50-61, update the test:
- Test name: `'renders a Back to Portfolio link'` becomes `'renders a Back to Case Files link'`
- Selector: `wrapper.find('[to="/portfolio"]')` becomes `wrapper.find('[to="/case-files"]')`
- Variable name: `portfolioLink` becomes `caseFilesLink`

**8. src/components/CtaButtons.stories.ts** (Discretion) — Two changes:
- Line 17 (Default story): `secondaryTo: '/portfolio'` becomes `secondaryTo: '/case-files'`
- Line 24 (AlternateLabels story): `primaryTo: '/portfolio'` becomes `primaryTo: '/case-files'` and `primaryLabel: 'See Portfolio'` becomes `primaryLabel: 'See Case Files'`
  </action>
  <verify>
    <automated>cd /home/xhiris/projects/pattern158-vue && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - src/components/NavBar.vue contains `{ to: '/case-files', label: 'Case Files' }`
    - src/components/NavBar.vue does NOT contain `/portfolio` or `/testimonials` or `Field Reports`
    - src/components/NavBar.vue navLinks array has exactly 6 entries (not 7)
    - src/components/HomeHero.vue contains `secondary-label="View Case Files"`
    - src/components/HomeHero.vue contains `secondary-to="/case-files"`
    - src/pages/HomePage.vue contains `to="/case-files"` and `View All Case Files`
    - src/pages/HomePage.vue does NOT contain `to="/testimonials"` or `View All Field Reports`
    - src/components/ContactMethods.vue contains `href="/case-files"` and `pattern158.solutions/case-files` and `<strong>Case Files</strong>`
    - src/components/exhibit/EngineeringBriefLayout.vue contains `to="/case-files"` and `Back to Case Files`
    - src/components/exhibit/InvestigationReportLayout.vue contains `to="/case-files"` and `Back to Case Files`
    - src/pages/ExhibitDetailPage.test.ts contains `Back to Case Files` and `[to="/case-files"]`
    - src/components/CtaButtons.stories.ts contains `secondaryTo: '/case-files'` and `primaryTo: '/case-files'`
    - `npx vitest run` exits 0 (full suite green)
    - `grep -r '"/portfolio"' src/ --include='*.vue' --include='*.ts'` returns ONLY hits in PortfolioPage.vue (its own useSeo path, Phase 13 scope) and TestimonialsPage.vue (Phase 13 scope)
  </acceptance_criteria>
  <done>All 13 hardcoded route references updated to /case-files. No stale /portfolio or /testimonials references remain in active code. Full test suite passes.</done>
</task>

</tasks>

<verification>
1. `npx vitest run` — full test suite green (all existing + 4 new router tests)
2. `grep -rn '"/portfolio"' src/ --include='*.vue' --include='*.ts'` — only hits in PortfolioPage.vue and TestimonialsPage.vue (Phase 13 deletion scope)
3. `grep -rn '"/testimonials"' src/ --include='*.vue' --include='*.ts'` — only hits in TestimonialsPage.vue (Phase 13 deletion scope)
4. `grep -c 'case-files' src/router.ts` — returns 3 (one route + two redirects)
</verification>

<success_criteria>
- /case-files route registered and CaseFilesPage lazy-loaded
- /portfolio and /testimonials redirect to /case-files (no component imports)
- NavBar shows 6 items: Home, Philosophy, FAQ, Technologies, Case Files, Contact
- All 13 old route references migrated to /case-files
- 4 new router tests + all existing tests pass
- Zero stale /portfolio or /testimonials references in active source files
</success_criteria>

<output>
After completion, create `.planning/phases/12-navigation-and-route-migration/12-01-SUMMARY.md`
</output>
