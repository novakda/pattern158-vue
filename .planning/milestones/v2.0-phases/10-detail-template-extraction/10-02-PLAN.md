---
phase: 10-detail-template-extraction
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/pages/ExhibitDetailPage.test.ts
  - src/components/exhibit/InvestigationReportLayout.test.ts
  - src/components/exhibit/EngineeringBriefLayout.test.ts
autonomous: true
requirements:
  - DTPL-01
  - DTPL-02
  - DTPL-03
  - DTPL-04

must_haves:
  truths:
    - "Dispatcher delegation to correct layout component is tested for both exhibit types"
    - "InvestigationReportLayout renders NTSB-style elements (timeline, probable cause, findings table, personnel table)"
    - "EngineeringBriefLayout renders with Engineering Brief badge and non-forensic contextHeading"
    - "Both layouts render type label badge with correct text and CSS class"
    - "Context fallback (contextText without sections) is tested in at least one layout"
  artifacts:
    - path: "src/components/exhibit/InvestigationReportLayout.test.ts"
      provides: "Unit tests for IR layout rendering"
      min_lines: 30
    - path: "src/components/exhibit/EngineeringBriefLayout.test.ts"
      provides: "Unit tests for EB layout rendering"
      min_lines: 30
    - path: "src/pages/ExhibitDetailPage.test.ts"
      provides: "Updated dispatcher tests verifying layout delegation"
      min_lines: 40
  key_links:
    - from: "src/pages/ExhibitDetailPage.test.ts"
      to: "src/pages/ExhibitDetailPage.vue"
      via: "import and mount"
      pattern: "import ExhibitDetailPage"
    - from: "src/components/exhibit/InvestigationReportLayout.test.ts"
      to: "src/components/exhibit/InvestigationReportLayout.vue"
      via: "import and mount"
      pattern: "import InvestigationReportLayout"
    - from: "src/components/exhibit/EngineeringBriefLayout.test.ts"
      to: "src/components/exhibit/EngineeringBriefLayout.vue"
      via: "import and mount"
      pattern: "import EngineeringBriefLayout"
---

<objective>
Create targeted test coverage for the new layout components and update the existing dispatcher tests to verify delegation behavior.

Purpose: Ensure DTPL-01 through DTPL-04 have automated verification -- dispatcher delegates correctly, IR layout renders NTSB-style content, EB layout renders with engineering framing, both show type badges.
Output: Two new test files for layout components, updated dispatcher test file
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-detail-template-extraction/10-CONTEXT.md
@.planning/phases/10-detail-template-extraction/10-RESEARCH.md
@.planning/phases/10-detail-template-extraction/10-01-SUMMARY.md

<interfaces>
<!-- Key types for test setup -->

From src/data/exhibits.ts:
```typescript
export type ExhibitType = 'investigation-report' | 'engineering-brief'
export interface Exhibit {
  label: string; client: string; date: string; title: string;
  exhibitType: ExhibitType; quotes?: ExhibitQuote[];
  contextHeading?: string; contextText?: string;
  resolutionTable?: ExhibitResolutionRow[];
  sections?: ExhibitSection[]; impactTags: string[];
  exhibitLink: string;
}
// exhibits array has 15 entries. Exhibit J (exhibit-j) is investigation-report. Exhibit A (exhibit-a) is engineering-brief.
```

From src/pages/ExhibitDetailPage.test.ts (current test patterns):
```typescript
// Uses vi.mock for vue-router and @unhead/vue
// Mounts with global: { stubs: { RouterLink: true, TechTags: true } }
// Sets route params via vi.mocked(useRoute).mockReturnValue
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create layout component test files</name>
  <files>src/components/exhibit/InvestigationReportLayout.test.ts, src/components/exhibit/EngineeringBriefLayout.test.ts</files>
  <read_first>src/pages/ExhibitDetailPage.test.ts, src/components/exhibit/InvestigationReportLayout.vue, src/components/exhibit/EngineeringBriefLayout.vue, src/data/exhibits.ts</read_first>
  <action>
**InvestigationReportLayout.test.ts** -- Create unit tests using Vitest + @vue/test-utils:

Import setup:
```typescript
import { describe, it, expect } from 'vitest'
import { mount } from '@vue/test-utils'
import InvestigationReportLayout from './InvestigationReportLayout.vue'
import { exhibits } from '@/data/exhibits'
```

Use real exhibit data from the exhibits array for test fixtures (no mocking needed since layouts are pure prop-driven components with no route/head dependencies):
- IR fixture: `exhibits.find(e => e.exhibitLink === '/exhibits/exhibit-j')!` (Exhibit J, GM investigation report -- has timeline, findings table, probable cause, personnel table)
- Minimal EB fixture for negative test: `exhibits.find(e => e.exhibitLink === '/exhibits/exhibit-a')!`

Test cases:
1. `renders exhibit title` -- mount with IR fixture, assert wrapper.text() contains the exhibit's title string
2. `renders Investigation Report badge with badge-aware class` (DTPL-04) -- mount with IR fixture, assert `wrapper.find('.exhibit-type-badge.badge-aware').exists()` is true and `wrapper.text()` contains `'Investigation Report'`
3. `renders sections when exhibit has sections` (DTPL-02) -- mount with IR fixture (exhibit-j has sections), assert `wrapper.findAll('.exhibit-section').length` is greater than 0
4. `renders timeline section for IR exhibit` (DTPL-02) -- mount with exhibit-j fixture, assert `wrapper.find('.exhibit-timeline').exists()` is true (exhibit-j has a timeline section)
5. `renders impact tags section` -- mount with IR fixture, assert wrapper.text() contains `'Impact'`
6. `renders context fallback when no sections` -- create a minimal Exhibit object with `exhibitType: 'investigation-report'`, no sections, but with `contextHeading: 'Test Heading'` and `contextText: 'Test context'`, assert wrapper.text() contains `'Test context'`

Mount config for all tests: `{ global: { stubs: { RouterLink: true, TechTags: true } } }`

**EngineeringBriefLayout.test.ts** -- Same structure with these differences:

EB fixture: `exhibits.find(e => e.exhibitLink === '/exhibits/exhibit-a')!` (Exhibit A, engineering-brief)

Test cases:
1. `renders exhibit title` -- mount with EB fixture, assert wrapper.text() contains the exhibit's title
2. `renders Engineering Brief badge with badge-deep class` (DTPL-04) -- mount with EB fixture, assert `wrapper.find('.exhibit-type-badge.badge-deep').exists()` is true and `wrapper.text()` contains `'Engineering Brief'`
3. `renders sections when exhibit has sections` (DTPL-03) -- mount with EB fixture, assert `wrapper.findAll('.exhibit-section').length` is greater than 0
4. `renders quotes when exhibit has quotes` -- mount with exhibit-a fixture (has quotes), assert `wrapper.find('.exhibit-quote').exists()` is true
5. `renders impact tags section` -- assert wrapper.text() contains `'Impact'`
6. `uses non-forensic contextHeading` (DTPL-03) -- exhibit-a has `contextHeading: 'Context'`, assert wrapper.text() contains `'Context'` and does NOT contain `'Investigation Summary'`
  </action>
  <verify>
    <automated>npx vitest run --project unit src/components/exhibit/InvestigationReportLayout.test.ts src/components/exhibit/EngineeringBriefLayout.test.ts</automated>
  </verify>
  <acceptance_criteria>
- `src/components/exhibit/InvestigationReportLayout.test.ts` exists and contains `import InvestigationReportLayout`
- `src/components/exhibit/InvestigationReportLayout.test.ts` contains `badge-aware` (badge class assertion)
- `src/components/exhibit/InvestigationReportLayout.test.ts` contains `Investigation Report` (badge text assertion)
- `src/components/exhibit/InvestigationReportLayout.test.ts` contains `exhibit-timeline` (timeline rendering assertion)
- `src/components/exhibit/InvestigationReportLayout.test.ts` contains `contextText` (fallback test)
- `src/components/exhibit/EngineeringBriefLayout.test.ts` exists and contains `import EngineeringBriefLayout`
- `src/components/exhibit/EngineeringBriefLayout.test.ts` contains `badge-deep` (badge class assertion)
- `src/components/exhibit/EngineeringBriefLayout.test.ts` contains `Engineering Brief` (badge text assertion)
- `src/components/exhibit/EngineeringBriefLayout.test.ts` contains `exhibit-quote` (quote rendering assertion)
- Both test files exit 0 when run with vitest
  </acceptance_criteria>
  <done>Both layout test files exist with 6 tests each covering badge rendering (DTPL-04), section rendering (DTPL-02/03), and content display. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update dispatcher tests for layout delegation</name>
  <files>src/pages/ExhibitDetailPage.test.ts</files>
  <read_first>src/pages/ExhibitDetailPage.test.ts, src/pages/ExhibitDetailPage.vue, src/components/exhibit/InvestigationReportLayout.vue, src/components/exhibit/EngineeringBriefLayout.vue</read_first>
  <action>
Update the existing ExhibitDetailPage.test.ts to verify dispatcher delegation (DTPL-01) while preserving the existing 6 tests that still pass.

Add these new tests to the existing `describe('ExhibitDetailPage', ...)` block:

1. `dispatches to InvestigationReportLayout for investigation-report exhibit` (DTPL-01) -- set route params to `{ slug: 'exhibit-j' }`, mount with `{ global: { stubs: { RouterLink: true, TechTags: true, InvestigationReportLayout: true, EngineeringBriefLayout: true } } }`, assert `wrapper.findComponent({ name: 'InvestigationReportLayout' }).exists()` is true OR find the stubbed component via `wrapper.find('investigation-report-layout-stub')`. Note: if findComponent doesn't work with stubs, use `wrapper.html()` to assert it contains `investigation-report-layout-stub` or the actual rendered content.

2. `dispatches to EngineeringBriefLayout for engineering-brief exhibit` (DTPL-01) -- set route params to `{ slug: 'exhibit-a' }`, same stub approach, assert `EngineeringBriefLayout` stub is rendered.

3. `does not render EngineeringBriefLayout for investigation-report exhibit` -- set route to exhibit-j, assert EB layout stub is NOT present.

IMPORTANT: The existing 6 tests mount WITHOUT stubbing the layout components (they stub RouterLink and TechTags only). This means the layouts render fully, which is correct for integration-level testing. The NEW delegation tests should stub both layout components to test the dispatcher logic in isolation.

Update the existing tests' stub configuration if needed. The existing tests that check for badge text, title, and label will still pass because they render through the layout components (which render the full content). If any existing test fails because the layout components need RouterLink/TechTags stubs, update the stubs list to include those components.

Run all tests to confirm: both existing 6 tests and new 3 tests pass.
  </action>
  <verify>
    <automated>npx vitest run --project unit src/pages/ExhibitDetailPage.test.ts</automated>
  </verify>
  <acceptance_criteria>
- `src/pages/ExhibitDetailPage.test.ts` contains `dispatches` or `delegates` in a test description
- `src/pages/ExhibitDetailPage.test.ts` references both `InvestigationReportLayout` and `EngineeringBriefLayout` (either as imports or stub names)
- `src/pages/ExhibitDetailPage.test.ts` contains at least 8 `it(` calls (6 existing + 2-3 new)
- `npx vitest run --project unit src/pages/ExhibitDetailPage.test.ts` exits 0
  </acceptance_criteria>
  <done>Dispatcher test file has 8-9 tests total. Original 6 tests pass unchanged. New tests verify DTPL-01 dispatcher delegation to correct layout based on exhibitType.</done>
</task>

</tasks>

<verification>
1. `npx vitest run --project unit src/pages/ExhibitDetailPage.test.ts src/components/exhibit/` -- all tests green
2. `npx vitest run --project unit` -- full unit test suite green
</verification>

<success_criteria>
- InvestigationReportLayout.test.ts has 6 tests covering DTPL-02 and DTPL-04, all passing
- EngineeringBriefLayout.test.ts has 6 tests covering DTPL-03 and DTPL-04, all passing
- ExhibitDetailPage.test.ts has 8-9 tests covering DTPL-01, all passing
- Full unit test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/10-detail-template-extraction/10-02-SUMMARY.md`
</output>
