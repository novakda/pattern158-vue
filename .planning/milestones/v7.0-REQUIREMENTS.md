# Requirements Archive: v7.0 Static Markdown Export Pipeline

**Archived:** 2026-04-19
**Status:** ABORTED — 2 of 9 phases shipped
**Abort notice:** `.planning/v7.0-ABORT-NOTICE.md`

For current requirements, see `.planning/PROJECT.md`.

---

# Requirements: Pattern 158 — Static Markdown Export Pipeline

**Defined:** 2026-04-10
**Aborted:** 2026-04-19
**Core Value:** Every page template should be scannable and self-documenting through well-named components that enforce design consistency

## v7.0 Scope — MVP + Quick Wins

Generate two static markdown artifacts alongside the site build: a monolithic document mirroring the site tree by heading levels, and an Obsidian-ready vault folder with one markdown page per site page organized to match the menu structure.

## Foundation (SHIPPED)

- [x] **SFC-01..07**: Hardcoded prose moved from 15 Vue files (7 pages + 8 delegated components) into 14 `src/content/*.ts` modules; SFCs `v-for` over imported arrays; 7 Playwright browser regression tests per refactored page — Phase 37
- [x] **LOAD-01**: Thin-loader invariant formalized — `src/data/*.ts` loaders may only import JSON + assert types + re-export; no sort/filter/computed/map/ref/reactive/watch; `as const satisfies` literal registries allowlisted; enforcement test in `src/data/__tests__/loaders.thin.test.ts` — Phase 37
- [x] **SCAF-01/02/03**: `scripts/markdown-export/` scaffold with separate `tsconfig.scripts.json`, pnpm migration, `tsx` + `yaml` + `github-slugger` devDeps, `build:markdown` + `test:scripts` npm scripts, Vitest `scripts` project — Phase 38
- [x] **AUDT-01**: `docs/` directory collision audit (Vite outDir, Storybook, Wrangler, Vitest) produced as `038-DOCS-AUDIT.md` — verdict GO — Phase 38

## IR + Shared Primitives (SHIPPED)

- [x] **IR-01/02**: `DocNode` discriminated union (6 block kinds) + `InlineSpan` inline tree (6 kinds) + `PageDoc` wrapper + `HeadingLevel` + `assertNever` exhaustiveness helper — Phase 38
- [x] **PRIM-01**: Nine IR primitive factories: `text`, `heading`, `paragraph`, `link`, `wikilink`, `caption`, `list`, `table`, `blockquote` — zero rendering/escape logic — Phase 38
- [x] **ESCP-01/02/03/04**: Context-specific escape helpers: `escapeProse`, `escapeTableCell`, `escapeWikilinkTarget`, `escapeCodeBlockContent` with 53 unit tests — Phase 38
- [x] **FM-01/02**: Frontmatter YAML serializer with canonical key order (title→aliases→tags→date→cssclasses), plural-key enforcement, block-style arrays — Phase 38

## Extractors (NOT EXECUTED — planning retained)

- [ ] **MAP-01**: `site-map.ts` with 7 static routes + dynamic exhibit children, excludes `/review`, `/diag`, 404
- [ ] **EXT-01/02/03/04/05/06/07**: One extractor per static page: home, philosophy, technologies, case-files, faq, contact, accessibility
- [ ] **EXB-01/02/03**: `exhibit.ts` extractor handling all 5 section types (text, table, timeline, metadata, flow) + typed personnel/technologies/findings arrays + quotes across all 15 exhibits

## Monolithic Renderer (NOT EXECUTED)

- [ ] **MONO-01**: Heading-level shifting by nav depth (T3) — full exhibit details inline
- [ ] **MONO-02**: Auto-generated ToC at top of monolithic file using `github-slugger` anchors (T4)
- [ ] **MONO-03**: Internal link rewriting: route strings → anchor links (T5)
- [ ] **MONO-04**: GFM only, no Obsidian-isms in monolithic output (D6)
- [ ] **MONO-05**: Generated-file warning banner (HTML comment at top) (D8)
- [ ] **MONO-06**: Monotonic heading hierarchy verified via `remark-parse` integration test

## Obsidian Vault Renderer (NOT EXECUTED)

- [ ] **VAULT-01**: Folder structure mirrors menu (T8)
- [ ] **VAULT-02**: YAML frontmatter: `title`, `aliases`, `tags`, optional `date` — plural keys only (T6)
- [ ] **VAULT-03**: Flat kebab-case exhibit tags from `exhibitType` + `findings[].category` (T9)
- [ ] **VAULT-04**: Wikilinks between vault notes with whitelist + uniqueness assertion on basenames (T7)
- [ ] **VAULT-05**: Aliases for exhibits (label + client) and page title variants (D4)
- [ ] **VAULT-06**: Exhibit filename format `Exhibit A - Short Title.md` with sanitized reserved chars (T14)
- [ ] **VAULT-07**: Obsidian callout blocks for exhibit quotes (`> [!quote]`) — graceful GitHub degradation (D3)
- [ ] **VAULT-08**: FAQ rendered as one note per page with questions as H3 + `^question-id` block anchors (T13)
- [ ] **VAULT-09**: Images skipped, alt text emitted as italicized captions (T10)
- [ ] **VAULT-10**: GFM tables for personnel / technologies; finding-per-H4 for findings with pill-style inline tags (T11)
- [ ] **VAULT-11**: Generated-file warning banner after frontmatter (D8)
- [ ] **VAULT-12**: All section types rendered (text / table / timeline / metadata / flow) (T15)
- [ ] **QA-01**: Manual Obsidian QA pass — tags visible in tag pane, wikilinks resolve, frontmatter parses

## File Writer + Orchestration (NOT EXECUTED)

- [ ] **WRIT-01**: Clean wipe of `docs/obsidian-vault/` + idempotent writes, `\n` line endings only
- [ ] **ORCH-01**: `scripts/markdown-export/index.ts` orchestrator wires site-map → extractors → renderers → writer

## Build Integration (NOT EXECUTED)

- [ ] **INTG-01**: `"build": "vue-tsc -b && vite build && npm run build:markdown"` chain
- [ ] **INTG-02**: `docs/**` tracked in git (`.gitignore` audit) + `.gitattributes` entry `docs/** text eol=lf linguist-generated`
- [ ] **INTG-03**: CI drift guard: `npm run build:markdown && git diff --exit-code docs/` fails PRs with stale artifacts
- [ ] **INTG-04**: Two-run determinism test in CI: regenerate twice, diff outputs, assert byte-identical

## Hard Constraints (preserved for reference)

- Forbidden: `@/` imports inside `scripts/markdown-export/**`
- Forbidden: `Date.now()`, `new Date()`, `process.hrtime`, `performance.now()` in generator
- Forbidden: `Promise.all` on reads feeding ordered output
- Forbidden: `os.EOL` — always `\n` literals
- Forbidden: manual editing of files under `docs/`
- Forbidden: `postinstall` / `prepare` hooks running the generator
- Forbidden: `assert { type: 'json' }` — use `fs.readFile` + `JSON.parse`
- Forbidden: singular frontmatter keys `tag`, `alias`, `cssclass`
- Forbidden: line wrapping of prose in generated markdown
- Forbidden: mtime/hash-based "skip unchanged" logic
- Forbidden: parsing `.vue` SFCs from the generator

## Abort Rationale (summary)

The rendered live site is the highest-fidelity representation of the site's actual content. v7.0's source-module extraction approach misses hardcoded component text, data modules rendered into prose, reading-order fidelity, composition decisions, and dynamic exhibit routes. For editorial work, capture the truth, not the source. Full reasoning in `.planning/v7.0-ABORT-NOTICE.md`.
