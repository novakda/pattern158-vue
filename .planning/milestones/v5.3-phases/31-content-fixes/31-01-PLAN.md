---
phase: 31-content-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/json/faq.json
autonomous: true
requirements:
  - REFS-01
  - REFS-02
  - ACCY-01
  - ACCY-02
  - ACCY-03
  - ACCY-04

must_haves:
  truths:
    - "FAQ Q5 says 'case files' not 'portfolio'"
    - "FAQ Q10 says 'case files' not 'featured projects'"
    - "FAQ Q4 lists React, Python, Power Platform, Claude Code as production experience"
    - "FAQ Q7 lists HSBC, Wells Fargo, TD Bank instead of JPMorgan Chase, Kmart Credit"
    - "FAQ Q7 says 'Weill Cornell Medicine' not 'Cornell Medical'"
    - "FAQ Q7 does not categorize GM as Retail"
    - "FAQ Q1 says 'WCAG 2.1 AA / Section 508' not 'WCAG 2.1 AA+'"
    - "FAQ Q2 does not contain async/sync communication detail (moved to Q8)"
    - "FAQ Q6 mentions Power Platform alongside Copilot Studio"
  artifacts:
    - path: "src/data/json/faq.json"
      provides: "All 14 FAQ answers with corrected content"
      contains: "case files"
  key_links:
    - from: "src/data/json/faq.json"
      to: "FaqPage.vue"
      via: "JSON import"
      pattern: "faq\\.json"
---

<objective>
Fix all factual inaccuracies, stale references, and terminology drift in faq.json identified by the Phase 30 cross-page content audit.

Purpose: FAQ answers should be factually accurate and use current site terminology so hiring managers get consistent information across all pages.
Output: Updated src/data/json/faq.json with all 6 requirement areas addressed.
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-content-fixes/31-CONTEXT.md
@.planning/phases/30-cross-page-content-audit/30-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale references and accessibility phrasing (REFS-01, ACCY-03, ACCY-04)</name>
  <files>src/data/json/faq.json</files>
  <read_first>src/data/json/faq.json</read_first>
  <action>
Edit src/data/json/faq.json with these exact text replacements:

**Q5 (index 4) — "Do you work with legacy systems?" (per REFS-01):**
Replace the second paragraph of the answer:
OLD: "See the portfolio for examples like the legacy CMS rescue and cross-domain SCORM framework."
NEW: "See the case files for examples like the legacy CMS rescue and cross-domain SCORM framework."

**Q10 (index 9) — "What's your approach to unclear requirements?" (per REFS-01):**
Replace the second paragraph of the answer:
OLD: "See the featured projects for examples of how I approach undocumented systems and rebuild them systematically."
NEW: "See the case files for examples of how I approach undocumented systems and rebuild them systematically."

**Q1 (index 0) — "Are you available for new projects?" (per ACCY-04):**
In the second paragraph, replace:
OLD: "accessibility remediation (WCAG 2.1 AA+)"
NEW: "accessibility remediation (WCAG 2.1 AA / Section 508)"

**Q6 (index 5) — "Are you experienced with AI and automation?" (per ACCY-02, ACCY-03):**
GitHub Spec Kit reference is verified correct — no change needed (per ACCY-03).
In the first paragraph, replace:
OLD: "built Copilot Studio agents for enterprise clients"
NEW: "built Copilot Studio and Power Platform agents for enterprise clients"
This adds Power Platform per ACCY-02. Keep everything else in Q6 unchanged.
  </action>
  <verify>
    <automated>grep -c "case files" src/data/json/faq.json | grep -q "2" && grep -c "portfolio" src/data/json/faq.json | grep -q "0" && grep -c "featured projects" src/data/json/faq.json | grep -q "0" && grep -c "Section 508" src/data/json/faq.json | grep -q "1" && grep -c "AA+" src/data/json/faq.json | grep -q "0" && grep -c "Power Platform" src/data/json/faq.json | grep -q "1" && grep -c "GitHub Spec Kit" src/data/json/faq.json | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "portfolio" src/data/json/faq.json returns 0 matches
    - grep "featured projects" src/data/json/faq.json returns 0 matches
    - grep "case files" src/data/json/faq.json returns 2 matches (Q5 and Q10)
    - grep "Section 508" src/data/json/faq.json returns 1 match (Q1)
    - grep "AA+" src/data/json/faq.json returns 0 matches
    - grep "Power Platform" src/data/json/faq.json returns 1 match (Q6)
    - grep "GitHub Spec Kit" src/data/json/faq.json returns 1 match (Q6, unchanged)
  </acceptance_criteria>
  <done>Q5 and Q10 reference "case files", Q1 uses "WCAG 2.1 AA / Section 508", Q6 mentions Power Platform, GitHub Spec Kit verified unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Fix tech listing, industry list, and internal duplication (REFS-02, ACCY-01)</name>
  <files>src/data/json/faq.json</files>
  <read_first>src/data/json/faq.json</read_first>
  <action>
Edit src/data/json/faq.json with these exact text replacements:

**Q4 (index 3) — "What technologies do you specialize in?" (per REFS-02):**
Replace the entire answer value with:
"Primary stack: JavaScript, TypeScript, Vue.js, Node.js, HTML5/CSS3, SQL\n\nAlso production experience with: React, Python, Power Platform, Claude Code\n\nSpecialized domains: Learning Management Systems (LMS), SCORM/AICC/xAPI, eLearning technologies, accessibility remediation (WCAG 2.1 AA). See also Q6 for AI and automation experience."

This adds the second-tier tech list per REFS-02 and cross-references Q6 per Claude's discretion from CONTEXT.md.

**Q7 (index 6) — "What industries have you worked in?" (per ACCY-01):**
Replace the entire first paragraph of the answer with:
"Banking (PNC, HSBC, Wells Fargo, TD Bank), Defense (GDEB Electric Boat), Energy (7+ utilities including FPL, NRG, Exelon), Automotive (General Motors), Healthcare (Weill Cornell Medicine), and Enterprise software (Microsoft, BP)."

Changes from old text:
- "JPMorgan Chase, Kmart Credit" replaced with "HSBC, Wells Fargo, TD Bank" (documented in site exhibit data)
- "Cornell Medical" replaced with "Weill Cornell Medicine" (matches project directory)
- "Retail (GM, Kmart)" replaced with "Automotive (General Motors)" — GM recategorized, Kmart removed (not documented on site)
Keep the second paragraph ("This cross-industry experience...") unchanged.

**Q2 (index 1) — "What's your work arrangement preference?" (internal dedup):**
Replace the entire answer value with:
"Remote is ideal. I'm also open to hybrid arrangements in the Portland, OR area (Vancouver, WA specifically). I have 28+ years of experience working effectively with distributed teams across time zones."

This removes the second paragraph about async/sync communication, which is a near-duplicate of Q8 content. The communication detail stays in Q8 where it topically belongs.
  </action>
  <verify>
    <automated>grep -c "React, Python, Power Platform, Claude Code" src/data/json/faq.json | grep -q "1" && grep -c "HSBC, Wells Fargo, TD Bank" src/data/json/faq.json | grep -q "1" && grep -c "Weill Cornell Medicine" src/data/json/faq.json | grep -q "1" && grep -c "Automotive (General Motors)" src/data/json/faq.json | grep -q "1" && grep -c "JPMorgan Chase" src/data/json/faq.json | grep -q "0" && grep -c "Kmart" src/data/json/faq.json | grep -q "0" && grep -c "Cornell Medical" src/data/json/faq.json | grep -q "0" && node -e "const f=require('./src/data/json/faq.json'); const q2=f[1].answer; if(q2.includes('async')) process.exit(1);" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "React, Python, Power Platform, Claude Code" src/data/json/faq.json returns 1 match (Q4)
    - grep "HSBC, Wells Fargo, TD Bank" src/data/json/faq.json returns 1 match (Q7)
    - grep "Weill Cornell Medicine" src/data/json/faq.json returns 1 match (Q7)
    - grep "Automotive (General Motors)" src/data/json/faq.json returns 1 match (Q7)
    - grep "JPMorgan Chase" src/data/json/faq.json returns 0 matches
    - grep "Kmart" src/data/json/faq.json returns 0 matches
    - grep "Cornell Medical" src/data/json/faq.json returns 0 matches
    - Q2 answer (index 1) does NOT contain the word "async" (dedup with Q8)
    - faq.json is valid JSON (node -e "require('./src/data/json/faq.json')" succeeds)
  </acceptance_criteria>
  <done>Q4 includes second-tier tech list with cross-reference to Q6, Q7 has corrected industry list with documented clients, Q2 trimmed of async/sync duplicate</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — this phase modifies static JSON content data only. No user input processing, no API endpoints, no authentication.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Information Disclosure | faq.json client references | accept | Client names are publicly disclosed career history, not confidential. Replacing unverifiable names with documented ones improves accuracy. |
</threat_model>

<verification>
1. `node -e "const f=require('./src/data/json/faq.json'); console.log(f.length)"` outputs 14 (all questions preserved)
2. `npm run build` succeeds with no errors
3. All acceptance criteria from both tasks pass
</verification>

<success_criteria>
- faq.json contains 14 items (no additions or removals)
- Zero instances of "portfolio", "featured projects", "AA+", "JPMorgan Chase", "Kmart", "Cornell Medical"
- Present: "case files" (2x), "Section 508" (1x), "Power Platform" (in Q6), "HSBC, Wells Fargo, TD Bank", "Weill Cornell Medicine", "Automotive (General Motors)", "React, Python, Power Platform, Claude Code" (in Q4)
- Q2 answer contains no async/sync communication detail
- Valid JSON, build passes
</success_criteria>

<output>
After completion, create `.planning/phases/31-content-fixes/31-01-SUMMARY.md`
</output>
