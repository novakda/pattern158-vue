---
phase: 32-overlap-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/json/faq.json
autonomous: true
requirements:
  - OVLP-01
  - OVLP-02
  - OVLP-03

must_haves:
  truths:
    - "FAQ Q2 (work arrangement) is a concise quick-scan summary that complements, not duplicates, Contact page CultureFitSection"
    - "FAQ Q12 (typical workflow) is a brief teaser referencing Philosophy page, not a reproduction of the three-step methodology"
    - "Cross-references to Contact and Philosophy pages appear where overlap exists"
  artifacts:
    - path: "src/data/json/faq.json"
      provides: "Updated FAQ answers with overlap resolved"
      contains: "Philosophy page"
  key_links:
    - from: "src/data/json/faq.json Q2"
      to: "ContactPage CultureFitSection"
      via: "complementary content — FAQ is facts-only summary, Contact has nuanced preferences"
    - from: "src/data/json/faq.json Q12"
      to: "PhilosophyPage HowIWorkSection"
      via: "cross-reference text directing reader to Philosophy page"
---

<objective>
Resolve content overlap between FAQ answers and Contact/Philosophy pages. Q2 (work arrangement) becomes a concise facts-only summary complementing Contact's CultureFitSection. Q12 (typical workflow) becomes a teaser with cross-reference to the Philosophy page instead of reproducing the full three-step methodology.

Purpose: Each page serves a distinct purpose — FAQ as quick-scan entry point, Contact/Philosophy as detailed content.
Output: Updated faq.json with shortened, differentiated answers and cross-references.
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-overlap-resolution/32-CONTEXT.md
@.planning/phases/30-cross-page-content-audit/30-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shorten Q2 work arrangement and add Contact cross-reference (OVLP-01)</name>
  <files>src/data/json/faq.json</files>
  <read_first>
    - src/data/json/faq.json (current Q2 answer, line 10)
    - .planning/phases/32-overlap-resolution/32-CONTEXT.md (Q2 decision)
  </read_first>
  <action>
In src/data/json/faq.json, find the FAQ item with question "What's your work arrangement preference?" (the second item in the array).

Replace its current answer value:
"Remote is ideal. I'm also open to hybrid arrangements in the Portland, OR area (Vancouver, WA specifically). I have 28+ years of experience working effectively with distributed teams across time zones."

With this new answer value:
"Remote is ideal. I'm also open to hybrid arrangements in the Portland, OR area (Vancouver, WA specifically).\n\nFor details on work culture preferences, see the Contact page."

Rationale per OVLP-01: The current Q2 duplicates CultureFitSection's remote/location content. The shortened version keeps the key facts (remote preference, Portland/Vancouver location) as a quick-scan entry point. The "28+ years with distributed teams" detail is removed since it overlaps with Q9 ("Do you work well with distributed teams?") which already covers that experience in depth. A cross-reference to the Contact page directs hiring managers to the full picture per OVLP-03.

Ensure valid JSON after the edit. No trailing commas, proper escaping.
  </action>
  <verify>
    <automated>node -e "const d=require('./src/data/json/faq.json'); const q2=d[1]; if(q2.question!=='What\\x27s your work arrangement preference?') throw 'Q2 not found at index 1'; if(q2.answer.includes('28+ years')) throw 'Still contains distributed teams detail'; if(!q2.answer.includes('Contact page')) throw 'Missing Contact cross-reference'; if(q2.answer.includes('async')) throw 'Still contains async detail'; console.log('Q2 overlap resolved');"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Contact page" src/data/json/faq.json returns a match
    - Q2 answer does NOT contain "28+ years"
    - Q2 answer does NOT contain "async"
    - Q2 answer contains "Remote is ideal"
    - Q2 answer contains "Portland, OR"
    - faq.json is valid JSON (node -e "require('./src/data/json/faq.json')" exits 0)
  </acceptance_criteria>
  <done>Q2 is a concise facts-only summary with Contact page cross-reference, no longer duplicating CultureFitSection or Q9 distributed teams content</done>
</task>

<task type="auto">
  <name>Task 2: Shorten Q12 typical workflow to teaser with Philosophy cross-reference (OVLP-02)</name>
  <files>src/data/json/faq.json</files>
  <read_first>
    - src/data/json/faq.json (current Q12 answer, line 59-60)
    - .planning/phases/32-overlap-resolution/32-CONTEXT.md (Q12 decision)
  </read_first>
  <action>
In src/data/json/faq.json, find the FAQ item with question "What's your typical workflow?" (the 12th item in the array, index 11).

Replace its current answer value:
"The pattern repeats across my career:\n\n1. Deconstruct the Chaos: Forensic investigation to understand the system \u2014 map dependencies, identify patterns, document what exists\n2. Build the Tool: Create reusable frameworks to solve root causes, not symptoms \u2014 automate repetitive tasks, standardize processes\n3. Empower the User: Hand off robust, documented tools that make complex processes simple \u2014 clear READMEs, inline comments, training materials\n\nThis pattern shows up whether I'm debugging SCORM packages, reverse-engineering legacy systems, or building automation tools."

With this new answer value:
"A three-step pattern recurs across my career: Deconstruct the Chaos, Build the Tool, Empower the User. Whether I'm debugging SCORM packages, reverse-engineering legacy systems, or building automation tools, the same cycle applies.\n\nThe full methodology is detailed on the Philosophy page."

Rationale per OVLP-02: The CONTEXT.md decision says "Shorten Q12 to a teaser with reference to Philosophy page" and "Replace full 3-step reproduction with brief mention." The new answer names the three steps without reproducing the full descriptions (which live on PhilosophyPage HowIWorkSection), keeps the one-liner context about the pattern recurring across career, and adds a cross-reference to the Philosophy page per OVLP-03.

Ensure valid JSON after the edit. Verify the unicode em-dash and escape sequences are correct.
  </action>
  <verify>
    <automated>node -e "const d=require('./src/data/json/faq.json'); const q12=d[11]; if(q12.question!=='What\\x27s your typical workflow?') throw 'Q12 not found at index 11'; if(q12.answer.includes('map dependencies, identify patterns')) throw 'Still contains full step descriptions'; if(!q12.answer.includes('Philosophy page')) throw 'Missing Philosophy cross-reference'; if(!q12.answer.includes('Deconstruct the Chaos')) throw 'Missing step names'; console.log('Q12 overlap resolved');"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Philosophy page" src/data/json/faq.json returns a match
    - Q12 answer contains "Deconstruct the Chaos, Build the Tool, Empower the User" (step names preserved)
    - Q12 answer does NOT contain "map dependencies, identify patterns" (full descriptions removed)
    - Q12 answer does NOT contain "clear READMEs, inline comments" (full descriptions removed)
    - Q12 answer contains "Philosophy page" cross-reference
    - faq.json is valid JSON (node -e "require('./src/data/json/faq.json')" exits 0)
  </acceptance_criteria>
  <done>Q12 is a brief teaser naming the three steps with Philosophy page cross-reference, no longer reproducing the full methodology that PhilosophyPage HowIWorkSection covers in depth</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected. This phase modifies static JSON content only — no user input, no API calls, no authentication.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | T (Tampering) | faq.json | accept | Static JSON committed to git — no runtime modification vector. File integrity tracked by version control. |
</threat_model>

<verification>
1. faq.json is valid JSON: `node -e "require('./src/data/json/faq.json')"`
2. Q2 answer is shorter and contains "Contact page" cross-reference
3. Q12 answer is shorter and contains "Philosophy page" cross-reference
4. Q2 does not contain "28+ years" or "async"
5. Q12 does not contain full step descriptions (no "map dependencies" or "clear READMEs")
6. All 14 FAQ items still present: `node -e "const d=require('./src/data/json/faq.json'); if(d.length!==14) throw 'Expected 14 items, got '+d.length; console.log('14 items OK')"`
</verification>

<success_criteria>
- FAQ Q2 complements Contact page CultureFitSection without duplicating it (OVLP-01)
- FAQ Q12 is a teaser referencing Philosophy page, not a reproduction of HowIWorkSection (OVLP-02)
- Both modified answers include cross-references to the relevant pages (OVLP-03)
- All 14 FAQ items remain intact with valid JSON structure
</success_criteria>

<output>
After completion, create `.planning/phases/32-overlap-resolution/32-01-SUMMARY.md`
</output>
