---
phase: 46-scaffold
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements:
  - SCAF-04
  - SCAF-05

must_haves:
  truths:
    - "package.json devDependencies include turndown@^7.2.4, @joplin/turndown-plugin-gfm@^1.0.64, @types/turndown@^5.0.6"
    - "package.json devDependencies playwright is bumped to ^1.59.1"
    - "package.json scripts include `editorial:capture` running `tsx scripts/editorial/index.ts`"
    - "pnpm-lock.yaml reflects all four dep changes (3 new + 1 bump)"
    - "pnpm install completes cleanly with no peer-dep errors"
  artifacts:
    - path: "package.json"
      provides: "Root manifest with editorial-capture script + 4 dep changes"
      contains: "editorial:capture"
    - path: "pnpm-lock.yaml"
      provides: "Lockfile reflecting installed deps"
      contains: "turndown"
  key_links:
    - from: "package.json scripts.editorial:capture"
      to: "scripts/editorial/index.ts"
      via: "tsx invocation"
      pattern: "tsx scripts/editorial/index\\.ts"
    - from: "package.json devDependencies.turndown"
      to: "node_modules/turndown"
      via: "pnpm install"
      pattern: "\"turndown\":"
---

<objective>
Wire the four dependency changes (3 new devDeps + Playwright bump) and the `editorial:capture` pnpm script into `package.json`, then run `pnpm install` to refresh `pnpm-lock.yaml`. After this plan, `pnpm editorial:capture` is callable (it will execute Plan 01's placeholder `index.ts` and exit cleanly), and Phases 49 (Turndown) + 48 (Playwright) have their deps available.

Purpose: Without this, Plan 05's smoke runs (`pnpm editorial:capture`, the Phase 49+ unit tests) cannot resolve their imports.
Output: `package.json` modified, `pnpm-lock.yaml` regenerated, deps installed.
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-scaffold/46-CONTEXT.md

# The file being modified (full current shape)
@package.json

<interfaces>
<!-- Current package.json scripts block (the analog `build:markdown` shows the pattern): -->

```json
"scripts": {
  "dev": "vite",
  "build": "vue-tsc -b && vite build && pnpm build:markdown",
  "build:markdown": "tsx scripts/markdown-export/index.ts",
  ...
}
```

<!-- Pattern: pnpm script -> tsx <entry-script>. editorial:capture follows this. -->

<!-- Current devDependencies — Playwright is at ^1.58.2, must bump to ^1.59.1 -->
<!-- (used by @vitest/browser-playwright; bumping is safe — minor version) -->
```json
"playwright": "^1.58.2"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit package.json — add editorial:capture script + dep changes (SCAF-04, SCAF-05)</name>
  <files>package.json</files>
  <read_first>
    - package.json (the file being modified — confirm current scripts block, current devDependencies block, current Playwright pin)
    - .planning/REQUIREMENTS.md (SCAF-04 requires `editorial:capture: tsx scripts/editorial/index.ts`; SCAF-05 dictates the four exact version pins)
    - .planning/phases/46-scaffold/46-CONTEXT.md (integration points block confirms exact deps + bump target)
    - scripts/markdown-export/index.ts (analog — confirms the `tsx scripts/<dir>/index.ts` invocation pattern matches what `editorial:capture` will run)
  </read_first>
  <action>
Make exactly these four edits to `package.json`. Do not modify any other field.

**Edit 1 — Add `editorial:capture` script** (insert immediately after `build:markdown` to keep build-related scripts grouped):

Before:
```json
"scripts": {
  "dev": "vite",
  "build": "vue-tsc -b && vite build && pnpm build:markdown",
  "build:markdown": "tsx scripts/markdown-export/index.ts",
  "preview": "pnpm build && wrangler dev",
  ...
}
```

After:
```json
"scripts": {
  "dev": "vite",
  "build": "vue-tsc -b && vite build && pnpm build:markdown",
  "build:markdown": "tsx scripts/markdown-export/index.ts",
  "editorial:capture": "tsx scripts/editorial/index.ts",
  "preview": "pnpm build && wrangler dev",
  ...
}
```

**IMPORTANT:** Do NOT add `editorial:capture` to the existing `build` script chain. SCAF-04 only requires it as a callable script — it is intentionally NOT part of `pnpm build` (the editorial tool talks to the live production site; running it during a build would be wrong).

**Edit 2 — Add 3 new devDependencies** (alphabetical order in the devDependencies block):

- `"@joplin/turndown-plugin-gfm": "^1.0.64"` — insert between `@cloudflare/vite-plugin` and `@storybook/addon-a11y`
- `"@types/turndown": "^5.0.6"` — insert between `@storybook/vue3-vite` and `@vitejs/plugin-vue`
- `"turndown": "^7.2.4"` — insert between `tsx` and `typescript`

**Edit 3 — Bump Playwright** in devDependencies:

Before:
```json
"playwright": "^1.58.2",
```

After:
```json
"playwright": "^1.59.1",
```

**Edit 4 — None.** (Listing here for clarity: there are exactly 3 dep edits, not 4; "Playwright bump" is one of the four total *dep changes* counted by SCAF-05.)

**Final devDependencies block (full expected shape after edits):**
```json
"devDependencies": {
  "@chromatic-com/storybook": "^5.0.1",
  "@cloudflare/vite-plugin": "^1.29.1",
  "@joplin/turndown-plugin-gfm": "^1.0.64",
  "@storybook/addon-a11y": "^10.2.19",
  "@storybook/addon-docs": "^10.2.19",
  "@storybook/addon-onboarding": "^10.2.19",
  "@storybook/addon-vitest": "^10.2.19",
  "@storybook/vue3-vite": "^10.2.19",
  "@types/turndown": "^5.0.6",
  "@vitejs/plugin-vue": "^5.2.0",
  "@vitest/browser-playwright": "^4.1.0",
  "@vitest/coverage-v8": "^4.1.0",
  "github-slugger": "2.0.0",
  "happy-dom": "^20.8.4",
  "playwright": "^1.59.1",
  "storybook": "^10.2.19",
  "tsx": "4.21.0",
  "turndown": "^7.2.4",
  "typescript": "~5.7.0",
  "vite": "^6.2.0",
  "vitest": "^4.1.0",
  "vitest-browser-vue": "^2.1.0",
  "vue-tsc": "^2.2.0",
  "wrangler": "^4.75.0",
  "yaml": "2.8.3"
}
```

Do NOT touch `dependencies`, `name`, `version`, `private`, `type`. Only `scripts` and `devDependencies` change.
  </action>
  <verify>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8'));if(p.scripts['editorial:capture']!=='tsx scripts/editorial/index.ts')process.exit(1);if(p.devDependencies['turndown']!=='^7.2.4')process.exit(2);if(p.devDependencies['@joplin/turndown-plugin-gfm']!=='^1.0.64')process.exit(3);if(p.devDependencies['@types/turndown']!=='^5.0.6')process.exit(4);if(p.devDependencies['playwright']!=='^1.59.1')process.exit(5);"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` parses as valid JSON.
    - `scripts['editorial:capture']` === `"tsx scripts/editorial/index.ts"`.
    - `devDependencies['turndown']` === `"^7.2.4"`.
    - `devDependencies['@joplin/turndown-plugin-gfm']` === `"^1.0.64"`.
    - `devDependencies['@types/turndown']` === `"^5.0.6"`.
    - `devDependencies['playwright']` === `"^1.59.1"` (bumped from `^1.58.2`).
    - `scripts['build']` is unchanged (still `"vue-tsc -b && vite build && pnpm build:markdown"`) — `editorial:capture` is NOT chained into build.
    - `scripts['build:markdown']` is unchanged.
    - `dependencies` block is unchanged (regression check on runtime deps).
    - `name`, `version`, `type` fields are unchanged.
  </acceptance_criteria>
  <done>
    `package.json` carries the new script and four dep changes, all other fields untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run pnpm install to refresh pnpm-lock.yaml (SCAF-05 lockfile assertion)</name>
  <files>pnpm-lock.yaml</files>
  <read_first>
    - package.json (just-edited in Task 1 — pnpm install reads this to compute the lockfile delta)
    - pnpm-lock.yaml (existing lockfile — confirm it exists; if missing, the project may use a different lockfile, which would be a project-state regression to flag, not silently fix)
  </read_first>
  <action>
Run `pnpm install` from the repo root. This will:

1. Resolve `turndown@^7.2.4`, `@joplin/turndown-plugin-gfm@^1.0.64`, `@types/turndown@^5.0.6` from the npm registry.
2. Resolve the bumped `playwright@^1.59.1`.
3. Update `pnpm-lock.yaml` with the new resolved versions and integrity hashes.
4. Install all four into `node_modules`.

Command:
```bash
pnpm install
```

Expected behavior:
- Exits 0.
- Produces output like `Packages: +N -M` indicating the four new/upgraded packages.
- `pnpm-lock.yaml` is modified.
- No peer-dep errors block the install.

If `pnpm install` reports peer-dep warnings about Playwright 1.59.1 vs `@vitest/browser-playwright@^4.1.0`, document the warning text in the SUMMARY but do not change versions — the bump is locked by SCAF-05. If a peer-dep error (not warning) blocks install, abort and surface the error; do NOT downgrade Playwright to silence it.

Do NOT run `pnpm install --frozen-lockfile` — the lockfile must be regenerated.
Do NOT delete `pnpm-lock.yaml` first — pnpm should compute a minimal delta from the existing lockfile.
Do NOT run any auto-fix commands (`pnpm dedupe`, `pnpm update`) — only `pnpm install`.
  </action>
  <verify>
    <automated>pnpm install &amp;&amp; grep -q "turndown" pnpm-lock.yaml &amp;&amp; grep -q "@joplin/turndown-plugin-gfm" pnpm-lock.yaml &amp;&amp; grep -q "@types/turndown" pnpm-lock.yaml &amp;&amp; grep -q "1.59.1" pnpm-lock.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install` exits 0.
    - `pnpm-lock.yaml` is modified relative to git HEAD.
    - `pnpm-lock.yaml` contains the string `turndown` (covers both `turndown` and `@joplin/turndown-plugin-gfm` and `@types/turndown`).
    - `pnpm-lock.yaml` contains the string `1.59.1` (Playwright bump landed in the lockfile).
    - `node_modules/turndown/package.json` exists (`turndown` actually installed).
    - `node_modules/@joplin/turndown-plugin-gfm/package.json` exists.
    - `node_modules/@types/turndown/package.json` exists.
    - `node_modules/playwright/package.json` `version` is in the `1.59.x` range.
  </acceptance_criteria>
  <done>
    All four dep changes installed and reflected in the lockfile; `pnpm editorial:capture` is now resolvable end-to-end (Plan 05 will smoke-run it).
  </done>
</task>

</tasks>

<verification>
After both tasks:

1. Script + deps reachable:
   ```bash
   node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8'));console.log('script:',p.scripts['editorial:capture']);console.log('turndown:',p.devDependencies.turndown);console.log('gfm:',p.devDependencies['@joplin/turndown-plugin-gfm']);console.log('types-turndown:',p.devDependencies['@types/turndown']);console.log('playwright:',p.devDependencies.playwright);"
   ```

2. Installed binaries match expectations:
   ```bash
   node -e "console.log('turndown:', require('./node_modules/turndown/package.json').version);console.log('playwright:', require('./node_modules/playwright/package.json').version);"
   # Expected: turndown 7.2.x, playwright 1.59.x
   ```

3. NOTE: `pnpm editorial:capture` is NOT smoke-run here — Plan 05 owns that end-to-end smoke once `tsconfig.editorial.json` (Plan 02) and Vitest include + .gitignore (Plan 04) have also landed. Running it here would smoke-test the script in isolation; deferring to Plan 05 lets us prove the whole scaffold lights up together.
</verification>

<success_criteria>
- `package.json` carries the `editorial:capture` script.
- `package.json` carries the 3 new turndown-family devDeps + Playwright 1.59.1 bump.
- `pnpm-lock.yaml` reflects all four dep changes.
- All four packages installed in `node_modules` with matching version ranges.
- No unrelated `package.json` fields changed.
</success_criteria>

<output>
After completion, create `.planning/phases/46-scaffold/46-03-SUMMARY.md` documenting:
- The exact dep version strings installed (resolved versions from lockfile, not just the ^ ranges).
- Any peer-dep warnings emitted by `pnpm install` (text + verdict — accepted/escalated).
- Confirmation that `editorial:capture` is NOT chained into `pnpm build` (intentional — runs against live site).
- Note that the actual `pnpm editorial:capture` smoke-run is deferred to Plan 05.
</output>
