---
phase: 46-scaffold
plan: 05
type: execute
wave: 2
depends_on:
  - 46-01
  - 46-02
  - 46-03
  - 46-04
files_modified: []
autonomous: true
requirements:
  - SCAF-02
  - SCAF-04
  - SCAF-06

must_haves:
  truths:
    - "`pnpm build` (vue-tsc -b) compiles all referenced projects including tsconfig.editorial.json with zero errors"
    - "`pnpm editorial:capture` exits 0 and emits the placeholder banner from index.ts"
    - "`pnpm test:scripts` discovers and passes scripts/editorial/__tests__/smoke.test.ts (in addition to any existing markdown-export tests)"
    - "`.tsbuildinfo-editorial/` exists after build but is absent from `git status` (proves SCAF-07 wired correctly)"
    - "Phase 46 success criteria 1-5 are all green end-to-end"
  artifacts: []
  key_links:
    - from: "pnpm build"
      to: "tsconfig.editorial.json"
      via: "vue-tsc -b walking root references[]"
      pattern: "Build succeeded|exit 0"
    - from: "pnpm editorial:capture"
      to: "scripts/editorial/index.ts (placeholder banner output)"
      via: "tsx invocation"
      pattern: "phase 46 scaffold placeholder"
    - from: "pnpm test:scripts"
      to: "scripts/editorial/__tests__/smoke.test.ts"
      via: "Vitest scripts project include glob"
      pattern: "editorial scaffold smoke"
---

<objective>
End-to-end smoke verification of Phase 46. Run the three commands that prove the scaffold is fully wired — `pnpm build`, `pnpm editorial:capture`, `pnpm test:scripts` — and capture their outputs. This plan touches no source files; it is purely a gate that asserts Plans 01–04 actually composed correctly. If any command fails, the failure surfaces here (not in Phase 47 when the team is trying to write real code).

Purpose: SCAF-02 success criterion 2 ("`pnpm build` completes with zero errors INCLUDING the new project") and the rest of Phase 46's success criteria can ONLY be asserted with all four Wave 1 plans landed. This plan is the assertion.
Output: 1 SUMMARY.md documenting the three smoke runs.
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-scaffold/46-CONTEXT.md

# Plan 01 outputs (placeholder files this plan compiles + smoke-tests)
@scripts/editorial/index.ts
@scripts/editorial/__tests__/smoke.test.ts

# Plan 02 outputs (the tsconfig project this plan compiles)
@tsconfig.editorial.json
@tsconfig.json

# Plan 03 outputs (the script + deps this plan invokes)
@package.json

# Plan 04 outputs (the Vitest config + .gitignore this plan exercises)
@vitest.config.ts
@.gitignore

<interfaces>
<!-- This plan runs commands. The expected output shapes: -->

<!-- `pnpm build` runs `vue-tsc -b && vite build && pnpm build:markdown`.
     The vue-tsc -b step walks tsconfig.json `references` and builds:
       1. tsconfig.scripts.json -> .tsbuildinfo-scripts/
       2. tsconfig.editorial.json -> .tsbuildinfo-editorial/  (NEW in Phase 46)
     Exit 0 = SCAF-02 success criterion 2 PASSES. -->

<!-- `pnpm editorial:capture` runs `tsx scripts/editorial/index.ts`.
     Plan 01's placeholder writes one stdout line and exits 0.
     Expected stdout: "<!-- editorial capture tool — phase 46 scaffold placeholder -->\n" -->

<!-- `pnpm test:scripts` runs `vitest run --project scripts`.
     Plan 04's include extension makes Vitest discover smoke.test.ts.
     Plan 01's smoke.test.ts has 2 `it()` blocks, both passing.
     Expected: "Test Files  N passed (M)" with N >= 1 (smoke.test.ts) and any
     existing scripts/markdown-export tests also passing. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke-run pnpm build (SCAF-02 success criterion 2)</name>
  <files></files>
  <read_first>
    - tsconfig.json (confirm references[] has BOTH scripts and editorial entries — Plan 02 output)
    - tsconfig.editorial.json (confirm composite=true and include glob — Plan 02 output)
    - scripts/editorial/index.ts (confirm placeholder compiles — Plan 01 output)
    - scripts/editorial/types.ts (confirm re-exports compile — Plan 01 output; this is the file with the most cross-module type traffic, so any Plan 01 typo surfaces here first)
    - package.json (confirm build script chain — Plan 03 untouched this, but verify scripts.build is still `"vue-tsc -b && vite build && pnpm build:markdown"`)
  </read_first>
  <action>
Run `pnpm build` from the repo root and capture the full output.

```bash
pnpm build
```

Expected outcomes:
- Exit code 0.
- `vue-tsc -b` step builds the editorial project (visible if running with `--verbose`, but default output is silent on success — exit 0 is the signal).
- `.tsbuildinfo-editorial/` directory is created (proves the editorial project was actually built, not silently skipped).
- `vite build` step completes (unchanged from before this phase).
- `pnpm build:markdown` step completes (unchanged from before this phase).

If `pnpm build` fails:
- Check for TypeScript errors in `scripts/editorial/*.ts` — most likely cause is a Plan 01 stub having a type mismatch under strict mode. Capture the full error output, identify the specific file, and fix the placeholder. Common failure: NodeNext module resolution requires `.ts` extension in import specifiers — confirm Plan 01 placeholders use `from './routes.ts'` not `from './routes'`.
- Check for `vue-tsc -b` reporting "build cancelled" — usually means `tsconfig.editorial.json` has an invalid composite-project setting; re-verify Plan 02's output.
- Do NOT silence errors with `--noEmit` or `--skipLibCheck` overrides; fix the root cause.

After successful run:
```bash
ls -la .tsbuildinfo-editorial/
```
This must show generated `.d.ts` files for the placeholder modules (proof the editorial project was actually compiled, not skipped).

Verify SCAF-07 is working — the new build-artifact directory must NOT appear in `git status`:
```bash
git status --porcelain | grep tsbuildinfo-editorial
# Expected: no output (the directory exists but is git-ignored)
```
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; test -d .tsbuildinfo-editorial &amp;&amp; test -z "$(git status --porcelain | grep tsbuildinfo-editorial)"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0.
    - `.tsbuildinfo-editorial/` directory exists after the build.
    - `.tsbuildinfo-editorial/` contains at least one `.d.ts` file (proves the project was compiled, not silently skipped — `tsc -b` only writes outputs when it actually built).
    - `git status --porcelain` shows no entries containing `tsbuildinfo-editorial` (SCAF-07 wired correctly).
    - Existing `.tsbuildinfo-scripts/` directory is also still present and still ignored (regression check).
    - `dist/` directory was produced by the `vite build` step (regression check).
  </acceptance_criteria>
  <done>
    SCAF-02 success criterion 2 PASSES end-to-end. The editorial tsconfig project is part of the build, builds clean, and its artifacts are git-ignored.
  </done>
</task>

<task type="auto">
  <name>Task 2: Smoke-run pnpm editorial:capture + pnpm test:scripts (SCAF-04, SCAF-06)</name>
  <files></files>
  <read_first>
    - package.json (confirm `editorial:capture` script and `test:scripts` script — Plan 03 + pre-existing)
    - scripts/editorial/index.ts (confirm placeholder writes the expected banner — Plan 01 output)
    - scripts/editorial/__tests__/smoke.test.ts (confirm the test file Vitest must discover — Plan 01 output)
    - vitest.config.ts (confirm `scripts` project include includes the editorial glob — Plan 04 output)
  </read_first>
  <action>
Run two smoke commands sequentially. Capture full stdout/stderr from each.

**Command 1 — `pnpm editorial:capture` (SCAF-04 success criterion 4):**

```bash
pnpm editorial:capture
```

Expected outcomes:
- Exit code 0.
- Stdout contains `<!-- editorial capture tool — phase 46 scaffold placeholder -->` (the exact banner emitted by Plan 01's `index.ts`).
- No stderr output.
- `tsx` resolves `scripts/editorial/index.ts` without import-resolution errors (proves NodeNext + .ts-extension imports work end-to-end).

If this fails:
- `tsx`-not-found error → `pnpm install` was incomplete in Plan 03; re-run.
- Module-not-found for `./routes.ts` etc. → Plan 01 placeholder used wrong import specifier; tsx + NodeNext requires `.ts` extension.
- Banner missing from stdout → Plan 01's `main()` function was not invoked or banner string drifted; check `index.ts` against Plan 01's spec.

**Command 2 — `pnpm test:scripts` (SCAF-06 success criterion 5):**

```bash
pnpm test:scripts
```

Expected outcomes:
- Exit code 0.
- Vitest output reports at least 1 test file passed under `scripts/editorial/__tests__/smoke.test.ts`.
- The 2 `it()` blocks in `smoke.test.ts` both pass.
- Any pre-existing tests under `scripts/markdown-export/**/*.test.ts` ALSO still pass (regression check that Plan 04's include extension didn't break the existing glob).
- No output mentioning a "scripts" project missing or duplicated (regression check that SCAF-06 "no new Vitest project" was honored).

If this fails:
- "No tests found" → Plan 04's include extension didn't actually land; re-verify `vitest.config.ts`.
- Smoke test file not compiling → Plan 01's smoke.test.ts has a type error; check that `import type { ... } from '../types.ts'` resolves.
- Globals undefined (`describe is not defined`) → Plan 04 accidentally removed `globals: true` from the scripts project; re-verify.

**Final cross-cutting check — SCAF-08 stayed clean across the directory after build:**
```bash
grep -RE "@/|Date\.now\(\)|new Date\(|os\.EOL|Promise\.all" scripts/editorial/
# Expected: no output (no SCAF-08 forbidden pattern leaked into the directory)
```

This is a regression check — Plan 01 enforced SCAF-08 for placeholder files, but if any Wave 1 plan accidentally introduced a forbidden pattern, this is the last gate before Phase 46 ships.
  </action>
  <verify>
    <automated>pnpm editorial:capture 2>&amp;1 | grep -q "phase 46 scaffold placeholder" &amp;&amp; pnpm test:scripts 2>&amp;1 | tee /tmp/p46-test.log | grep -qE "smoke\.test\.ts" &amp;&amp; grep -qE "([0-9]+ passed|Test Files .*passed)" /tmp/p46-test.log &amp;&amp; ! grep -RE "@/|Date\.now\(\)|new Date\(|os\.EOL|Promise\.all" scripts/editorial/</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm editorial:capture` exits 0.
    - Its stdout contains `phase 46 scaffold placeholder`.
    - `pnpm test:scripts` exits 0.
    - Its output references the smoke test (`smoke.test.ts` or the describe string `editorial scaffold smoke`).
    - The 2 `it()` blocks from `smoke.test.ts` are both reported as passing.
    - Any pre-existing `scripts/markdown-export/**/*.test.ts` tests still pass (regression — though if zero tests exist there, that is also acceptable).
    - `grep -RE "@/|Date\.now\(\)|new Date\(|os\.EOL|Promise\.all" scripts/editorial/` returns no output (SCAF-08 still clean).
    - `git status --porcelain` shows only the expected file changes from Wave 1 (no rogue artifact files leaking — `.tsbuildinfo-editorial/` is ignored, `node_modules/` is ignored).
  </acceptance_criteria>
  <done>
    All 5 Phase 46 success criteria pass end-to-end:
    1. `scripts/editorial/` flat layout exists ✓ (Plan 01 + grep proof)
    2. `tsconfig.editorial.json` builds clean under `pnpm build` ✓ (Task 1 above)
    3. 4 dep changes installed and reflected in lockfile ✓ (Plan 03 + this task's `pnpm editorial:capture` actually resolves)
    4. `pnpm editorial:capture` runs and exits cleanly ✓ (this task)
    5. `pnpm test:scripts` discovers `scripts/editorial/__tests__/` ✓ (this task)
  </done>
</task>

</tasks>

<verification>
End-to-end Phase 46 gate. After both tasks:

1. All three smoke commands green:
   ```bash
   pnpm build && pnpm editorial:capture && pnpm test:scripts
   # Expected: each exits 0 in sequence
   ```

2. Build artifact correctly ignored:
   ```bash
   ls .tsbuildinfo-editorial/ && git status --porcelain | grep -c tsbuildinfo-editorial
   # Expected: directory listing shows .d.ts files; grep returns 0 (no untracked artifact)
   ```

3. SCAF-08 forbidden patterns still absent from the directory:
   ```bash
   grep -RE "@/|Date\.now\(\)|new Date\(|os\.EOL|Promise\.all" scripts/editorial/
   # Expected: no output
   ```

4. Vitest project topology unchanged (still 3 projects):
   ```bash
   grep -c "name: '" vitest.config.ts
   # Expected: 3 (unit, browser, scripts — no new project added)
   ```
</verification>

<success_criteria>
- `pnpm build` exits 0 with the editorial project built.
- `pnpm editorial:capture` exits 0 and prints the placeholder banner.
- `pnpm test:scripts` exits 0 with the smoke test passing.
- `.tsbuildinfo-editorial/` is git-ignored.
- SCAF-08 forbidden patterns absent across the directory.
- All 5 Phase 46 success criteria from ROADMAP are demonstrably TRUE.
</success_criteria>

<output>
After completion, create `.planning/phases/46-scaffold/46-05-SUMMARY.md` documenting:
- Full output (or relevant excerpts) of the three smoke commands.
- Confirmation per Phase 46 success criterion (1-5) with the file/command that proves each.
- Any warnings encountered (peer-dep warnings from Plan 03, vue-tsc -b verbose output, etc.) and a verdict (accepted / escalated).
- Hand-off note: Phase 47 can now write real `config.ts` and `routes.ts` against this scaffold.
</output>
