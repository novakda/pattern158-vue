---
phase: 46-scaffold
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - .gitignore
autonomous: true
requirements:
  - SCAF-06
  - SCAF-07

must_haves:
  truths:
    - "The `scripts` Vitest project's include array covers scripts/editorial/**/*.test.ts"
    - "No new Vitest project is created — only the existing `scripts` project is extended"
    - ".gitignore contains an entry for .tsbuildinfo-editorial"
    - "pnpm test:scripts can discover scripts/editorial/__tests__/*.test.ts (verified end-to-end in Plan 05)"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest config with `scripts` project include extended"
      contains: "scripts/editorial/**/*.test.ts"
    - path: ".gitignore"
      provides: "Git-ignore entry for the new tsconfig project's build artifact"
      contains: ".tsbuildinfo-editorial"
  key_links:
    - from: "vitest.config.ts scripts project include"
      to: "scripts/editorial/__tests__/smoke.test.ts (created in Plan 01)"
      via: "glob match"
      pattern: "scripts/editorial/\\*\\*/\\*\\.test\\.ts"
    - from: ".gitignore .tsbuildinfo-editorial"
      to: "tsconfig.editorial.json outDir (set in Plan 02)"
      via: "outDir directory name"
      pattern: "\\.tsbuildinfo-editorial"
---

<objective>
Wire the editorial directory into the existing Vitest `scripts` project (SCAF-06 — extend, do NOT create a new project) and add a `.gitignore` entry for the new tsconfig project's build artifact directory (SCAF-07). After this plan, Plan 05 can run `pnpm test:scripts` and Vitest will discover Plan 01's smoke test.

Purpose: Without SCAF-06, Plan 01's smoke test exists on disk but is never executed. Without SCAF-07, Plan 02's `.tsbuildinfo-editorial` directory leaks into git the moment Plan 05 runs `pnpm build`.
Output: 2 file edits.
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-scaffold/46-CONTEXT.md

# Files being modified (full current shape)
@vitest.config.ts
@.gitignore

<interfaces>
<!-- Current vitest.config.ts `scripts` project (the block to extend): -->

```ts
{
  extends: true,
  test: {
    name: 'scripts',
    include: ['scripts/markdown-export/**/*.test.ts'],
    environment: 'node',
    globals: true,
  },
},
```

<!-- After this plan, include becomes a 2-element array — both globs share the
     same `scripts` project name (SCAF-06: "no new Vitest project"). -->

<!-- Current .gitignore relevant section: -->
```
# TypeScript project-references build artifacts (scripts project)
.tsbuildinfo-scripts/
tsconfig.scripts.tsbuildinfo
*.tsbuildinfo
```

<!-- The catch-all `*.tsbuildinfo` would match `.tsbuildinfo-editorial`'s root file
     IF it sits at the repo root, but it does NOT match a directory of that name.
     SCAF-07 explicitly requires the named entry — keep both for clarity. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Vitest scripts project include for scripts/editorial/ (SCAF-06)</name>
  <files>vitest.config.ts</files>
  <read_first>
    - vitest.config.ts (the file being modified — confirm exact current `scripts` project block, especially the `include` array shape and that `globals: true` is present)
    - scripts/editorial/__tests__/smoke.test.ts (created in Plan 01 — confirm the file the new include glob must reach; also confirm it relies on globals=true so test discovery and execution actually pair)
    - .planning/REQUIREMENTS.md (SCAF-06 explicitly forbids creating a new Vitest project — this MUST be a glob extension)
  </read_first>
  <action>
Edit `vitest.config.ts` to extend the `include` array of the existing `scripts` project from a 1-element array to a 2-element array. NO other change.

**Before (lines 41–48 in current file):**
```ts
{
  extends: true,
  test: {
    name: 'scripts',
    include: ['scripts/markdown-export/**/*.test.ts'],
    environment: 'node',
    globals: true,
  },
},
```

**After:**
```ts
{
  extends: true,
  test: {
    name: 'scripts',
    include: [
      'scripts/markdown-export/**/*.test.ts',
      'scripts/editorial/**/*.test.ts',
    ],
    environment: 'node',
    globals: true,
  },
},
```

Implementation notes:
- Multi-line array reformat is intentional — it makes future additions (e.g., a v9.0 script directory) a one-line append rather than a string-split edit.
- `name: 'scripts'` MUST stay the same — SCAF-06 explicitly rules out a new Vitest project.
- `environment: 'node'` and `globals: true` MUST stay — Plan 01's smoke test relies on `globals: true` (no `import { describe, it, expect }` in that file) and on `node` (no DOM globals leaking).
- DO NOT touch the `unit` or `browser` projects.
- DO NOT touch the top-level `plugins` or `resolve` blocks.
  </action>
  <verify>
    <automated>grep -q "scripts/editorial/\*\*/\*\.test\.ts" vitest.config.ts &amp;&amp; grep -q "scripts/markdown-export/\*\*/\*\.test\.ts" vitest.config.ts &amp;&amp; grep -c "name: 'scripts'" vitest.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` contains the literal substring `scripts/editorial/**/*.test.ts`.
    - `vitest.config.ts` STILL contains `scripts/markdown-export/**/*.test.ts` (regression check).
    - `vitest.config.ts` contains exactly ONE `name: 'scripts'` declaration (SCAF-06: no new Vitest project — extending, not duplicating).
    - `vitest.config.ts` contains exactly ONE `name: 'unit'` and exactly ONE `name: 'browser'` (regression check on the other two projects).
    - The `scripts` project block still contains `environment: 'node'` and `globals: true`.
    - File parses as valid TypeScript (`pnpm exec tsc --noEmit -p tsconfig.json` not run here — defer to Plan 05's `pnpm build`; instead a basic Node syntax check via `node -e "require('typescript').transpileModule(require('fs').readFileSync('vitest.config.ts','utf8'),{})"` is sufficient if needed).
  </acceptance_criteria>
  <done>
    Vitest `scripts` project discovers tests under both `scripts/markdown-export/**/*.test.ts` and `scripts/editorial/**/*.test.ts`; no new project added.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add .tsbuildinfo-editorial entry to .gitignore (SCAF-07)</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (the file being modified — confirm current entries, especially the existing `.tsbuildinfo-scripts/` block which is the analog)
    - tsconfig.editorial.json (created in Plan 02 — confirm `outDir` is `.tsbuildinfo-editorial` exactly; the .gitignore entry MUST match the directory name written by tsc)
    - .planning/REQUIREMENTS.md (SCAF-07 dictates the exact entry name)
  </read_first>
  <action>
Append the editorial tsbuildinfo entry to `.gitignore` immediately after the existing scripts-project entries. Group it visually with the existing block so future maintainers see the pattern.

**Before (last 4 lines of `.gitignore`):**
```
# TypeScript project-references build artifacts (scripts project)
.tsbuildinfo-scripts/
tsconfig.scripts.tsbuildinfo
*.tsbuildinfo
```

**After:**
```
# TypeScript project-references build artifacts (scripts project)
.tsbuildinfo-scripts/
tsconfig.scripts.tsbuildinfo
*.tsbuildinfo

# TypeScript project-references build artifacts (editorial project)
.tsbuildinfo-editorial/
tsconfig.editorial.tsbuildinfo
```

Implementation notes:
- Adding both `.tsbuildinfo-editorial/` (the directory `tsc` writes to via `outDir`) AND `tsconfig.editorial.tsbuildinfo` (the marker file `tsc -b` writes alongside the tsconfig) mirrors the scripts-project block.
- The existing `*.tsbuildinfo` catch-all already matches `tsconfig.editorial.tsbuildinfo` — this redundant explicit entry is intentional, mirroring the analog and making the per-project ignore intent self-documenting.
- DO NOT remove or reformat any existing `.gitignore` lines.
- Use `\n` line endings (project default — verified by absence of CRLF in the existing file).
  </action>
  <verify>
    <automated>grep -q "^\.tsbuildinfo-editorial/$" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains a line exactly matching `.tsbuildinfo-editorial/` (the directory entry that SCAF-07 requires).
    - `.gitignore` contains a line matching `tsconfig.editorial.tsbuildinfo` (parallel to the scripts-project entry).
    - All previously-existing entries still present (regression check on `node_modules`, `dist`, `.tsbuildinfo-scripts/`, `*.tsbuildinfo`).
    - File ends with a single newline (no trailing blank lines added).
    - No CRLF line endings introduced (`grep -l $'\r' .gitignore` returns no match).
  </acceptance_criteria>
  <done>
    `.tsbuildinfo-editorial/` is git-ignored. When Plan 05 runs `pnpm build`, the resulting build-artifact directory will not appear in `git status`.
  </done>
</task>

</tasks>

<verification>
After both tasks:

1. Vitest discovery glob is wired:
   ```bash
   grep -n "scripts/editorial" vitest.config.ts
   # Expected: at least one line in the `scripts` project's include array
   ```

2. Git-ignore is wired:
   ```bash
   grep -n "tsbuildinfo-editorial" .gitignore
   # Expected: at least one match
   ```

3. End-to-end test discovery + ignored-artifact behavior is NOT verified here — Plan 05 owns the `pnpm test:scripts` and `pnpm build` smoke runs.
</verification>

<success_criteria>
- `vitest.config.ts` `scripts` project `include` array covers `scripts/editorial/**/*.test.ts`.
- No new Vitest project introduced (still exactly 3 projects: unit / browser / scripts).
- `.gitignore` includes `.tsbuildinfo-editorial/` entry.
- Existing `.gitignore` and `vitest.config.ts` content preserved (no regressions).
</success_criteria>

<output>
After completion, create `.planning/phases/46-scaffold/46-04-SUMMARY.md` documenting:
- The exact diff applied to `vitest.config.ts` (before/after of the `include` array).
- The exact lines added to `.gitignore`.
- Confirmation that the `scripts` Vitest project still has `name: 'scripts'`, `environment: 'node'`, `globals: true` (no SCAF-06 violation).
- Note that smoke-run validation is deferred to Plan 05.
</output>
