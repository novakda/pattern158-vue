---
phase: 47-config-routes-pure-logic
plan: 06
type: execute
wave: 4
depends_on: ["47-04", "47-05"]
files_modified: []
autonomous: true
requirements:
  - CAPT-01
  - CAPT-02
  - WRIT-01
  - WRIT-02
tags: [editorial-capture, smoke-gate, verification, phase-47]

must_haves:
  truths:
    - "pnpm build exits 0 — vue-tsc -b walks tsconfig.editorial.json with zero errors after all Phase 47 implementations land"
    - "pnpm test:scripts exits 0 — config.test.ts + routes.test.ts + smoke.test.ts (+ markdown-export tests) all green"
    - "SCAF-08 grep gate is clean across the entire scripts/editorial/ tree (top-level + __tests__/)"
    - "All 5 Phase 47 ROADMAP success criteria are demonstrably true"
    - "loadEditorialConfig + buildRoutes wired together work against the REAL src/data/json/exhibits.json (15 entries → 22 total routes) — proven by an ad-hoc tsx invocation, NOT by index.ts orchestration which is Phase 50's job"
  artifacts:
    - path: .planning/phases/47-config-routes-pure-logic/47-06-SUMMARY.md
      provides: "Verification summary with proof matrix for all 5 phase success criteria"
      contains: "success criteria"
  key_links:
    - from: .planning/phases/47-config-routes-pure-logic/47-VERIFICATION.md
      to: scripts/editorial/config.ts + scripts/editorial/routes.ts
      via: "tsx ad-hoc smoke run that imports both modules and runs them against the real exhibits.json"
      pattern: "tsx -e.*loadEditorialConfig|buildRoutes"
---

<objective>
Wave 4 smoke gate. After Plans 01–05 land, verify end-to-end that Phase 47 is complete: build is green, all tests pass, SCAF-08 gate clean, and the two real implementations integrate against the real `src/data/json/exhibits.json` (without touching index.ts, which is Phase 50's territory). Produces a `47-VERIFICATION.md`-style proof matrix for the SUMMARY.

Purpose: Catch any regression introduced by Plans 01–05's individual landings before Phase 48 starts. This is the equivalent of Plan 46-05 — a dedicated gate, not folded into the last implementation plan.
Output: One verification document + the SUMMARY for this plan. No source code changes.
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/47-config-routes-pure-logic/47-CONTEXT.md
@.planning/phases/47-config-routes-pure-logic/47-01-PLAN.md
@.planning/phases/47-config-routes-pure-logic/47-02-PLAN.md
@.planning/phases/47-config-routes-pure-logic/47-03-PLAN.md
@.planning/phases/47-config-routes-pure-logic/47-04-PLAN.md
@.planning/phases/47-config-routes-pure-logic/47-05-PLAN.md
@.planning/phases/46-scaffold/46-05-SUMMARY.md
@scripts/editorial/config.ts
@scripts/editorial/routes.ts

<interfaces>
<!-- ROADMAP Phase 47 success criteria (the proof matrix this plan must populate): -->
1. `config.ts` parses --output, --base-url, --headful, --mirror + EDITORIAL_OUT_PATH/EDITORIAL_BASE_URL env fallbacks; missing required fails loud.
2. `config.ts` preflight validates absolute output, parent exists, parent writable; ENOENT/EACCES surface with clear error.
3. `routes.ts` loads exhibits.json via fs.readFile + JSON.parse and produces ordered Route[]: 7 static then exhibits A–O in source order.
4. Excluded routes (/review, /diag/*, /portfolio, /testimonials, 404) never appear.
5. Unit tests cover all the above — green in scripts Vitest project.

<!-- Real exhibits.json shape — verified to have 15 entries: -->
- 15 exhibit entries → 7 static + 15 exhibit = 22 total routes after buildRoutes.
- Slugs: exhibit-a, exhibit-b, …, exhibit-o (alphabetical because exhibits.json is alphabetical).

<!-- The ad-hoc tsx smoke MUST NOT modify scripts/editorial/index.ts — that file is reserved for Phase 50.
     Use `tsx -e '<inline script>'` to import the two modules and run them in-process. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run pnpm build + pnpm test:scripts; assert SCAF-08 grep gate clean</name>
  <files></files>
  <read_first>
    - scripts/editorial/config.ts (final post-Plan-02 state)
    - scripts/editorial/routes.ts (final post-Plan-03 state)
    - scripts/editorial/__tests__/config.test.ts (post-Plan-04)
    - scripts/editorial/__tests__/routes.test.ts (post-Plan-05)
  </read_first>
  <behavior>
    - Behavior 1: `pnpm build` exits 0 — vue-tsc -b walks all composite projects (root, scripts, editorial) with zero errors.
    - Behavior 2: `pnpm test:scripts` exits 0; the test summary line shows ≥ 50 passing tests across ≥ 17 test files (Phase 46 baseline was 145 tests / 16 files; Phase 47 adds 27+ config tests + 25+ routes tests = 50+ new tests across 2 new files = ≥ 195 tests / 18 files).
    - Behavior 3: `pnpm test:scripts` output mentions both new test files: `scripts/editorial/__tests__/config.test.ts` AND `scripts/editorial/__tests__/routes.test.ts`.
    - Behavior 4: SCAF-08 forbidden-pattern grep returns no matches across the entire `scripts/editorial/` tree (top-level + `__tests__/`).
    - Behavior 5: SCAF-08 banner present in every file in `scripts/editorial/` (descriptive form — Plan 46 convention).
    - Behavior 6: No throwing-stub remains in `config.ts` or `routes.ts` — `grep -r "not implemented" scripts/editorial/config.ts scripts/editorial/routes.ts` returns nothing (Plan 02 + Plan 03 replaced them).
  </behavior>
  <action>
    Execute the following commands in sequence and capture output for the SUMMARY:

    1. **pnpm build:**
       ```bash
       pnpm build 2>&1 | tee /tmp/p47-06-build.log
       echo BUILD_EXIT=$?
       ```
       Expect: exit 0; tail of log shows `vite build` success and either silent success from `vue-tsc -b` (no output is the success signal) OR explicit completion lines.

    2. **pnpm test:scripts:**
       ```bash
       pnpm test:scripts 2>&1 | tee /tmp/p47-06-tests.log
       echo TESTS_EXIT=$?
       ```
       Expect: exit 0; output contains `config.test.ts` AND `routes.test.ts` AND `smoke.test.ts`; summary line shows `Tests N passed` with N ≥ 195.

    3. **SCAF-08 forbidden-pattern grep (top-level + __tests__/):**
       ```bash
       grep -RE "@/|Date\.now\(\)|new Date\(|os\.EOL|Promise\.all" scripts/editorial/
       echo SCAF_EXIT=$?
       ```
       Expect: empty output; exit 1 (no matches).

    4. **SCAF-08 banner present in every file:**
       ```bash
       for f in scripts/editorial/*.ts scripts/editorial/__tests__/*.ts; do
         grep -q "SCAF-08 forbidden in this directory" "$f" || echo "MISSING_BANNER: $f"
       done
       ```
       Expect: no `MISSING_BANNER:` lines printed.

    5. **No throwing-stub bodies remain in implemented modules:**
       ```bash
       grep "not implemented" scripts/editorial/config.ts scripts/editorial/routes.ts
       echo STUBS_EXIT=$?
       ```
       Expect: empty output; exit 1 (no matches).

    Constraints:
    - This task is verification-only. NO source code changes are produced. If any check fails, abort and report the failure for the gap-closure workflow.
    - All log files go to `/tmp/p47-06-*.log` so the SUMMARY can quote them by path.
    - Do NOT modify scripts/editorial/index.ts — Phase 50 territory.
    - Do NOT run `pnpm editorial:capture` in this task — it would invoke the placeholder index.ts and emit the Phase 46 banner; useful but covered in Task 2's broader integration smoke.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tee /tmp/p47-06-build.log; build_exit=$?; pnpm test:scripts 2>&1 | tee /tmp/p47-06-tests.log; tests_exit=$?; scaf=$(grep -RE "@/|Date\.now\(\)|new Date\(|os\.EOL|Promise\.all" scripts/editorial/ 2>&1); banner_missing=""; for f in scripts/editorial/*.ts scripts/editorial/__tests__/*.ts; do grep -q "SCAF-08 forbidden in this directory" "$f" || banner_missing="$banner_missing $f"; done; stubs=$(grep "not implemented" scripts/editorial/config.ts scripts/editorial/routes.ts 2>&1); echo "build_exit=$build_exit tests_exit=$tests_exit scaf_empty=$([ -z \"$scaf\" ] && echo yes || echo no) banner_missing=[${banner_missing}] stubs_empty=$([ -z \"$stubs\" ] && echo yes || echo no)"; [ "$build_exit" = "0" ] && [ "$tests_exit" = "0" ] && [ -z "$scaf" ] && [ -z "$banner_missing" ] && [ -z "$stubs" ]</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build 2>&1 | tee /tmp/p47-06-build.log; echo $?` shows `0` on the last line
    - `pnpm test:scripts 2>&1 | tee /tmp/p47-06-tests.log; echo $?` shows `0` on the last line
    - `/tmp/p47-06-tests.log` contains `config.test.ts` (case-sensitive)
    - `/tmp/p47-06-tests.log` contains `routes.test.ts` (case-sensitive)
    - `/tmp/p47-06-tests.log` contains `smoke.test.ts` (Phase 46 file still discovered)
    - `/tmp/p47-06-tests.log` contains `passed` and does NOT contain `failed` or `FAIL`
    - `grep -RE "@/|Date\.now\(\)|new Date\(|os\.EOL|Promise\.all" scripts/editorial/ ; echo $?` shows `1` (no matches)
    - For each file in `scripts/editorial/*.ts` and `scripts/editorial/__tests__/*.ts`, `grep -q "SCAF-08 forbidden in this directory"` succeeds
    - `grep "not implemented" scripts/editorial/config.ts scripts/editorial/routes.ts; echo $?` shows `1` (no matches — stubs replaced)
    - File `scripts/editorial/index.ts` has NOT been modified since Phase 46 (`git diff scripts/editorial/index.ts | wc -l` returns 0 — Phase 50's territory)
  </acceptance_criteria>
  <done>
    pnpm build green, pnpm test:scripts green with both new test files discovered and passing alongside the smoke + markdown-export suites, SCAF-08 grep gate clean, banner present in all 9 files (7 top-level + 2 test files), no throwing-stub bodies remain in config.ts/routes.ts, index.ts untouched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Ad-hoc tsx integration smoke against real exhibits.json + emit 47-VERIFICATION.md proof matrix</name>
  <files>.planning/phases/47-config-routes-pure-logic/47-VERIFICATION.md</files>
  <read_first>
    - /tmp/p47-06-build.log (from Task 1)
    - /tmp/p47-06-tests.log (from Task 1)
    - .planning/ROADMAP.md (Phase 47 success criteria — populate the matrix)
    - src/data/json/exhibits.json (real file — confirm length is 15)
  </read_first>
  <behavior>
    - Behavior 1: A one-shot tsx invocation imports `loadEditorialConfig` and `buildRoutes`, calls `loadEditorialConfig(['--output', '/tmp/p47-smoke-out.md'], { ...process.env })`, then calls `buildRoutes(config.exhibitsJsonPath)` and prints the result count.
    - Behavior 2: The tsx run reports `total routes: 22` (7 static + 15 exhibits).
    - Behavior 3: The tsx run reports the first route as `/` and the last route as `/exhibits/exhibit-o`.
    - Behavior 4: The tsx run does NOT touch the network (no fetch, no Playwright launch — Phase 47 is pure logic).
    - Behavior 5: A `47-VERIFICATION.md` file is created with a proof matrix mapping each of the 5 ROADMAP success criteria to its evidence (test name, command output, log file path).
    - Behavior 6: The ad-hoc tsx invocation is invoked WITHOUT writing any new file in `scripts/editorial/` — uses `tsx -e '<inline script>'` form.
  </behavior>
  <action>
    1. **Pre-flight: confirm parent dir for the smoke output path exists** (`/tmp` is writable on every dev machine; this validates preflight passes):
       ```bash
       test -d /tmp -a -w /tmp || { echo "FAIL: /tmp not writable"; exit 1; }
       ```

    2. **Confirm the real exhibits.json has 15 entries** (sanity check before the smoke run):
       ```bash
       jq -c 'length' src/data/json/exhibits.json
       ```
       Expect: `15`

    3. **Run the integration smoke via tsx -e** (in-process; no new file):
       ```bash
       pnpm exec tsx -e "
       import { loadEditorialConfig } from './scripts/editorial/config.ts';
       import { buildRoutes } from './scripts/editorial/routes.ts';
       const cfg = loadEditorialConfig(['--output', '/tmp/p47-smoke-out.md'], process.env);
       const routes = await buildRoutes(cfg.exhibitsJsonPath);
       console.log('total routes:', routes.length);
       console.log('first:', routes[0].path);
       console.log('last:', routes[routes.length - 1].path);
       console.log('static count:', routes.filter(r => r.category === 'static').length);
       console.log('exhibit count:', routes.filter(r => r.category === 'exhibit').length);
       console.log('first exhibit slug:', routes.find(r => r.category === 'exhibit')?.sourceSlug);
       console.log('last exhibit slug:', routes.filter(r => r.category === 'exhibit').slice(-1)[0]?.sourceSlug);
       " 2>&1 | tee /tmp/p47-06-integration.log
       echo INTEGRATION_EXIT=$?
       ```
       Expect:
       - `total routes: 22`
       - `first: /`
       - `last: /exhibits/exhibit-o`
       - `static count: 7`
       - `exhibit count: 15`
       - `first exhibit slug: exhibit-a`
       - `last exhibit slug: exhibit-o`
       - `INTEGRATION_EXIT=0`

    4. **Write `.planning/phases/47-config-routes-pure-logic/47-VERIFICATION.md`** with the following structure (use the Write tool; substitute the actual log excerpts captured above):

       ```markdown
       ---
       phase: 47-config-routes-pure-logic
       gate: smoke
       status: green
       generated: <ISO date from `node -p "new Date().toISOString()"` — for VERIFICATION docs only; per SCAF-08, source code never uses Date()/Date.now(), but docs are exempt>
       verified_by: 47-06-PLAN.md Task 2
       ---

       # Phase 47 Verification: Config + Routes (pure logic)

       ## Smoke Gate Result

       PASS — all 5 Phase 47 ROADMAP success criteria proven true.

       ## Proof Matrix

       | # | ROADMAP Success Criterion | Evidence |
       |---|---------------------------|----------|
       | 1 | config.ts parses --output, --base-url, --headful, --mirror + EDITORIAL_OUT_PATH/EDITORIAL_BASE_URL env fallbacks; missing required fails loud | `config.test.ts > parseArgs > parses ...` (10 cases) + `config.test.ts > mergeConfig > CLI --output wins ...` (precedence cases) + `config.test.ts > mergeConfig > throws ConfigError when neither --output nor EDITORIAL_OUT_PATH is set` — all green in `/tmp/p47-06-tests.log` |
       | 2 | config.ts preflight validates absolute output, parent exists, parent writable; ENOENT/EACCES surface clearly | `config.test.ts > runPreflight` (8 cases including ENOENT, EACCES with root-skip, non-absolute, URL parse, non-https) — all green in `/tmp/p47-06-tests.log` |
       | 3 | routes.ts loads exhibits.json via fs.readFile + JSON.parse; ordered Route[] static-then-exhibits-in-source-order | `routes.test.ts > buildRoutes > appends exhibits AFTER the 7 static routes in source order` + `routes.test.ts > buildRoutes > preserves exhibit source order` + integration: `total routes: 22` (7 static + 15 exhibits A–O) from `/tmp/p47-06-integration.log` |
       | 4 | Excluded routes (/review, /diag/*, /portfolio, /testimonials, 404) never appear | `routes.test.ts > isExcluded` (it.each: 4 exact matches + 3 subpath matches + 4 substring-not-segment false cases + 7 static-not-excluded + 2 exhibit-not-excluded) + `routes.test.ts > buildRoutes > result contains no excluded paths` |
       | 5 | Unit tests cover all the above — green in scripts Vitest project | `pnpm test:scripts` exit 0 with 27+ config tests + 25+ routes tests = 50+ new tests, all passing — see `/tmp/p47-06-tests.log` test summary line |

       ## Build / Test Output Summary

       ### `pnpm build`

       Exit code: 0
       Log: `/tmp/p47-06-build.log`
       Tail (last 10 lines):
       ```
       <paste tail -n 10 /tmp/p47-06-build.log here>
       ```

       ### `pnpm test:scripts`

       Exit code: 0
       Log: `/tmp/p47-06-tests.log`
       Test summary line:
       ```
       <paste the "Test Files X passed" + "Tests N passed" lines here>
       ```

       ### Integration smoke (ad-hoc tsx -e)

       Exit code: 0
       Log: `/tmp/p47-06-integration.log`
       Output:
       ```
       <paste the tsx output verbatim here, all 7 console.log lines>
       ```

       ## SCAF-08 Gate

       - Forbidden-pattern grep across `scripts/editorial/` (top-level + `__tests__/`): no matches.
       - Banner present in all 9 files (7 top-level: index.ts, config.ts, routes.ts, capture.ts, convert.ts, write.ts, types.ts; 2 test files: smoke.test.ts, config.test.ts, routes.test.ts).
       - No "not implemented" stub bodies in config.ts or routes.ts.
       - `scripts/editorial/index.ts` unchanged from Phase 46 (Phase 50 territory).

       ## Hand-off to Phase 48

       - `loadEditorialConfig()` and `buildRoutes(path)` are stable — Phase 48 capture.ts can import both directly.
       - `EditorialConfig.exhibitsJsonPath` is the source-of-truth path passed into buildRoutes; capture.ts iterates the resulting Route[] sequentially (no Promise.all per SCAF-08).
       - `Route.sourceSlug` is the screenshot-filename anchor (Phase 48 will use `<vault>/career/website/site-editorial-capture/screenshots/${sourceSlug}.png` for exhibit pages; static pages use `path.replace(/\//g, '_') || 'home'` or similar — Phase 48's call).
       - No blockers. No deferred work. No open questions.
       ```

    5. **Verify the VERIFICATION doc was written**:
       ```bash
       test -f .planning/phases/47-config-routes-pure-logic/47-VERIFICATION.md && grep -q "PASS" .planning/phases/47-config-routes-pure-logic/47-VERIFICATION.md && grep -q "total routes: 22" .planning/phases/47-config-routes-pure-logic/47-VERIFICATION.md
       ```
       Expect: all three checks pass.

    Constraints:
    - The tsx -e script MUST use the project's existing `pnpm exec tsx` (not a global tsx). NodeNext path resolution requires `.ts` extensions in the import strings.
    - The tsx invocation argv passes `['--output', '/tmp/p47-smoke-out.md']` — `/tmp` is universally writable, so preflight passes; the file is NEVER actually written (Phase 50 wires the write step).
    - Do NOT add `process.env.EDITORIAL_OUT_PATH` to the env passed in — explicit CLI proves the precedence path. (`{ ...process.env }` spreads the real env; tests for env-fallback live in `config.test.ts`.)
    - Do NOT use `Promise.all`, `Date.now()`, `new Date()`, `os.EOL`, `@/` in the tsx -e snippet.
    - The VERIFICATION.md "generated" frontmatter timestamp uses `node -p "new Date().toISOString()"` because docs are not subject to SCAF-08's deterministic-output rule (SCAF-08 governs editorial-tool source code, not planning artifacts). If the executor prefers, omit the field — it's informational only.
  </action>
  <verify>
    <automated>jq -c 'length' src/data/json/exhibits.json | grep -q '^15$' && pnpm exec tsx -e "import { loadEditorialConfig } from './scripts/editorial/config.ts'; import { buildRoutes } from './scripts/editorial/routes.ts'; const cfg = loadEditorialConfig(['--output', '/tmp/p47-smoke-out.md'], process.env); const routes = await buildRoutes(cfg.exhibitsJsonPath); console.log('total routes:', routes.length); console.log('first:', routes[0].path); console.log('last:', routes[routes.length - 1].path); console.log('static count:', routes.filter(r => r.category === 'static').length); console.log('exhibit count:', routes.filter(r => r.category === 'exhibit').length);" 2>&1 | tee /tmp/p47-06-integration.log && grep -q "total routes: 22" /tmp/p47-06-integration.log && grep -q "first: /" /tmp/p47-06-integration.log && grep -q "last: /exhibits/exhibit-o" /tmp/p47-06-integration.log && grep -q "static count: 7" /tmp/p47-06-integration.log && grep -q "exhibit count: 15" /tmp/p47-06-integration.log && test -f .planning/phases/47-config-routes-pure-logic/47-VERIFICATION.md && grep -q "Phase 47 Verification" .planning/phases/47-config-routes-pure-logic/47-VERIFICATION.md && grep -q "total routes: 22" .planning/phases/47-config-routes-pure-logic/47-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `jq -c 'length' src/data/json/exhibits.json` returns `15`
    - The tsx -e integration smoke run prints `total routes: 22`
    - The tsx -e integration smoke prints `first: /`
    - The tsx -e integration smoke prints `last: /exhibits/exhibit-o`
    - The tsx -e integration smoke prints `static count: 7`
    - The tsx -e integration smoke prints `exhibit count: 15`
    - `/tmp/p47-06-integration.log` exists and contains the above lines
    - `.planning/phases/47-config-routes-pure-logic/47-VERIFICATION.md` exists
    - `grep -q "PASS" .planning/phases/47-config-routes-pure-logic/47-VERIFICATION.md` passes
    - `grep -q "Proof Matrix" .planning/phases/47-config-routes-pure-logic/47-VERIFICATION.md` passes
    - `grep -c "^| [12345] " .planning/phases/47-config-routes-pure-logic/47-VERIFICATION.md` returns ≥ 5 (one matrix row per success criterion)
    - `grep -q "/tmp/p47-06-build.log" .planning/phases/47-config-routes-pure-logic/47-VERIFICATION.md` passes (build log referenced)
    - `grep -q "/tmp/p47-06-tests.log" .planning/phases/47-config-routes-pure-logic/47-VERIFICATION.md` passes (test log referenced)
    - `grep -q "/tmp/p47-06-integration.log" .planning/phases/47-config-routes-pure-logic/47-VERIFICATION.md` passes (integration log referenced)
    - No file in `scripts/editorial/` was modified by this task: `git diff scripts/editorial/ | wc -l` returns `0`
    - `git diff scripts/editorial/index.ts | wc -l` returns `0` (Phase 50's territory untouched)
  </acceptance_criteria>
  <done>
    Integration smoke against the REAL exhibits.json proves the two implementations integrate (22 routes, A–O in source order). VERIFICATION.md exists with the 5-row proof matrix mapping each success criterion to test names + log files. No source code was modified in this task. Phase 47 is verified complete.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- All 5 Phase 47 ROADMAP success criteria have a green check via `47-VERIFICATION.md`'s proof matrix.
- `pnpm build` and `pnpm test:scripts` both green.
- SCAF-08 grep gate clean across the entire `scripts/editorial/` tree.
- `scripts/editorial/index.ts` unchanged from Phase 46 (Phase 50 territory).
- No source code modifications in this plan — verification + docs only.
</verification>

<success_criteria>
- `pnpm build` exits 0; `pnpm test:scripts` exits 0 with both new test files discovered and passing.
- SCAF-08 grep gate clean; SCAF-08 banner present in every file under `scripts/editorial/`.
- Integration smoke via `tsx -e` proves `loadEditorialConfig` + `buildRoutes` work against the real exhibits.json (22 routes total).
- `47-VERIFICATION.md` exists with the proof matrix linking each ROADMAP success criterion to its evidence.
- `scripts/editorial/index.ts` unchanged from Phase 46.
</success_criteria>

<output>
After completion, create `.planning/phases/47-config-routes-pure-logic/47-06-SUMMARY.md` covering: which checks ran, which passed, key log file paths, and confirmation that Phase 47 is complete and Phase 48 is unblocked.
</output>
