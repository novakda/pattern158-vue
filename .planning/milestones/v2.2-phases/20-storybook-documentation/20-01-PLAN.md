---
phase: 20-storybook-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/PersonnelCard.stories.ts
autonomous: false
requirements:
  - DOC-01
must_haves:
  truths:
    - "Storybook has a PersonnelCard story showing a named person with all fields populated"
    - "Storybook has a PersonnelCard story showing an anonymous person (no name)"
    - "Storybook has a PersonnelCard story showing a self-highlighted entry (isSelf: true)"
  artifacts:
    - path: "src/components/PersonnelCard.stories.ts"
      provides: "PersonnelCard Storybook stories for all three display variants"
      exports: ["NamedPerson", "AnonymousPerson", "SelfHighlighted"]
      contains: "Components/PersonnelCard"
  key_links:
    - from: "src/components/PersonnelCard.stories.ts"
      to: "src/components/PersonnelCard.vue"
      via: "default import"
      pattern: "import PersonnelCard from './PersonnelCard.vue'"
    - from: "src/components/PersonnelCard.stories.ts"
      to: "@storybook/vue3-vite"
      via: "type import"
      pattern: "import type.*Meta.*StoryObj.*@storybook/vue3-vite"
---

<objective>
Add Storybook stories for PersonnelCard covering all three display variants: named person (full fields), anonymous person (no name), and self-highlighted entry (isSelf: true).

Purpose: Document the PersonnelCard component in Storybook so all rendering modes are visually browsable and verifiable.
Output: `src/components/PersonnelCard.stories.ts` with three named story exports.
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/PersonnelCard.vue
@src/components/FindingCard.stories.ts

<interfaces>
<!-- PersonnelCard component interface -- executor needs this to write correct mock data -->

From src/data/exhibits.ts:
```typescript
export interface ExhibitPersonnelEntry {
  name?: string
  title?: string
  organization?: string
  role?: string
  isSelf?: boolean
}
```

From src/components/PersonnelCard.vue:
```typescript
defineProps<{
  personnel: ExhibitPersonnelEntry[]
}>()
```

Component renders:
- Named person: `.personnel-name` shows name, `.personnel-role` shows role, `.personnel-title` shows title, `.personnel-org` shows organization
- Anonymous person (no name): `.personnel-name.personnel-name--anonymous` shows title in name position
- Self entry: `.personnel-card--self` CSS class applied to card wrapper

From src/components/FindingCard.stories.ts (CSF3 pattern to follow):
```typescript
import type { Meta, StoryObj } from '@storybook/vue3-vite'
import FindingCard from './FindingCard.vue'

const meta = {
  title: 'Components/FindingCard',
  component: FindingCard,
} satisfies Meta<typeof FindingCard>

export default meta
type Story = StoryObj<typeof meta>

export const Default: Story = {
  args: { /* prop data */ },
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PersonnelCard Storybook stories</name>
  <files>src/components/PersonnelCard.stories.ts</files>
  <read_first>
    - src/components/PersonnelCard.vue
    - src/components/FindingCard.stories.ts
    - src/data/exhibits.ts (ExhibitPersonnelEntry interface)
  </read_first>
  <action>
Create `src/components/PersonnelCard.stories.ts` following the exact CSF3 pattern from FindingCard.stories.ts.

File structure:
1. Import `Meta` and `StoryObj` types from `@storybook/vue3-vite` (NOT `@storybook/vue3`)
2. Import `PersonnelCard` from `./PersonnelCard.vue`
3. Define `meta` with `title: 'Components/PersonnelCard'` and `component: PersonnelCard`, using `satisfies Meta<typeof PersonnelCard>`
4. Export `default meta`, define `type Story = StoryObj<typeof meta>`

Create three named story exports:

**NamedPerson** -- Shows named persons with all fields populated. Use an array with 2 entries to demonstrate the grid layout:
- Entry 1: `{ name: 'Jane Smith', title: 'Program Manager', organization: 'Client Corp', role: 'Project oversight' }`
- Entry 2: `{ name: 'Robert Chen', title: 'Senior Developer', organization: 'Acme Technologies', role: 'Technical implementation' }`

**AnonymousPerson** -- Shows anonymous person (no `name` field). Use an array with 2 entries to show the anonymous styling:
- Entry 1: `{ title: 'Program Manager', organization: 'GP Strategies', role: 'Coordinated engagement' }`
- Entry 2: `{ title: 'Quality Analyst', organization: 'Client Corp' }`

**SelfHighlighted** -- Shows entry with `isSelf: true`. Include a mix to show contrast:
- Entry 1: `{ name: 'Dan Novak', title: 'Technical Lead', organization: 'GP Strategies', isSelf: true }`
- Entry 2: `{ name: 'Jane Smith', title: 'Program Manager', organization: 'Client Corp' }`

All entries must be wrapped in arrays (`personnel: [...]`) since the component prop is `ExhibitPersonnelEntry[]`.

Do NOT import real exhibit data. Use inline mock data per project convention.
  </action>
  <verify>
    <automated>cd /home/xhiris/projects/pattern158-vue && npx storybook build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/components/PersonnelCard.stories.ts
    - Contains `import type { Meta, StoryObj } from '@storybook/vue3-vite'`
    - Contains `import PersonnelCard from './PersonnelCard.vue'`
    - Contains `title: 'Components/PersonnelCard'`
    - Contains `satisfies Meta<typeof PersonnelCard>`
    - Contains `export const NamedPerson: Story`
    - Contains `export const AnonymousPerson: Story`
    - Contains `export const SelfHighlighted: Story`
    - NamedPerson args.personnel has 2 entries, both with `name` field
    - AnonymousPerson args.personnel has entries without `name` field
    - SelfHighlighted args.personnel has entry with `isSelf: true`
    - `npx storybook build` completes without error
  </acceptance_criteria>
  <done>
    PersonnelCard.stories.ts exists with three named exports (NamedPerson, AnonymousPerson, SelfHighlighted), each passing correctly shaped ExhibitPersonnelEntry[] mock data. Storybook build succeeds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification in Storybook</name>
  <files>src/components/PersonnelCard.stories.ts</files>
  <read_first>
    - src/components/PersonnelCard.stories.ts
  </read_first>
  <action>
Present Storybook for visual inspection. Start Storybook dev server if not already running: `npx storybook dev -p 6006`. Navigate to Components > PersonnelCard and verify all three story variants render correctly.
  </action>
  <what-built>PersonnelCard Storybook stories covering all three display variants: named persons with full fields, anonymous persons without names, and self-highlighted entries.</what-built>
  <how-to-verify>
    1. Run `npx storybook dev -p 6006` from the project root
    2. Navigate to Components > PersonnelCard in the sidebar
    3. Check "NamedPerson" story: two cards with names, titles, organizations, and roles visible
    4. Check "AnonymousPerson" story: two cards showing titles in the name position with anonymous/italic styling, no "Unknown" text
    5. Check "SelfHighlighted" story: Dan Novak card has visible highlight treatment (border or background), Jane Smith card does not
  </how-to-verify>
  <verify>User confirms all three variants display correctly</verify>
  <acceptance_criteria>
    - NamedPerson story shows two cards with visible name, title, organization, role fields
    - AnonymousPerson story shows cards with title in name position, no "Unknown" placeholder
    - SelfHighlighted story shows Dan Novak card with distinct visual treatment vs Jane Smith card
  </acceptance_criteria>
  <done>User has visually confirmed all three PersonnelCard story variants render correctly in Storybook.</done>
  <resume-signal>Type "approved" or describe any visual issues</resume-signal>
</task>

</tasks>

<verification>
1. `grep -c 'export const.*Story' src/components/PersonnelCard.stories.ts` returns 3
2. `npx storybook build` succeeds (exit code 0)
3. File follows same CSF3 pattern as FindingCard.stories.ts
</verification>

<success_criteria>
- src/components/PersonnelCard.stories.ts exists with NamedPerson, AnonymousPerson, and SelfHighlighted story exports
- Storybook build compiles without errors
- Visual inspection confirms all three variant displays render correctly
</success_criteria>

<output>
After completion, create `.planning/phases/20-storybook-documentation/20-01-SUMMARY.md`
</output>
