---
phase: 038-ir-markdown-primitives-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - pnpm-lock.yaml
  - tsconfig.json
  - tsconfig.scripts.json
  - vitest.config.ts
  - scripts/markdown-export/index.ts
autonomous: true
requirements: [SCAF-01, SCAF-02, SCAF-03]
must_haves:
  truths:
    - "`pnpm install` succeeds and `package-lock.json` is absent from the repo"
    - "`pnpm test` runs all three Vitest projects (unit, browser, scripts) without config errors"
    - "`pnpm build:markdown` exits 0 (placeholder orchestrator logs a banner and returns)"
    - "`pnpm build` runs `vue-tsc -b && vite build && pnpm build:markdown` in sequence"
    - "`tsx`, `yaml`, and `github-slugger` are listed in package.json devDependencies"
    - "`scripts/markdown-export/` exists with ONLY `index.ts` + the four concern subdirs (`ir/`, `primitives/`, `frontmatter/`, `escape/`) — no `extractors/`, no `renderers/`, no `fs-writer/`, no `.gitkeep` files"
  artifacts:
    - path: "tsconfig.scripts.json"
      provides: "Separate TS project for scripts/markdown-export/ with NodeNext + strict"
      contains: '"module": "NodeNext"'
    - path: "tsconfig.json"
      provides: "Root TS config with project reference to scripts config"
      contains: '"references"'
    - path: "vitest.config.ts"
      provides: "Three-project config including scripts project"
      contains: "name: 'scripts'"
    - path: "package.json"
      provides: "pnpm devDeps + build:markdown + test:scripts scripts"
      contains: '"build:markdown"'
    - path: "scripts/markdown-export/index.ts"
      provides: "Placeholder orchestrator (Phase 43 wires it)"
      min_lines: 1
    - path: "pnpm-lock.yaml"
      provides: "pnpm lockfile committed to repo"
  key_links:
    - from: "tsconfig.json"
      to: "tsconfig.scripts.json"
      via: "project references array"
      pattern: "references.*tsconfig\\.scripts\\.json"
    - from: "package.json build script"
      to: "scripts/markdown-export/index.ts"
      via: "pnpm build:markdown → tsx scripts/markdown-export/index.ts"
      pattern: "tsx scripts/markdown-export/index\\.ts"
    - from: "vitest.config.ts"
      to: "scripts/markdown-export/**/*.test.ts"
      via: "scripts project include glob"
      pattern: "scripts/markdown-export/\\*\\*/\\*\\.test\\.ts"
---

<objective>
Establish the Phase 38 foundation layer: migrate the repo from npm to pnpm (D-01/D-02), install the three new devDeps (`tsx`, `yaml`, `github-slugger`), create `tsconfig.scripts.json` (D-11), wire it into the root tsconfig via project references (D-12), add the third `scripts` project to `vitest.config.ts` (D-14), add the new npm scripts (`build:markdown`, `test:scripts`) and update the `build` chain (D-13, D-15), and scaffold `scripts/markdown-export/` with ONLY the four concern subdirs — no `.gitkeep` placeholders for future-phase dirs (D-03, D-05).

Purpose: every subsequent Phase 38 plan (IR, escape, frontmatter, primitives) depends on pnpm + tsconfig + vitest wiring being in place. This plan is serial-blocking and must land before any Wave 2 plan runs.

Output: a repo that installs with pnpm, runs `pnpm test` across three Vitest projects, passes `pnpm build`, and exposes an empty-but-working `pnpm build:markdown` command.
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/038-ir-markdown-primitives-scaffold/038-CONTEXT.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md
@package.json
@tsconfig.json
@vitest.config.ts

<interfaces>
<!-- Current state of files this plan modifies. Executor should read the files directly before editing. -->

Current package.json scripts (to be extended):
```json
{
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "npm run build && wrangler dev",
    "storybook": "storybook dev -p 6006",
    "build-storybook": "storybook build",
    "test": "vitest run",
    "test:unit": "vitest run --project unit",
    "test:browser": "vitest run --project browser",
    "deploy": "npm run build && wrangler deploy"
  }
}
```

Current tsconfig.json (root, to be extended with references only — all other fields unchanged):
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "jsx": "preserve",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "esModuleInterop": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "skipLibCheck": true,
    "noEmit": true,
    "paths": { "@/*": ["./src/*"] },
    "baseUrl": ".",
    "types": ["vite/client"]
  },
  "include": ["src/**/*.ts", "src/**/*.vue", "env.d.ts"]
}
```

Current vitest.config.ts (existing `unit` + `browser` projects MUST NOT change):
```ts
export default defineConfig({
  plugins: [vue()],
  resolve: { alias: { '@': fileURLToPath(new URL('./src', import.meta.url)) } },
  test: {
    projects: [
      { extends: true, test: { name: 'unit', include: ['src/**/*.test.ts'], exclude: ['src/**/*.browser.test.ts'], environment: 'happy-dom', globals: true } },
      { extends: true, test: { name: 'browser', include: ['src/**/*.browser.test.ts'], browser: { /* ... */ }, globals: true } },
    ],
  },
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate from npm to pnpm and install new devDeps</name>
  <files>
    - package-lock.json (deleted)
    - pnpm-lock.yaml (committed)
    - package.json (modified — devDependencies section, "packageManager" field optional)
  </files>
  <read_first>
    - package.json (current scripts + devDeps)
    - pnpm-lock.yaml (current untracked lockfile from worktree-agent-af18ea90 merge; confirm it exists in working tree via `ls pnpm-lock.yaml`)
    - package-lock.json (confirm it is tracked via `git ls-files package-lock.json`)
  </read_first>
  <action>
Execute the package-manager migration per D-01 and D-02 in this exact sequence. Do NOT perform any other tsconfig / vitest / scaffold work in this task — Task 2 and Task 3 handle those.

1. Delete the committed `package-lock.json` at repo root:
   ```bash
   rm package-lock.json
   ```

2. Run `pnpm install` once to rehydrate `node_modules/` from the existing `pnpm-lock.yaml`:
   ```bash
   pnpm install
   ```
   Expected: exits 0, installs all current deps from pnpm-lock.yaml, no lockfile modifications needed.

3. Run the existing test suite under pnpm to confirm the migration did not break anything:
   ```bash
   pnpm test
   ```
   Expected: existing unit + browser projects all green. (Phase 37 state: 149/149 passing per STATE.md — same count expected here since we have not touched any source yet.)

4. Install the three new Phase 38 devDeps:
   ```bash
   pnpm add -D tsx yaml github-slugger
   ```
   Expected: package.json `devDependencies` gains `tsx`, `yaml`, `github-slugger` entries; `pnpm-lock.yaml` updates with their resolutions.

5. Do NOT add a `"packageManager": "pnpm@x.y.z"` field yet — that is a separate hygiene decision and is out of scope for Phase 38.

6. Verify the result:
   - `package-lock.json` no longer exists
   - `pnpm-lock.yaml` is now a tracked file (not untracked)
   - `package.json` devDependencies contains `"tsx"`, `"yaml"`, `"github-slugger"`
   - `pnpm test` still passes

Forbidden in this task:
- Adding any tsconfig or vitest changes (that is Tasks 2-3)
- Running `npm install` for any reason
- Pinning `packageManager` field
- Touching any file other than `package.json`, `package-lock.json`, `pnpm-lock.yaml`
  </action>
  <verify>
    <automated>test ! -f package-lock.json &amp;&amp; test -f pnpm-lock.yaml &amp;&amp; node -e "const p=require('./package.json'); if(!p.devDependencies.tsx)throw'no tsx'; if(!p.devDependencies.yaml)throw'no yaml'; if(!p.devDependencies['github-slugger'])throw'no github-slugger'; console.log('ok')" &amp;&amp; pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `package-lock.json` does NOT exist at repo root (`test ! -f package-lock.json` exits 0)
    - `pnpm-lock.yaml` exists at repo root AND is tracked by git (`git ls-files pnpm-lock.yaml` returns the path)
    - `package.json` devDependencies contains an entry for `tsx` (grep: `"tsx":`)
    - `package.json` devDependencies contains an entry for `yaml` (grep: `"yaml":`)
    - `package.json` devDependencies contains an entry for `github-slugger` (grep: `"github-slugger":`)
    - `pnpm install` exits 0 and requires no lockfile regeneration (no `--no-frozen-lockfile` needed)
    - `pnpm test` exits 0 with the same unit + browser test counts as before the migration (Phase 37 baseline: ≥149 tests passing)
    - `node_modules/tsx`, `node_modules/yaml`, `node_modules/github-slugger` directories exist after install
  </acceptance_criteria>
  <done>pnpm is the package manager, three new devDeps are installed, existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create tsconfig.scripts.json, wire project references, scaffold scripts/markdown-export/</name>
  <files>
    - tsconfig.scripts.json (new)
    - tsconfig.json (modified — add `references` array ONLY)
    - scripts/markdown-export/index.ts (new — placeholder orchestrator)
    - scripts/markdown-export/ir/ (new directory, created implicitly by Wave 2)
    - scripts/markdown-export/primitives/ (new directory, created implicitly by Wave 2)
    - scripts/markdown-export/frontmatter/ (new directory, created implicitly by Wave 2)
    - scripts/markdown-export/escape/ (new directory, created implicitly by Wave 2)
  </files>
  <read_first>
    - tsconfig.json (current shape — must not change except for `references`)
    - .planning/phases/038-ir-markdown-primitives-scaffold/038-CONTEXT.md §"tsconfig.scripts.json (D-11, D-12, D-13)" — exact config shape
    - .planning/PROJECT.md §"Hard constraints / forbidden list" (lines 139-150) — verify forbidden APIs for the placeholder
  </read_first>
  <action>
Create the scripts-project TypeScript config, wire it into root, and create the one-file scaffold needed by Wave 2. Directory creation for `ir/`, `primitives/`, `frontmatter/`, `escape/` happens implicitly when Wave 2 plans write files into those paths — this task does NOT pre-create empty directories with `.gitkeep` (forbidden by D-05).

1. Create `tsconfig.scripts.json` at repo root with this exact content (D-11):

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "noEmit": true,
    "isolatedModules": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "skipLibCheck": true,
    "lib": ["ES2022"],
    "types": ["node"],
    "paths": {},
    "baseUrl": "."
  },
  "include": ["scripts/markdown-export/**/*.ts"]
}
```

Rationale for each field (do NOT change these — they are locked by D-11):
- `module: NodeNext` + `moduleResolution: NodeNext` — matches how tsx loads `.ts` files at runtime, catches extension-missing import bugs at compile time
- `strict: true` — matches root strictness (D-11 floor)
- `noEmit: true` — type-check only, never emit JS (tsx runs the `.ts` files directly)
- `paths: {}` — REQUIRED empty (scripts/markdown-export/** may NEVER use `@/` imports per the forbidden list); this config makes `@/` resolution impossible inside scripts
- `target: ES2022` + `lib: ['ES2022']` — Node 24 runtime target, no DOM
- `types: ['node']` — Node stdlib types only, no Vite/DOM globals
- `include` — only scripts/markdown-export/**/*.ts, so root `src/` files are NOT type-checked a second time

2. Modify `tsconfig.json` at repo root to add the `references` array per D-12. The `compilerOptions` and `include` arrays MUST remain byte-for-byte identical. Add ONE new top-level key `"references"`:

```json
{
  "compilerOptions": { /* unchanged */ },
  "include": [ /* unchanged */ ],
  "references": [
    { "path": "./tsconfig.scripts.json" }
  ]
}
```

Do NOT add `composite: true` anywhere — `vue-tsc -b` works with references without it for noEmit leaf projects. Do NOT modify any other field.

3. Create `scripts/markdown-export/index.ts` as a placeholder orchestrator. Phase 43 will wire real logic; Phase 38 just needs `tsx scripts/markdown-export/index.ts` to exit 0 so `build:markdown` works during this phase. Content:

```ts
// scripts/markdown-export/index.ts
// Phase 38 placeholder — real orchestrator is wired in Phase 43 (ORCH-01).
// DO NOT import from src/ or use @/ aliases here (forbidden in scripts/markdown-export/**).

const GENERATED_BANNER = '<!-- auto-generated by scripts/markdown-export — do not edit by hand -->'

function main(): void {
  // Intentionally empty: Phase 38 only ships primitives, escape helpers,
  // frontmatter serializer, and IR types. Phase 39 adds extractors, Phase 41/42
  // add renderers, Phase 43 wires this orchestrator.
  process.stdout.write(`${GENERATED_BANNER}\n`)
}

main()
```

Forbidden in this placeholder (will be checked):
- `import` from `@/...` (no path alias — tsconfig.scripts.json `paths: {}` enforces)
- `import` from `src/...`
- `Date.now()`, `new Date()`, `process.hrtime()`, `performance.now()` (forbidden list)
- `os.EOL` — use `\n` literal (forbidden list)
- `assert { type: 'json' }` — not needed here and forbidden by PROJECT.md
- Any `postinstall` / `prepare` hook in package.json referencing this file

4. Verify the type-check picks up the scripts project via references:
```bash
pnpm exec vue-tsc -b
```
Expected: exits 0, type-checks both the Vue src/ and the new scripts/markdown-export/index.ts with no errors.

5. Verify the placeholder runs:
```bash
pnpm exec tsx scripts/markdown-export/index.ts
```
Expected: exits 0, prints the generated banner line.

Do NOT create empty directories. Do NOT add `.gitkeep` files. D-05 forbids pre-creating `extractors/`, `renderers/`, `fs-writer/` directories — those arrive in Phases 39, 41, 43 respectively.
  </action>
  <verify>
    <automated>test -f tsconfig.scripts.json &amp;&amp; grep -q '"module": "NodeNext"' tsconfig.scripts.json &amp;&amp; grep -q '"moduleResolution": "NodeNext"' tsconfig.scripts.json &amp;&amp; grep -q '"strict": true' tsconfig.scripts.json &amp;&amp; grep -q '"paths": {}' tsconfig.scripts.json &amp;&amp; grep -q '"target": "ES2022"' tsconfig.scripts.json &amp;&amp; grep -q 'tsconfig.scripts.json' tsconfig.json &amp;&amp; test -f scripts/markdown-export/index.ts &amp;&amp; ! grep -rE '(@/|from .src/|Date\.now|new Date|process\.hrtime|performance\.now|os\.EOL)' scripts/markdown-export/ &amp;&amp; pnpm exec vue-tsc -b &amp;&amp; pnpm exec tsx scripts/markdown-export/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tsconfig.scripts.json` exists at repo root
    - `tsconfig.scripts.json` contains `"module": "NodeNext"` (grep exact string)
    - `tsconfig.scripts.json` contains `"moduleResolution": "NodeNext"` (grep exact string)
    - `tsconfig.scripts.json` contains `"strict": true` (grep exact string)
    - `tsconfig.scripts.json` contains `"noEmit": true` (grep exact string)
    - `tsconfig.scripts.json` contains `"paths": {}` (grep exact string)
    - `tsconfig.scripts.json` contains `"target": "ES2022"` (grep exact string)
    - `tsconfig.scripts.json` contains `"types": ["node"]` (grep exact string)
    - `tsconfig.scripts.json` contains `"include": ["scripts/markdown-export/**/*.ts"]` (grep exact string)
    - `tsconfig.json` contains a `"references"` key with `"./tsconfig.scripts.json"` entry (grep: `tsconfig.scripts.json`)
    - `tsconfig.json` compilerOptions and include arrays are byte-for-byte identical to the pre-task version (diff against git HEAD shows ONLY the references addition)
    - `scripts/markdown-export/index.ts` exists and is non-empty
    - `scripts/markdown-export/index.ts` contains NO `@/` imports (grep: `from ['"]@/` returns zero matches)
    - `scripts/markdown-export/index.ts` contains NO `src/` imports (grep: `from ['"]\.\./\.\./src` returns zero matches)
    - `scripts/markdown-export/index.ts` contains NO `Date.now(`, `new Date(`, `process.hrtime(`, `performance.now(`, `os.EOL` (grep returns zero matches for each)
    - `scripts/markdown-export/` does NOT contain `extractors/`, `renderers/`, `fs-writer/`, or any `.gitkeep` files (`find scripts/markdown-export -name '.gitkeep'` returns empty)
    - `pnpm exec vue-tsc -b` exits 0 and type-checks both src/ and scripts/markdown-export/
    - `pnpm exec tsx scripts/markdown-export/index.ts` exits 0
  </acceptance_criteria>
  <done>tsconfig.scripts.json created per D-11, wired into root per D-12, placeholder orchestrator type-checks and runs, no `.gitkeep` pollution.</done>
</task>

<task type="auto">
  <name>Task 3: Add scripts Vitest project, build:markdown + test:scripts npm scripts, update build chain</name>
  <files>
    - vitest.config.ts (modified — add third `scripts` project entry)
    - package.json (modified — add `build:markdown`, `test:scripts`; update `build` chain)
  </files>
  <read_first>
    - vitest.config.ts (verify current `unit` and `browser` project shapes; they MUST NOT change)
    - package.json (verify current scripts after Task 1)
    - .planning/phases/038-ir-markdown-primitives-scaffold/038-CONTEXT.md §"Vitest Wiring (D-14, D-15)" and §"tsconfig.scripts.json (D-13)"
  </read_first>
  <action>
Wire the scripts project into Vitest and add the new npm scripts. Do NOT touch the existing `unit` or `browser` project configs.

1. Modify `vitest.config.ts` — add a THIRD project entry inside `test.projects` array, after `browser`. The new entry MUST use these exact fields per D-14:

```ts
{
  extends: true,
  test: {
    name: 'scripts',
    include: ['scripts/markdown-export/**/*.test.ts'],
    environment: 'node',
    globals: true,
  },
},
```

Do NOT change anything else in the file. After the edit, the `projects` array has exactly three entries: `unit`, `browser`, `scripts`, in that order. The top-level `plugins: [vue()]` and `resolve.alias` blocks remain unchanged (they do not affect the scripts project because it uses `environment: 'node'` and no Vue imports).

2. Modify `package.json` `scripts` section — make exactly these three changes (D-13, D-15, SCAF-02):

   a. Change `"build"` from `"vue-tsc -b && vite build"` to `"vue-tsc -b && vite build && pnpm build:markdown"`.

   b. Add a new `"build:markdown"` script immediately after `"build"`:
   ```
   "build:markdown": "tsx scripts/markdown-export/index.ts"
   ```

   c. Add a new `"test:scripts"` script after `"test:browser"`:
   ```
   "test:scripts": "vitest run --project scripts"
   ```

   d. Also update `"preview"` and `"deploy"` to use `pnpm` instead of `npm`:
   - `"preview": "pnpm build && wrangler dev"`
   - `"deploy": "pnpm build && wrangler deploy"`

Do NOT add `postinstall` or `prepare` hooks referencing `build:markdown` — both are in the forbidden list.

Final expected `scripts` block after Task 3:
```json
{
  "dev": "vite",
  "build": "vue-tsc -b && vite build && pnpm build:markdown",
  "build:markdown": "tsx scripts/markdown-export/index.ts",
  "preview": "pnpm build && wrangler dev",
  "storybook": "storybook dev -p 6006",
  "build-storybook": "storybook build",
  "test": "vitest run",
  "test:unit": "vitest run --project unit",
  "test:browser": "vitest run --project browser",
  "test:scripts": "vitest run --project scripts",
  "deploy": "pnpm build && wrangler deploy"
}
```

3. Verify the vitest scripts project is discoverable even though no test files exist yet:
```bash
pnpm test:scripts
```
Expected: exits 0 with a message like "No test files found" (Vitest 4 treats this as a pass unless `--passWithNoTests=false` is set; if it errors, add `passWithNoTests: true` to the scripts project config as a last-resort fix — but first verify that the existing `unit`/`browser` projects don't already enable it).

4. Verify the full test runner still exits 0:
```bash
pnpm test
```
Expected: existing unit + browser counts unchanged + scripts project prints "no test files" or similar.

5. Verify the build chain:
```bash
pnpm build
```
Expected: exits 0, runs `vue-tsc -b`, then `vite build`, then `pnpm build:markdown` (which prints the banner from Task 2's placeholder).

Forbidden in this task:
- `postinstall` or `prepare` hooks running the generator (PROJECT.md forbidden list)
- Modifying the `unit` or `browser` project configs in vitest.config.ts
- Leaving `npm` references in `preview` or `deploy` scripts
  </action>
  <verify>
    <automated>grep -q "name: 'scripts'" vitest.config.ts &amp;&amp; grep -q "scripts/markdown-export/\*\*/\*\.test\.ts" vitest.config.ts &amp;&amp; grep -q "environment: 'node'" vitest.config.ts &amp;&amp; node -e "const p=require('./package.json'); if(p.scripts.build !== 'vue-tsc -b \u0026\u0026 vite build \u0026\u0026 pnpm build:markdown') throw 'build chain wrong: '+p.scripts.build; if(p.scripts['build:markdown'] !== 'tsx scripts/markdown-export/index.ts') throw 'build:markdown wrong'; if(p.scripts['test:scripts'] !== 'vitest run --project scripts') throw 'test:scripts wrong'; if(p.scripts.postinstall || p.scripts.prepare) throw 'forbidden hook'; console.log('ok')" &amp;&amp; pnpm test &amp;&amp; pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` contains `name: 'scripts'` (grep exact string)
    - `vitest.config.ts` contains `include: ['scripts/markdown-export/**/*.test.ts']` (grep the glob)
    - `vitest.config.ts` contains `environment: 'node'` (grep exact string — appears in scripts project block)
    - `vitest.config.ts` still contains the pre-existing `name: 'unit'` and `name: 'browser'` entries (grep both)
    - `vitest.config.ts` diff shows ONLY the addition of the scripts project entry; unit and browser blocks are byte-identical
    - `package.json` `scripts.build` equals exactly `"vue-tsc -b && vite build && pnpm build:markdown"`
    - `package.json` `scripts['build:markdown']` equals exactly `"tsx scripts/markdown-export/index.ts"`
    - `package.json` `scripts['test:scripts']` equals exactly `"vitest run --project scripts"`
    - `package.json` `scripts.preview` contains `pnpm build` (not `npm run build`)
    - `package.json` `scripts.deploy` contains `pnpm build` (not `npm run build`)
    - `package.json` does NOT contain a `postinstall` or `prepare` script (forbidden list)
    - `pnpm test` exits 0 (all three projects — unit, browser, scripts — discovered; scripts passes with no test files)
    - `pnpm build` exits 0 and runs the full chain (vue-tsc → vite build → build:markdown)
    - `pnpm build:markdown` exits 0 independently
  </acceptance_criteria>
  <done>Vitest knows about the scripts project, npm scripts expose build:markdown + test:scripts, build chain runs end-to-end.</done>
</task>

</tasks>

<verification>
After all three tasks complete, run the following gate:

```bash
# Lockfile swap
test ! -f package-lock.json && test -f pnpm-lock.yaml

# New devDeps present
node -e "const p=require('./package.json'); ['tsx','yaml','github-slugger'].forEach(d=>{if(!p.devDependencies[d])throw d})"

# tsconfig.scripts.json shape
grep -q '"module": "NodeNext"' tsconfig.scripts.json
grep -q '"paths": {}' tsconfig.scripts.json

# Root tsconfig has reference
grep -q 'tsconfig.scripts.json' tsconfig.json

# Vitest scripts project wired
grep -q "name: 'scripts'" vitest.config.ts

# Scaffold exists but no polluted dirs
test -f scripts/markdown-export/index.ts
test ! -d scripts/markdown-export/extractors
test ! -d scripts/markdown-export/renderers
test ! -d scripts/markdown-export/fs-writer
find scripts/markdown-export -name '.gitkeep' | (! grep .)

# Scripts source respects forbidden list
! grep -rE '(@/|from .\.\./\.\./src|Date\.now|new Date|process\.hrtime|performance\.now|os\.EOL)' scripts/markdown-export/

# Full build chain runs
pnpm install
pnpm test
pnpm build
pnpm build:markdown
```
</verification>

<success_criteria>
- `pnpm install` replaces `npm install` for this repo going forward
- `tsx`, `yaml`, `github-slugger` are installed devDeps
- `tsconfig.scripts.json` exists with NodeNext + strict + `paths: {}` + include `scripts/markdown-export/**/*.ts`
- Root `tsconfig.json` references `tsconfig.scripts.json` (no other changes)
- `vitest.config.ts` has three projects: `unit`, `browser`, `scripts`
- `pnpm test`, `pnpm test:scripts`, `pnpm build`, `pnpm build:markdown` all exit 0
- `scripts/markdown-export/index.ts` placeholder exists and prints a banner
- No `.gitkeep` pollution — only directories with real content exist
- No `postinstall` / `prepare` hooks added
- Scripts scaffold contains zero forbidden API uses (grep-verified)
</success_criteria>

<output>
After completion, create `.planning/phases/038-ir-markdown-primitives-scaffold/038-01-SUMMARY.md` per the summary template.
</output>
