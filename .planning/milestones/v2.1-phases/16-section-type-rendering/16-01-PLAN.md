---
phase: 16-section-type-rendering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/exhibit/InvestigationReportLayout.vue
  - src/components/exhibit/EngineeringBriefLayout.vue
  - src/components/exhibit/InvestigationReportLayout.test.ts
  - src/components/exhibit/EngineeringBriefLayout.test.ts
autonomous: false
requirements: [SECT-01, SECT-02, SECT-03, SECT-04]

must_haves:
  truths:
    - "Timeline sections render entries showing dates and descriptions"
    - "Metadata sections render key-value items in a card grid layout"
    - "The flow section renders steps with labels and details"
    - "Sections with empty/missing content arrays produce no DOM output"
  artifacts:
    - path: "src/components/exhibit/InvestigationReportLayout.vue"
      provides: "IR layout with timeline, metadata, flow rendering + empty section guard"
      contains: "sectionHasContent"
    - path: "src/components/exhibit/EngineeringBriefLayout.vue"
      provides: "EB layout with timeline, metadata, flow rendering + empty section guard"
      contains: "sectionHasContent"
    - path: "src/components/exhibit/InvestigationReportLayout.test.ts"
      provides: "Unit tests for timeline, metadata, and empty section suppression on IR layout"
    - path: "src/components/exhibit/EngineeringBriefLayout.test.ts"
      provides: "Unit tests for timeline, metadata, flow, and empty section suppression on EB layout"
  key_links:
    - from: "src/components/exhibit/InvestigationReportLayout.vue"
      to: "src/assets/css/main.css"
      via: "CSS class names: exhibit-timeline, timeline-entry, timeline-marker, timeline-date, timeline-heading, timeline-body, timeline-quote, exhibit-metadata, metadata-card, flow-step, flow-node, flow-label, flow-detail, flow-arrow"
      pattern: "class=\"exhibit-timeline\""
    - from: "src/components/exhibit/InvestigationReportLayout.vue"
      to: "src/data/exhibits.ts"
      via: "ExhibitSection type union with entries, items, steps arrays"
      pattern: "section\\.entries"
---

<objective>
Add rendering for timeline, metadata, and flow section types in both exhibit detail layout components, and suppress empty sections from producing DOM output.

Purpose: Fix the rendering gap where 22 sections (6 timeline, 15 metadata, 1 flow) across all 15 exhibits silently produce empty divs with orphaned headings instead of rendering their content. The CSS design system already contains all needed styles -- this is a template-only fix.

Output: Updated InvestigationReportLayout.vue and EngineeringBriefLayout.vue with complete section type rendering, plus unit tests covering all four requirements.
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-section-type-rendering/16-RESEARCH.md

<interfaces>
<!-- Data types the executor needs -->

From src/data/exhibits.ts:
```typescript
export interface ExhibitFlowStep {
  label: string
  detail: string
}

export interface ExhibitTimelineEntry {
  date: string
  heading: string
  body: string
  quote?: string
  quoteAttribution?: string
}

export interface ExhibitMetadataItem {
  label: string
  value: string
}

export interface ExhibitSection {
  heading?: string
  type: 'text' | 'table' | 'flow' | 'timeline' | 'metadata'
  body?: string
  columns?: string[]
  rows?: string[][]
  steps?: ExhibitFlowStep[]
  entries?: ExhibitTimelineEntry[]
  items?: ExhibitMetadataItem[]
}
```

CSS class inventory (from src/assets/css/main.css, all scoped under `.page-exhibit-detail`):
- Timeline: `.exhibit-timeline`, `.timeline-entry`, `.timeline-marker`, `.timeline-date`, `.timeline-heading`, `.timeline-body`, `.timeline-quote`
- Metadata: `.exhibit-metadata` (on `<dl>`), `.metadata-card` (on `<div>` wrapping `<dt>`/`<dd>`)
- Flow: `.flow-step`, `.flow-node`, `.flow-label`, `.flow-detail`, `.flow-arrow`

Test fixtures available:
- Exhibit A (`/exhibits/exhibit-a`): Engineering Brief with 4 timeline sections, 1 metadata section. Used in EB test.
- Exhibit J (`/exhibits/exhibit-j`): Investigation Report with 1 timeline section, 1 metadata section. Used in IR test.
- Exhibit L (`/exhibits/exhibit-l`): Engineering Brief with the single flow section + metadata.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add unit tests for timeline, metadata, flow rendering and empty section suppression</name>
  <files>src/components/exhibit/InvestigationReportLayout.test.ts, src/components/exhibit/EngineeringBriefLayout.test.ts</files>
  <read_first>
    - src/components/exhibit/InvestigationReportLayout.test.ts
    - src/components/exhibit/EngineeringBriefLayout.test.ts
    - src/components/exhibit/InvestigationReportLayout.vue
    - src/components/exhibit/EngineeringBriefLayout.vue
    - src/data/exhibits.ts (lines 1-62 for types, lines 1065-1170 for exhibit-j fixture, lines 1280-1360 for exhibit-l fixture)
  </read_first>
  <behavior>
    SECT-01 Timeline tests:
    - IR test (exhibit-j fixture): `.exhibit-timeline` element exists, `.timeline-entry` count >= 1, text contains timeline entry date "Initial Report", text contains timeline entry heading "Anomalous Completion Rates Flagged"
    - EB test (exhibit-a fixture): `.exhibit-timeline` element exists, `.timeline-entry` count >= 1, text contains "September 5, 2017", text contains "SVP Directs Engagement"
    - Timeline quote test (exhibit-a has quotes on entries): `.timeline-quote` element exists

    SECT-02 Metadata tests:
    - IR test (exhibit-j has metadata): `.exhibit-metadata` element exists, `.metadata-card` count >= 1, text contains "Industry", text contains "Automotive / Dealership Training"
    - EB test (exhibit-a has metadata): `.exhibit-metadata` element exists, text contains "Industry", text contains metadata value

    SECT-03 Flow test:
    - EB test needs exhibit-l fixture: `.flow-step` element exists, `.flow-node` count >= 1, text contains "PowerPoint", text contains "Vendor decks", text contains "Azure DevOps"

    SECT-04 Empty section suppression tests:
    - Both test files: Create a synthetic exhibit with a section that has type 'timeline' but empty entries array (or undefined). Verify no `.exhibit-section` div is rendered for it. Also test with type 'metadata' and empty items. The heading text should NOT appear in the rendered output.
  </behavior>
  <action>
    Add the following tests to InvestigationReportLayout.test.ts (using existing `irFixture` which is exhibit-j):

    1. "renders timeline entries with dates and descriptions" -- mount with irFixture, assert `.exhibit-timeline` exists, `.timeline-entry` elements exist, text contains "Initial Report" and "Anomalous Completion Rates Flagged"
    2. "renders metadata section with key-value cards" -- mount with irFixture, assert `.exhibit-metadata` exists, `.metadata-card` elements have length >= 1, text contains "Industry" and "Automotive / Dealership Training"
    3. "does not render section with empty timeline entries" -- create synthetic exhibit with `exhibitType: 'investigation-report'`, sections array containing one section: `{ heading: 'Empty Timeline', type: 'timeline', entries: [] }`. Mount, assert text does NOT contain "Empty Timeline", assert `.exhibit-timeline` does not exist, assert `.exhibit-section` count is 0
    4. "does not render section with empty metadata items" -- same pattern with `{ heading: 'Empty Meta', type: 'metadata', items: [] }`. Assert text does NOT contain "Empty Meta"

    Add the following tests to EngineeringBriefLayout.test.ts (using existing `ebFixture` which is exhibit-a):

    1. "renders timeline entries with dates and descriptions" -- mount with ebFixture, assert `.exhibit-timeline` exists, `.timeline-entry` elements exist, text contains "September 5, 2017" and "SVP Directs Engagement"
    2. "renders timeline entry quotes" -- mount with ebFixture, assert `.timeline-quote` exists (exhibit-a timeline entries have quotes)
    3. "renders metadata section with key-value cards" -- mount with ebFixture, assert `.exhibit-metadata` exists, `.metadata-card` elements >= 1
    4. "renders flow section with steps" -- add a second fixture: `const flowFixture = exhibits.find(e => e.exhibitLink === '/exhibits/exhibit-l')!` at top of file. Mount with flowFixture, assert `.flow-step` exists, `.flow-node` elements >= 1, text contains "PowerPoint" and "Azure DevOps"
    5. "renders flow section introductory body text" -- mount flowFixture, assert text contains "Requirements degraded through multiple format conversions"
    6. "does not render section with empty entries/items/steps" -- synthetic exhibit with `exhibitType: 'engineering-brief'`, sections containing `{ heading: 'Ghost Section', type: 'timeline', entries: [] }`. Assert "Ghost Section" NOT in text, `.exhibit-section` count is 0

    For all synthetic exhibits, use this minimal shape:
    ```typescript
    const emptyExhibit: Exhibit = {
      label: 'Test', client: 'Test', date: '2025', title: 'Test',
      exhibitType: 'investigation-report', // or 'engineering-brief'
      impactTags: ['test'], exhibitLink: '/exhibits/test',
      sections: [{ heading: 'Ghost Section', type: 'timeline', entries: [] }],
    }
    ```

    Run tests -- they MUST FAIL (RED phase). The layout components do not yet render timeline/metadata/flow or suppress empty sections.
  </action>
  <verify>
    <automated>npx vitest run src/components/exhibit/InvestigationReportLayout.test.ts src/components/exhibit/EngineeringBriefLayout.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - InvestigationReportLayout.test.ts contains `renders timeline entries with dates and descriptions`
    - InvestigationReportLayout.test.ts contains `renders metadata section with key-value cards`
    - InvestigationReportLayout.test.ts contains `does not render section with empty`
    - InvestigationReportLayout.test.ts contains `exhibit-timeline`
    - InvestigationReportLayout.test.ts contains `exhibit-metadata`
    - InvestigationReportLayout.test.ts contains `metadata-card`
    - InvestigationReportLayout.test.ts contains `Anomalous Completion Rates Flagged`
    - InvestigationReportLayout.test.ts contains `Automotive / Dealership Training`
    - EngineeringBriefLayout.test.ts contains `renders timeline entries with dates and descriptions`
    - EngineeringBriefLayout.test.ts contains `renders metadata section with key-value cards`
    - EngineeringBriefLayout.test.ts contains `renders flow section with steps`
    - EngineeringBriefLayout.test.ts contains `renders flow section introductory body text`
    - EngineeringBriefLayout.test.ts contains `does not render section with empty`
    - EngineeringBriefLayout.test.ts contains `flow-step`
    - EngineeringBriefLayout.test.ts contains `flow-node`
    - EngineeringBriefLayout.test.ts contains `PowerPoint`
    - EngineeringBriefLayout.test.ts contains `Azure DevOps`
    - EngineeringBriefLayout.test.ts contains `exhibit-l`
    - New tests fail (RED) because layout components don't render these section types yet
  </acceptance_criteria>
  <done>Both test files contain new tests for SECT-01 through SECT-04. Tests fail because template rendering is not yet implemented. Committed as RED phase.</done>
</task>

<task type="auto">
  <name>Task 2: Add timeline, metadata, flow rendering and empty section guard to both layout components</name>
  <files>src/components/exhibit/InvestigationReportLayout.vue, src/components/exhibit/EngineeringBriefLayout.vue</files>
  <read_first>
    - src/components/exhibit/InvestigationReportLayout.vue
    - src/components/exhibit/EngineeringBriefLayout.vue
    - src/data/exhibits.ts (lines 1-62 for ExhibitSection type)
    - src/assets/css/main.css (lines 3818-3912 for timeline CSS, 3920-4006 for flow CSS, 4008-4042 for metadata CSS)
    - .planning/phases/16-section-type-rendering/16-RESEARCH.md
  </read_first>
  <action>
    Make identical changes to BOTH InvestigationReportLayout.vue and EngineeringBriefLayout.vue:

    **Step 1: Add sectionHasContent helper to script block.**
    Change the import line from:
    ```typescript
    import type { Exhibit } from '@/data/exhibits'
    ```
    To:
    ```typescript
    import type { Exhibit, ExhibitSection } from '@/data/exhibits'
    ```

    After `defineProps<{ exhibit: Exhibit }>()`, add:

    ```typescript
    function sectionHasContent(section: ExhibitSection): boolean {
      switch (section.type) {
        case 'text': return !!section.body
        case 'table': return !!(section.rows?.length)
        case 'timeline': return !!(section.entries?.length)
        case 'metadata': return !!(section.items?.length)
        case 'flow': return !!(section.steps?.length)
        default: return false
      }
    }
    ```

    **Step 2: Update the section loop to use sectionHasContent guard.**
    Change the outer `v-for` from:
    ```html
    <div v-for="(section, i) in exhibit.sections" :key="i" class="exhibit-section">
    ```
    To:
    ```html
    <template v-for="(section, i) in exhibit.sections" :key="i">
      <div v-if="sectionHasContent(section)" class="exhibit-section">
        <!-- existing + new content -->
      </div>
    </template>
    ```

    **Step 3: Change the table v-if to v-else-if for a clean conditional chain.**
    - `<p v-if="section.type === 'text' && section.body">` stays as v-if (first branch)
    - `<table v-if="section.type === 'table' && section.rows?.length">` becomes `<table v-else-if="section.type === 'table' && section.rows?.length">`

    **Step 4: Add three new v-else-if rendering branches AFTER the table block.**
    Inside the `.exhibit-section` div, after the closing `</table>` tag, add these three blocks in order:

    Timeline branch:
    ```html
    <div v-else-if="section.type === 'timeline'" class="exhibit-timeline">
      <p v-if="section.body">{{ section.body }}</p>
      <div v-for="(entry, ei) in section.entries" :key="ei" class="timeline-entry">
        <span class="timeline-marker"></span>
        <span class="timeline-date">{{ entry.date }}</span>
        <h3 v-if="entry.heading" class="timeline-heading">{{ entry.heading }}</h3>
        <p v-if="entry.body" class="timeline-body">{{ entry.body }}</p>
        <blockquote v-if="entry.quote" class="timeline-quote">
          <p>{{ entry.quote }}</p>
          <footer v-if="entry.quoteAttribution">{{ entry.quoteAttribution }}</footer>
        </blockquote>
      </div>
    </div>
    ```

    Metadata branch:
    ```html
    <dl v-else-if="section.type === 'metadata'" class="exhibit-metadata">
      <div v-for="(item, mi) in section.items" :key="mi" class="metadata-card">
        <dt>{{ item.label }}</dt>
        <dd>{{ item.value }}</dd>
      </div>
    </dl>
    ```

    Flow branch:
    ```html
    <div v-else-if="section.type === 'flow'">
      <p v-if="section.body">{{ section.body }}</p>
      <div class="exhibit-flow" style="display:flex;flex-wrap:wrap;align-items:center;">
        <template v-for="(step, si) in section.steps" :key="si">
          <div v-if="si > 0" class="flow-arrow"></div>
          <div class="flow-step">
            <div class="flow-node">
              <span class="flow-label">{{ step.label }}</span>
              <span class="flow-detail">{{ step.detail }}</span>
            </div>
          </div>
        </template>
      </div>
    </div>
    ```

    **IMPORTANT:** Both layout files get IDENTICAL changes. The only difference between the two files is the badge text/class in the header section (line 21-22), which is NOT being changed.

    After changes, run `npx vitest run` -- all new tests from Task 1 plus all existing tests must pass (GREEN phase).
  </action>
  <verify>
    <automated>npx vitest run src/components/exhibit/InvestigationReportLayout.test.ts src/components/exhibit/EngineeringBriefLayout.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - InvestigationReportLayout.vue contains `function sectionHasContent`
    - InvestigationReportLayout.vue contains `case 'timeline': return !!(section.entries?.length)`
    - InvestigationReportLayout.vue contains `case 'metadata': return !!(section.items?.length)`
    - InvestigationReportLayout.vue contains `case 'flow': return !!(section.steps?.length)`
    - InvestigationReportLayout.vue contains `v-if="sectionHasContent(section)"`
    - InvestigationReportLayout.vue contains `class="exhibit-timeline"`
    - InvestigationReportLayout.vue contains `class="timeline-entry"`
    - InvestigationReportLayout.vue contains `class="timeline-marker"`
    - InvestigationReportLayout.vue contains `class="timeline-date"`
    - InvestigationReportLayout.vue contains `class="timeline-heading"`
    - InvestigationReportLayout.vue contains `class="timeline-body"`
    - InvestigationReportLayout.vue contains `class="timeline-quote"`
    - InvestigationReportLayout.vue contains `class="exhibit-metadata"`
    - InvestigationReportLayout.vue contains `class="metadata-card"`
    - InvestigationReportLayout.vue contains `class="flow-step"`
    - InvestigationReportLayout.vue contains `class="flow-node"`
    - InvestigationReportLayout.vue contains `class="flow-label"`
    - InvestigationReportLayout.vue contains `class="flow-detail"`
    - InvestigationReportLayout.vue contains `class="flow-arrow"`
    - EngineeringBriefLayout.vue contains `function sectionHasContent`
    - EngineeringBriefLayout.vue contains `class="exhibit-timeline"`
    - EngineeringBriefLayout.vue contains `class="exhibit-metadata"`
    - EngineeringBriefLayout.vue contains `class="flow-step"`
    - EngineeringBriefLayout.vue contains `v-if="sectionHasContent(section)"`
    - `npx vitest run` exits 0 with all tests passing
    - `npx vite build` exits 0 (clean production build)
  </acceptance_criteria>
  <done>Both layout components render all 5 section types (text, table, timeline, metadata, flow). Empty sections produce no DOM. All unit tests pass. Production build clean.</done>
</task>

<task type="checkpoint:human-verify" gate="informational">
  <name>Task 3: Visual verification of rendered section types</name>
  <files>none</files>
  <action>Present the verification checklist to the user for visual confirmation of CSS styling on the rendered section types.</action>
  <what-built>Timeline, metadata, and flow sections now render in exhibit detail pages. Empty sections are suppressed.</what-built>
  <how-to-verify>
    1. Run `npx vite dev` and open browser
    2. Navigate to /exhibits/exhibit-a (Engineering Brief with 4 timelines + metadata)
       - Verify timeline sections show vertical line with dot markers, dates in mono font, entry headings, body text, and blockquote with attribution
       - Scroll to bottom metadata section -- verify card grid layout with label/value pairs
    3. Navigate to /exhibits/exhibit-j (Investigation Report with 1 timeline + metadata)
       - Verify "Sequence of Events" timeline renders with entries
       - Verify "Engagement Metadata" card grid renders
    4. Navigate to /exhibits/exhibit-l (Engineering Brief with flow section)
       - Verify "The Requirements Degradation Chain" shows horizontal arrow chain: PowerPoint -> Word -> Excel -> Azure DevOps
       - Verify introductory body paragraph appears above the flow
       - On mobile viewport (resize to <768px), verify arrows point downward and steps stack vertically
    5. Spot-check any other exhibit -- verify no orphaned headings with empty content beneath them
  </how-to-verify>
  <verify>Visual inspection by user</verify>
  <done>User confirms timeline, metadata, and flow sections render with correct CSS styling. No orphaned headings visible.</done>
  <resume-signal>Type "approved" or describe visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run` -- all tests pass (existing + new)
- `npx vite build` -- clean production build with no errors
- Timeline sections (6 across exhibits A, E, J) render dates, headings, body text, and optional quotes
- Metadata sections (15, one per exhibit) render label/value card grids
- Flow section (1, exhibit L) renders horizontal step chain with arrows
- Synthetic exhibits with empty content arrays produce zero `.exhibit-section` DOM nodes
</verification>

<success_criteria>
- SECT-01: Exhibit J and Exhibit A timeline sections render with `.exhibit-timeline`, `.timeline-entry`, dates, headings, and body text visible
- SECT-02: All exhibits' metadata sections render with `.exhibit-metadata` and `.metadata-card` elements containing label/value pairs
- SECT-03: Exhibit L flow section renders with `.flow-step`, `.flow-node` elements showing PowerPoint through Azure DevOps step chain
- SECT-04: A section with type 'timeline' and empty entries array produces no `.exhibit-section` div and no heading text in DOM
- All 54+ existing tests continue to pass
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/16-section-type-rendering/16-01-SUMMARY.md`
</output>
