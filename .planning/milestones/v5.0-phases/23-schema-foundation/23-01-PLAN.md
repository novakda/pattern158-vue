---
phase: 23-schema-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/exhibit.ts
  - src/data/json/exhibits.json
  - src/components/exhibit/EngineeringBriefLayout.vue
  - src/components/exhibit/InvestigationReportLayout.vue
  - src/data/exhibits.test.ts
autonomous: true
requirements: [SCHM-01, SCHM-02]

must_haves:
  truths:
    - "FindingEntry type has all unified fields: finding, description?, severity?, resolution?, outcome?, category?"
    - "FindingEntry type no longer has a background field"
    - "Exhibit A findings in exhibits.json use description instead of background"
    - "Layout components detect the description+resolution variant instead of background"
    - "All existing tests pass and vite build succeeds"
  artifacts:
    - path: "src/types/exhibit.ts"
      provides: "Updated FindingEntry with outcome and category, no background"
      contains: "outcome?"
    - path: "src/data/json/exhibits.json"
      provides: "Exhibit A findings with description field"
      contains: "description"
    - path: "src/components/exhibit/EngineeringBriefLayout.vue"
      provides: "Updated variant detection using resolution instead of background"
      contains: "resolution"
    - path: "src/components/exhibit/InvestigationReportLayout.vue"
      provides: "Updated variant detection using resolution instead of background"
      contains: "resolution"
    - path: "src/data/exhibits.test.ts"
      provides: "Updated tests checking description instead of background"
      contains: "description"
  key_links:
    - from: "src/types/exhibit.ts"
      to: "src/data/json/exhibits.json"
      via: "FindingEntry type shapes JSON data"
      pattern: "FindingEntry"
    - from: "src/components/exhibit/EngineeringBriefLayout.vue"
      to: "src/types/exhibit.ts"
      via: "v-if checks on FindingEntry fields"
      pattern: "findings\\[0\\]"
---

<objective>
Update FindingEntry type with the full unified field set (adding outcome and category, removing background) and normalize Exhibit A's data to use description instead of background. Update layout variant detection and tests to match.

Purpose: Establish the schema foundation that later phases (24-26) build on for backfill, enrichment, and rendering.
Output: Updated type, data, layouts, and tests -- no visual change to rendered pages.
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-schema-foundation/23-CONTEXT.md

<interfaces>
<!-- Current FindingEntry type that will be modified -->
From src/types/exhibit.ts (lines 43-49):
```typescript
export interface FindingEntry {
  finding: string
  description?: string
  background?: string
  resolution?: string
  severity?: string
}
```

<!-- Layout variant detection pattern (identical in both layouts, lines 105-122) -->
From src/components/exhibit/EngineeringBriefLayout.vue:
```vue
<template v-if="exhibit.findings[0].background !== undefined">
  <th>Background</th>
  <th>Resolution</th>
</template>
<template v-else-if="exhibit.findings[0].severity !== undefined">
  <th>Severity</th>
  <th>Description</th>
</template>
<template v-else>
  <th>Description</th>
</template>
```

<!-- Row-level detection (identical in both layouts, lines 121-122) -->
```vue
<template v-if="f.background !== undefined">
  <td data-label="Background">{{ f.background }}</td>
  <td data-label="Resolution">{{ f.resolution }}</td>
</template>
```

<!-- Test assertions that reference background (lines 154-172) -->
From src/data/exhibits.test.ts:
```typescript
it('Exhibit A findings has background/resolution variant', () => {
  expect(exhibitA?.findings?.[0].background).toBeTruthy()
  expect(exhibitA?.findings?.[0].resolution).toBeTruthy()
})
it('Exhibit E findings has description variant', () => {
  expect(exhibitE?.findings?.[0].background).toBeUndefined()
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update FindingEntry type (SCHM-01)</name>
  <files>src/types/exhibit.ts</files>
  <read_first>
    - src/types/exhibit.ts
  </read_first>
  <action>
Update the FindingEntry interface at lines 43-49 of src/types/exhibit.ts:

1. Remove the `background?: string` field (line 46)
2. Add `outcome?: string` after the `resolution` field
3. Add `category?: string` after the `outcome` field

The resulting interface must be:
```typescript
export interface FindingEntry {
  finding: string
  description?: string
  resolution?: string
  outcome?: string
  category?: string
  severity?: string
}
```

Field order: finding (required), then optional fields description, resolution, outcome, category, severity. This groups the new enrichment fields (outcome, category) together with the existing optional fields.
  </action>
  <verify>
    <automated>cd /home/xhiris/projects/pattern158-vue && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "outcome?: string" src/types/exhibit.ts returns a match
    - grep "category?: string" src/types/exhibit.ts returns a match
    - grep "background" src/types/exhibit.ts returns NO match
    - FindingEntry interface has exactly 6 fields: finding, description?, resolution?, outcome?, category?, severity?
  </acceptance_criteria>
  <done>FindingEntry type has the full unified field set with outcome and category added, background removed. TypeScript compilation succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Rename background to description in Exhibit A data and update consumers (SCHM-02)</name>
  <files>src/data/json/exhibits.json, src/components/exhibit/EngineeringBriefLayout.vue, src/components/exhibit/InvestigationReportLayout.vue, src/data/exhibits.test.ts</files>
  <read_first>
    - src/data/json/exhibits.json (lines 314-340 for Exhibit A findings)
    - src/components/exhibit/EngineeringBriefLayout.vue (lines 100-135 for findings rendering)
    - src/components/exhibit/InvestigationReportLayout.vue (lines 100-135 for findings rendering)
    - src/data/exhibits.test.ts (lines 139-184 for findings tests)
  </read_first>
  <action>
**Step 1: Rename background to description in exhibits.json (Exhibit A only)**

In src/data/json/exhibits.json, find Exhibit A's findings array (starts around line 314). For each of the 5 finding entries, rename the `"background"` key to `"description"`. The values stay identical -- only the key name changes.

There are exactly 5 occurrences of `"background":` in the findings array at lines 317, 322, 327, 332, 337. Each becomes `"description":`.

IMPORTANT: Do NOT touch the "Background" section heading in Exhibit A's sections array (line 34, `"heading": "Background"`) -- that is a narrative section heading, not a findings field.

**Step 2: Update layout variant detection in both layout components**

In BOTH src/components/exhibit/EngineeringBriefLayout.vue AND src/components/exhibit/InvestigationReportLayout.vue, update the findings variant detection logic. The changes are identical in both files:

Line 105 (header detection): Change
  `v-if="exhibit.findings[0].background !== undefined"`
to
  `v-if="exhibit.findings[0].resolution !== undefined && exhibit.findings[0].severity === undefined"`

Line 106: Change `<th>Background</th>` to `<th>Description</th>`

Line 121 (row detection): Change
  `v-if="f.background !== undefined"`
to
  `v-if="f.resolution !== undefined && f.severity === undefined"`

Line 122: Change
  `<td data-label="Background">{{ f.background }}</td>`
to
  `<td data-label="Description">{{ f.description }}</td>`

The new detection logic: if `resolution` is present BUT `severity` is NOT present, render Description + Resolution columns. This correctly identifies the Exhibit A variant (description + resolution) without relying on the removed `background` field.

**Step 3: Update tests in exhibits.test.ts**

In src/data/exhibits.test.ts, update the findings test block (lines 154-173):

Line 154: Change test name from `'Exhibit A findings has background/resolution variant'` to `'Exhibit A findings has description/resolution variant'`

Line 157: Change `expect(exhibitA?.findings?.[0].background).toBeTruthy()` to `expect(exhibitA?.findings?.[0].description).toBeTruthy()`

Line 172: Change `expect(exhibitE?.findings?.[0].background).toBeUndefined()` to remove this line entirely (background no longer exists in the type, so checking for it is meaningless). Replace with a positive assertion: `expect(exhibitE?.findings?.[0].description).toBeTruthy()` which confirms Exhibit E has the description-only variant.
  </action>
  <verify>
    <automated>cd /home/xhiris/projects/pattern158-vue && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "background" src/data/json/exhibits.json returns NO matches in findings arrays (only the narrative section heading "Background" remains)
    - grep -c '"background"' src/data/json/exhibits.json returns exactly 0 (the section heading uses "heading": "Background" which has the value in quotes but the KEY is "heading")
    - grep "background" src/components/exhibit/EngineeringBriefLayout.vue returns NO matches
    - grep "background" src/components/exhibit/InvestigationReportLayout.vue returns NO matches
    - grep "background" src/data/exhibits.test.ts returns NO matches
    - grep "description/resolution variant" src/data/exhibits.test.ts returns a match
    - npx vitest run exits with code 0 (all tests pass)
    - npx vite build exits with code 0 (clean production build)
  </acceptance_criteria>
  <done>Exhibit A findings use description instead of background in JSON data. Both layout components detect the description+resolution variant without referencing background. All tests updated and passing. Build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This phase modifies type definitions and static JSON data -- no user input, no API calls, no runtime data flows.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | I (Information Disclosure) | exhibits.json | accept | Static JSON bundled at build time, no secrets, public portfolio data |
| T-23-02 | T (Tampering) | FindingEntry type | accept | Type changes validated by TypeScript compiler and unit tests |
</threat_model>

<verification>
1. `npx tsc --noEmit` -- TypeScript compilation clean (no errors)
2. `npx vitest run` -- all tests pass
3. `npx vite build` -- production build succeeds
4. grep confirms no remaining references to `background` in findings-related code (type, JSON data fields, layout templates, tests)
</verification>

<success_criteria>
- FindingEntry type has 6 fields: finding, description?, resolution?, outcome?, category?, severity? (no background)
- Exhibit A's 5 findings entries use "description" key in exhibits.json
- Both layout components use resolution-presence detection instead of background-presence
- All 64+ tests pass, clean build
</success_criteria>

<output>
After completion, create `.planning/phases/23-schema-foundation/23-01-SUMMARY.md`
</output>
