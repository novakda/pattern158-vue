# Phase 58 Verification

**Phase:** 58 — Tzk-Style Structure
**Executed:** 2026-04-22
**Status:** PASSED

## Requirement Coverage

| REQ-ID | Requirement | Status | Evidence |
| ------ | ----------- | ------ | -------- |
| TZK-01 | Tiddlers tagged `public` by default; `private` tag available | PASSED | `scripts/tiddlywiki/generate.ts:withPublicTag` applied to full composed list; 367/367 generated tiddlers emit with `public` as first tag |
| TZK-02 | `pnpm tiddlywiki:build-public` builds deploy-ready HTML with filter `[!tag[private]]` | PASSED | `tiddlywiki/output/index.html` emitted (~2.8MB); filter verified via canary tiddler (sentinel absent from public, present in all) |
| TZK-03 | Git-backed workflow with regenerable `tiddlers/*.tid` source-of-truth | PASSED | `tiddlywiki/tiddlers/` canonical source committed (367 tiddlers tracked); `pnpm tiddlywiki:generate` deterministic (re-runs produce byte-identical output); workflow documented in `tiddlywiki/README.md` |
| TZK-04 | Deploy-ready public wiki with Pattern 158 theme, no private content | PASSED | `tiddlywiki/output/index.html` built; Phase 57 theme tiddlers (`$__plugins_pattern158_*`) tagged `public` and included; verified via SiteSubtitle string "Evidence-Based Portfolio" appears 5x in output |
| TZK-05 | Tzk directory layout: `tiddlers/`, `output/`, `config/`, `build/`, `README.md` | PASSED | All directories exist; `README.md` seeded with workflow docs; `output/` gitignored; `config/` + `build/` scaffolded with `.gitkeep` |

## Smoke Gate Results

### `pnpm build` — PASSED

```
✓ built in 739ms
> build:markdown
<!-- auto-generated by scripts/markdown-export — do not edit by hand -->
```

Exit code: 0. vue-tsc + vite build + markdown-export all clean. Pre-existing
TypeScript errors in previously-excluded `html-to-wikitext.ts` resolved via a
single DOM cast at the producer boundary (same pattern as
`extractors/types.ts:parseHtml`).

### `pnpm test:scripts --run` — PASSED

```
 Test Files  44 passed (44)
      Tests  593 passed (593)
   Duration  1.06s
```

Exit code: 0. Includes 9 new tests for `withPublicTag` transform:

- Prepends `public` when absent
- Prepends `public` to empty tag list
- No-op when `public` already present (returns same reference)
- No-op when `private` present (does NOT add `public`)
- No-op when both `public` and `private` present
- Does not mutate input tiddler
- Idempotent — `withPublicTag(withPublicTag(t)) === withPublicTag(t)`
- Preserves title, type, fields, text unchanged
- Works on frozen tag array (readonly contract)

### `pnpm tiddlywiki:generate` — PASSED

```
[tiddlywiki:generate] Wrote 367 tiddlers → tiddlywiki/tiddlers
                      3 meta, 5 pages, 16 exhibits, 25 FAQ
```

Exit code: 0. Every generated tiddler carries `public` as its first tag
(verified by spot-check of `$__DefaultTiddlers.tid`,
`$__SiteSubtitle.tid`, `$__SiteTitle.tid`, and `2 Additional Testers.tid`
diffs: each gained `tags: public` on regeneration).

### `pnpm tiddlywiki:build-public` — PASSED

```
syncer-server-filesystem: Dispatching 'save' task: $:/StoryList
```

Exit code: 0. Produced `tiddlywiki/output/index.html` (2,810,984 bytes).

**Filter verification:** A canary tiddler `PRIVATE_SMOKE_CANARY` tagged
`private` with body `UNIQUESENTINEL_PRIVATE_ONLY_SHOULD_NOT_APPEAR_IN_PUBLIC_BUILD_XYZ123`
was dropped into `tiddlywiki/tiddlers/` and rebuilt. Results:

- `grep -c UNIQUESENTINEL... output/index.html` → 0 ✅ (filtered out)
- `grep -c UNIQUESENTINEL... output/all.html` → 1 ✅ (present)
- `grep -c PRIVATE_SMOKE_CANARY output/index.html` → 0 ✅
- `grep -c PRIVATE_SMOKE_CANARY output/all.html` → 2 ✅

Canary removed before final commit; the test proved the
`publishFilter=+[!tag[private]]` pathway actually excludes `private`-tagged
tiddlers from the single-file HTML export.

### `pnpm tiddlywiki:build-all` — PASSED

```
syncer-server-filesystem: Dispatching 'save' task: $:/StoryList
```

Exit code: 0. Produced `tiddlywiki/output/all.html` (2,810,984 bytes).

### Output file sizes

| File | Bytes |
| ---- | ----- |
| `tiddlywiki/output/index.html` | 2,810,984 |
| `tiddlywiki/output/all.html` | 2,810,984 |

Identical byte counts expected — no `private`-tagged tiddlers currently
exist in the corpus, so the filter is a no-op in practice. Content
presence verified: `Evidence-Based Portfolio` subtitle appears 5x in each
build; `Exhibit A` appears in both.

## Commits

| # | Hash | Subject |
| - | ---- | ------- |
| 1 | `6f75aa1` | feat(58-01): add withPublicTag transform in generate.ts (TZK-01) |
| 2 | `0b0184f` | feat(58-02): extend tiddlywiki.info build targets + fix subtitle (TZK-02) |
| 3 | `f5c0a48` | chore(58-03): add tiddlywiki devDep + build-public/build-all scripts (TZK-02,TZK-04) |
| 4 | `d3d7f23` | docs(58-04): add tzk-style directory scaffolding + workflow README (TZK-05) |
| 5 | `6d00f72` | fix(58-05): filter + path + tsconfig fixes for public/all build targets |
| 6 | `bbe212a` | chore(58-06): regenerate tiddlers with public tag + updated tiddlywiki.info |

## Conclusion

All 5 Phase 58 requirements (TZK-01 through TZK-05) PASSED. Build and
test smoke gates green. Public/private tag pipeline proven correct via
canary-tiddler filter test. Directory structure matches the tzk-inspired
layout. Phase 57 `siteMetaTiddlers` subtitle follow-up resolved — source
now aligns with the Phase 57 override, eliminating the regenerate-drift
risk flagged in `57-SUMMARY.md` Deferred Issue #1.
