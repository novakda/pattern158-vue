---
phase: 53-dom-extraction
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - scripts/tiddlywiki/extractors/personnel.ts
  - scripts/tiddlywiki/extractors/personnel.test.ts
autonomous: true
requirements: [EXTR-03]
tags: [extractor, personnel]

must_haves:
  truths:
    - "emitPersonnel parses .personnel-table on an exhibit detail page and returns PersonnelEntry[]"
    - "Each tbody <tr> becomes one PersonnelEntry"
    - "name from .personnel-name span (may be absent — anonymized entries)"
    - "title from .personnel-title span"
    - "organization from td[data-label='organization'] textContent"
    - "role from td[data-label='role'] textContent (absent → empty string)"
    - "entryType derived from <tr> class: personnel-entry-anonymized → 'anonymized', personnel-entry-group → 'group', default → 'individual'"
    - "sourceExhibitLabel caller-supplied via opts.sourceExhibitLabel"
    - "Missing .personnel-table yields empty array (no throw)"
    - "Idempotent"
  artifacts:
    - path: "scripts/tiddlywiki/extractors/personnel.ts"
      provides: "emitPersonnel(html, opts?) → PersonnelEntry[]"
      exports:
        - "emitPersonnel"
    - path: "scripts/tiddlywiki/extractors/personnel.test.ts"
      provides: "3+ scenarios + idempotency"
      contains: "describe"
  key_links:
    - from: "scripts/tiddlywiki/extractors/personnel.ts"
      to: "scripts/tiddlywiki/extractors/types.ts"
      via: "import { parseHtml, PersonnelEntry, PersonnelEntryType }"
      pattern: "from './types.ts'"
---

<objective>
EXTR-03: parse the personnel table on an exhibit detail page into `PersonnelEntry[]` with name, title, role, organization, entryType + source-exhibit back-reference.

Purpose: Phase 54 ATOM-01 depends on this output to build per-person atomic tiddlers (individuals, groups, anonymized entries each get distinct tag taxonomies).

Output: `personnel.ts` + `personnel.test.ts`.
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-dom-extraction/53-CONTEXT.md
@scripts/tiddlywiki/extractors/types.ts
@static-site/exhibits/exhibit-a.html

<interfaces>
From types.ts:
```typescript
export type PersonnelEntryType = 'individual' | 'group' | 'anonymized'
export interface PersonnelEntry {
  readonly name: string
  readonly title: string
  readonly organization: string
  readonly role: string
  readonly entryType: PersonnelEntryType
  readonly sourceExhibitLabel: string
}
```

DOM shape (from static-site/exhibits/exhibit-a.html):
```html
<table class="exhibit-table personnel-table">
  <thead>
    <tr><th>Name / Title</th><th>Organization</th><th>Role on Project</th></tr>
  </thead>
  <tbody>
    <!-- individual with name + title -->
    <tr class="">
      <td data-label="person">
        <span class="personnel-name">Dan Novak</span>
        <span class="personnel-title">Solution Architect</span>
      </td>
      <td data-label="organization">GP Strategies</td>
      <td data-label="role">Lead Investigator</td>
    </tr>
    <!-- anonymized (no .personnel-name) -->
    <tr class="personnel-entry-anonymized">
      <td data-label="person">
        <!---->
        <span class="personnel-title">Chief of Learning Services</span>
      </td>
      <td data-label="organization">Electric Boat</td>
      <td data-label="role">Primary client contact</td>
    </tr>
    <!-- group -->
    <tr class="personnel-entry-group">
      <td data-label="person">
        <span class="personnel-name">Multiple EB Personnel</span>
        <span class="personnel-title">49 contacts across departments</span>
      </td>
      <td data-label="organization">Electric Boat</td>
      <td></td>
    </tr>
  </tbody>
</table>
```

Field derivation:
- `name`: `.personnel-name` textContent (empty string if absent — anonymized rows).
- `title`: `.personnel-title` textContent (always present).
- `organization`: `td[data-label="organization"]` textContent.
- `role`: `td[data-label="role"]` textContent. Cell may be an empty `<td></td>` → empty string.
- `entryType`:
  - `tr.classList.contains('personnel-entry-anonymized')` → `'anonymized'`
  - `tr.classList.contains('personnel-entry-group')` → `'group'`
  - default → `'individual'`
- `sourceExhibitLabel`: `opts?.sourceExhibitLabel ?? ''`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write personnel.test.ts (RED)</name>
  <files>scripts/tiddlywiki/extractors/personnel.test.ts</files>
  <read_first>
    - scripts/tiddlywiki/extractors/types.ts (PersonnelEntry, PersonnelEntryType)
    - static-site/exhibits/exhibit-a.html (personnel-table DOM — confirm personnel-name, personnel-title, td[data-label='organization'], td[data-label='role'] + class-based tr variants)
    - .planning/phases/53-dom-extraction/53-02-PLAN.md (test shape reference)
  </read_first>
  <behavior>
    - Test 1 (individual): tr without anon/group class + .personnel-name=Dan Novak → PersonnelEntry{name:'Dan Novak', title:'Solution Architect', entryType:'individual'}.
    - Test 2 (anonymized): tr.personnel-entry-anonymized with no .personnel-name → name='', title present, entryType='anonymized'.
    - Test 3 (group): tr.personnel-entry-group with name+title → entryType='group'.
    - Test 4 (empty role cell): `<td data-label="role"></td>` → role=''.
    - Test 5 (sourceExhibitLabel threading): opts.sourceExhibitLabel='Exhibit A' appears on every entry.
    - Test 6 (no personnel-table): empty HTML returns [], no throw.
    - Test 7 (idempotency): two calls produce byte-identical JSON.
  </behavior>
  <action>
Create `scripts/tiddlywiki/extractors/personnel.test.ts`:

```typescript
// scripts/tiddlywiki/extractors/personnel.test.ts
// EXTR-03 tests. SCAF-08 clean. Hermetic.

import { emitPersonnel } from './personnel.ts'

const PERSONNEL_HTML = `
<div class="exhibit-section">
  <h2>Personnel</h2>
  <table class="exhibit-table personnel-table">
    <thead><tr><th>Name / Title</th><th>Organization</th><th>Role on Project</th></tr></thead>
    <tbody>
      <tr class="">
        <td data-label="person">
          <span class="personnel-name">Dan Novak</span>
          <span class="personnel-title">Solution Architect</span>
        </td>
        <td data-label="organization">GP Strategies</td>
        <td data-label="role">Lead Investigator</td>
      </tr>
      <tr class="personnel-entry-anonymized">
        <td data-label="person">
          <span class="personnel-title">Chief of Learning Services</span>
        </td>
        <td data-label="organization">Electric Boat</td>
        <td data-label="role">Primary client contact</td>
      </tr>
      <tr class="personnel-entry-group">
        <td data-label="person">
          <span class="personnel-name">Multiple EB Personnel</span>
          <span class="personnel-title">49 contacts across departments</span>
        </td>
        <td data-label="organization">Electric Boat</td>
        <td></td>
      </tr>
    </tbody>
  </table>
</div>
`

describe('emitPersonnel — individual row', () => {
  it('extracts name, title, organization, role, entryType="individual"', () => {
    const rows = emitPersonnel(PERSONNEL_HTML, { sourceExhibitLabel: 'Exhibit A' })
    expect(rows[0].name).toBe('Dan Novak')
    expect(rows[0].title).toBe('Solution Architect')
    expect(rows[0].organization).toBe('GP Strategies')
    expect(rows[0].role).toBe('Lead Investigator')
    expect(rows[0].entryType).toBe('individual')
    expect(rows[0].sourceExhibitLabel).toBe('Exhibit A')
  })
})

describe('emitPersonnel — anonymized row', () => {
  it('has empty name, entryType="anonymized"', () => {
    const rows = emitPersonnel(PERSONNEL_HTML, { sourceExhibitLabel: 'Exhibit A' })
    expect(rows[1].name).toBe('')
    expect(rows[1].title).toBe('Chief of Learning Services')
    expect(rows[1].entryType).toBe('anonymized')
  })
})

describe('emitPersonnel — group row', () => {
  it('has name + title, entryType="group"', () => {
    const rows = emitPersonnel(PERSONNEL_HTML, { sourceExhibitLabel: 'Exhibit A' })
    expect(rows[2].name).toBe('Multiple EB Personnel')
    expect(rows[2].entryType).toBe('group')
  })
})

describe('emitPersonnel — empty role cell', () => {
  it('role defaults to empty string', () => {
    const rows = emitPersonnel(PERSONNEL_HTML, { sourceExhibitLabel: 'Exhibit A' })
    expect(rows[2].role).toBe('')
  })
})

describe('emitPersonnel — sourceExhibitLabel default', () => {
  it('defaults to empty when opts omitted', () => {
    const rows = emitPersonnel(PERSONNEL_HTML)
    expect(rows[0].sourceExhibitLabel).toBe('')
  })
})

describe('emitPersonnel — no personnel-table', () => {
  it('returns empty array', () => {
    expect(emitPersonnel('<div>no table</div>', { sourceExhibitLabel: 'X' })).toEqual([])
  })
})

describe('emitPersonnel — idempotency', () => {
  it('byte-identical JSON across two calls', () => {
    const a = JSON.stringify(emitPersonnel(PERSONNEL_HTML, { sourceExhibitLabel: 'Exhibit A' }))
    const b = JSON.stringify(emitPersonnel(PERSONNEL_HTML, { sourceExhibitLabel: 'Exhibit A' }))
    expect(b).toBe(a)
  })
})
```

Tests fail (RED).
  </action>
  <verify>
    <automated>test -f scripts/tiddlywiki/extractors/personnel.test.ts &amp;&amp; ! pnpm test:scripts --run scripts/tiddlywiki/extractors/personnel.test.ts 2>&amp;1 | grep -q "passed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^describe" scripts/tiddlywiki/extractors/personnel.test.ts` >= 6
    - Imports `./personnel.ts`: `grep -q "from './personnel.ts'" scripts/tiddlywiki/extractors/personnel.test.ts`
    - No fs/fixture reads: `! grep -qE "readFile|__fixtures__" scripts/tiddlywiki/extractors/personnel.test.ts`
    - Tests fail: `pnpm test:scripts --run scripts/tiddlywiki/extractors/personnel.test.ts; test $? -ne 0`
    - SCAF-08 clean: `! grep -nE "setTimeout\\(|Date\\.now\\(|new Date\\(|Promise\\.all\\(" scripts/tiddlywiki/extractors/personnel.test.ts`
  </acceptance_criteria>
  <done>
RED achieved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement personnel.ts (GREEN)</name>
  <files>scripts/tiddlywiki/extractors/personnel.ts</files>
  <read_first>
    - scripts/tiddlywiki/extractors/types.ts
    - scripts/tiddlywiki/extractors/personnel.test.ts
    - scripts/tiddlywiki/extractors/faq.ts (if Plan 02 landed first — sibling pattern for extractor with opts bag)
  </read_first>
  <action>
Create `scripts/tiddlywiki/extractors/personnel.ts`:

```typescript
// scripts/tiddlywiki/extractors/personnel.ts
// EXTR-03 — personnel-table extractor.
// SCAF-08: no setTimeout, no Date.now(), no new Date(), no Promise.all.

import {
  parseHtml,
  type PersonnelEntry,
  type PersonnelEntryType,
} from './types.ts'

const ROW_SELECTOR = 'table.personnel-table tbody tr'
const NAME_SELECTOR = '.personnel-name'
const TITLE_SELECTOR = '.personnel-title'
const ORG_CELL_SELECTOR = 'td[data-label="organization"]'
const ROLE_CELL_SELECTOR = 'td[data-label="role"]'

function textOf(el: Element | null): string {
  return (el?.textContent ?? '').trim()
}

function deriveEntryType(row: Element): PersonnelEntryType {
  if (row.classList.contains('personnel-entry-anonymized')) return 'anonymized'
  if (row.classList.contains('personnel-entry-group')) return 'group'
  return 'individual'
}

export function emitPersonnel(
  html: string,
  opts?: { sourceExhibitLabel?: string },
): readonly PersonnelEntry[] {
  const doc = parseHtml(html)
  const rows = doc.querySelectorAll(ROW_SELECTOR)
  const sourceExhibitLabel = opts?.sourceExhibitLabel ?? ''
  const out: PersonnelEntry[] = []
  rows.forEach((row) => {
    out.push({
      name: textOf(row.querySelector(NAME_SELECTOR)),
      title: textOf(row.querySelector(TITLE_SELECTOR)),
      organization: textOf(row.querySelector(ORG_CELL_SELECTOR)),
      role: textOf(row.querySelector(ROLE_CELL_SELECTOR)),
      entryType: deriveEntryType(row),
      sourceExhibitLabel,
    })
  })
  return out
}
```

Notes:
- Scoped selectors (`table.personnel-table tbody tr`) prevent cross-table leakage if a page contains both personnel AND technologies tables.
- `row.querySelector(ROLE_CELL_SELECTOR)` returns `null` when the cell is empty or absent — `textOf(null)` returns `''`. Matches test 4.
- `classList.contains` on happy-dom elements works under ES2022 lib (classList is part of the DOM reference lib).
- `.forEach` not `for-of` — same type-check constraint as other extractors.

Tests all pass (GREEN).
  </action>
  <verify>
    <automated>pnpm test:scripts --run scripts/tiddlywiki/extractors/personnel.test.ts &amp;&amp; pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/tiddlywiki/extractors/personnel.ts`
    - `grep -q "export function emitPersonnel" scripts/tiddlywiki/extractors/personnel.ts`
    - Required selectors: `grep -q "personnel-table" scripts/tiddlywiki/extractors/personnel.ts`, `grep -q "personnel-name" scripts/tiddlywiki/extractors/personnel.ts`, `grep -q "personnel-title" scripts/tiddlywiki/extractors/personnel.ts`
    - Entry-type class hooks: `grep -q "personnel-entry-anonymized" scripts/tiddlywiki/extractors/personnel.ts`, `grep -q "personnel-entry-group" scripts/tiddlywiki/extractors/personnel.ts`
    - SCAF-08 clean: `! grep -nE "setTimeout\\(|Date\\.now\\(|new Date\\(|Promise\\.all\\(" scripts/tiddlywiki/extractors/personnel.ts`
    - Tests pass: `pnpm test:scripts --run scripts/tiddlywiki/extractors/personnel.test.ts; test $? -eq 0`
    - Build clean: `pnpm build; test $? -eq 0`
  </acceptance_criteria>
  <done>
EXTR-03 shipped.
  </done>
</task>

</tasks>

<verification>
- `pnpm test:scripts --run scripts/tiddlywiki/extractors/personnel.test.ts` exits 0
- `pnpm build` exits 0
- SCAF-08 clean on both files
</verification>

<success_criteria>
EXTR-03 delivered: `emitPersonnel(html, opts?)` returns PersonnelEntry[] discriminating individual/group/anonymized entry types via row class hooks; sourceExhibitLabel threaded via opts; no-table yields empty array.
</success_criteria>

<output>
Create `.planning/phases/53-dom-extraction/53-04-SUMMARY.md`.
</output>
