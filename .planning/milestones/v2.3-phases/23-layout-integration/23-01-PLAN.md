---
phase: 23-layout-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/exhibit/InvestigationReportLayout.vue
  - src/components/exhibit/InvestigationReportLayout.test.ts
  - src/components/exhibit/EngineeringBriefLayout.vue
  - src/components/exhibit/EngineeringBriefLayout.test.ts
autonomous: true
requirements: [RNDR-05, RNDR-06]

must_haves:
  truths:
    - "InvestigationReportLayout renders FindingsTable when exhibit has findings data"
    - "EngineeringBriefLayout renders FindingsTable when exhibit has findings data"
    - "Neither layout renders a findings section when exhibit has no findings data"
    - "FindingsTable receives correct props (findings array and optional heading)"
  artifacts:
    - path: "src/components/exhibit/InvestigationReportLayout.vue"
      provides: "FindingsTable integration in IR layout"
      contains: "FindingsTable"
    - path: "src/components/exhibit/EngineeringBriefLayout.vue"
      provides: "FindingsTable integration in EB layout"
      contains: "FindingsTable"
    - path: "src/components/exhibit/InvestigationReportLayout.test.ts"
      provides: "IR layout findings integration tests"
      contains: "FindingsTable"
    - path: "src/components/exhibit/EngineeringBriefLayout.test.ts"
      provides: "EB layout findings integration tests"
      contains: "FindingsTable"
  key_links:
    - from: "src/components/exhibit/InvestigationReportLayout.vue"
      to: "src/components/FindingsTable.vue"
      via: "import + v-if guard + prop binding"
      pattern: "v-if=\"exhibit\\.findings\\?\\.length\""
    - from: "src/components/exhibit/EngineeringBriefLayout.vue"
      to: "src/components/FindingsTable.vue"
      via: "import + v-if guard + prop binding"
      pattern: "v-if=\"exhibit\\.findings\\?\\.length\""
---

<objective>
Wire FindingsTable into both InvestigationReportLayout and EngineeringBriefLayout with empty-state suppression, following the exact Phase 19 PersonnelCard wiring pattern.

Purpose: Exhibit detail pages display findings through FindingsTable wherever findings data exists (RNDR-05, RNDR-06).
Output: Both layout components render FindingsTable with v-if guard and correct props.
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/exhibit/InvestigationReportLayout.vue
@src/components/exhibit/InvestigationReportLayout.test.ts
@src/components/exhibit/EngineeringBriefLayout.vue
@src/components/exhibit/EngineeringBriefLayout.test.ts
@src/components/FindingsTable.vue

<interfaces>
<!-- FindingsTable component props (from src/components/FindingsTable.vue) -->
```typescript
defineProps<{
  findings: ExhibitFindingEntry[]
  heading?: string
}>()
```

<!-- Exhibit interface relevant fields (from src/data/exhibits.ts) -->
```typescript
interface Exhibit {
  // ...other fields...
  findings?: ExhibitFindingEntry[]
  findingsHeading?: string
}
```

<!-- Personnel wiring pattern already in both layouts (lines 123-126) -->
```vue
<div v-if="exhibit.personnel?.length" class="exhibit-section">
  <h2>Project Team</h2>
  <PersonnelCard :personnel="exhibit.personnel" />
</div>
```

<!-- Test stubs pattern (from existing tests) -->
```typescript
const mountOptions = {
  global: { stubs: { RouterLink: true, TechTags: true, PersonnelCard: true } },
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — Add findings integration tests to both layout test files</name>
  <files>
    src/components/exhibit/InvestigationReportLayout.test.ts
    src/components/exhibit/EngineeringBriefLayout.test.ts
  </files>
  <read_first>
    src/components/exhibit/InvestigationReportLayout.test.ts
    src/components/exhibit/EngineeringBriefLayout.test.ts
    src/components/exhibit/InvestigationReportLayout.vue
    src/components/exhibit/EngineeringBriefLayout.vue
  </read_first>
  <behavior>
    - Test: IR layout renders FindingsTable stub when exhibit has findings (irFixture = exhibit-j, which has findings[] and findingsHeading)
    - Test: IR layout does NOT render FindingsTable when exhibit has no findings (use noPersonnelExhibit-style minimal exhibit without findings)
    - Test: EB layout renders FindingsTable stub when exhibit has findings (ebFixture = exhibit-a, which has findings[])
    - Test: EB layout does NOT render FindingsTable when exhibit has no findings (use minimal exhibit without findings)
  </behavior>
  <action>
    Add FindingsTable to the stubs object in mountOptions for BOTH test files: `{ RouterLink: true, TechTags: true, PersonnelCard: true, FindingsTable: true }`

    In InvestigationReportLayout.test.ts, add two tests after the existing personnel tests (around line 128):

    1. `it('renders findings section when exhibit has findings', ...)` — mount with irFixture (exhibit-j, which has findings[] and findingsHeading). Assert `wrapper.findComponent({ name: 'FindingsTable' }).exists()` is true OR assert `wrapper.find('findings-table-stub').exists()` is true. Also check heading text contains the custom findingsHeading value.

    2. `it('does not render findings section when exhibit has no findings', ...)` — mount with a minimal exhibit (no findings property). Assert FindingsTable stub does NOT exist.

    In EngineeringBriefLayout.test.ts, add the same two tests after the existing personnel tests (around line 137):

    1. `it('renders findings section when exhibit has findings', ...)` — mount with ebFixture (exhibit-a, which has findings[]). Assert FindingsTable stub exists.

    2. `it('does not render findings section when exhibit has no findings', ...)` — mount with a minimal exhibit (no findings). Assert FindingsTable stub does NOT exist.

    Run tests — the 4 new tests MUST FAIL (RED phase). The existing tests must still pass.
  </action>
  <verify>
    <automated>cd /home/xhiris/projects/pattern158-vue && npx vitest run src/components/exhibit/InvestigationReportLayout.test.ts src/components/exhibit/EngineeringBriefLayout.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "FindingsTable" src/components/exhibit/InvestigationReportLayout.test.ts returns at least 2
    - grep -c "FindingsTable" src/components/exhibit/EngineeringBriefLayout.test.ts returns at least 2
    - grep -c "renders findings section" src/components/exhibit/InvestigationReportLayout.test.ts returns 1
    - grep -c "does not render findings section" src/components/exhibit/InvestigationReportLayout.test.ts returns 1
    - grep -c "renders findings section" src/components/exhibit/EngineeringBriefLayout.test.ts returns 1
    - grep -c "does not render findings section" src/components/exhibit/EngineeringBriefLayout.test.ts returns 1
  </acceptance_criteria>
  <done>4 new failing tests exist (2 per layout) covering findings rendering and empty-state suppression. Existing tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Wire FindingsTable into both layouts</name>
  <files>
    src/components/exhibit/InvestigationReportLayout.vue
    src/components/exhibit/EngineeringBriefLayout.vue
  </files>
  <read_first>
    src/components/exhibit/InvestigationReportLayout.vue
    src/components/exhibit/EngineeringBriefLayout.vue
    src/components/FindingsTable.vue
  </read_first>
  <action>
    In BOTH layout files, make these identical changes:

    1. Add import in script setup (after the PersonnelCard import, line 4):
       `import FindingsTable from '@/components/FindingsTable.vue'`

    2. Add findings section in template AFTER the personnel section (after line 126, before the exhibit-impact-tags div):
       ```vue
       <div v-if="exhibit.findings?.length" class="exhibit-section">
         <FindingsTable :findings="exhibit.findings" :heading="exhibit.findingsHeading" />
       </div>
       ```

    NOTE: FindingsTable already renders its own heading internally via the `heading` prop (defaults to "Findings"). Do NOT add an `<h2>` wrapper — that would create a duplicate heading. The PersonnelCard pattern uses `<h2>Project Team</h2>` because PersonnelCard does not render its own heading, but FindingsTable does.

    Run all tests — all tests (existing + 4 new) MUST PASS (GREEN phase).
  </action>
  <verify>
    <automated>cd /home/xhiris/projects/pattern158-vue && npx vitest run src/components/exhibit/InvestigationReportLayout.test.ts src/components/exhibit/EngineeringBriefLayout.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "FindingsTable" src/components/exhibit/InvestigationReportLayout.vue returns at least 2
    - grep -c "FindingsTable" src/components/exhibit/EngineeringBriefLayout.vue returns at least 2
    - grep "exhibit.findings" src/components/exhibit/InvestigationReportLayout.vue returns a match
    - grep "exhibit.findings" src/components/exhibit/EngineeringBriefLayout.vue returns a match
    - npx vitest run src/components/exhibit/InvestigationReportLayout.test.ts exits 0
    - npx vitest run src/components/exhibit/EngineeringBriefLayout.test.ts exits 0
  </acceptance_criteria>
  <done>Both layouts render FindingsTable when findings data exists, suppress when absent. All tests pass.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/components/exhibit/` — all layout tests pass
2. `npx tsc --noEmit` — no TypeScript errors
3. Exhibit J (investigation-report with findings + custom heading) renders FindingsTable
4. Exhibit A (engineering-brief with findings) renders FindingsTable
5. Exhibits without findings show no findings section
</verification>

<success_criteria>
- FindingsTable wired into InvestigationReportLayout with v-if guard (RNDR-05)
- FindingsTable wired into EngineeringBriefLayout with v-if guard (RNDR-06)
- Empty-state suppression works (no findings = no section rendered)
- All existing + new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/23-layout-integration/23-01-SUMMARY.md`
</output>
