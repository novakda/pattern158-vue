---
phase: 21-type-definition-data-extraction
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - src/data/exhibits.ts
  - src/data/exhibits.test.ts
autonomous: true
requirements:
  - DATA-06

must_haves:
  truths:
    - "No findings table sections remain in migrated exhibits' sections[] arrays"
    - "Non-findings table sections (Personnel, Technologies, etc.) are untouched"
    - "All 6 DATA requirements pass automated tests"
  artifacts:
    - path: "src/data/exhibits.ts"
      provides: "Clean sections[] with findings tables removed from 7 exhibits"
    - path: "src/data/exhibits.test.ts"
      provides: "Test coverage for all DATA-01 through DATA-06 requirements"
      contains: "Phase 21: Findings Data Extraction"
  key_links:
    - from: "src/data/exhibits.test.ts"
      to: "src/data/exhibits.ts"
      via: "import { exhibits } from '@/data/exhibits'"
      pattern: "findings.*array|findingsHeading|sections.*Findings"
---

<objective>
Remove old findings table sections from migrated exhibits and add comprehensive test coverage for all Phase 21 DATA requirements.

Purpose: Eliminate duplicate data (findings now live in typed arrays, not table sections) and verify the complete migration with automated tests.
Output: Clean sections[] arrays on 7 exhibits, test block covering DATA-01 through DATA-06.
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-type-definition-data-extraction/21-RESEARCH.md
@.planning/phases/21-type-definition-data-extraction/21-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->
From src/data/exhibits.ts (after Plan 01):
```typescript
export interface ExhibitFindingEntry {
  finding: string
  description?: string
  background?: string
  resolution?: string
  severity?: string
}

export interface Exhibit {
  // ... existing fields ...
  personnel?: ExhibitPersonnelEntry[]
  findings?: ExhibitFindingEntry[]
  findingsHeading?: string
  // ... remaining fields ...
}
```

<!-- Existing test pattern from Phase 17 -->
From src/data/exhibits.test.ts:
```typescript
describe('Phase 17: Personnel Data Extraction', () => {
  describe('DATA-01: personnel arrays exist', () => {
    const exhibitLetters = ['a','b','c','d','e','f','g','h','i','j','k','l','m','n']
    exhibitLetters.forEach(letter => {
      it(`Exhibit ${letter.toUpperCase()} has personnel array with entries`, () => {
        // ...
      })
    })
  })
  // ...
})
```
</interfaces>

**Section removal criteria:** Match on BOTH `type === 'table'` AND heading starting with `'Findings'`. Do NOT remove personnel tables, technologies tables, or any other table sections.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove old findings table sections from migrated exhibits</name>
  <files>src/data/exhibits.ts</files>
  <read_first>
    - src/data/exhibits.ts (FULL FILE — locate each exhibit's sections[] array and identify the findings table section to remove)
  </read_first>
  <action>
For each of the 7 migrated exhibits (A, E, J, L, M, N, O), remove the findings table section from that exhibit's `sections[]` array. The section to remove matches ALL of these criteria:
- `type: 'table'`
- `heading` starts with `'Findings'`
- `columns` array has first element `'Finding'`

**DO NOT** remove any section that does not match all three criteria. Specifically preserve:
- Personnel table sections (columns start with 'Name' or 'Role')
- Technologies/Skills table sections
- Any text-type sections with heading 'Findings' (these are text, not table — Exhibits D, F have these)

For each exhibit, delete the entire object `{ type: 'table', heading: 'Findings...', columns: [...], rows: [...] }` from the `sections` array. Remove any trailing comma if it becomes the last element.

**Exhibits and their findings table heading values to match:**
- Exhibit A: `heading: 'Findings'`
- Exhibit E: `heading: 'Findings'`
- Exhibit J: `heading: 'Findings \u2014 Five Concurrent Systemic Failures (Swiss Cheese Model)'`
- Exhibit L: `heading: 'Findings \u2014 Five Foundational Gaps'`
- Exhibit M: `heading: 'Findings'`
- Exhibit N: `heading: 'Findings'`
- Exhibit O: `heading: 'Findings'`
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit 2>&1 | tail -3 && grep -c "heading: 'Findings" src/data/exhibits.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "heading: 'Findings" src/data/exhibits.ts` returns exactly 2 matches (Exhibits D and F text-type findings sections, which must NOT be removed)
    - No `type: 'table'` section with heading starting with 'Findings' exists in exhibits A, E, J, L, M, N, O
    - Personnel table sections still present on all exhibits that had them
    - `npx vue-tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Old findings table sections removed from all 7 migrated exhibits. Text-type findings sections (D, F) preserved. All other table sections untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add test coverage for all Phase 21 DATA requirements</name>
  <files>src/data/exhibits.test.ts</files>
  <read_first>
    - src/data/exhibits.test.ts (current test structure, Phase 17 test patterns)
    - src/data/exhibits.ts (verify findings data is populated from Plan 01)
  </read_first>
  <action>
Add a new `describe` block at the end of the test file, AFTER the Phase 17 block. Follow the Phase 17 test pattern (forEach over exhibit letters, individual `it` blocks per requirement).

```typescript
describe('Phase 21: Findings Data Extraction', () => {

  // DATA-01: ExhibitFindingEntry fields (compile-time check + runtime shape)
  describe('DATA-01: findings entries have correct shape', () => {
    it('Exhibit A findings have finding, background, resolution fields', () => {
      const exhibit = exhibits.find(e => e.exhibitLink === '/exhibits/exhibit-a')
      exhibit!.findings!.forEach(f => {
        expect(f.finding).toBeTruthy()
        expect(f.background).toBeTruthy()
        expect(f.resolution).toBeTruthy()
      })
    })
    it('Exhibit L findings have finding, severity, description fields', () => {
      const exhibit = exhibits.find(e => e.exhibitLink === '/exhibits/exhibit-l')
      exhibit!.findings!.forEach(f => {
        expect(f.finding).toBeTruthy()
        expect(f.severity).toBeTruthy()
        expect(f.description).toBeTruthy()
      })
    })
  })

  // DATA-02: findings[] arrays exist on all 7 exhibits
  describe('DATA-02: findings arrays exist', () => {
    const findingsExhibits = ['a', 'e', 'j', 'l', 'm', 'n', 'o']
    findingsExhibits.forEach(letter => {
      it(`Exhibit ${letter.toUpperCase()} has findings array with entries`, () => {
        const exhibit = exhibits.find(e => e.exhibitLink === `/exhibits/exhibit-${letter}`)
        expect(exhibit?.findings).toBeDefined()
        expect(exhibit!.findings!.length).toBeGreaterThan(0)
      })
    })
  })

  // DATA-03: findingsHeading on J and L only
  describe('DATA-03: findingsHeading field', () => {
    it('Exhibit J has findingsHeading', () => {
      const exhibit = exhibits.find(e => e.exhibitLink === '/exhibits/exhibit-j')
      expect(exhibit?.findingsHeading).toBeTruthy()
    })
    it('Exhibit L has findingsHeading', () => {
      const exhibit = exhibits.find(e => e.exhibitLink === '/exhibits/exhibit-l')
      expect(exhibit?.findingsHeading).toBeTruthy()
    })
    it('exhibits without custom headings do not have findingsHeading', () => {
      const noHeading = ['a', 'e', 'm', 'n', 'o']
      noHeading.forEach(letter => {
        const exhibit = exhibits.find(e => e.exhibitLink === `/exhibits/exhibit-${letter}`)
        expect(exhibit?.findingsHeading).toBeUndefined()
      })
    })
  })

  // DATA-04: correct row counts (5 per exhibit = 35 total)
  describe('DATA-04: findings data counts', () => {
    const findingsExhibits = ['a', 'e', 'j', 'l', 'm', 'n', 'o']
    findingsExhibits.forEach(letter => {
      it(`Exhibit ${letter.toUpperCase()} has 5 findings`, () => {
        const exhibit = exhibits.find(e => e.exhibitLink === `/exhibits/exhibit-${letter}`)
        expect(exhibit!.findings!.length).toBe(5)
      })
    })
    it('total findings across all exhibits is 35', () => {
      const total = exhibits.reduce((sum, e) => sum + (e.findings?.length || 0), 0)
      expect(total).toBe(35)
    })
  })

  // DATA-05: custom headings with em-dashes
  describe('DATA-05: custom headings preserved', () => {
    it('Exhibit J heading contains Swiss Cheese Model', () => {
      const exhibit = exhibits.find(e => e.exhibitLink === '/exhibits/exhibit-j')
      expect(exhibit?.findingsHeading).toContain('Swiss Cheese Model')
    })
    it('Exhibit L heading contains Five Foundational Gaps', () => {
      const exhibit = exhibits.find(e => e.exhibitLink === '/exhibits/exhibit-l')
      expect(exhibit?.findingsHeading).toContain('Five Foundational Gaps')
    })
    it('headings use em-dash not hyphen', () => {
      const j = exhibits.find(e => e.exhibitLink === '/exhibits/exhibit-j')
      const l = exhibits.find(e => e.exhibitLink === '/exhibits/exhibit-l')
      expect(j?.findingsHeading).toContain('\u2014')
      expect(l?.findingsHeading).toContain('\u2014')
    })
  })

  // DATA-06: old findings table sections removed
  describe('DATA-06: findings table sections removed', () => {
    const findingsExhibits = ['a', 'e', 'j', 'l', 'm', 'n', 'o']
    findingsExhibits.forEach(letter => {
      it(`Exhibit ${letter.toUpperCase()} has no findings table section`, () => {
        const exhibit = exhibits.find(e => e.exhibitLink === `/exhibits/exhibit-${letter}`)
        const findingsTableSection = exhibit!.sections?.find(
          s => s.type === 'table' && s.heading?.startsWith('Findings')
        )
        expect(findingsTableSection).toBeUndefined()
      })
    })
    it('Exhibit D still has text-type findings section (not migrated)', () => {
      const exhibit = exhibits.find(e => e.exhibitLink === '/exhibits/exhibit-d')
      const textSection = exhibit!.sections?.find(
        s => s.type === 'text' && s.heading === 'Findings'
      )
      expect(textSection).toBeDefined()
    })
    it('Exhibit F still has text-type findings section (not migrated)', () => {
      const exhibit = exhibits.find(e => e.exhibitLink === '/exhibits/exhibit-f')
      const textSection = exhibit!.sections?.find(
        s => s.type === 'text' && s.heading === 'Findings'
      )
      expect(textSection).toBeDefined()
    })
  })
})
```
  </action>
  <verify>
    <automated>npx vitest run --project unit src/data/exhibits.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/data/exhibits.test.ts contains `describe('Phase 21: Findings Data Extraction'`
    - src/data/exhibits.test.ts contains test blocks for DATA-01 through DATA-06
    - `npx vitest run --project unit src/data/exhibits.test.ts` exits 0 with all tests passing
    - At least 20 new test assertions covering findings arrays, headings, counts, and section removal
  </acceptance_criteria>
  <done>All Phase 21 DATA requirements covered by automated tests. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run --project unit src/data/exhibits.test.ts` — all tests pass
- `npx vue-tsc --noEmit` — no type errors
- `grep -c "heading: 'Findings" src/data/exhibits.ts` returns exactly 2 (D and F text sections only)
</verification>

<success_criteria>
- Old findings table sections removed from all 7 migrated exhibits
- Text-type findings sections on D and F preserved
- All other table sections (personnel, technologies) untouched
- Test block covers DATA-01 through DATA-06 with all tests green
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/21-type-definition-data-extraction/21-02-SUMMARY.md`
</output>
