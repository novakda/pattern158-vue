---
phase: 22-findingstable-component
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - src/assets/css/main.css
autonomous: true
requirements:
  - RNDR-01
  - RNDR-02
  - RNDR-03
  - RNDR-04
must_haves:
  truths:
    - "Desktop table is visible above 768px with semantic table styling (header bg, cell borders, hover)"
    - "Mobile cards are visible at 768px and below with card styling (border, radius, padding)"
    - "Desktop table is hidden at 768px and below"
    - "Mobile cards are hidden above 768px"
    - "Severity badges display as colored pills with correct light and dark mode treatments"
    - "All CSS uses .findings-table-* prefix to avoid class collisions"
  artifacts:
    - path: "src/assets/css/main.css"
      provides: "FindingsTable component CSS within @layer components"
      contains: ".findings-table-desktop"
  key_links:
    - from: "src/assets/css/main.css"
      to: "src/components/FindingsTable.vue"
      via: "CSS class names matching template classes"
      pattern: "\\.findings-table-(desktop|mobile|card|badge)"
---

<objective>
Add all FindingsTable CSS to main.css within the existing @layer components block.

Purpose: Visual styling for the dual-DOM responsive component — desktop table, mobile cards, severity badges, responsive media query toggle, light/dark mode support.
Output: Complete CSS for FindingsTable in main.css.
</objective>

<execution_context>
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/projects/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-findingstable-component/22-UI-SPEC.md
@.planning/phases/22-findingstable-component/22-RESEARCH.md
@.planning/phases/22-findingstable-component/22-01-SUMMARY.md

<interfaces>
<!-- CSS classes used in FindingsTable.vue that need styling -->
Classes from FindingsTable.vue template:
- .findings-table-section (root section wrapper)
- .findings-table-heading (h3 section heading)
- .findings-table-desktop (table element — visible >= 769px)
- .findings-table-mobile (card container div — visible <= 768px)
- .findings-table-card (individual card div)
- .findings-table-label (field label span in cards)
- .findings-table-value (field value span in cards)
- .findings-table-badge (severity badge base class)
- .findings-table-badge--critical (Critical severity variant)
- .findings-table-badge--high (High severity variant)

Existing CSS references in main.css:
- @layer components block starts at line ~593
- .personnel-grid/.personnel-card pattern at lines ~758-774 (card reference)
- .exhibit-table pattern at lines ~4000-4025 (table reference)
- Design tokens: --space-xs, --space-sm, --space-md, --space-lg
- Colors: --color-surface, --color-background-alt, --color-border, --color-text, --color-text-muted
- Severity: --color-danger, --color-accent, --color-accent-on-alt
- Radius: --radius-md, --radius-pill
- Font: --font-size-xs, --font-size-sm, --font-size-lg
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FindingsTable CSS to main.css</name>
  <files>src/assets/css/main.css</files>
  <read_first>
    src/assets/css/main.css (lines 593-600 for @layer components start)
    src/assets/css/main.css (lines 758-790 for .personnel-grid/.personnel-card pattern)
    src/assets/css/main.css (lines 4000-4030 for .exhibit-table pattern)
    src/assets/css/main.css (lines 170-180 for dark theme color tokens)
    src/components/FindingsTable.vue
    .planning/phases/22-findingstable-component/22-UI-SPEC.md
  </read_first>
  <action>
    Add FindingsTable CSS inside the existing `@layer components { }` block in main.css, after the personnel card CSS section. Use a clear section comment: `/* ========== FINDINGS TABLE ========== */`

    CSS blocks to add (all using existing design tokens per UI-SPEC):

    **1. Section wrapper and heading:**
    ```css
    .findings-table-heading {
        font-size: var(--font-size-lg);
        font-weight: 600;
        line-height: var(--line-height-lg);
        margin-bottom: var(--space-sm);
    }
    ```

    **2. Desktop table (matches .exhibit-table pattern):**
    ```css
    .findings-table-desktop {
        display: table;
        width: 100%;
        border-collapse: collapse;
    }
    .findings-table-desktop th {
        background: var(--color-background-alt);
        padding: var(--space-sm) var(--space-md);
        font-size: var(--font-size-sm);
        font-weight: 600;
        line-height: var(--line-height-sm);
        text-align: left;
        border-bottom: 2px solid var(--color-border);
    }
    .findings-table-desktop td {
        padding: var(--space-sm) var(--space-md);
        font-size: var(--font-size-sm);
        line-height: 1.6;
        border-bottom: 1px solid var(--color-border);
        vertical-align: top;
    }
    .findings-table-desktop tr:hover td {
        background: var(--color-background-alt);
    }
    ```

    **3. Mobile cards (hidden by default, matches .personnel-card pattern):**
    ```css
    .findings-table-mobile {
        display: none;
    }
    .findings-table-card {
        padding: var(--space-md);
        border: 1px solid var(--color-border);
        border-radius: var(--radius-md);
        background: var(--color-surface);
        margin-bottom: var(--space-md);
    }
    .findings-table-card:last-child {
        margin-bottom: 0;
    }
    .findings-table-label {
        display: block;
        font-size: var(--font-size-sm);
        font-weight: 600;
        color: var(--color-text-muted);
        line-height: var(--line-height-sm);
    }
    .findings-table-value {
        display: block;
        font-size: var(--font-size-sm);
        line-height: 1.6;
        margin-bottom: var(--space-sm);
    }
    .findings-table-value:last-child {
        margin-bottom: 0;
    }
    ```

    **4. Severity badges (light mode):**
    ```css
    .findings-table-badge {
        display: inline-block;
        border-radius: var(--radius-pill);
        padding: var(--space-xs) var(--space-sm);
        font-size: var(--font-size-xs);
        font-weight: 700;
        text-transform: uppercase;
        line-height: 1;
    }
    .findings-table-badge--critical {
        background: var(--color-danger);
        color: #fff;
    }
    .findings-table-badge--high {
        background: var(--color-accent-on-alt);
        color: #fff;
    }
    ```

    **5. Dark mode severity badges:**
    ```css
    [data-theme="dark"] .findings-table-badge--critical {
        background: transparent;
        color: var(--color-danger);
        border: 1px solid currentColor;
    }
    [data-theme="dark"] .findings-table-badge--high {
        background: transparent;
        color: var(--color-accent);
        border: 1px solid currentColor;
    }
    ```

    **6. Responsive media query (768px breakpoint):**
    ```css
    @media (max-width: 768px) {
        .findings-table-desktop {
            display: none;
        }
        .findings-table-mobile {
            display: block;
        }
    }
    ```

    After adding CSS, verify the build still compiles and all tests pass.

    Commit: `style(22-02): add FindingsTable CSS with responsive toggle and severity badges`
  </action>
  <verify>
    <automated>npx vitest run --project unit src/components/FindingsTable.test.ts && echo "--- Build check ---" && npx vue-tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - main.css contains .findings-table-desktop class
    - main.css contains .findings-table-mobile class
    - main.css contains .findings-table-card class
    - main.css contains .findings-table-badge--critical class
    - main.css contains .findings-table-badge--high class
    - main.css contains @media (max-width: 768px) rule toggling desktop/mobile visibility
    - main.css contains [data-theme="dark"] severity badge overrides
    - All classes use .findings-table-* prefix (no .finding-* or .exhibit-table collisions)
    - All unit tests still pass
  </acceptance_criteria>
  <done>FindingsTable CSS is complete in main.css with desktop table styling, mobile card styling, severity badges (light + dark), and responsive media query toggle at 768px.</done>
</task>

</tasks>

<verification>
npx vitest run --project unit
grep -c "findings-table-desktop" src/assets/css/main.css
grep -c "findings-table-mobile" src/assets/css/main.css
grep -c "findings-table-badge--critical" src/assets/css/main.css
grep -c "@media.*768px" src/assets/css/main.css
</verification>

<success_criteria>
- FindingsTable CSS exists in main.css within @layer components
- Desktop table styled with header bg, cell borders, row hover (matching .exhibit-table pattern)
- Mobile cards styled with border, radius, padding (matching .personnel-card pattern)
- Severity badges styled as colored pills with dark mode overrides
- Responsive media query toggles desktop/mobile at 768px
- No CSS class collisions with existing .finding-* or .exhibit-table classes
</success_criteria>

<output>
After completion, create `.planning/phases/22-findingstable-component/22-02-SUMMARY.md`
</output>
