---
phase: 03-remaining-pages-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/FaqPage.vue
  - src/pages/PortfolioPage.vue
  - src/pages/TestimonialsPage.vue
autonomous: true
requirements: [PAGE-02, PAGE-03, PAGE-05]
must_haves:
  truths:
    - "FaqPage renders all 15 Q&A pairs across 4 categories with no TODO text"
    - "PortfolioPage renders narratives, flagships, and directory table with no TODO text"
    - "TestimonialsPage renders all 14 exhibit cards with no TODO text"
    - "All three pages use HeroMinimal component for hero section"
    - "No .html hrefs exist in any of the three pages"
  artifacts:
    - path: "src/pages/FaqPage.vue"
      provides: "Full FAQ content from 11ty source"
      contains: "HeroMinimal"
    - path: "src/pages/PortfolioPage.vue"
      provides: "Full portfolio content from 11ty source"
      contains: "HeroMinimal"
    - path: "src/pages/TestimonialsPage.vue"
      provides: "Full testimonials content from 11ty source"
      contains: "HeroMinimal"
  key_links:
    - from: "src/pages/FaqPage.vue"
      to: "HeroMinimal component"
      via: "import and usage"
      pattern: "import HeroMinimal from '@/components/HeroMinimal.vue'"
    - from: "src/pages/PortfolioPage.vue"
      to: "HeroMinimal component"
      via: "import and usage"
      pattern: "import HeroMinimal from '@/components/HeroMinimal.vue'"
    - from: "src/pages/TestimonialsPage.vue"
      to: "HeroMinimal component"
      via: "import and usage"
      pattern: "import HeroMinimal from '@/components/HeroMinimal.vue'"
---

<objective>
Port the three data-heavy pages (FAQ, Portfolio, Testimonials) from the published 11ty HTML into Vue SFCs with full content, HeroMinimal adoption, and all internal links converted to router-link.

Purpose: Wave 1 content porting — content must exist in Vue SFCs before component extraction and data file creation can happen (port-first, extract-second per locked decision).

Output: Three fully-ported Vue page files with complete content from the 11ty source. Content is inline in templates (not yet extracted to data files or components — that happens in Wave 2).
</objective>

<execution_context>
@C:/main/pattern158-vue/.claude/get-shit-done/workflows/execute-plan.md
@C:/main/pattern158-vue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-remaining-pages-completion/03-CONTEXT.md
@.planning/phases/03-remaining-pages-completion/03-RESEARCH.md

<interfaces>
<!-- HeroMinimal component — use on all three pages -->
From src/components/HeroMinimal.vue:
```vue
<script setup lang="ts">
defineProps<{
  title: string
  subtitle?: string
}>()
</script>
<template>
  <section class="hero-minimal">
    <div class="container">
      <h1>{{ title }}</h1>
      <p v-if="subtitle" class="subtitle">{{ subtitle }}</p>
      <slot />
    </div>
  </section>
</template>
```

From src/components/TestimonialQuote.vue:
```vue
defineProps<{
  quote: string
  cite?: string
  context?: string
  variant?: 'primary' | 'secondary'
}>()
```

From src/components/StatItem.vue:
```vue
defineProps<{
  number: string
  label: string
}>()
```
</interfaces>

Canonical 11ty source: https://github.com/novakda/pattern158.solutions/tree/deploy/20260315-feat-auto-generate-deploy-branch-names-f
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port FaqPage from 11ty HTML</name>
  <files>src/pages/FaqPage.vue</files>
  <read_first>
    - src/pages/FaqPage.vue (current TODO stub — see existing useBodyClass/useSeo calls to preserve)
    - src/components/HeroMinimal.vue (component interface)
    - src/components/TestimonialQuote.vue (for bottom testimonial section)
    - src/pages/HomePage.vue (established page template pattern — composable calls, imports, fragment structure)
    - Fetch 11ty source: https://github.com/novakda/pattern158.solutions/blob/deploy/20260315-feat-auto-generate-deploy-branch-names-f/faq.html
  </read_first>
  <action>
    Replace the FaqPage.vue TODO stub with full content from the 11ty faq.html source.

    Script setup section:
    - Keep existing useBodyClass('page-faq') and useSeo() calls
    - Add imports: `import HeroMinimal from '@/components/HeroMinimal.vue'` and `import TestimonialQuote from '@/components/TestimonialQuote.vue'`

    Template structure (Vue 3 fragment — multiple root elements, no wrapper div):
    1. `<HeroMinimal title="Frequently Asked Questions" subtitle="Common questions about working with Dan Novak" />`
    2. FAQ content sections — 4 categories from 11ty source:
       - "Hiring Logistics" (3 Q&A pairs)
       - "Technical Expertise" (4 Q&A pairs)
       - "Working Style" (4 Q&A pairs)
       - "Process & Methodology" (4 Q&A pairs)
    3. Each Q&A rendered as static content (NO accordion, NO details/summary, NO v-show — locked decision: static rendering)
    4. Preserve all CSS classes from 11ty source (faq-category, faq-item, etc.)
    5. Bottom testimonial section using TestimonialQuote components (copy quotes from 11ty source)
    6. Convert ANY .html hrefs to `<router-link :to="...">` — all internal links must use router-link
    7. Content is inline in the template for now (data extraction happens in Plan 03)

    IMPORTANT: Copy ALL 15 Q&A pairs from the 11ty source verbatim. Do not summarize or skip any content.
  </action>
  <verify>
    <automated>grep -c "TODO" src/pages/FaqPage.vue | grep "^0$" && grep -c "HeroMinimal" src/pages/FaqPage.vue | grep -v "^0$" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/FaqPage.vue contains `import HeroMinimal from '@/components/HeroMinimal.vue'`
    - src/pages/FaqPage.vue contains `<HeroMinimal title="Frequently Asked Questions"`
    - src/pages/FaqPage.vue contains NO string "TODO"
    - src/pages/FaqPage.vue contains `useBodyClass('page-faq')`
    - src/pages/FaqPage.vue contains at least 15 question headings (h3 or dt elements with question text)
    - `grep '\.html"' src/pages/FaqPage.vue` returns zero results
    - Template uses Vue 3 fragment pattern (no wrapper div at root)
    - FAQ items are statically rendered (no `<details>`, no `<summary>`, no `v-show`, no accordion)
  </acceptance_criteria>
  <done>FaqPage renders all 15 Q&A pairs across 4 categories from the 11ty source, uses HeroMinimal, has no TODO text, and all internal links use router-link</done>
</task>

<task type="auto">
  <name>Task 2: Port PortfolioPage from 11ty HTML</name>
  <files>src/pages/PortfolioPage.vue</files>
  <read_first>
    - src/pages/PortfolioPage.vue (current TODO stub)
    - src/components/HeroMinimal.vue (component interface)
    - src/components/StatItem.vue (for stats bar — number/label props)
    - src/components/TestimonialQuote.vue (if testimonials section exists)
    - src/pages/HomePage.vue (established page template pattern)
    - Fetch 11ty source: https://github.com/novakda/pattern158.solutions/blob/deploy/20260315-feat-auto-generate-deploy-branch-names-f/portfolio.html
  </read_first>
  <action>
    Replace the PortfolioPage.vue TODO stub with full content from the 11ty portfolio.html source.

    Script setup section:
    - Keep existing useBodyClass and useSeo() calls (update useSeo description to match 11ty content)
    - Add imports: HeroMinimal, StatItem (for stats bar if present), TestimonialQuote (if present)

    Template structure (Vue 3 fragment — no wrapper div):
    1. `<HeroMinimal title="Portfolio" subtitle="38 client engagements spanning 28 years of documented project work" />`
    2. Stats bar section if present in 11ty source (reuse StatItem component)
    3. Portfolio Narratives section (`section.portfolio-narratives`) — 3 narrative cards with title, description, clients
    4. Featured Engagements section (`section.portfolio-flagships`) — flagship cards with client, title, dates, email count, role, summary, tags, quote, exhibit link
    5. Complete Project Directory section (`section.portfolio-directory`) — responsive `<table>` with industry headings and project rows
       - CRITICAL: Include `data-label` attributes on all `<td>` elements — the responsive table CSS uses `[data-label]` pseudo-content for mobile stacked display
    6. Convert ALL .html hrefs to `<router-link :to="...">` — exhibit links become `<router-link to="/exhibits/exhibit-X">`
    7. Content is inline in the template for now (data extraction happens in Plan 03)

    IMPORTANT: Copy ALL content from the 11ty source. The directory table has ~15 industries with multiple rows each — include every row. Do not truncate.
  </action>
  <verify>
    <automated>grep -c "TODO" src/pages/PortfolioPage.vue | grep "^0$" && grep -c "HeroMinimal" src/pages/PortfolioPage.vue | grep -v "^0$" && grep -c "data-label" src/pages/PortfolioPage.vue | grep -v "^0$" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/PortfolioPage.vue contains `import HeroMinimal from '@/components/HeroMinimal.vue'`
    - src/pages/PortfolioPage.vue contains `<HeroMinimal title="Portfolio"`
    - src/pages/PortfolioPage.vue contains NO string "TODO"
    - src/pages/PortfolioPage.vue contains `useBodyClass('page-portfolio')` (or similar)
    - src/pages/PortfolioPage.vue contains `portfolio-narratives` class
    - src/pages/PortfolioPage.vue contains `portfolio-flagships` class
    - src/pages/PortfolioPage.vue contains `portfolio-directory` class
    - src/pages/PortfolioPage.vue contains at least 5 `data-label` attributes on td elements
    - `grep '\.html"' src/pages/PortfolioPage.vue` returns zero results
    - Template uses Vue 3 fragment pattern (no wrapper div at root)
  </acceptance_criteria>
  <done>PortfolioPage renders all narratives, flagships, and directory table from the 11ty source, uses HeroMinimal, has no TODO text, and all internal links use router-link</done>
</task>

<task type="auto">
  <name>Task 3: Port TestimonialsPage from 11ty HTML</name>
  <files>src/pages/TestimonialsPage.vue</files>
  <read_first>
    - src/pages/TestimonialsPage.vue (current TODO stub)
    - src/components/HeroMinimal.vue (component interface — note: Testimonials hero needs the default slot for classification span)
    - src/components/StatItem.vue (for stats summary section)
    - src/pages/HomePage.vue (established page template pattern)
    - Fetch 11ty source: https://github.com/novakda/pattern158.solutions/blob/deploy/20260315-feat-auto-generate-deploy-branch-names-f/testimonials.html
  </read_first>
  <action>
    Replace the TestimonialsPage.vue TODO stub with full content from the 11ty testimonials.html source.

    Script setup section:
    - Keep existing useBodyClass and useSeo() calls
    - Add imports: HeroMinimal, StatItem (for stats bar)

    Template structure (Vue 3 fragment — no wrapper div):
    1. HeroMinimal with slot for classification text:
       ```vue
       <HeroMinimal title="FIELD REPORTS" subtitle="Documented evidence from incident sites across 28+ years of engineering work">
         <span class="classification">Corroborated · Primary Sources · 2005-2022</span>
       </HeroMinimal>
       ```
       Note: The 11ty source uses `class="hero"` (not hero-minimal) and has a `pattern-number` span on "REPORTS". Adapt to use HeroMinimal component. The `pattern-number` span can be part of the title string or omitted — the CSS class may not exist in main.css.
    2. 14 Exhibit Cards (A through N) — each has:
       - Exhibit label (e.g., "EXHIBIT A")
       - Client name and date
       - Title
       - Blockquote with attribution
       - Context section
       - Impact tags
       - Exhibit link (router-link to /exhibits/exhibit-X)
    3. Render all 14 exhibit cards as inline HTML sections (NOT using TestimonialQuote — the exhibit card structure is richer and does not map to TestimonialQuote props)
    4. Stats summary section with StatItem components if present in 11ty source
    5. Convert ALL .html hrefs to `<router-link :to="...">` — exhibit links must use router-link
    6. Preserve all CSS classes from 11ty source (exhibit-card, exhibit-header, exhibit-label, etc.)
    7. Content is inline — ExhibitCard component extraction happens in Plan 04

    IMPORTANT: Copy ALL 14 exhibit cards. Do NOT use TestimonialQuote for exhibit cards (pitfall from research). Do NOT truncate or summarize quotes.
  </action>
  <verify>
    <automated>grep -c "TODO" src/pages/TestimonialsPage.vue | grep "^0$" && grep -c "HeroMinimal" src/pages/TestimonialsPage.vue | grep -v "^0$" && grep -c "exhibit-card\|exhibit-label" src/pages/TestimonialsPage.vue | grep -v "^0$" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/TestimonialsPage.vue contains `import HeroMinimal from '@/components/HeroMinimal.vue'`
    - src/pages/TestimonialsPage.vue contains `<HeroMinimal title="FIELD REPORTS"`
    - src/pages/TestimonialsPage.vue contains `classification` class (slot content)
    - src/pages/TestimonialsPage.vue contains NO string "TODO"
    - src/pages/TestimonialsPage.vue contains at least 14 instances of `exhibit-label` or `EXHIBIT` text
    - src/pages/TestimonialsPage.vue does NOT import TestimonialQuote (exhibit cards use different structure)
    - `grep '\.html"' src/pages/TestimonialsPage.vue` returns zero results
    - Template uses Vue 3 fragment pattern (no wrapper div at root)
  </acceptance_criteria>
  <done>TestimonialsPage renders all 14 exhibit cards from the 11ty source, uses HeroMinimal with slot for classification, has no TODO text, and all internal links use router-link</done>
</task>

</tasks>

<verification>
After all three tasks:
- `grep -r 'TODO' src/pages/FaqPage.vue src/pages/PortfolioPage.vue src/pages/TestimonialsPage.vue` returns zero results
- `grep -r '\.html"' src/pages/FaqPage.vue src/pages/PortfolioPage.vue src/pages/TestimonialsPage.vue` returns zero results
- All three pages import and use HeroMinimal
- `npm run build` completes without errors
</verification>

<success_criteria>
Three data-heavy pages are fully ported from 11ty HTML with complete content, HeroMinimal adoption, and no TODO stubs. Content is inline in templates, ready for Wave 2 extraction.
</success_criteria>

<output>
After completion, create `.planning/phases/03-remaining-pages-completion/03-01-SUMMARY.md`
</output>
